## Blueprint — config
name: config
language: typescript
target_dir: src/config
description: ''
constraints:
- 'CONTRACT: SettingsManager.preferences is Preference — use its API: getKey(): string, getValue(): string | number | boolean,
  setValue(value: string | number | boolean): void'
references: &id001
- calculator_ava/config/settings.yaml
types:
- name: SettingsManager
  kind: class
  status: translated
  target_file: src/config/settings-manager.ts
  description: Centralized application settings manager
  fields:
  - name: preferences
    type: Map<string, Preference>
  - name: configLoader
    type: ConfigLoader
  - name: autoSave
    type: boolean
  methods:
  - name: loadSettings
    sig: '(path: string): Promise<void>'
    hint: Load from file
  - name: saveSettings
    sig: '(path: string): Promise<void>'
    hint: Save to file
  - name: getPreference
    sig: '(key: string): Preference | undefined'
    hint: Retrieve preference
  - name: setPreference
    sig: '(key: string, value: Preference): void'
    hint: Update preference
  - name: resetToDefaults
    sig: '(): void'
    hint: Reset all defaults
  - name: exportSettings
    sig: '(): string'
    hint: Export as JSON
  - name: importSettings
    sig: '(data: string): void'
    hint: Import from JSON
- name: Preference
  kind: interface
  status: pending
  target_file: src/config/preference.ts
  description: User preference key-value pair
  fields:
  - name: key
    type: string
  - name: value
    type: string | number | boolean
  methods:
  - name: getKey
    sig: '(): string'
    hint: Return preference key
  - name: getValue
    sig: '(): string | number | boolean'
    hint: Return preference value
  - name: setValue
    sig: '(value: string | number | boolean): void'
    hint: Update preference value
- name: ConfigLoader
  kind: class
  status: translated
  target_file: src/config/config-loader.ts
  references: *id001
