## Blueprint — engine
name: engine
language: typescript
target_dir: src/engine
description: ''
constraints:
- 'CONTRACT: Evaluator.sciLib is ScientificLibrary — use its API: sin(angle: number): number, cos(angle: number): number,
  tan(angle: number): number, asin(value: number): number'
- 'CONTRACT: Evaluator uses ASTNode from parser [class] — import from ''../parser'', API: evaluate(): number, toString():
  string, isLeaf(): boolean, clone(): ASTNode, replaceChild(old: ASTNode, next: ASTNode): void'
- 'CONTRACT: Evaluator uses ExpressionParser from parser [class] — import from ''../parser'', API: parse(expression: string):
  ASTNode, parseExpression(): ASTNode, parseTerm(): ASTerokeNode, parseFactor(): ASTerokeNode, peek(): Token'
references: &id001
- calculator_ava/engine/eval.yaml
- calculator_ava/engine/scilib.yaml
types:
- name: Evaluator
  kind: class
  status: translated
  target_file: src/engine/evaluator.ts
  description: Evaluates parsed AST expressions
  fields:
  - name: parser
    type: ExpressionParser
  - name: sciLib
    type: ScientificLibrary
  - name: constReg
    type: ConstantRegistry
  - name: history
    type: number[]
  methods:
  - name: evaluate
    sig: '(input: string): number'
    hint: parse and compute
  - name: evaluateAST
    sig: '(node: ASTNode): number'
    hint: recursive AST walk
  - name: applyOperator
    sig: '(op: Operator, left: number, right: number): number'
    hint: execute operation
  - name: applyFunction
    sig: '(name: string, arg: number): number'
    hint: call sci function
  - name: getConstant
    sig: '(name: string): number'
    hint: lookup constant
  - name: recordHistory
    sig: '(value: number): void'
    hint: store result
- name: ScientificLibrary
  kind: class
  status: pending
  target_file: src/engine/scientific-library.ts
  description: Provides scientific functions (trig, exp, log) for calculator
  fields:
  - name: constants
    type: Map<string, number>
  methods:
  - name: sin
    sig: '(angle: number): number'
    hint: sine of angle
  - name: cos
    sig: '(angle: number): number'
    hint: cosine of angle
  - name: tan
    sig: '(angle: number): number'
    hint: tangent of angle
  - name: asin
    sig: '(value: number): number'
    hint: arccosine of value
  - name: atan
    sig: '(value: number): number'
    hint: arctangent of value
  - name: sqrt
    sig: '(value: number): number'
    hint: square root
  - name: pow
    sig: '(base: number, exponent: number): number'
    hint: power function
  - name: ln
    sig: '(value: number): number'
    hint: natural logarithm
  - name: log
    sig: '(value: number): number'
    hint: base-10 logarithm
  - name: exp
    sig: '(value: number): number'
    hint: e^power
- name: CalculatorEngine
  kind: class
  status: pending
  target_file: src/engine/calculator-engine.ts
  references: *id001
- name: ConstantRegistry
  kind: class
  status: translated
  target_file: src/engine/constant-registry.ts
  references: *id001
