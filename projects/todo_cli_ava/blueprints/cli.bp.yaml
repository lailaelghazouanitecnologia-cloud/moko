## Blueprint — cli
name: cli
language: typescript
target_dir: src/cli
description: ''
constraints:
- 'CONTRACT: CLI.formatter is Formatter — use its API: formatTask(task: Task): string, formatTaskList, formatTaskTable(tasks:
  Task[]): string, formatError(message: string): string'
- 'CONTRACT: CLI uses CommandExecutor from commands [class] — import from ''../commands'', API: executeAdd(title: string,
  priority?: number): void, executeRemove(id: string): void, executeList(): void, executeFilterByStatus(status: string): void,
  executeSortByPriority(): void'
references: []
types:
- name: CLI
  kind: class
  status: translated
  target_file: src/cli/cli.ts
  description: Command-line interface handler
  fields:
  - name: formatter
    type: Formatter
  - name: commandExecutor
    type: CommandExecutor
  - name: config
    type: Config
  methods:
  - name: run
    sig: '(argv: string[]): Promise<void>'
    hint: Entry point
  - name: parseArgs
    sig: '(argv: string[]): ParsedArgs'
    hint: Parse command line
  - name: dispatch
    sig: '(args: ParsedArgs): Promise<void>'
    hint: Route to command
  - name: showHelp
    sig: '(): void'
    hint: Display usage
  - name: showVersion
    sig: '(): void'
    hint: Show version
  - name: handleError
    sig: '(err: Error): void'
    hint: Print error message
- name: Formatter
  kind: class
  status: translated
  target_file: src/cli/formatter.ts
  description: Formats terminal output for todo list
  fields:
  - name: config
    type: Config
  methods:
  - name: formatTask
    sig: '(task: Task): string'
    hint: render single task
  - name: formatTaskList
    hint: array of strings
  - name: formatTaskTable
    sig: '(tasks: Task[]): string'
    hint: table with headers
  - name: formatError
    sig: '(message: string): string'
  - name: formatSuccess
    sig: '(message: string): string'
    hint: green text
  - name: formatHelp
    sig: '(commands: Command[]): string'
    hint: usage summary
  - name: colorize
    sig: '(text: string, color: string): string'
    hint: ansi color
  - name: stripColor
    sig: '(text: string): string'
    hint: remove color codes
