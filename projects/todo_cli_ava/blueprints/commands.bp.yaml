## Blueprint — commands
name: commands
language: typescript
target_dir: src/commands
description: ''
constraints:
- Command pattern implementation
- TypeScript generics for context
- Async execution support
references: []
types:
- name: Command
  kind: interface
  status: translated
  target_file: src/commands/command.ts
  description: Base command interface
  methods:
  - name: execute
    sig: '(context?: TContext): Promise<TResult>'
    hint: Execute command with context
  - name: canExecute
    sig: '(context?: TContext): boolean'
    hint: Check if command can execute
  - name: validate
    sig: '(context?: T context): ValidationError[]'
    hint: Validate command parameters
  - name: getName
    sig: '(): string'
    hint: Get command name
  - name: getDescription"
    sig: '(): string'
    hint: Get command description
