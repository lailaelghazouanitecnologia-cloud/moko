## Blueprint — commands
name: commands
language: typescript
target_dir: src/commands
description: ''
constraints:
- 'CONTRACT: CommandExecutor uses Repository from store [class] — import from ''../store'', API: add(todo: Todo): void, remove(id:
  string): boolean, update(id: string, updates: Partial<Todo>): boolean, findById(id: string): Todo | undefined, findAll():
  Todo[]'
references: []
types:
- name: CommandExecutor
  kind: class
  status: translated
  target_file: src/commands/command-executor.ts
  description: Executes validated CLI commands
  fields:
  - name: repository
    type: Repository
  - name: config
    type: Config
  methods:
  - name: executeAdd
    sig: '(title: string, priority?: number): void'
    hint: add new task
  - name: executeRemove
    sig: '(id: string): void'
    hint: delete task
  - name: executeList
    sig: '(): void'
    hint: show all tasks
  - name: executeFilterByStatus
    sig: '(status: string): void'
    hint: filter tasks
  - name: executeSortByPriority
    sig: '(): void'
    hint: sort by priority
  - name: executeSortByDate
    sig: '(): void'
    hint: sort by date
  - name: executeUpdate
    sig: '(id: string, updates: Partial<Task>): void'
    hint: update task
  - name: validatePriority
    sig: '(priority: number): boolean'
    hint: check priority
  - name: validateStatus
    sig: '(status: string): boolean'
    hint: check status
- name: CommandParser
  kind: class
  status: pending
  target_file: src/commands/command-parser.ts
