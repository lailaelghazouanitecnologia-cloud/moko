export {
  Direction,
  GameStatus,
  CellType,
  GameEvent,
} from './types';

export type {
  Position,
  GameConfig,
  GameEventMap,
  CollisionResult,
} from './types';

export { Vector2D } from './vector';

export {
  GRID_WIDTH,
  GRID_HEIGHT,
  TICK_RATE,
  INITIAL_LENGTH,
  POINTS_PER_FOOD,
  MIN_TICK_RATE,
  TICK_RATE_DECREASE,
  HIGH_SCORE_FILE,
  OPPOSITE_DIRECTIONS,
  DEFAULT_CONFIG,
} from './constants';
