## Blueprint — audio
name: audio
language: typescript
target_dir: src/audio
description: "Module 'audio' with types: AudioManager, Sound, Channel, Channel3d, Listener. 3D audio system. Part of: Build\
  \ z86 game engine\n\nAVAILABLE FROM PRIOR LAYERS:\n  core: EventEmitter(), Timer(), ResourceLoader(), Tags(), Platform()\n\
  \  math: Vec2(x,y,add,sub,mul,dot,normalize,length,lerp,distance,equals), Vec3(x:number,y:number,z:number,add,sub,mul,mulScalar,div,dot,cross,normalize,length,lengthSq,lerp,distance,min,max,floor,...),\
  \ Vec4(), Mat3(), Mat4(data:Float32Array(16),translate,rotate,scale,invert,transpose,setIdentity,setTRS,getTranslation,getScale,getEulerAngl...),\
  \ Quat(x,y,z,slerp,setFromEulerAngles,setFromMat4,transformVector,mul,normalize,getEulerAngles), Color(), Ray(), BoundingBox(),\
  \ BoundingSphere(), Frustum(), Curve()"
constraints: []
references: []
types:
- name: AudioManager
  kind: class
  status: translated
  target_file: src/audio/audiomanager.ts
  description: Composed from 0 sources
  constraints:
  - Missing: initialize, play, pause, stop, setVolume
- name: Sound
  kind: class
  status: translated
  target_file: src/audio/sound.ts
  description: Composed from 0 sources
  constraints:
  - Missing: play(), pause(), stop() lifecycle controls
  - Missing: volume, loop, currentTime properties
  - Missing: onEnded callback support
- name: Channel
  kind: class
  status: translated
  target_file: src/audio/channel.ts
  description: Composed from 0 sources
  constraints:
  - Missing AudioNode connection methods
  - Missing parameter setters/getters
- name: Channel3d
  kind: class
  status: translated
  target_file: src/audio/channel3d.ts
  description: Composed from 0 sources
  constraints:
  - Missing AudioNode connect/disconnect methods
  - Missing PannerNode, GainNode type definitions
- name: Listener
  kind: class
  status: translated
  target_file: src/audio/listener.ts
  description: Composed from 0 sources
  constraints:
  - Missing: position, orientation, velocity setters
  - Missing: doppler factor, speed of sound
