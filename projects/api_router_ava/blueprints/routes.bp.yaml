## Blueprint — routes
name: routes
language: typescript
target_dir: src/routes
description: ''
constraints: []
references: []
types:
- name: Router
  kind: class
  status: translated
  target_file: src/routes/router.ts
  description: HTTP request router
  fields:
  - name: routes
    type: Map<string,RouteHandler>
  - name: middlewares
    type: RouteHandler[]
  - name: basePath
    type: string
  methods:
  - name: addRoute
    sig: '(method: string, path: string, handler: RouteHandler): void'
    hint: register single route
  - name: use
    sig: '(middleware: RouteHandler): Router'
    hint: attach middleware
  - name: get
    sig: '(path: string, handler: RouteHandler): Router'
    hint: register GET route
  - name: post
    sig: '(path: string, handler: RouteHandler): Router'
    hint: register POST route
  - name: put
    sig: '(path: string, handler: RouteHandler): Router'
    hint: register PUT route
  - name: delete
    sig: '(path: string, handler: RouteHandler): Router'
    hint: register DELETE route
  - name: resolve
    sig: '(method: string, url: string): RouteHandler | null'
    hint: find matching handler
  - name: group
    sig: '(prefix: string, fn: (r: Router) => void): Router'
    hint: create route group
- name: ResponseBuilder
  kind: class
  status: translated
  target_file: src/routes/response-builder.ts
  description: Fluent helper to craft HTTP responses
  fields:
  - name: statusCode
    type: number
  - name: headers
    type: Record<string, string>
  - name: body
    type: any
  methods:
  - name: status
    sig: '(code: number): ResponseBuilder'
    hint: Set status code
  - name: header
    sig: '(name: string, value: string): ResponseBuilder'
    hint: Add header
  - name: json
    sig: '(data: any): ResponseBuilder'
    hint: Send JSON body
  - name: text
    sig: '(text: string): ResponseBuilder'
    hint: Send text body
  - name: redirect
    sig: '(url: string, code?: number): ResponseBuilder'
    hint: Redirect response
  - name: error
    sig: '(message: string, code?: number): ResponseBuilder'
    hint: Error response
  - name: cookie
    sig: '(name: string, value: string, opts?: CookieOpts): ResponseBuilder'
    hint: Set cookie
  - name: build
    sig: '(): HttpResponse'
    hint: Finalize response
