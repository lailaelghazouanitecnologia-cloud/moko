## Blueprint — cli
name: cli
language: typescript
target_dir: src/cli
description: ''
constraints: []
references: []
types:
- name: Formatter
  kind: class
  status: translated
  target_file: src/cli/formatter.ts
  description: Terminal text formatting utilities
  fields:
  - name: theme
    type: Theme
  - name: width
    type: number
  methods:
  - name: bold
    sig: '(text: string): string'
    hint: wrap with bold
  - name: color
    sig: '(text: string, color: string): string'
    hint: apply color code
  - name: table
    sig: '(rows: string[][]): string'
    hint: format as table
  - name: wrap
    sig: '(text: string, maxWidth?: number): string'
    hint: wrap long lines
  - name: pad
    sig: '(text: string, len: number): string'
    hint: pad to length
  - name: header
    sig: '(title: string): string'
    hint: format header
  - name: error
    sig: '(msg: string): string'
    hint: style error text
  - name: success
    sig: '(msg: string): string'
    hint: style success text
