## Blueprint — commands
name: commands
language: typescript
target_dir: src/commands
description: ''
constraints: []
references: []
types:
- name: CommandParser
  kind: class
  status: translated
  target_file: src/commands/command-parser.ts
  description: Parses raw input into executable commands
  fields:
  - name: registry
    type: Map<string,CommandExecutor>
  - name: validators
    type: Map<string,ValidatorFn>
  - name: aliases
    type: Map<string,string>
  methods:
  - name: parse
    sig: '(input: string): ParsedCommand'
    hint: tokenize and resolve
  - name: register
    sig: '(name: string, executor: CommandExecutor): void'
    hint: add new command
  - name: validate
    sig: '(cmd: ParsedCommand): ValidationResult'
    hint: check constraints
  - name: resolveAlias
    sig: '(token: string): string'
    hint: map short names
  - name: buildHelp
    sig: '(cmd?: string): string'
    hint: generate usage text
  - name: parseArgs
    sig: '(tokens: string[]): ArgMap'
    hint: extract key-values
