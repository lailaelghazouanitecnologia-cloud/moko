## Blueprint — io
name: io
language: typescript
target_dir: src/io
description: ''
constraints:
- 'CONTRACT: IOPortSpace.deviceMap is IODevice — use its API: read(port: number, size: IOSize): number, write(port: number,
  value: number, size: IOSize): void, getBasePort(): number, getPortCount(): number'
references:
- x86vm/src/io/ports.yaml
- x86vm/src/io/devices.yaml
types:
- name: IOPortSpace
  kind: class
  status: translated
  target_file: src/io/io-port-space.ts
  description: Manages I/O port address space for x86 I/O operations
  fields:
  - name: ports
    type: Map<number, IOPort>
  - name: deviceMap
    type: Map<number, IODevice>
  methods:
  - name: registerDevice
    sig: '(device: IODevice, basePort: number, portCount: number): void'
    hint: register device at port range
  - name: readPort
    sig: '(port: number, size: IOSize): number'
    hint: read from port with size
  - name: writePort
    sig: '(port: number, value: number, size: IOSize): void'
    hint: write value to port
  - name: getDevice
    sig: '(port: number): IODevice | null'
    hint: find device for port
  - name: checkPortRange
    sig: '(basePort: number, count: number): boolean'
    hint: check if range available
  - name: unregisterDevice
    sig: '(device: IODevice): void'
    hint: remove device from ports
  - name: reset
    sig: '(): void'
    hint: clear all ports
  references:
  - io/ports/space.yaml
- name: IODevice
  kind: interface
  status: translated
  target_file: src/io/io-device.ts
  description: Base interface for I/O devices in the system
  methods:
  - name: read
    sig: '(port: number, size: IOSize): number'
    hint: read data from device
  - name: write
    sig: '(port: number, value: number, size: IOSize): void'
    hint: write data to device
  - name: getBasePort
    sig: '(): number'
    hint: get starting port number
  - name: getPortCount
    sig: '(): number'
    hint: get number of ports
  - name: getName
    sig: '(): string'
    hint: get device name
  - name: isPortValid
    sig: '(port: number): boolean'
    hint: check if port valid
  - name: reset
    sig: '(): void'
    hint: reset device state
  - name: getIRQ
    sig: '(): number | null'
    hint: get interrupt request line
  - name: canInterrupt
    sig: '(): boolean'
    hint: check if can interrupt
  - name: getInterruptVector
    sig: '(): number'
    hint: get interrupt vector
  references:
  - io/devices/base.yaml
- name: InterruptController
  kind: class
  status: translated
  target_file: src/io/interrupt-controller.ts
  description: Manures hardware interrupts and interrupt requests
  fields:
  - name: irqStatus
    type: Uint8Array
  - name: irqMask
    type: Uint8Array
  - name: isMaster
    type: boolean
  - name: vectorBase
    type: number
  - name: interruptPending
    type: boolean
  - name: priority
    type: number
  methods:
  - name: requestInterrupt
    sig: '(irq: number): void'
    hint: raise interrupt request
  - name: ''
    sig: '(irq: number): void'
    hint: acknowledge interrupt
  - name: getPendingIRQ
    sig: '(): number | null'
    hint: get highest priority irq
  - name: isIRQPending
    sig: '(irq: number): boolean'
    hint: check if irq pending
  - name: setIRQMask
    sig: '(irq: number, masked: boolean): void'
    hint: enable/disable irq
  - name: isIRQMasked
    sig: '(irq: number): boolean'
    hint: check if masked
  - name: readCommand
    sig: '(port: number): number'
    hint: read command register
  - name: writeCommand
    sig: '(port: number, value: number): void'
    hint: write command register
  - name: readData
    sig: '(port: number): number'
    hint: read data register
  - name: writeData
    sig: '(port: number, value: number): void'
    hint: write data register
  - name: getInterruptVector
    sig: '(irq: number): number'
    hint: calculate vector for irq
  - name: isInService
    sig: '(irq: number): boolean'
    hint: check if in service
  - name: endOfInterrupt
    sig: '(irq: number): void'
    hint: send eoi for irq
  - name: reset
    sig: '(): void'
    hint: reset controller state
  - name: getIRR
    sig: '(): number'
    hint: get interrupt request register
  - name: getISR
    sig: '(): number'
    hint: get in-service register
  - name: getIMR
    sig: '(): number'
    hint: get interrupt mask register
  references:
  - io/interrupts/controller.yaml
