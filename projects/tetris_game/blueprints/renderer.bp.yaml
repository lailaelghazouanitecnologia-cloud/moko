## Blueprint — renderer
name: renderer
language: typescript
target_dir: src/renderer
description: ''
constraints:
- console output
- terminal display
- text mode
references: []
types:
- name: Renderer
  kind: class
  status: translated
  target_file: src/renderer/renderer.ts
  description: console display handler
  fields:
  - name: terminal
    type: Terminal
  methods:
  - name: init
    sig: '(width: number, height: number): void'
    hint: setup display
  - name: clear
    sig: '(): void'
    hint: clear screen
  - name: draw
    sig: '(x: number, y: number, text: string): void'
    hint: draw text
  - name: update
    sig: '(): void'
    hint: refresh display
  - name: resize
    sig: '(width: number, height: number): void'
    hint: adjust size
  static:
  - name: VERSION
    hint: module version
