## Blueprint — diff
name: diff
language: typescript
target_dir: src/diff
description: ''
constraints: []
references: []
types:
- name: DiffEngine
  kind: class
  status: translated
  target_file: src/diff/diff-engine.ts
  description: Core text diff and merge engine
  fields:
  - name: ignoreWhitespace
    type: boolean
  - name: lineEnding
    type: string
  methods:
  - name: createTwoWayDiff
    sig: '(oldText: string, newText: string): Hunk[]'
    hint: generate two-way hunks
  - name: createThreeWayMerge
    hint: generate three-way hunks
  - name: applyHunks
    sig: '(text: string, hunks: Hunk[]): string'
    hint: apply hunks to text
  - name: mergeHunks
    hint: merge two hunk arrays
  - name: detectConflicts
    sig: '(hunks: Hunk[]): boolean'
    hint: check for conflicts
  - name: convertToPatch
    sig: '(hunks: Hunk[]): string'
    hint: export hunks to patch
  - name: parsePatch
    sig: '(patch: string): Hunk[]'
    hint: parse patch to hunks
- name: Hunk
  kind: class
  status: translated
  target_file: src/diff/hunk.ts
  description: Represents a contiguous block of lines in a diff
  fields:
  - name: oldStart
    type: number
  - name: oldLines
    type: number
  - name: newStart
    type: number
  - name: newLines
    type: number
  - name: lines
    type: string[]
  methods:
  - name: isAddition
    sig: '(line: string): boolean'
    hint: check if added
  - name: isDeletion
    sig: '(line: string): boolean'
    hint: check if removed
  - name: isContext
    sig: '(line: string): boolean'
    hint: check unchanged line
  - name: stripMarkers
    sig: '(): string[]'
    hint: remove diff markers
  - name: apply
    sig: '(oldText: string): string'
    hint: apply to text
  - name: toString
    sig: '(): string'
    hint: formatted output
