## Blueprint — commands
name: commands
language: typescript
target_dir: src/commands
description: ''
constraints: []
references: []
types:
- name: CommandExecutor
  kind: class
  status: translated
  target_file: src/commands/command-executor.ts
  description: Executes validated commands
  fields:
  - name: commandName
    type: string
  - name: validator
    type: CommandValidator
  - name: logger
    type: Logger
  methods:
  - name: execute
    sig: '(context: CommandContext): Promise<CommandResult>'
    hint: run validated command
  - name: validate
    sig: '(input: CommandInput): ValidationResult'
    hint: check input validity
  - name: rollback
    sig: '(context: CommandContext): Promise<void>'
    hint: undo execution
  - name: canExecute
    sig: '(context: CommandContext): boolean'
    hint: check permissions
  - name: getHelp
    sig: '(): string'
    hint: return usage info
- name: CommandParser
  kind: class
  status: translated
  target_file: src/commands/command-parser.ts
  description: Parses raw command strings into executable command objects
  fields:
  - name: commandMap
    type: Map<string, CommandExecutor>
  - name: delimiter
    type: string
  - name: validators
    type: 'Map<string, (args: string[]) => boolean>'
  methods:
  - name: parse
    sig: '(input: string): CommandExecutor'
    hint: convert string to executor
  - name: registerCommand
    sig: '(name: string, executor: CommandExecutor): void'
    hint: add new command
  - name: validateSyntax
    sig: '(input: string): boolean'
    hint: check input format
  - name: extractArgs
    sig: '(input: string): string[]'
    hint: split into tokens
  - name: getCommandName
    sig: '(input: string): string'
    hint: parse first token
  - name: setDelimiter
    sig: '(delimiter: string): void'
    hint: change arg separator
  - name: addValidator
    sig: '(command: string, fn: (args: string[]) => boolean): void'
    hint: attach arg checker
