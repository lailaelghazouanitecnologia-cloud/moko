## Blueprint — core
name: core
language: typescript
target_dir: src/core
description: ''
constraints:
- 'CONTRACT: Direction.delta is Position — use its API: constructor(row: number, col: number), equals(other: Position): boolean,
  clone(): Position, add'
references: []
types:
- name: GameConfig
  kind: class
  status: pending
  target_file: src/core/game-config.ts
  description: Configuration settings for a 2048 game session
  fields:
  - name: gridSize
    type: number
  - name: winningTile
    type: number
  - name: allowUndo
    type: boolean
  - name: seed
    type: number
  methods:
  - name: getGridSize
    sig: '(): number'
    hint: retrieve grid dimension
  - name: setGridSize
    sig: '(size: number): void'
    hint: update grid dimension
  - name: getWinningTile
    sig: '(): number'
    hint: retrieve target tile
  - name: setWinningTile
    sig: '(value: number): void'
    hint: set target tile
  - name: isUndoAllowed
    sig: '(): boolean'
    hint: check undo flag
  - name: setAllowUndo
    sig: '(allow: boolean): void'
    hint: toggle undo flag
  - name: getSeed
    sig: '(): number'
    hint: retrieve rng seed
  - name: setSeed
    sig: '(seed: number): void'
    hint: update rng seed
- name: Position
  kind: class
  status: translated
  target_file: src/core/position.ts
  description: Grid coordinate pair
  fields:
  - name: row
    type: number
  - name: col
    type: number
  methods:
  - name: constructor
    sig: '(row: number, col: number)'
    hint: Create new position
  - name: equals
    sig: '(other: Position): boolean'
    hint: Check same spot
  - name: clone
    sig: '(): Position'
    hint: Copy position
  - name: add
    hint: Move by direction
  - name: toString
    sig: '(): string'
    hint: String format
  - name: isValid
    sig: '(size: number): boolean'
    hint: Check in bounds
- name: Direction
  kind: enum
  status: translated
  target_file: src/core/direction.ts
  description: Move direction for tiles
  fields:
  - name: delta
    type: Position
  methods:
  - name: name
    sig: '(): string'
    hint: enum member name
  - name: value
    hint: row/col offset
  - name: opposite
    sig: '(): Direction'
- name: GameState
  kind: class
  status: pending
  target_file: src/core/game-state.ts
