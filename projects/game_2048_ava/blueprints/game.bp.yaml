## Blueprint — game
name: game
language: typescript
target_dir: src/game
description: ''
constraints: []
references: []
types:
- name: GameBoard
  kind: class
  status: translated
  target_file: src/game/game-board.ts
  description: Represents the game board state
  fields:
  - name: width
    type: number
  - name: height
    type: number
  - name: cells
    type: Cell[][]
  - name: currentPlayer
    type: Player
  methods:
  - name: initialize
    sig: '(width: number, height: number): void'
    hint: Set up board
  - name: placePiece
    sig: '(x: number, y: number, piece: Piece): boolean'
    hint: Add piece to cell
  - name: getCell
    sig: '(x: number, y: number): Cell'
    hint: Retrieve cell data
  - name: isValidMove
    sig: '(x: number, y: number): boolean'
    hint: Check move legality
  - name: clearBoard
    sig: '(): void'
    hint: Reset all cells
  - name: getAvailableMoves
    sig: '(): Position[]'
    hint: List valid moves
  - name: clone
    sig: '(): GameBoard'
    hint: Deep copy board
- name: GameLogic
  kind: enum
  status: translated
  target_file: src/game/game-logic.ts
  description: Game start conditions
  fields:
  - name: START
    type: number
  - name: PAUSE
    type: number
  - name: RESUME
    type: number
  - name: STOP
    type: number
- name: ScoreTracker
  kind: class
  status: translated
  target_file: src/game/score-tracker.ts
  description: Tracks and calculates game scores
  fields:
  - name: scores
    type: Map<string, number>
  - name: bonusPoints
    type: number
  - name: multiplier
    type: number
  methods:
  - name: addScore
    sig: '(playerId: string, points: number): void'
    hint: add points to player
  - name: getScore
    sig: '(playerId: string): number'
    hint: retrieve player score
  - name: resetScores
    sig: '(): void'
    hint: clear all scores
  - name: applyMultiplier
    sig: '(factor: number): void'
    hint: multiply all scores
  - name: addBonus
    sig: '(playerId: string, bonus: number): void'
    hint: grant bonus points
  - name: getLeader
    sig: '(): string | null'
    hint: highest scoring player
  - name: getRankings
    sig: '(): Array<{playerId: string, score: number}>'
    hint: sorted player list
