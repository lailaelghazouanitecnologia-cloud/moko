## Blueprint — game
name: game
language: typescript
target_dir: src/game
description: ''
constraints:
- 'CONTRACT: GameLogic.board is GameBoard — use its API: constructor(size: number), clone(): GameBoard, getTile(pos: Position):
  number, setTile(pos: Position, value: number): void'
- 'CONTRACT: GameLogic.scoreTracker is ScoreTracker — use its API: addScore(points: number): void, resetScore(): void, getCurrentScore():
  number, getHighScore(): number'
- 'CONTRACT: GameBoard uses Position from core [class] — import from ''../core'', API: constructor(row: number, col: number),
  equals(other: Position): boolean, clone(): Position, add, toString(): string'
- 'CONTRACT: GameLogic uses Direction from core [enum] values: delta'
references: []
types:
- name: GameBoard
  kind: class
  status: translated
  target_file: src/game/game-board.ts
  description: Manages 2048 grid state
  fields:
  - name: size
    type: number
  - name: grid
    type: number[][]
  methods:
  - name: constructor
    sig: '(size: number)'
    hint: initialize empty board
  - name: clone
    sig: '(): GameBoard'
    hint: deep copy board
  - name: getTile
    sig: '(pos: Position): number'
    hint: value at position
  - name: setTile
    sig: '(pos: Position, value: number): void'
    hint: place tile value
  - name: isEmpty
    sig: '(pos: Position): boolean'
    hint: check empty cell
  - name: spawnRandom
    sig: '(): Position'
    hint: add new tile
  - name: canMove
    sig: '(): boolean'
    hint: check any moves left
  - name: isFull
    sig: '(): boolean'
    hint: no empty cells
  - name: toString
    sig: '(): string'
    hint: grid string dump
- name: GameLogic
  kind: class
  status: translated
  target_file: src/game/game-logic.ts
  description: Encapsulates 2048 game rules and state transitions
  fields:
  - name: board
    type: GameBoard
  - name: scoreTracker
    type: ScoreTracker
  - name: state
    type: '''idle'' | ''playing'' | ''won'' | ''lost'''
  methods:
  - name: move
    sig: '(direction: Direction): boolean'
    hint: Execute slide and merge
  - name: canMove
    sig: '(direction: Direction): boolean'
    hint: Check move validity
  - name: spawnTile
    sig: '(): void'
    hint: Add random tile
  - name: checkWin
    sig: '(): boolean'
    hint: Detect 2048 tile
  - name: checkLoss
    sig: '(): boolean'
    hint: Detect no moves left
  - name: reset
    sig: '(): void'
    hint: Restart game state
  - name: getScore
    sig: '(): number'
    hint: Return current score
  - name: getState
    sig: '(): ''idle'' | ''playing'' | ''won'' | ''lost'''
    hint: Return game state
- name: ScoreTracker
  kind: class
  status: pending
  target_file: src/game/score-tracker.ts
  description: tracks score and high score
  fields:
  - name: currentScore
    type: number
  - name: highScore
    type: number
  methods:
  - name: addScore
    sig: '(points: number): void'
    hint: increase current score
  - name: resetScore
    sig: '(): void'
    hint: zero current score
  - name: getCurrentScore
    sig: '(): number'
    hint: return current score
  - name: getHighScore
    sig: '(): number'
    hint: return high score
  - name: updateHighScore
    sig: '(): void'
    hint: save if higher
  - name: loadHighScore
    sig: '(): void'
    hint: load from storage
