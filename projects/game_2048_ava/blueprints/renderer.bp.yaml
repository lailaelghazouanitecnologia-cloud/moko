## Blueprint — renderer
name: renderer
language: typescript
target_dir: src/renderer
description: ''
constraints:
- 'CONTRACT: Renderer uses GameBoard from game [class] — import from ''../game'', API: constructor(size: number), clone():
  GameBoard, getTile(pos: Position): number, setTile(pos: Position, value: number): void, isEmpty(pos: Position): boolean'
- 'CONTRACT: BoardDisplay uses GameBoard from game [class] — import from ''../game'', API: constructor(size: number), clone():
  GameBoard, getTile(pos: Position): number, setTile(pos: Position, value: number): void, isEmpty(pos: Position): boolean'
references: []
types:
- name: Renderer
  kind: class
  status: translated
  target_file: src/renderer/renderer.ts
  description: Handles terminal display of game state
  fields:
  - name: width
    type: number
  - name: height
    type: number
  - name: colors
    type: Map<number, string>
  methods:
  - name: clear
    sig: '(): void'
    hint: clear terminal screen
  - name: drawBoard
    sig: '(board: GameBoard): void'
    hint: render game grid
  - name: drawScore
    sig: '(score: number): void'
    hint: show score
  - name: drawState
    sig: '(state: ''idle'' | ''playing'' | ''won'' | ''lost''): void'
    hint: display game state
  - name: drawHelp
    sig: '(): void'
    hint: show controls
  - name: drawMessage
    sig: '(msg: string): void'
    hint: print message
  - name: getTileColor
    sig: '(value: number): string'
    hint: color for value
- name: BoardDisplay
  kind: class
  status: pending
  target_file: src/renderer/board-display.ts
  description: Renders game board to console
  fields:
  - name: size
    type: number
  - name: spacing
    type: number
  - name: colorMap
    type: Map<number, string>
  methods:
  - name: render
    sig: '(board: GameBoard, score: number): void'
    hint: print board and score
  - name: drawTile
    sig: '(value: number, size: number): string'
    hint: format single tile
  - name: drawBorder
    sig: '(size: number): string'
    hint: top bottom line
  - name: printRow
    sig: '(row: number[]): void'
    hint: render one row
  - name: clear
    sig: '(): void'
    hint: clear console
  - name: printScore
    sig: '(score: number): void'
    hint: show score line
  - name: printState
    sig: '(state: string): void'
    hint: show game state
  - name: printMessage
    sig: '(message: string): void'
    hint: show message
