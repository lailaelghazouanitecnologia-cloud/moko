## Blueprint — mutation
name: mutation
language: typescript
target_dir: src/mutation
description: ''
constraints:
- max 150 lines
- no extra types
- short hints only
references: []
types:
- name: ScriptMutator
  kind: class
  status: translated
  target_file: src/mutation/script-mutator.ts
  description: Parses and modifies training scripts based on mutation rules
  fields:
  - name: parser
    type: any
  - name: strategies
    type: Map<string, MutationStrategy>
  - name: results
    type: TrainingResults
  methods:
  - name: constructor
    sig: '(parser: any, results: TrainingResults)'
    hint: initialize parser and results
  - name: registerStrategy
    sig: '(name: string, strategy: MutationStrategy): void'
    hint: add mutation strategy
  - name: mutate
    sig: '(script: string): string'
    hint: apply mutations to script
  - name: parseScript
    sig: '(script: string): AST'
    hint: parse into AST
  - name: applyStrategies
    sig: '(ast: AST): AST'
    hint: run all strategies
  - name: generateCode
    sig: '(ast: AST): string'
    hint: convert AST to code
  - name: getResults
    sig: '(): TrainingResults'
    hint: return training results
  - name: setResults
    sig: '(results: TrainingResults): void'
    hint: update results
- name: MutationStrategy
  kind: interface
  status: translated
  target_file: src/mutation/mutation-strategy.ts
  description: Defines contract for script mutation strategies
  methods:
  - name: canApply
    sig: '(ast: AST, results: TrainingResults): boolean'
    hint: check if applicable
  - name: apply
    sig: '(ast: AST, results: TrainingResults): AST'
    hint: mutate the AST
  - name: getName
    sig: '(): string'
    hint: strategy identifier
  - name: getPriority
    sig: '(): number'
    hint: execution order
  - name: validate
    sig: '(ast: AST): boolean'
    hint: validate mutation
