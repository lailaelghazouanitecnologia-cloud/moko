## Blueprint — display
name: display
language: typescript
target_dir: src/display
description: ''
constraints: []
references: []
types:
- name: IDisplay
  kind: interface
  status: translated
  target_file: src/display/idisplay.ts
  description: 64x32 1-bit framebuffer with XOR sprite drawing
  fields:
  - name: width
    type: number
  - name: height
    type: number
  methods:
  - name: clear
    sig: '(): void'
    hint: zero framebuffer
  - name: draw_sprite
    sig: '(x: number, y: number, height: number, bytes: Uint8Array): number'
    hint: XOR sprite collision
  - name: get_pixel
    sig: '(x: number, y: number): number'
    hint: read pixel 0/1
  - name: set_pixel
    sig: '(x: number, y: number, val: number): void'
    hint: XOR set pixel
  - name: get_framebuffer
    sig: '(): Uint8Array'
    hint: return buffer ref
- name: Display
  kind: class
  status: translated
  target_file: src/display/display.ts
  description: 64x32 1-bit framebuffer with XOR sprite drawing
  fields:
  - name: buffer
    type: Uint8Array
  - name: width
    type: number
  - name: height
    type: number
  methods:
  - name: clear
    sig: '(): void'
    hint: zero framebuffer
  - name: draw_sprite
    sig: '(x: number, y: number, height: number, bytes: Uint8Array): number'
    hint: XOR sprite return collision
  - name: get_pixel
    sig: '(x: number, y: number): number'
    hint: read pixel 0/1
  - name: set_pixel
    sig: '(x: number, y: number, val: number): void'
    hint: XOR pixel into buffer
  - name: get_buffer
    sig: '(): Uint8Array'
    hint: return framebuffer
  - name: set_buffer
    sig: '(buffer: Uint8Array): void'
    hint: replace framebuffer
