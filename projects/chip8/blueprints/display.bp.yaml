## Blueprint — display
name: display
language: typescript
target_dir: src/display
description: ''
constraints: []
references: []
types:
- name: IDisplay
  kind: interface
  status: translated
  target_file: src/display/idisplay.ts
  description: Pixel buffer abstraction
  fields:
  - name: width
    type: number
  - name: height
    type: number
  methods:
  - name: clear
    sig: '(): void'
    hint: blank buffer
  - name: getPixel
    sig: '(x: number, y: number): boolean'
    hint: read pixel
  - name: setPixel
    sig: '(x: number, y: number, on: boolean): void'
    hint: write pixel
  - name: drawSprite
    sig: '(x: number, y: number, bytes: Uint8Array): boolean'
    hint: XOR sprite
  - name: getBuffer
    sig: '(): Uint8Array'
    hint: raw pixels
- name: Display
  kind: class
  status: translated
  target_file: src/display/display.ts
  description: 64x32 pixel buffer with XOR sprite drawing
  fields:
  - name: width
    type: number
  - name: height
    type: number
  - name: buffer
    type: boolean[][]
  - name: collision
    type: boolean
  methods:
  - name: clear
    sig: '(): void'
    hint: clear pixel buffer
  - name: drawSprite
    sig: '(x: number, y: number, sprite: number[]): boolean'
    hint: XOR draw sprite
  - name: getPixel
    sig: '(x: number, y: number): boolean'
    hint: get pixel state
  - name: setPixel
    sig: '(x: number, y: number, value: boolean): void'
    hint: set pixel state
  - name: getCollision
    sig: '(): boolean'
    hint: get collision flag
  - name: resetCollision
    sig: '(): void'
    hint: reset collision flag
