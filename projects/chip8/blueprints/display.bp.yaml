## Blueprint — display
name: display
language: typescript
target_dir: src/display
description: ''
constraints: []
references: []
types:
- name: IDisplay
  kind: interface
  status: translated
  target_file: src/display/idisplay.ts
  description: 64x32 monochrome display interface
  methods:
  - name: drawSprite
    sig: '(x: number, y: number, sprite: Uint8Array): boolean'
    hint: draw sprite return collision
  - name: clear
    sig: '(): void'
    hint: clear entire display
  - name: getPixel
    sig: '(x: number, y: number): boolean'
    hint: get pixel state
  - name: setPixel
    sig: '(x: number, y: number, value: boolean): void'
    hint: set pixel state
  - name: getFramebuffer
    sig: '(): Uint8Array'
    hint: get raw framebuffer
- name: Display
  kind: class
  status: translated
  target_file: src/display/display.ts
  description: 64x32 monochrome display with sprite drawing and collision detection
  fields:
  - name: framebuffer
    type: Uint8Array
  - name: width
    type: number
  - name: height
    type: number
  methods:
  - name: drawSprite
    sig: '(x: number, y: number, sprite: Uint8Array): boolean'
    hint: Draw sprite with collision
  - name: clear
    sig: '(): void'
    hint: Clear entire display
  - name: getPixel
    sig: '(x: number, y: number): boolean'
    hint: Get pixel state
  - name: setPixel
    sig: '(x: number, y: number, value: boolean): void'
    hint: Set pixel state
  - name: getFramebuffer
    sig: '(): Uint8Array'
    hint: Get raw framebuffer
