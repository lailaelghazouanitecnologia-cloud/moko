## Blueprint — display
name: display
language: typescript
target_dir: src/display
description: ''
constraints: []
references: []
types:
- name: IDisplay
  kind: interface
  status: translated
  target_file: src/display/idisplay.ts
  description: 64x32 monochrome framebuffer with XOR sprite drawing
  methods:
  - name: drawSprite
    sig: '(x: number, y: number, sprite: Uint8Array, height: number): boolean'
    hint: XOR draw with collision
  - name: clear
    sig: '(): void'
    hint: Clear all pixels
  - name: getPixel
    sig: '(x: number, y: number): boolean'
    hint: Get pixel state
  - name: getFramebuffer
    sig: '(): Uint8Array'
    hint: Return 256 bytes
- name: Display
  kind: class
  status: translated
  target_file: src/display/display.ts
  description: 64x32 monochrome framebuffer with XOR sprite drawing
  fields:
  - name: framebuffer
    type: Uint8Array
  - name: width
    type: number
  - name: height
    type: number
  methods:
  - name: drawSprite
    sig: '(x: number, y: number, sprite: Uint8Array, height: number): boolean'
    hint: XOR draw with collision
  - name: clear
    sig: '(): void'
    hint: Clear all pixels
  - name: getPixel
    sig: '(x: number, y: number): boolean'
    hint: Return pixel state
  - name: getFramebuffer
    sig: '(): Uint8Array'
    hint: Return 256 byte buffer
  - name: reset
    sig: '(): void'
    hint: Initialize display
