## Blueprint — Timer
name: Timer
language: typescript
target_dir: src/Timer
description: ''
constraints:
- Must count down at 60Hz
- SoundTimer beeps when non-zero
- DelayTimer used for game timing
- 8-bit values (0-255)
references:
- roska/projects/chip8/timer.yaml
types:
- name: DelayTimer
  kind: class
  status: pending
  target_file: src/Timer/delay-timer.ts
  description: 60Hz countdown timer for game delays
  fields:
  - name: value
    type: number
    default: '0'
  - name: lastUpdate
    type: number
    default: '0'
  methods:
  - name: set
    sig: '(val: number): void'
    hint: clamp 0-255 and store
  - name: get
    sig: '(): number'
    hint: return current value
  - name: tick
    sig: '(now: number): void'
    hint: decrement if elapsed 16.67ms
  - name: isActive
    sig: '(): boolean'
    hint: return value > 0
  - name: reset
    sig: '(): void'
    hint: set value to 0
  - name: getTimeRemaining
    sig: '(): number'
    hint: value * 16.67ms
  - name: sleep
    sig: '(ms: number): Promise<void>'
    hint: wait until tick reaches 0
  references:
  - roska/modules/Timer.yaml
- name: SoundTimer
  kind: class
  status: translated
  target_file: src/Timer/sound-timer.ts
  description: 60Hz countdown timer that beeps while active
  fields:
  - name: value
    type: number
    default: '0'
  - name: lastUpdate
    type: number
    default: '0'
  - name: audioCtx
    type: AudioContext
    default: None
  - name: oscillator
    type: OscillatorNode
    default: None
  - name: gainNode
    type: GainNode
    default: None
  methods:
  - name: set
    sig: '(val: number): void'
    hint: clamp 0-255, start/stop beep
  - name: get
    sig: '(): number'
    hint: return current value
  - name: tick
    sig: '(now: number): void'
    hint: decrement and stop beep at 0
  - name: isActive
    sig: '(): boolean'
    hint: return value > 0
  - name: reset
    sig: '(): void'
    hint: set value 0 and stop beep
  - name: startBeep
    sig: '(): void'
    hint: create 400Hz tone
  - name: stopBeep
    sig: '(): void'
    hint: disconnect and cleanup audio
  - name: toggleMute
    sig: '(): void'
    hint: toggle audio output
  - name: isMuted
    sig: '(): boolean'
    hint: return mute state
  references:
  - roska/modules/Timer.yaml
