## Blueprint — input
name: input
language: typescript
target_dir: src/input
description: ''
constraints:
- 'CONTRACT: InputHandler.keyMapper is KeyMapper — use its API: constructor(keyMap?: Map< string, string >), bind(key: string,
  command: string): void, unbind(key: string): void, commandFor'
references: []
types:
- name: InputHandler
  kind: class
  status: translated
  target_file: src/input/input-handler.ts
  description: Manages keyboard input queue
  fields:
  - name: keyMapper
    type: KeyMapper
  - name: queue
    type: string[]
  - name: enabled
    type: boolean
  methods:
  - name: constructor
    sig: '(keyMapper: KeyMapper): void'
    hint: bind key mappings
  - name: start
    sig: '(): void'
    hint: attach listeners
  - name: stop
    sig: '(): void'
    hint: detach listeners
  - name: poll
    sig: '(): string | undefined'
    hint: pop next key
  - name: peek
    sig: '(): string | undefined'
    hint: view next key
  - name: clear
    sig: '(): void'
    hint: empty queue
  - name: isEnabled
    sig: '(): boolean'
    hint: check enabled
  - name: setEnabled
    sig: '(enabled: boolean): void'
    hint: toggle input
- name: KeyMapper
  kind: class
  status: translated
  target_file: src/input/key-mapper.ts
  description: Maps keyboard events to game commands
  fields:
  - name: keyMap
    type: Map<string, string>
  - name: inverseMap
    type: Map<string, string>
  methods:
  - name: constructor
    sig: '(keyMap?: Map< string, string >)'
    hint: builds maps
  - name: bind
    sig: '(key: string, command: string): void'
  - name: unbind
    sig: '(key: string): void'
    hint: remove key
  - name: commandFor
    hint: resolve command
  - name: keyFor
    sig: '(command: string): string'
    hint: reverse lookup
  - name: hasKey
    sig: '(key: string): boolean'
    hint: check key
  - name: hascommand
    sig: '(command: string): boolean'
    hint: check command
  - name: clear
    sig: '(): void'
  - name: clone
    sig: '(): KeyMapper'
    hint: copy self
