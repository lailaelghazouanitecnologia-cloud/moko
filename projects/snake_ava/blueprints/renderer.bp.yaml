## Blueprint — renderer
name: renderer
language: typescript
target_dir: src/renderer
description: ''
constraints:
- 'CONTRACT: Renderer uses Entity from core [class] — import from ''../core'', API: move(direction: Vector2D): void, collidesWith(other:
  Entity): boolean, getBounds(): {topLeft: Vector2D, bottomRight: Vector2D}, wrap(bounds: Vector2D): void, clone(): Entity'
- 'CONTRACT: Renderer uses Vector2D from core [class] — import from ''../core'', API: constructor(x: number, y: number): Vector2D,
  add(other: Vector2): Vector2D, sub(other: Vector2D): Vector2D, mul(scalar: number): Vector2D, div(scalar: number): Vector2D'
references: []
types:
- name: Renderer
  kind: class
  status: translated
  target_file: src/renderer/renderer.ts
  description: Double-buffered console renderer
  fields:
  - name: width
    type: number
  - name: height
    type: number
  - name: frontBuffer
    type: ScreenBuffer
  - name: backBuffer
    type: ScreenBuffer
  methods:
  - name: clear
    sig: '(): void'
    hint: clear back buffer
  - name: swap
    sig: '(): void'
    hint: swap buffers
  - name: drawEntity
    sig: '(entity: Entity, char: string): void'
    hint: draw entity char
  - name: drawText
    sig: '(pos: Vector2D, text: string): void'
    hint: draw text at pos
  - name: drawBorder
    sig: '(): void'
    hint: draw game border
  - name: render
    sig: '(): void'
    hint: output front buffer
  - name: drawFood
    sig: '(pos: Vector2D): void'
    hint: draw food symbol
- name: ScreenBuffer
  kind: class
  status: translated
  target_file: src/renderer/screen-buffer.ts
