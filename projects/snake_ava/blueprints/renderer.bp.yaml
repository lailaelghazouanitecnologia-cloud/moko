## Blueprint — renderer
name: renderer
language: typescript
target_dir: src/renderer
description: ''
constraints: []
references: []
types:
- name: CanvasRenderer
  kind: class
  status: translated
  target_file: src/renderer/canvas-renderer.ts
  description: Renders entities using HTML5 Canvas
  fields:
  - name: canvas
    type: HTMLCanvasElement
  - name: ctx
    type: CanvasRenderingContext2D
  - name: width
    type: number
  - name: height
    type: number
  - name: pixelRatio
    type: number
  methods:
  - name: clear
    sig: '(): void'
    hint: clears entire canvas
  - name: drawRect
    sig: '(x: number, y: number, w: number, h: number, color: string): void'
    hint: draws filled rectangle
  - name: drawCircle
    sig: '(x: number, y: number, r: number, color: string): void'
    hint: draws filled circle
  - name: drawLine
    sig: '(x1: number, y1: number, x2: number, y2: number, color: string, width: number): void'
    hint: draws stroked line
  - name: drawText
    sig: '(text: string, x: number, y: number, font: string, color: string): void'
    hint: draws text string
  - name: save
    sig: '(): void'
    hint: saves canvas state
  - name: restore
    sig: '(): void'
    hint: restores canvas state
  - name: scale
    sig: '(x: number, y: number): void'
    hint: applies scaling transform
  - name: translate
    sig: '(x: number, y: number): void'
    hint: applies translation
- name: HtmlRenderer
  kind: class
  status: translated
  target_file: src/renderer/html-renderer.ts
  description: Renders entities using HTML elements
  fields:
  - name: container
    type: HTMLElement
  - name: entityMap
    type: Map<string, HTMLElement>
  - name: zIndex
    type: number
  methods:
  - name: render
    sig: '(entity: Entity): void'
    hint: draw single entity
  - name: clear
    sig: '(): void'
    hint: remove all elements
  - name: createElement
    sig: '(entity: Entity): HTMLElement'
    hint: build DOM node
  - name: updateElement
    sig: '(entity: Entity, element: HTMLElement): void'
    hint: sync entity state
  - name: removeElement
    sig: '(id: string): void'
    hint: delete by ID
  - name: setPosition
    sig: '(element: HTMLElement, x: number, y: number): void'
    hint: apply CSS transform
  - name: setStyle
    sig: '(element: HTMLElement, style: Record<string, string>): void'
    hint: apply CSS rules
