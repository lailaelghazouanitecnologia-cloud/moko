## Blueprint — game
name: game
language: typescript
target_dir: src/game
description: ''
constraints:
- 'CONTRACT: CollisionDetector uses Entity from core [class] — import from ''../core'', API: move(direction: Vector2D): void,
  collidesWith(other: Entity): boolean, getBounds(): {topLeft: Vector2D, bottomRight: Vector2D}, wrap(bounds: Vector2D): void,
  clone(): Entity'
- 'CONTRACT: CollisionDetector uses Vector2D from core [class] — import from ''../core'', API: constructor(x: number, y: number):
  Vector2D, add(other: Vector2): Vector2D, sub(other: Vector2D): Vector2D, mul(scalar: number): Vector2D, div(scalar: number):
  Vector2D'
references: []
types:
- name: CollisionDetector
  kind: class
  status: pending
  target_file: src/game/collision-detector.ts
  description: Handles collision detection between entities
  fields:
  - name: gridSize
    type: Vector2D
  - name: worldBounds
    type: Vector2D
  methods:
  - name: checkCollision
    sig: '(a: Entity, b: Entity): boolean'
    hint: entity vs entity
  - name: checkWallCollision
    sig: '(entity: Entity): boolean'
    hint: entity vs wall
  - name: checkSelfCollision
    sig: '(snake: Entity[]): boolean'
    hint: snake head vs body
  - name: checkFoodCollision
    sig: '(snake: Entity, food: Entity): boolean'
    hint: snake head vs food
  - name: getCollidingEntities
    sig: '(entity: Entity, others: Entity[]): Entity[]'
    hint: find all overlaps
  - name: clampToGrid
    sig: '(entity: Entity): void'
    hint: snap to grid
  - name: isOutOfBounds
    sig: '(entity: Entity): boolean'
    hint: beyond world limits
- name: GameWorld
  kind: class
  status: pending
  target_file: src/game/game-world.ts
- name: EntityManager
  kind: class
  status: translated
  target_file: src/game/entity-manager.ts
