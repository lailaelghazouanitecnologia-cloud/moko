## Roska File Descriptor — workflow/parallel.py
file: workflow/parallel.py
lines: 927
purpose: "Parallel — concurrent step execution via ThreadPoolExecutor (sync) or asyncio.gather (async)"
tags: [concurrency, parallel, gather, threadpool]
ctx: "@dataclass. Variadic constructor supports Parallel(step1, step2), Parallel('name', step1, step2), Parallel(step1, name='name'). Results aggregated into single StepOutput."

types:
  - name: Parallel
    kind: struct
    vis: pub
    fields:
      - {name: name, type: "Optional[str]", ctx: "Parallel name"}
      - {name: description, type: "Optional[str]", ctx: "Parallel description"}
      - {name: steps, type: "WorkflowSteps", ctx: "Steps to execute concurrently (default: [])"}
