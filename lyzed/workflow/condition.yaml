## Roska File Descriptor — workflow/condition.py
file: workflow/condition.py
lines: 1181
purpose: "Condition — if/else branching via callable, boolean literal, or CEL expression"
tags: [branching, conditional, cel, hitl]
ctx: "@dataclass. Evaluates condition, runs steps (if True) or else_steps (if False). Supports HITL confirmation."

types:
  - name: Condition
    kind: struct
    vis: pub
    fields:
      - {name: steps, type: WorkflowSteps, ctx: "Steps to run if condition is True (REQUIRED)"}
      - {name: evaluator, type: "Union[Callable, Awaitable, bool, str]", ctx: "Condition — callable(StepInput)->bool, bool literal, or CEL expression (default: True)"}
      - {name: else_steps, type: "Optional[WorkflowSteps]", ctx: "Steps to run if condition is False"}
      - {name: name, type: "Optional[str]", ctx: "Condition name"}
      - {name: description, type: "Optional[str]", ctx: "Condition description"}
      - {name: requires_confirmation, type: bool, ctx: "Pause for confirmation"}
      - {name: confirmation_message, type: "Optional[str]", ctx: "Confirmation prompt"}
      - {name: on_reject, type: "Union[OnReject, str]", ctx: "Action on rejection (default: else_branch)"}

data:
  - name: CEL_VARIABLES
    type: "conceptual"
    ctx: |
      Available in CEL evaluator expressions:
        input — current step input
        previous_step_content — output of previous step
        previous_step_outputs — all previous outputs
        additional_data — extra data
        session_state — workflow session state
