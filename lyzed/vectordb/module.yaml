## Roska Module Descriptor — vectordb/
name: vectordb
purpose: "19 vector database adapters under VectorDb ABC — similarity search, hybrid search, reranking"
tags: [vectordb, similarity, hybrid-search, embeddings]

files:
  - file: base.py
    lines: ~100
    purpose: "VectorDb ABC — 26 abstract methods for vector CRUD and search"
    tags: [abc, base, vector-search]
    descriptor: base.yaml

  - file: distance.py
    purpose: "Distance metrics enum (cosine, l2, max_inner_product)"

  - file: search.py
    purpose: "SearchType enum and search configurations"

  - file: score.py
    purpose: "Scoring utilities"

submodules:
  - name: pgvector
    purpose: "PostgreSQL pgvector extension — HNSW/IVFFlat indexes, hybrid search"
    files: [pgvector.py]

  - name: milvus
    purpose: "Milvus vector database"

  - name: pinecone
    purpose: "Pinecone hosted vector DB"

  - name: qdrant
    purpose: "Qdrant vector DB"

  - name: weaviate
    purpose: "Weaviate vector DB"

  - name: chroma
    purpose: "ChromaDB"

  - name: lancedb
    purpose: "LanceDB"

  - name: mongodb
    purpose: "MongoDB Atlas vector search"

  - name: redis
    purpose: "Redis vector search"

  - name: cassandra
    purpose: "Cassandra vector support"

  - name: clickhouse
    purpose: "ClickHouse vector DB"

  - name: couchbase
    purpose: "Couchbase vector search"

  - name: singlestore
    purpose: "SingleStore vector DB"

  - name: surrealdb
    purpose: "SurrealDB"

  - name: upstash
    purpose: "Upstash Redis"

  - name: langchain
    purpose: "LangChain wrapper"

  - name: llamaindex
    purpose: "LlamaIndex wrapper"

  - name: lightrag
    purpose: "LightRAG integration"

  - name: elasticsearch
    purpose: "ElasticSearch vector search"

exports:
  - {name: VectorDb, kind: type}
  - {name: PgVector, kind: type}
  - {name: Distance, kind: type}
  - {name: SearchType, kind: type}
