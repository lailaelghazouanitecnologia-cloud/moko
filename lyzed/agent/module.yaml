name: agent
purpose: "Core autonomous agent — single LLM with tools, memory, knowledge, reasoning"
tags: [core, agent, execution, streaming, structured-output, dataclass]

files:
  - file: agent.py
    lines: 1714
    purpose: "Agent dataclass — 80+ configurable fields, public API (run/arun/stream/print_response)"
    tags: [dataclass, public-api, entry-point]
    descriptor: agent.yaml

  - file: _run.py
    lines: 4576
    purpose: "Execution engine — 13-step run pipeline, sync/async, stream/non-stream, retry, cancel"
    tags: [engine, pipeline, execution, retry, cancel]
    descriptor: _run.yaml

  - file: _messages.py
    lines: ~600
    purpose: "System/user message construction — template resolution, knowledge injection, context building"
    tags: [messages, system-prompt, context, templates]

  - file: _response.py
    lines: ~800
    purpose: "Response handling — reasoning steps, output model, parser model, structured format conversion"
    tags: [response, reasoning, parser, structured-output]

  - file: _tools.py
    lines: ~500
    purpose: "Tool resolution — determine tools for model, handle tool call updates, filter tools"
    tags: [tools, resolution, filtering]

  - file: _hooks.py
    lines: ~300
    purpose: "Hook execution — pre/post hooks, guardrails, evals as hooks"
    tags: [hooks, guardrails, evals]

  - file: _storage.py
    lines: ~400
    purpose: "Session persistence — read/write session, metadata management"
    tags: [storage, session, persistence]

  - file: _session.py
    lines: ~300
    purpose: "Session lifecycle — create, load, save, delete sessions"
    tags: [session, lifecycle]

  - file: _managers.py
    lines: ~400
    purpose: "Background task management — memory futures, learning tasks, cultural knowledge"
    tags: [background, futures, asyncio, threads]

  - file: _init.py
    lines: ~300
    purpose: "Agent initialization — model setup, tool connection, MCP init"
    tags: [init, setup]

  - file: _default_tools.py
    lines: ~200
    purpose: "Built-in tools — search_knowledge, read_chat_history, update_knowledge"
    tags: [default-tools, knowledge-search]

  - file: _telemetry.py
    lines: ~100
    purpose: "Telemetry logging — anonymous usage analytics"
    tags: [telemetry, analytics]

  - file: _cli.py
    lines: ~150
    purpose: "Interactive CLI — terminal-based agent chat loop"
    tags: [cli, interactive, terminal]

  - file: _utils.py
    lines: ~100
    purpose: "Utilities — input validation, media extraction"
    tags: [utils, validation]

exports:
  - {name: Agent, kind: type}
