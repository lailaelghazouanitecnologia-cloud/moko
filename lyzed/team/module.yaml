## Roska Module Descriptor — team/
name: team
purpose: "Multi-agent coordination with 4 execution modes"
tags: [multi-agent, coordination, delegation, tasks, dataclass]

files:
  - file: team.py
    lines: 1783
    purpose: "Team dataclass — 96 fields, 60+ methods. Mirrors Agent API but adds member management and team modes."
    tags: [dataclass, public-api]
    descriptor: team.yaml

  - file: _run.py
    lines: 6262
    purpose: "Team execution engine — 40+ functions for sync/async dispatch, task loops, member delegation, continue_run"
    tags: [engine, pipeline, tasks, delegation]
    descriptor: _run.yaml

  - file: mode.py
    lines: 24
    purpose: "TeamMode enum — coordinate, route, broadcast, tasks"
    tags: [enum, modes]

exports:
  - {name: Team, kind: type}
  - {name: TeamMode, kind: type}
