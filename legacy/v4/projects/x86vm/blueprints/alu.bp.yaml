## Blueprint — alu
name: alu
language: typescript
target_dir: src/alu
description: ''
constraints: []
references: []
types:
- name: IAlu
  kind: interface
  status: translated
  target_file: src/alu/ialu.ts
  description: Arithmetic logic unit interface for x86 operations
  methods:
  - name: add
    sig: '(dest: number, src: number, size: number): number'
    hint: add with flags
  - name: sub
    sig: '(dest: number, src: number, size: number): number'
    hint: subtract with flags
  - name: mul
    sig: '(value: number, size: number): number'
    hint: unsigned multiply
  - name: imul
    sig: '(value: number, size: number): number'
    hint: signed multiply
  - name: div
    sig: '(value: number, size: number): number'
    hint: unsigned divide
  - name: idiv
    sig: '(value: number, size: number): number'
    hint: signed divide
  - name: and
    sig: '(dest: number, src: number, size: number): number'
    hint: bitwise and
  - name: or
    sig: '(dest: number, src: number, size: number): number'
    hint: bitwise or
  - name: xor
    sig: '(dest: number, src: number, size: number): number'
    hint: bitwise xor
  - name: not
    sig: '(value: number, size: number): number'
    hint: bitwise not
- name: Alu
  kind: class
  status: translated
  target_file: src/alu/alu.ts
  description: Arithmetic logic unit for x86 emulator
  fields:
  - name: cpu
    type: ICpu
  methods:
  - name: add
    sig: '(dest: number, src: number, size: number): number'
    hint: add with carry
  - name: sub
    sig: '(dest: number, src: number, size: number): number'
    hint: subtract with borrow
  - name: mul
    sig: '(value: number, size: number): number'
    hint: unsigned multiply
  - name: imul
    sig: '(value: number, size: number): number'
    hint: signed multiply
  - name: div
    sig: '(value: number, size: number): number'
    hint: unsigned divide
  - name: idiv
    sig: '(value: number, size: number): number'
    hint: signed divide
  - name: and
    sig: '(dest: number, src: number, size: number): number'
    hint: bitwise and
  - name: or
    sig: '(dest: number, src: number, size: number): number'
    hint: bitwise or
  - name: xor
    sig: '(dest: number, src: number, size: number): number'
    hint: bitwise xor
  - name: not
    sig: '(value: number, size: number): number'
    hint: bitwise not
