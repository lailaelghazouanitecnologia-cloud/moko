## Blueprint — display
name: display
language: typescript
target_dir: src/display
description: ''
constraints: []
references: []
types:
- name: IDisplay
  kind: interface
  status: translated
  target_file: src/display/idisplay.ts
  description: 64x32 pixel framebuffer interface
  methods:
  - name: drawSprite
    sig: '(x: number, y: number, spriteData: Uint8Array, height: number): boolean'
    hint: draw sprite return collision
  - name: clear
    sig: '(): void'
    hint: clear entire screen
  - name: getPixel
    sig: '(x: number, y: number): boolean'
    hint: get pixel state
  - name: setPixel
    sig: '(x: number, y: number, state: boolean): void'
    hint: set pixel state
  - name: refresh
    sig: '(): void'
    hint: notify display updated
  - name: getWidth
    sig: '(): number'
    hint: return width 64
  - name: getHeight
    sig: '(): number'
    hint: return height 32
- name: Display
  kind: class
  status: translated
  target_file: src/display/display.ts
  description: 64x32 pixel framebuffer with sprite drawing and collision detection
  fields:
  - name: framebuffer
    type: boolean[][]
  methods:
  - name: drawSprite
    sig: '(x: number, y: number, spriteData: Uint8Array, height: number): boolean'
    hint: draw sprite return collision
  - name: clear
    sig: '(): void'
    hint: clear entire screen
  - name: getPixel
    sig: '(x: number, y: number): boolean'
    hint: get pixel state
  - name: setPixel
    sig: '(x: number, y: number, state: boolean): void'
    hint: set pixel state
  - name: refresh
    sig: '(): void'
    hint: notify display updated
  - name: getWidth
    sig: '(): number'
    hint: return 64
  - name: getHeight
    sig: '(): number'
    hint: return 32
