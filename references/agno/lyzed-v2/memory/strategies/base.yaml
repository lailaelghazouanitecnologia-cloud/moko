## Roska File Descriptor — strategies/base.py
file: strategies/base.py
lines: 66
imports:
- sym: abc.ABC
- sym: abc.abstractmethod
- sym: typing.List
- sym: agno.db.schemas.UserMemory
- sym: agno.models.base.Model
- sym: agno.utils.tokens.count_text_tokens
types:
- name: MemoryOptimizationStrategy
  kind: trait
  ctx: Abstract base class for memory optimization strategies. Subclasses must implement optimize() and aoptimize(). get_system_prompt() is optional and only needed for LLM-based strategies.
  bases:
  - ABC
  methods:
  - name: get_system_prompt
    sig: get_system_prompt(self) -> str
    lines:
    - 16
    - 22
    detail: {}
  - name: optimize
    sig: |-
      optimize(
              self,
              memories: List[UserMemory],
              model: Model,
          ) -> List[UserMemory]
    decorators:
    - abstractmethod
    lines:
    - 25
    - 39
    detail: {}
  - name: aoptimize
    sig: |-
      aoptimize(
              self,
              memories: List[UserMemory],
              model: Model,
          ) -> List[UserMemory]
    is_async: true
    decorators:
    - abstractmethod
    lines:
    - 42
    - 56
    detail: {}
  - name: count_tokens
    sig: 'count_tokens(self, memories: List[UserMemory]) -> int'
    lines:
    - 58
    - 66
    detail:
      calls:
      - count_text_tokens
      - sum
  lines:
  - 9
  - 66
exports:
- name: MemoryOptimizationStrategy
  kind: type
