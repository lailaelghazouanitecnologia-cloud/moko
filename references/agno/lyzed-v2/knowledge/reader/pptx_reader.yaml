## Roska File Descriptor — reader/pptx_reader.py
file: reader/pptx_reader.py
lines: 100
imports:
- sym: asyncio
- sym: pathlib.Path
- sym: typing.IO
- sym: typing.Any
- sym: typing.List
- sym: typing.Optional
- sym: typing.Union
- sym: uuid.uuid4
- sym: agno.knowledge.chunking.document.DocumentChunking
- sym: agno.knowledge.chunking.strategy.ChunkingStrategy
- sym: agno.knowledge.chunking.strategy.ChunkingStrategyType
- sym: agno.knowledge.document.base.Document
- sym: agno.knowledge.reader.base.Reader
- sym: agno.knowledge.types.ContentType
- sym: agno.utils.log.log_debug
- sym: agno.utils.log.log_error
types:
- name: PPTXReader
  kind: struct
  ctx: Reader for PPTX files
  bases:
  - Reader
  methods:
  - name: __init__
    sig: '__init__(self, chunking_strategy: Optional[ChunkingStrategy] = DocumentChunking(), **kwargs)'
    lines:
    - 22
    - 23
    detail:
      calls:
      - super
      - super().__init__
  - name: get_supported_chunking_strategies
    sig: get_supported_chunking_strategies(cls) -> List[ChunkingStrategyType]
    decorators:
    - classmethod
    lines:
    - 26
    - 35
    detail: {}
  - name: get_supported_content_types
    sig: get_supported_content_types(cls) -> List[ContentType]
    decorators:
    - classmethod
    lines:
    - 38
    - 39
    detail: {}
  - name: read
    sig: 'read(self, file: Union[Path, IO[Any]], name: Optional[str] = None) -> List[Document]'
    lines:
    - 41
    - 92
    detail:
      calls:
      - '"\n".join'
      - '"\n\n".join'
      - Document
      - FileNotFoundError
      - Presentation
      - chunked_documents.extend
      - enumerate
      - file.exists
      - getattr
      - getattr(file, "name", "pptx_file").split
      - hasattr
      - isinstance
      - log_debug
      - log_error
      - self.chunk_document
      - shape.text.strip
      - slide_texts.append
      - str
      - text_content.append
      - uuid4
  - name: async_read
    sig: 'async_read(self, file: Union[Path, IO[Any]], name: Optional[str] = None) -> List[Document]'
    is_async: true
    lines:
    - 94
    - 100
    detail:
      calls:
      - asyncio.to_thread
      - log_error
  lines:
  - 19
  - 100
exports:
- name: PPTXReader
  kind: type
