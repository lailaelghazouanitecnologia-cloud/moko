## Roska File Descriptor — chunking/code.py
file: chunking/code.py
lines: 90
imports:
- sym: typing.Any
- sym: typing.Dict
- sym: typing.List
- sym: typing.Literal
- sym: typing.Optional
- sym: typing.Union
- sym: agno.knowledge.chunking.strategy.ChunkingStrategy
- sym: agno.knowledge.document.base.Document
types:
- name: CodeChunking
  kind: struct
  ctx: 'Splits code into chunks based on its structure, leveraging Abstract Syntax Trees (ASTs) to create contextually relevant segments using Chonkie. Args: tokenizer: The tokenizer to use. Can be a string name or a TokenizerProtocol instance. chunk_size: The size of the chunks to create. language: The language to parse. Use "auto" for detection or specify a tree-sitter-language-pack language. include_nodes: Whether to include AST nodes (Note: Chonkie''s base Chunk type does not store node information). chunker_params: Additional parameters to pass to Chonkie''s CodeChunker.'
  bases:
  - ChunkingStrategy
  fields:
  - name: tokenizer
    type: Any
    default: tokenizer
  - name: chunk_size
    type: Any
    default: chunk_size
  - name: language
    type: Any
    default: language
  - name: include_nodes
    type: Any
    default: include_nodes
  - name: chunker_params
    type: Any
    default: chunker_params
  - name: chunker
    type: Any
    default: Optional[CodeChunker]
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              tokenizer: Union[str, TokenizerProtocol] = "character",
              chunk_size: int = 2048,
              language: Union[Literal["auto"], Any] = "auto",
              include_nodes: bool = False,
              chunker_params: Optional[Dict[str, Any]] = None,
          )
    lines:
    - 27
    - 40
    detail: {}
  - name: _initialize_chunker
    sig: _initialize_chunker(self)
    vis: protected
    lines:
    - 42
    - 64
    detail:
      calls:
      - CodeChunker
      - ImportError
      - _chunker_params.update
      - str
  - name: chunk
    sig: 'chunk(self, document: Document) -> List[Document]'
    lines:
    - 66
    - 90
    detail:
      calls:
      - Document
      - RuntimeError
      - chunked_documents.append
      - document.meta_data.copy
      - enumerate
      - len
      - self._generate_chunk_id
      - self._initialize_chunker
      - self.chunker.chunk
  lines:
  - 16
  - 90
exports:
- name: CodeChunking
  kind: type
