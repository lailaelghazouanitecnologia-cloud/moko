## Roska File Descriptor — chunking/agentic.py
file: chunking/agentic.py
lines: 75
imports:
- sym: typing.List
- sym: typing.Optional
- sym: typing.Union
- sym: agno.knowledge.chunking.strategy.ChunkingStrategy
- sym: agno.knowledge.document.base.Document
- sym: agno.models.base.Model
- sym: agno.models.defaults.DEFAULT_OPENAI_MODEL_ID
- sym: agno.models.message.Message
- sym: agno.models.utils.get_model
types:
- name: AgenticChunking
  kind: struct
  ctx: Chunking strategy that uses an LLM to determine natural breakpoints in the text
  bases:
  - ChunkingStrategy
  fields:
  - name: chunk_size
    type: Any
    default: max_chunk_size
  - name: model
    type: Any
    default: model
  methods:
  - name: __init__
    sig: '__init__(self, model: Optional[Union[Model, str]] = None, max_chunk_size: int = 5000)'
    lines:
    - 14
    - 24
    detail:
      calls:
      - OpenAIChat
      - ValueError
      - get_model
  - name: chunk
    sig: 'chunk(self, document: Document) -> List[Document]'
    lines:
    - 26
    - 75
    detail:
      calls:
      - Document
      - Message
      - chunk_meta_data.copy
      - chunks.append
      - int
      - len
      - min
      - remaining_text[:break_point].strip
      - remaining_text[break_point:].strip
      - response.content.strip
      - self._generate_chunk_id
      - self.clean_text
      - self.model.response
  lines:
  - 11
  - 75
exports:
- name: AgenticChunking
  kind: type
