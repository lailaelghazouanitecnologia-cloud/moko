## Roska File Descriptor — tools/fal.py
file: tools/fal.py
lines: 127
purpose: pip install fal-client
imports:
- sym: os.getenv
- sym: typing.Optional
- sym: typing.Union
- sym: uuid.uuid4
- sym: agno.agent.Agent
- sym: agno.media.Image
- sym: agno.media.Video
- sym: agno.team.team.Team
- sym: agno.tools.Toolkit
- sym: agno.tools.function.ToolResult
- sym: agno.utils.log.log_info
- sym: agno.utils.log.logger
types:
- name: FalTools
  kind: struct
  bases:
  - Toolkit
  fields:
  - name: api_key
    type: Any
    default: api_key or getenv("FAL_API_KEY")
  - name: model
    type: Any
    default: model
  - name: seen_logs
    type: Any
    default: set[str]
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              api_key: Optional[str] = None,
              model: str = "fal-ai/hunyuan-video",
              enable_generate_media: bool = True,
              enable_image_to_image: bool = False,
              all: bool = False,
              **kwargs,
          )
    lines:
    - 23
    - 44
    detail:
      calls:
      - getenv
      - logger.error
      - set
      - super
      - super().__init__
      - tools.append
  - name: on_queue_update
    sig: on_queue_update(self, update)
    lines:
    - 46
    - 52
    detail:
      calls:
      - isinstance
      - log_info
      - self.seen_logs.add
  - name: generate_media
    sig: 'generate_media(self, agent: Union[Agent, Team], prompt: str) -> ToolResult'
    lines:
    - 54
    - 93
    detail:
      calls:
      - Image
      - ToolResult
      - Video
      - fal_client.subscribe
      - logger.error
      - result.get
      - result.get("image", {}).get
      - result.get("video", {}).get
      - str
      - uuid4
  - name: image_to_image
    sig: 'image_to_image(self, agent: Union[Agent, Team], prompt: str, image_url: Optional[str] = None) -> ToolResult'
    lines:
    - 95
    - 127
    detail:
      calls:
      - Image
      - ToolResult
      - fal_client.subscribe
      - logger.error
      - result.get
      - result.get("images", [{}])[0].get
      - str
      - uuid4
  lines:
  - 22
  - 127
exports:
- name: FalTools
  kind: type
