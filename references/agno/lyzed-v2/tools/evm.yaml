## Roska File Descriptor — tools/evm.py
file: tools/evm.py
lines: 129
imports:
- sym: os.getenv
- sym: typing.Optional
- sym: agno.tools.Toolkit
- sym: agno.utils.log.log_debug
- sym: agno.utils.log.log_error
types:
- name: EvmTools
  kind: struct
  bases:
  - Toolkit
  fields:
  - name: private_key
    type: Any
    default: private_key or getenv("EVM_PRIVATE_KEY")
  - name: rpc_url
    type: Any
    default: rpc_url or getenv("EVM_RPC_URL")
  - name: web3_client
    type: str
    default: '"Web3Type"'
  - name: account
    type: str
    default: '"LocalAccount"'
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              private_key: Optional[str] = None,
              rpc_url: Optional[str] = None,
              enable_send_transaction: bool = True,
              all: bool = False,
              **kwargs,
          )
    lines:
    - 20
    - 59
    detail:
      calls:
      - HTTPProvider
      - ValueError
      - Web3
      - getenv
      - log_debug
      - log_error
      - self.private_key.startswith
      - self.web3_client.eth.account.from_key
      - super
      - super().__init__
      - tools.append
  - name: get_max_priority_fee_per_gas
    sig: get_max_priority_fee_per_gas(self) -> int
    lines:
    - 61
    - 68
    detail:
      calls:
      - self.web3_client.to_wei
  - name: get_max_fee_per_gas
    sig: 'get_max_fee_per_gas(self, max_priority_fee_per_gas: int) -> int'
    lines:
    - 70
    - 85
    detail:
      calls:
      - ValueError
      - latest_block.get
      - log_error
      - self.web3_client.eth.get_block
  - name: send_transaction
    sig: 'send_transaction(self, to_address: str, amount_in_wei: int) -> str'
    lines:
    - 87
    - 129
    detail:
      calls:
      - Exception
      - log_debug
      - log_error
      - self.get_max_fee_per_gas
      - self.get_max_priority_fee_per_gas
      - self.web3_client.eth.account.sign_transaction
      - self.web3_client.eth.get_transaction_count
      - self.web3_client.eth.send_raw_transaction
      - self.web3_client.eth.wait_for_transaction_receipt
      - transaction_hash.hex
      - transaction_receipt.get
  lines:
  - 19
  - 129
exports:
- name: EvmTools
  kind: type
