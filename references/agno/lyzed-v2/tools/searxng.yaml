## Roska File Descriptor — tools/searxng.py
file: tools/searxng.py
lines: 152
imports:
- sym: json
- sym: urllib.parse
- sym: typing.Any
- sym: typing.List
- sym: typing.Optional
- sym: httpx
- sym: agno.tools.Toolkit
- sym: agno.utils.log.log_info
data:
- name: SearxngTools
  type: Any
  val: Searxng
types:
- name: Searxng
  kind: struct
  bases:
  - Toolkit
  fields:
  - name: host
    type: Any
    default: host
  - name: engines
    type: Any
    default: engines
  - name: fixed_max_results
    type: Any
    default: fixed_max_results
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              host: str,
              engines: List[str] = [],
              fixed_max_results: Optional[int] = None,
              **kwargs,
          )
    lines:
    - 12
    - 34
    detail:
      calls:
      - super
      - super().__init__
  - name: search_web
    sig: 'search_web(self, query: str, max_results: int = 5) -> str'
    lines:
    - 36
    - 46
    detail:
      calls:
      - self._search
  - name: image_search
    sig: 'image_search(self, query: str, max_results: int = 5) -> str'
    lines:
    - 48
    - 58
    detail:
      calls:
      - self._search
  - name: it_search
    sig: 'it_search(self, query: str, max_results: int = 5) -> str'
    lines:
    - 60
    - 70
    detail:
      calls:
      - self._search
  - name: map_search
    sig: 'map_search(self, query: str, max_results: int = 5) -> str'
    lines:
    - 72
    - 82
    detail:
      calls:
      - self._search
  - name: music_search
    sig: 'music_search(self, query: str, max_results: int = 5) -> str'
    lines:
    - 84
    - 94
    detail:
      calls:
      - self._search
  - name: news_search
    sig: 'news_search(self, query: str, max_results: int = 5) -> str'
    lines:
    - 96
    - 106
    detail:
      calls:
      - self._search
  - name: science_search
    sig: 'science_search(self, query: str, max_results: int = 5) -> str'
    lines:
    - 108
    - 118
    detail:
      calls:
      - self._search
  - name: video_search
    sig: 'video_search(self, query: str, max_results: int = 5) -> str'
    lines:
    - 120
    - 130
    detail:
      calls:
      - self._search
  - name: _search
    sig: '_search(self, query: str, category: Optional[str] = None, max_results: int = 5) -> str'
    vis: protected
    lines:
    - 132
    - 148
    detail:
      calls:
      - ''',''.join'
      - httpx.get
      - httpx.get(url).json
      - json.dumps
      - log_info
      - urllib.parse.quote
  lines:
  - 11
  - 148
exports:
- name: Searxng
  kind: type
- name: SearxngTools
  kind: data
