## Roska File Descriptor — tools/clickup.py
file: tools/clickup.py
lines: 244
imports:
- sym: json
- sym: re
- sym: os.getenv
- sym: typing.Any
- sym: typing.Dict
- sym: typing.List
- sym: typing.Optional
- sym: agno.tools.Toolkit
- sym: agno.utils.log.log_debug
- sym: agno.utils.log.logger
types:
- name: ClickUpTools
  kind: struct
  bases:
  - Toolkit
  fields:
  - name: api_key
    type: Any
    default: api_key or getenv("CLICKUP_API_KEY")
  - name: master_space_id
    type: Any
    default: master_space_id or getenv("MASTER_SPACE_ID")
  - name: base_url
    type: str
    default: '"https://api.clickup.com/api/v2"'
  - name: headers
    type: dict
    default: '{"Authorization": self.api_key}'
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              api_key: Optional[str] = None,
              master_space_id: Optional[str] = None,
              **kwargs,
          )
    lines:
    - 16
    - 42
    detail:
      calls:
      - ValueError
      - getenv
      - super
      - super().__init__
  - name: _make_request
    sig: |-
      _make_request(
              self, method: str, endpoint: str, params: Optional[Dict] = None, data: Optional[Dict] = None
          ) -> Dict[str, Any]
    vis: protected
    lines:
    - 44
    - 55
    detail:
      calls:
      - logger.error
      - requests.request
      - response.json
      - response.raise_for_status
      - str
  - name: _find_by_name
    sig: '_find_by_name(self, items: List[Dict[str, Any]], name: str) -> Optional[Dict[str, Any]]'
    vis: protected
    lines:
    - 57
    - 78
    detail:
      calls:
      - item["name"].lower
      - name.lower
      - pattern.search
      - re.compile
  - name: _get_space
    sig: '_get_space(self, space_name: str) -> Dict[str, Any]'
    vis: protected
    lines:
    - 80
    - 93
    detail:
      calls:
      - self._find_by_name
      - self._make_request
      - spaces.get
  - name: _get_list
    sig: '_get_list(self, space_id: str, list_name: str) -> Dict[str, Any]'
    vis: protected
    lines:
    - 95
    - 108
    detail:
      calls:
      - lists.get
      - self._find_by_name
      - self._make_request
  - name: _get_tasks
    sig: '_get_tasks(self, list_id: str) -> List[Dict[str, Any]]'
    vis: protected
    lines:
    - 110
    - 117
    detail:
      calls:
      - self._make_request
      - tasks.get
  - name: list_tasks
    sig: 'list_tasks(self, space_name: str) -> str'
    lines:
    - 119
    - 147
    detail:
      calls:
      - all_tasks.extend
      - json.dumps
      - lists.get
      - self._get_space
      - self._get_tasks
      - self._make_request
  - name: create_task
    sig: 'create_task(self, space_name: str, task_name: str, task_description: str) -> str'
    lines:
    - 149
    - 178
    detail:
      calls:
      - json.dumps
      - log_debug
      - response.get
      - self._get_space
      - self._make_request
  - name: list_spaces
    sig: list_spaces(self) -> str
    lines:
    - 180
    - 187
    detail:
      calls:
      - json.dumps
      - self._make_request
  - name: list_lists
    sig: 'list_lists(self, space_name: str) -> str'
    lines:
    - 189
    - 205
    detail:
      calls:
      - json.dumps
      - self._get_space
      - self._make_request
  - name: get_task
    sig: 'get_task(self, task_id: str) -> str'
    lines:
    - 207
    - 217
    detail:
      calls:
      - json.dumps
      - self._make_request
  - name: update_task
    sig: 'update_task(self, task_id: str, **kwargs) -> str'
    lines:
    - 219
    - 230
    detail:
      calls:
      - json.dumps
      - self._make_request
  - name: delete_task
    sig: 'delete_task(self, task_id: str) -> str'
    lines:
    - 232
    - 244
    detail:
      calls:
      - json.dumps
      - self._make_request
  lines:
  - 15
  - 244
exports:
- name: ClickUpTools
  kind: type
