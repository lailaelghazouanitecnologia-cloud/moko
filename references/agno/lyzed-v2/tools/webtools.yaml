## Roska File Descriptor — tools/webtools.py
file: tools/webtools.py
lines: 45
imports:
- sym: httpx
- sym: agno.tools.Toolkit
- sym: agno.utils.log.logger
types:
- name: WebTools
  kind: struct
  ctx: A toolkit for working with web-related tools.
  bases:
  - Toolkit
  fields:
  - name: retries
    type: Any
    default: retries
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              retries: int = 3,
              enable_expand_url: bool = True,
              all: bool = False,
              **kwargs,
          )
    lines:
    - 12
    - 25
    detail:
      calls:
      - super
      - super().__init__
      - tools.append
  - name: expand_url
    sig: 'expand_url(self, url: str) -> str'
    lines:
    - 27
    - 45
    detail:
      calls:
      - httpx.head
      - logger.error
      - logger.info
      - range
      - str
  lines:
  - 7
  - 45
exports:
- name: WebTools
  kind: type
