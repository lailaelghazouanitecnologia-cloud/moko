## Roska File Descriptor — tools/agentql.py
file: tools/agentql.py
lines: 120
imports:
- sym: os.getenv
- sym: typing.Any
- sym: typing.List
- sym: typing.Optional
- sym: agno.tools.Toolkit
- sym: agno.utils.log.log_info
types:
- name: AgentQLTools
  kind: struct
  bases:
  - Toolkit
  fields:
  - name: api_key
    type: Any
    default: api_key or getenv("AGENTQL_API_KEY")
  - name: agentql_query
    type: Any
    default: agentql_query
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              api_key: Optional[str] = None,
              enable_scrape_website: bool = True,
              enable_custom_scrape_website: bool = False,
              all: bool = False,
              agentql_query: str = "",
              **kwargs,
          )
    lines:
    - 15
    - 38
    detail:
      calls:
      - ValueError
      - getenv
      - log_info
      - super
      - super().__init__
      - tools.append
  - name: scrape_website
    sig: 'scrape_website(self, url: str) -> str'
    lines:
    - 40
    - 80
    detail:
      calls:
      - '" ".join'
      - agentql.wrap
      - browser.new_page
      - isinstance
      - item.strip
      - list
      - page.goto
      - page.query_data
      - playwright.chromium.launch
      - set
      - sync_playwright
  - name: custom_scrape_website
    sig: 'custom_scrape_website(self, url: str) -> str'
    lines:
    - 82
    - 120
    detail:
      calls:
      - '" ".join'
      - agentql.wrap
      - browser.new_page
      - isinstance
      - list
      - page.goto
      - page.query_data
      - playwright.chromium.launch
      - set
      - sync_playwright
  lines:
  - 14
  - 120
exports:
- name: AgentQLTools
  kind: type
