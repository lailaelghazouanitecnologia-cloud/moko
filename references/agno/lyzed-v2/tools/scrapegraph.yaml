## Roska File Descriptor — tools/scrapegraph.py
file: tools/scrapegraph.py
lines: 222
imports:
- sym: json
- sym: os.getenv
- sym: typing.Any
- sym: typing.List
- sym: typing.Optional
- sym: agno.tools.Toolkit
- sym: agno.utils.log.log_debug
- sym: agno.utils.log.log_error
types:
- name: ScrapeGraphTools
  kind: struct
  bases:
  - Toolkit
  fields:
  - name: api_key
    type: Any
    default: Optional[str]
  - name: client
    type: Any
    default: Client(api_key=self.api_key)
  - name: render_heavy_js
    type: Any
    default: render_heavy_js
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              api_key: Optional[str] = None,
              enable_smartscraper: bool = True,
              enable_markdownify: bool = False,
              enable_crawl: bool = False,
              enable_searchscraper: bool = False,
              enable_agentic_crawler: bool = False,
              enable_scrape: bool = False,
              render_heavy_js: bool = False,
              all: bool = False,
              **kwargs,
          )
    lines:
    - 19
    - 55
    detail:
      calls:
      - Client
      - getenv
      - super
      - super().__init__
      - tools.append
  - name: smartscraper
    sig: 'smartscraper(self, url: str, prompt: str) -> str'
    lines:
    - 57
    - 72
    detail:
      calls:
      - json.dumps
      - log_debug
      - log_error
      - self.client.smartscraper
      - str
  - name: markdownify
    sig: 'markdownify(self, url: str) -> str'
    lines:
    - 74
    - 88
    detail:
      calls:
      - log_debug
      - log_error
      - self.client.markdownify
      - str
  - name: crawl
    sig: |-
      crawl(
              self,
              url: str,
              prompt: str,
              schema: dict,
              cache_website: bool = True,
              depth: int = 2,
              max_pages: int = 2,
              same_domain_only: bool = True,
              batch_size: int = 1,
          ) -> str
    lines:
    - 90
    - 130
    detail:
      calls:
      - json.dumps
      - log_debug
      - log_error
      - self.client.crawl
      - str
  - name: agentic_crawler
    sig: |-
      agentic_crawler(
              self,
              url: str,
              steps: List[str],
              use_session: bool = True,
              user_prompt: Optional[str] = None,
              output_schema: Optional[dict] = None,
              ai_extraction: bool = False,
          ) -> str
    lines:
    - 132
    - 179
    detail:
      calls:
      - json.dumps
      - log_debug
      - log_error
      - self.client.agenticscraper
      - str
  - name: searchscraper
    sig: 'searchscraper(self, user_prompt: str) -> str'
    lines:
    - 181
    - 195
    detail:
      calls:
      - json.dumps
      - log_debug
      - log_error
      - self.client.searchscraper
      - str
  - name: scrape
    sig: |-
      scrape(
              self,
              website_url: str,
              headers: Optional[dict] = None,
          ) -> str
    lines:
    - 197
    - 222
    detail:
      calls:
      - json.dumps
      - log_debug
      - log_error
      - self.client.scrape
      - str
  lines:
  - 18
  - 222
exports:
- name: ScrapeGraphTools
  kind: type
