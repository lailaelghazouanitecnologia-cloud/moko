## Roska File Descriptor — tools/notion.py
file: tools/notion.py
lines: 204
imports:
- sym: json
- sym: os
- sym: typing.Any
- sym: typing.Dict
- sym: typing.List
- sym: typing.Optional
- sym: typing.cast
- sym: agno.tools.Toolkit
- sym: agno.utils.log.log_debug
- sym: agno.utils.log.logger
types:
- name: NotionTools
  kind: struct
  ctx: 'Notion toolkit for creating and managing Notion pages. Args: api_key (Optional[str]): Notion API key (integration token). If not provided, uses NOTION_API_KEY env var. database_id (Optional[str]): The ID of the database to work with. If not provided, uses NOTION_DATABASE_ID env var. enable_create_page (bool): Enable creating pages. Default is True. enable_update_page (bool): Enable updating pages. Default is True. enable_search_pages (bool): Enable searching pages. Default is True. all (bool): Enable all tools. Overrides individual flags when True. Default is False.'
  bases:
  - Toolkit
  fields:
  - name: api_key
    type: Any
    default: api_key or os.getenv("NOTION_API_KEY")
  - name: database_id
    type: Any
    default: database_id or os.getenv("NOTION_DATABASE_ID")
  - name: client
    type: Any
    default: Client(auth=self.api_key)
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              api_key: Optional[str] = None,
              database_id: Optional[str] = None,
              enable_create_page: bool = True,
              enable_update_page: bool = True,
              enable_search_pages: bool = True,
              all: bool = False,
              **kwargs,
          )
    lines:
    - 27
    - 59
    detail:
      calls:
      - Client
      - ValueError
      - os.getenv
      - super
      - super().__init__
      - tools.append
  - name: create_page
    sig: 'create_page(self, title: str, tag: str, content: str) -> str'
    lines:
    - 61
    - 96
    detail:
      calls:
      - cast
      - json.dumps
      - log_debug
      - logger.exception
      - self.client.pages.create
      - str
  - name: update_page
    sig: 'update_page(self, page_id: str, content: str) -> str'
    lines:
    - 98
    - 128
    detail:
      calls:
      - json.dumps
      - log_debug
      - logger.exception
      - self.client.blocks.children.append
      - str
  - name: search_pages
    sig: 'search_pages(self, tag: str) -> str'
    lines:
    - 130
    - 204
    detail:
      calls:
      - data.get
      - httpx.post
      - json.dumps
      - len
      - log_debug
      - logger.exception
      - page.get
      - page.get("properties", {}).get
      - page.get("properties", {}).get("Name", {}).get
      - page.get("properties", {}).get("Tag", {}).get
      - pages.append
      - response.json
      - str
  lines:
  - 14
  - 204
exports:
- name: NotionTools
  kind: type
