## Roska File Descriptor — tools/newspaper4k.py
file: tools/newspaper4k.py
lines: 93
imports:
- sym: json
- sym: typing.Any
- sym: typing.Dict
- sym: typing.Optional
- sym: agno.tools.Toolkit
- sym: agno.utils.log.log_debug
- sym: agno.utils.log.logger
types:
- name: Newspaper4kTools
  kind: struct
  ctx: 'Newspaper4kTools is a toolkit for getting the text of an article from a URL. Args: enable_read_article (bool): Whether to read an article from a URL. include_summary (bool): Whether to include the summary of an article. article_length (Optional[int]): The length of the article to read.'
  bases:
  - Toolkit
  fields:
  - name: include_summary
    type: Any
    default: bool
  - name: article_length
    type: Any
    default: Optional[int]
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              include_summary: bool = False,
              article_length: Optional[int] = None,
              enable_read_article: bool = True,
              all: bool = False,
              **kwargs,
          )
    lines:
    - 22
    - 37
    detail:
      calls:
      - super
      - super().__init__
      - tools.append
  - name: get_article_data
    sig: 'get_article_data(self, url: str) -> Optional[Dict[str, Any]]'
    lines:
    - 39
    - 70
    detail:
      calls:
      - article.publish_date.isoformat
      - logger.warning
      - newspaper.article
  - name: read_article
    sig: 'read_article(self, url: str) -> str'
    lines:
    - 72
    - 93
    detail:
      calls:
      - json.dumps
      - log_debug
      - self.get_article_data
  lines:
  - 13
  - 93
exports:
- name: Newspaper4kTools
  kind: type
