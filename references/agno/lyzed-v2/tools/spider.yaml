## Roska File Descriptor — tools/spider.py
file: tools/spider.py
lines: 116
imports:
- sym: json
- sym: typing.Any
- sym: typing.List
- sym: typing.Optional
- sym: agno.tools.Toolkit
- sym: agno.utils.log.log_info
- sym: agno.utils.log.logger
types:
- name: SpiderTools
  kind: struct
  ctx: 'Spider is a toolkit for web searching, scraping, and crawling. Args: enable_search (bool): Enable web search functionality. Default is True. enable_scrape (bool): Enable web scraping functionality. Default is True. enable_crawl (bool): Enable web crawling functionality. Default is True. all (bool): Enable all tools. Overrides individual flags when True. Default is False. max_results (Optional[int]): Default maximum number of results. url (Optional[str]): Default URL for operations. optional_params (Optional[dict]): Additional parameters for operations.'
  bases:
  - Toolkit
  fields:
  - name: max_results
    type: Any
    default: max_results
  - name: url
    type: Any
    default: url
  - name: optional_params
    type: Any
    default: optional_params or {}
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              max_results: Optional[int] = None,
              url: Optional[str] = None,
              optional_params: Optional[dict] = None,
              enable_search: bool = True,
              enable_scrape: bool = True,
              enable_crawl: bool = True,
              all: bool = False,
              **kwargs,
          )
    lines:
    - 28
    - 51
    detail:
      calls:
      - super
      - super().__init__
      - tools.append
  - name: search_web
    sig: 'search_web(self, query: str, max_results: int = 5) -> str'
    lines:
    - 53
    - 62
    detail:
      calls:
      - self._search
  - name: scrape
    sig: 'scrape(self, url: str) -> str'
    lines:
    - 64
    - 71
    detail:
      calls:
      - self._scrape
  - name: crawl
    sig: 'crawl(self, url: str, limit: Optional[int] = None) -> str'
    lines:
    - 73
    - 81
    detail:
      calls:
      - self._crawl
  - name: _search
    sig: '_search(self, query: str, max_results: int = 1) -> str'
    vis: protected
    lines:
    - 83
    - 92
    detail:
      calls:
      - ExternalSpider
      - app.search
      - json.dumps
      - log_info
      - logger.error
  - name: _scrape
    sig: '_scrape(self, url: str) -> str'
    vis: protected
    lines:
    - 94
    - 103
    detail:
      calls:
      - ExternalSpider
      - app.scrape_url
      - json.dumps
      - log_info
      - logger.error
  - name: _crawl
    sig: '_crawl(self, url: str, limit: Optional[int] = None) -> str'
    vis: protected
    lines:
    - 105
    - 116
    detail:
      calls:
      - ExternalSpider
      - app.crawl_url
      - json.dumps
      - log_info
      - logger.error
  lines:
  - 14
  - 116
exports:
- name: SpiderTools
  kind: type
