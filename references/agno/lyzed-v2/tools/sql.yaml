## Roska File Descriptor — tools/sql.py
file: tools/sql.py
lines: 154
imports:
- sym: json
- sym: typing.Any
- sym: typing.Dict
- sym: typing.List
- sym: typing.Optional
- sym: agno.tools.Toolkit
- sym: agno.utils.log.log_debug
- sym: agno.utils.log.logger
types:
- name: SQLTools
  kind: struct
  bases:
  - Toolkit
  fields:
  - name: db_engine
    type: Any
    default: Engine
  - name: Session
    type: Any
    default: sessionmaker[Session]
  - name: schema
    type: Any
    default: schema
  - name: tables
    type: Any
    default: Optional[Dict[str, Any]]
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              db_url: Optional[str] = None,
              db_engine: Optional[Engine] = None,
              user: Optional[str] = None,
              password: Optional[str] = None,
              host: Optional[str] = None,
              port: Optional[int] = None,
              schema: Optional[str] = None,
              dialect: Optional[str] = None,
              tables: Optional[Dict[str, Any]] = None,
              enable_list_tables: bool = True,
              enable_describe_table: bool = True,
              enable_run_sql_query: bool = True,
              all: bool = False,
              **kwargs,
          )
    lines:
    - 17
    - 64
    detail:
      calls:
      - ValueError
      - create_engine
      - sessionmaker
      - super
      - super().__init__
      - tools.append
  - name: list_tables
    sig: list_tables(self) -> str
    lines:
    - 66
    - 86
    detail:
      calls:
      - inspect
      - inspector.get_table_names
      - json.dumps
      - log_debug
      - logger.error
  - name: describe_table
    sig: 'describe_table(self, table_name: str) -> str'
    lines:
    - 88
    - 110
    detail:
      calls:
      - inspect
      - inspector.get_columns
      - json.dumps
      - log_debug
      - logger.error
      - str
  - name: run_sql_query
    sig: 'run_sql_query(self, query: str, limit: Optional[int] = 10) -> str'
    lines:
    - 112
    - 128
    detail:
      calls:
      - json.dumps
      - logger.error
      - self.run_sql
  - name: run_sql
    sig: 'run_sql(self, sql: str, limit: Optional[int] = None) -> List[dict]'
    lines:
    - 130
    - 154
    detail:
      calls:
      - log_debug
      - logger.error
      - result.fetchall
      - result.fetchmany
      - row._asdict
      - self.Session
      - sess.begin
      - sess.execute
      - text
  lines:
  - 16
  - 154
exports:
- name: SQLTools
  kind: type
