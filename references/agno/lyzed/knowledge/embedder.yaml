## Roska File Descriptor — knowledge/embedder/base.py
file: knowledge/embedder/base.py
lines: ~30
purpose: "Embedder ABC — abstract base for 18 embedding providers"
tags: [abc, embedder, vectors]
ctx: "@dataclass. All embedding providers inherit this. Defines get_embedding/async_get_embedding as core interface."

types:
  - name: Embedder
    kind: trait
    vis: pub
    ctx: "Abstract base for all embedding providers"
    fields:
      - {name: dimensions, type: "Optional[int]", ctx: "Embedding dimensions (default: 1536)"}
      - {name: enable_batch, type: bool, ctx: "Enable batch embedding (default: False)"}
      - {name: batch_size, type: int, ctx: "Batch size (default: 100)"}
    methods:
      - "get_embedding(text: str) -> List[float]"
      - "get_embedding_and_usage(text: str) -> Tuple[List[float], Optional[Dict]]"
      - "async_get_embedding(text: str) -> List[float]"
      - "async_get_embedding_and_usage(text: str) -> Tuple[List[float], Optional[Dict]]"

data:
  - name: providers
    type: "conceptual"
    ctx: |
      18 embedding providers:
        OpenAI, Azure OpenAI, Anthropic, Google, Cohere, Mistral, Jina,
        Voyage AI, Together, Fireworks, AWS Bedrock, HuggingFace,
        Sentence Transformer, FastEmbed, LangDB, Ollama, VLLM, Nebius
