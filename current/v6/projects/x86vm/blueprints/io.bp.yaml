## Blueprint — io
name: io
language: typescript
target_dir: src/io
description: ''
constraints: []
references: []
types:
- name: IIo
  kind: interface
  status: translated
  target_file: src/io/iio.ts
  description: I/O subsystem interface for port-mapped I/O and device routing
  methods:
  - name: readPort
    sig: '(port: number, size: number): number'
    hint: read from port
  - name: writePort
    sig: '(port: number, value: number, size: number): void'
    hint: write to port
  - name: registerDevice
    sig: '(port: number, device: IDevice): void'
    hint: register device handler
  - name: setPortPermission
    sig: '(port: number, allowed: boolean): void'
    hint: set port access
- name: Io
  kind: class
  status: translated
  target_file: src/io/io.ts
  description: Manages 65536 port-mapped I/O accesses and device routing
  fields:
  - name: devices
    type: Map<number, IDevice>
  - name: permissions
    type: boolean[]
  methods:
  - name: readPort
    sig: '(port: number, size: number): number'
    hint: read from port
  - name: writePort
    sig: '(port: number, value: number, size: number): void'
    hint: write to port
  - name: registerDevice
    sig: '(port: number, device: IDevice): void'
    hint: bind device to port
  - name: setPortPermission
    sig: '(port: number, allowed: boolean): void'
    hint: set port access
  - name: hasDevice
    sig: '(port: number): boolean'
    hint: check device exists
  - name: checkPermission
    sig: '(port: number): boolean'
    hint: verify port access
