"""
views — Terminal UI components for the ava CLI.

Zero external dependencies. Uses only ANSI escape codes
for colors, styles, and formatting. Degrades gracefully
when NO_COLOR is set or terminal doesn't support colors.
"""
