## Roska File Descriptor — chunking/fixed.py
file: chunking/fixed.py
lines: 56
imports:
- sym: typing.List
- sym: agno.knowledge.chunking.strategy.ChunkingStrategy
- sym: agno.knowledge.document.base.Document
types:
- name: FixedSizeChunking
  kind: struct
  ctx: Chunking strategy that splits text into fixed-size chunks with optional overlap
  bases:
  - ChunkingStrategy
  fields:
  - name: chunk_size
    type: Any
    default: chunk_size
  - name: overlap
    type: Any
    default: overlap
  methods:
  - name: __init__
    sig: '__init__(self, chunk_size: int = 5000, overlap: int = 0)'
    lines:
    - 10
    - 16
    detail:
      calls:
      - ValueError
  - name: chunk
    sig: 'chunk(self, document: Document) -> List[Document]'
    lines:
    - 18
    - 56
    detail:
      calls:
      - Document
      - chunk_meta_data.copy
      - chunked_documents.append
      - len
      - max
      - min
      - self._generate_chunk_id
      - self.clean_text
  lines:
  - 7
  - 56
exports:
- name: FixedSizeChunking
  kind: type
