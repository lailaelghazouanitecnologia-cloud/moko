## Roska File Descriptor — utils/mcp.py
file: utils/mcp.py
lines: 255
imports:
- sym: json
- sym: functools.partial
- sym: typing.TYPE_CHECKING
- sym: typing.Optional
- sym: typing.Union
- sym: uuid.uuid4
- sym: agno.utils.log.log_debug
- sym: agno.utils.log.log_exception
- sym: agno.media.Image
- sym: agno.tools.function.ToolResult
functions:
- name: get_entrypoint_for_tool
  sig: |-
    get_entrypoint_for_tool(
        tool: MCPTool,
        session: ClientSession,
        mcp_tools_instance: Optional[Union["MCPTools", "MultiMCPTools"]] = None,
        server_idx: int = 0,
    )
  lines:
  - 27
  - 169
  detail:
    calls:
    - partial
- name: prepare_command
  sig: 'prepare_command(command: str) -> list[str]'
  lines:
  - 172
  - 255
  detail:
    calls:
    - ValueError
    - any
    - executable.startswith
    - first_part.split
    - first_part.startswith
    - os.path.isfile
    - parts[0].split
    - shutil.which
    - split
exports:
- name: get_entrypoint_for_tool
  kind: func
- name: prepare_command
  kind: func
