## Roska File Descriptor — tools/bravesearch.py
file: tools/bravesearch.py
lines: 106
imports:
- sym: json
- sym: os.getenv
- sym: typing.Optional
- sym: agno.tools.Toolkit
- sym: agno.utils.log.log_info
types:
- name: BraveSearchTools
  kind: struct
  ctx: 'BraveSearch is a toolkit for searching Brave easily. Args: api_key (str, optional): Brave API key. If not provided, will use BRAVE_API_KEY environment variable. fixed_max_results (Optional[int]): A fixed number of maximum results. fixed_language (Optional[str]): A fixed language for the search results.'
  bases:
  - Toolkit
  fields:
  - name: api_key
    type: Any
    default: api_key or getenv("BRAVE_API_KEY")
  - name: fixed_max_results
    type: Any
    default: fixed_max_results
  - name: fixed_language
    type: Any
    default: fixed_language
  - name: brave_client
    type: Any
    default: Brave(api_key=self.api_key)
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              api_key: Optional[str] = None,
              fixed_max_results: Optional[int] = None,
              fixed_language: Optional[str] = None,
              enable_brave_search: bool = True,
              all: bool = False,
              **kwargs,
          )
    lines:
    - 24
    - 50
    detail:
      calls:
      - Brave
      - ValueError
      - getenv
      - super
      - super().__init__
      - tools.append
  - name: brave_search
    sig: |-
      brave_search(
              self,
              query: str,
              max_results: int = 5,
              country: str = "US",
              search_lang: str = "en",
          ) -> str
    lines:
    - 52
    - 106
    detail:
      calls:
      - hasattr
      - json.dumps
      - len
      - log_info
      - self.brave_client.search
      - str
      - web_results.append
  lines:
  - 14
  - 106
exports:
- name: BraveSearchTools
  kind: type
