## Roska File Descriptor — tools/resend.py
file: tools/resend.py
lines: 62
imports:
- sym: os.getenv
- sym: typing.Any
- sym: typing.List
- sym: typing.Optional
- sym: agno.tools.Toolkit
- sym: agno.utils.log.log_info
- sym: agno.utils.log.logger
types:
- name: ResendTools
  kind: struct
  bases:
  - Toolkit
  fields:
  - name: from_email
    type: Any
    default: from_email
  - name: api_key
    type: Any
    default: api_key or getenv("RESEND_API_KEY")
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              api_key: Optional[str] = None,
              from_email: Optional[str] = None,
              enable_send_email: bool = True,
              all: bool = False,
              **kwargs,
          )
    lines:
    - 14
    - 31
    detail:
      calls:
      - getenv
      - logger.error
      - super
      - super().__init__
      - tools.append
  - name: send_email
    sig: 'send_email(self, to_email: str, subject: str, body: str) -> str'
    lines:
    - 33
    - 62
    detail:
      calls:
      - log_info
      - logger.error
      - resend.Emails.send
  lines:
  - 13
  - 62
exports:
- name: ResendTools
  kind: type
