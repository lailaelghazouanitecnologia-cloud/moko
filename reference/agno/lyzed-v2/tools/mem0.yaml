## Roska File Descriptor — tools/mem0.py
file: tools/mem0.py
lines: 194
imports:
- sym: json
- sym: os.getenv
- sym: typing.Any
- sym: typing.Dict
- sym: typing.List
- sym: typing.Optional
- sym: typing.Union
- sym: agno.run.RunContext
- sym: agno.tools.Toolkit
- sym: agno.utils.log.log_debug
- sym: agno.utils.log.log_error
- sym: agno.utils.log.log_warning
types:
- name: Mem0Tools
  kind: struct
  bases:
  - Toolkit
  fields:
  - name: api_key
    type: Any
    default: api_key or getenv("MEM0_API_KEY")
  - name: user_id
    type: Any
    default: user_id
  - name: org_id
    type: Any
    default: org_id or getenv("MEM0_ORG_ID")
  - name: project_id
    type: Any
    default: project_id or getenv("MEM0_PROJECT_ID")
  - name: client
    type: Any
    default: Union[Memory, MemoryClient]
  - name: infer
    type: Any
    default: infer
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              config: Optional[Dict[str, Any]] = None,
              api_key: Optional[str] = None,
              user_id: Optional[str] = None,
              org_id: Optional[str] = None,
              project_id: Optional[str] = None,
              infer: bool = True,
              enable_add_memory: bool = True,
              enable_search_memory: bool = True,
              enable_get_all_memories: bool = True,
              enable_delete_all_memories: bool = True,
              all: bool = False,
              **kwargs,
          )
    lines:
    - 17
    - 67
    detail:
      calls:
      - ConnectionError
      - Memory
      - Memory.from_config
      - MemoryClient
      - getenv
      - log_debug
      - log_error
      - super
      - super().__init__
      - tools.append
  - name: _get_user_id
    sig: |-
      _get_user_id(
              self,
              method_name: str,
              run_context: RunContext,
          ) -> str
    vis: protected
    lines:
    - 69
    - 85
    detail:
      calls:
      - log_error
  - name: add_memory
    sig: |-
      add_memory(
              self,
              run_context: RunContext,
              content: Union[str, Dict[str, str]],
          ) -> str
    lines:
    - 87
    - 121
    detail:
      calls:
      - isinstance
      - json.dumps
      - log_debug
      - log_error
      - resolved_user_id.startswith
      - self._get_user_id
      - self.client.add
      - str
  - name: search_memory
    sig: |-
      search_memory(
              self,
              run_context: RunContext,
              query: str,
          ) -> str
    lines:
    - 123
    - 153
    detail:
      calls:
      - isinstance
      - json.dumps
      - log_error
      - log_warning
      - resolved_user_id.startswith
      - results.get
      - self._get_user_id
      - self.client.search
      - str
      - type
  - name: get_all_memories
    sig: 'get_all_memories(self, run_context: RunContext) -> str'
    lines:
    - 155
    - 179
    detail:
      calls:
      - isinstance
      - json.dumps
      - log_error
      - log_warning
      - resolved_user_id.startswith
      - results.get
      - self._get_user_id
      - self.client.get_all
      - str
      - type
  - name: delete_all_memories
    sig: 'delete_all_memories(self, run_context: RunContext) -> str'
    lines:
    - 181
    - 194
    detail:
      calls:
      - isinstance
      - log_error
      - resolved_user_id.startswith
      - self._get_user_id
      - self.client.delete_all
  lines:
  - 16
  - 194
exports:
- name: Mem0Tools
  kind: type
