## Roska File Descriptor — src/osRelease.ts
file: src/osRelease.ts
lines: 34
layer: logic
imports:
- sym: node:fs.readFileSync
- sym: ./types.OsReleaseInfo
functions:
- name: parseOsRelease
  sig: 'parseOsRelease(contents: string): OsReleaseInfo'
  lines:
  - 4
  - 25
  detail:
    calls:
    - (info.id || "").toLowerCase
    - |-
      (info.id_like || "")
          .split
    - |-
      (info.id_like || "")
          .split(/\s+/)
          .map
    - |-
      (info.id_like || "")
          .split(/\s+/)
          .map((item) => item.trim().toLowerCase())
          .filter
    - contents.split
    - contents.split("\n").filter
    - line.split
    - rawKey.toLowerCase
    - rawValue.replace
    - rawValue.replace(/^"/, "").replace
- name: readOsRelease
  sig: 'readOsRelease(pathname = "/etc/os-release"): OsReleaseInfo'
  lines:
  - 27
  - 34
  detail:
    calls:
    - parseOsRelease
    - readFileSync
exports:
- name: parseOsRelease
  kind: func
- name: readOsRelease
  kind: func
