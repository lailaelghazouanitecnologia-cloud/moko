## Roska File Descriptor — components/recipe-card.tsx
file: components/recipe-card.tsx
lines: 188
layer: ui
purpose: Full updated RecipeCard.tsx
imports:
- sym: react.useState
- sym: react-hot-toast.toast
- sym: '@docusaurus/Link.Link'
data:
- name: authorHandle
  type: any
  val: recipe.author?.contact || null
- name: '[showParamPrompt, setShowParamPrompt]'
  type: any
  val: useState(false)
- name: '[paramValues, setParamValues]'
  type: any
  val: useState<Record<string, string>>({})
- name: requiredParams
  type: any
  val: recipe.parameters?.filter((p) => p.requirement === "required") || []
- name: optionalParams
  type: any
  val: recipe.parameters?.filter((p) => p.requirement !== "required") || []
- name: hasRequiredParams
  type: any
  val: requiredParams.length > 0
types:
- name: Recipe
  kind: typealias
  decorators:
  - type
  fields:
  - name: id
    type: string
  - name: description
    type: string
  - name: extensions
    type: string[]
  - name: activities
    type: string[]
  - name: recipeUrl
    type: string
  - name: action
    type: string
  - name: author
    type: |-
      {
          contact?: string;
        }
  - name: persona
    type: string
  - name: parameters
    type: '{ key: string; requirement: string; value?: string }[]'
  lines:
  - 6
  - 19
functions:
- name: handleCopyCLI
  sig: handleCopyCLI()
  lines:
  - 30
  - 39
  detail:
    calls:
    - navigator.clipboard.writeText
    - setParamValues
    - setShowParamPrompt
    - toast.success
- name: handleSubmitParams
  sig: handleSubmitParams()
  lines:
  - 41
  - 49
  detail:
    calls:
    - Object.entries
    - |-
      Object.entries(paramValues)
            .map
    - |-
      Object.entries(paramValues)
            .map(([key, val]) => `${key}=${val}`)
            .join
    - navigator.clipboard.writeText
    - setShowParamPrompt
    - toast.success
exports:
- name: Recipe
  kind: type
- name: handleCopyCLI
  kind: func
- name: handleSubmitParams
  kind: func
- name: authorHandle
  kind: data
- name: '[showParamPrompt, setShowParamPrompt]'
  kind: data
- name: '[paramValues, setParamValues]'
  kind: data
- name: requiredParams
  kind: data
- name: optionalParams
  kind: data
- name: hasRequiredParams
  kind: data
