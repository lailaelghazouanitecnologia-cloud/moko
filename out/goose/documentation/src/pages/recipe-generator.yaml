## Roska File Descriptor — pages/recipe-generator.tsx
file: pages/recipe-generator.tsx
lines: 446
layer: ui
imports:
- sym: react.useState
- sym: react.useCallback
- sym: react.useMemo
- sym: '@theme/Layout.Layout'
- sym: lucide-react.ArrowLeft
- sym: lucide-react.Copy
- sym: lucide-react.Check
- sym: lucide-react.Plus
- sym: lucide-react.X
- sym: '@site/src/components/ui/button.Button'
- sym: '@docusaurus/Link.Link'
data:
- name: '[title, setTitle]'
  type: any
  val: useState('')
- name: '[description, setDescription]'
  type: any
  val: useState('')
- name: '[instructions, setInstructions]'
  type: any
  val: useState('')
- name: '[activities, setActivities]'
  type: any
  val: useState([])
- name: '[newActivity, setNewActivity]'
  type: any
  val: useState('')
- name: '[copied, setCopied]'
  type: any
  val: useState(false)
- name: '[errors, setErrors]'
  type: any
  val: 'useState<{[key: string]: string}>({})'
- name: '[outputFormat, setOutputFormat]'
  type: any
  val: useState('url')
- name: '[authorContact, setAuthorContact]'
  type: any
  val: useState('')
- name: '[extensionsList, setExtensionsList]'
  type: any
  val: |-
    useState([
        { type: 'builtin', name: 'developer', display_name: 'Developer', timeout: 300, bundled: true, enabled: fa...
- name: '[prompt, setPrompt]'
  type: any
  val: useState('')
- name: handleAddActivity
  type: any
  val: |-
    useCallback(() => {
        if (newActivity.trim()) {
          setActivities(prev => [...prev, newActivity.trim()]);
          setN...
- name: handleRemoveActivity
  type: any
  val: |-
    useCallback((index) => {
        setActivities(prev => prev.filter((_, i) => i !== index));
      }, [])
- name: toggleExtension
  type: any
  val: |-
    useCallback((index) => {
        setExtensionsList(prev => {
          const updated = [...prev];
          updated[index] = { ...up...
- name: validateForm
  type: any
  val: "useCallback(() => {\n    const newErrors: {[key: string]: string} = {};\n    \n    if (!title.trim()) {\n      newErrors.tit..."
- name: recipeOutput
  type: any
  val: |-
    useMemo(() => {
        // Only generate if we have the required fields
        if (!title.trim() || !description.trim() || !ins...
- name: handleCopy
  type: any
  val: |-
    useCallback(() => {
        if (validateForm() && recipeOutput) {
          navigator.clipboard.writeText(recipeOutput)
            ...
exports:
- name: '[title, setTitle]'
  kind: data
- name: '[description, setDescription]'
  kind: data
- name: '[instructions, setInstructions]'
  kind: data
- name: '[activities, setActivities]'
  kind: data
- name: '[newActivity, setNewActivity]'
  kind: data
- name: '[copied, setCopied]'
  kind: data
- name: '[errors, setErrors]'
  kind: data
- name: '[outputFormat, setOutputFormat]'
  kind: data
- name: '[authorContact, setAuthorContact]'
  kind: data
- name: '[extensionsList, setExtensionsList]'
  kind: data
- name: '[prompt, setPrompt]'
  kind: data
- name: handleAddActivity
  kind: data
- name: handleRemoveActivity
  kind: data
- name: toggleExtension
  kind: data
- name: validateForm
  kind: data
- name: recipeOutput
  kind: data
- name: handleCopy
  kind: data
