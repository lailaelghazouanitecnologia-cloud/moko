## Roska File Descriptor — recipes/detail.tsx
file: recipes/detail.tsx
lines: 294
layer: logic
imports:
- sym: '@theme/Layout.Layout'
- sym: lucide-react.ArrowLeft
- sym: '@docusaurus/router.useLocation'
- sym: react.useEffect
- sym: react.useState
- sym: '@docusaurus/Link.Link'
- sym: '@theme/Admonition.Admonition'
- sym: '@theme/CodeBlock.CodeBlock'
- sym: '@site/src/components/ui/button.Button'
- sym: '@site/src/utils/recipes.getRecipeById'
- sym: '@site/src/components/recipe-card.Recipe'
  kind: typing
- sym: react-hot-toast.toast
- sym: react-markdown.ReactMarkdown
data:
- name: colorMap
  type: '{ [key: string]: string }'
  val: |-
    {
      "GitHub MCP": "bg-yellow-100 text-yellow-800 border-yellow-200",
      "Context7 MCP": "bg-purple-100 text-purple-800 bo...
- name: location
  type: any
  val: useLocation()
- name: '[recipe, setRecipe]'
  type: any
  val: useState<Recipe | null>(null)
- name: '[loading, setLoading]'
  type: any
  val: useState(true)
- name: '[error, setError]'
  type: any
  val: useState<string | null>(null)
- name: '[showParamsPrompt, setShowParamsPrompt]'
  type: any
  val: useState(false)
- name: '[paramValues, setParamValues]'
  type: any
  val: useState<Record<string, string>>({})
- name: allParams
  type: any
  val: recipe?.parameters || []
- name: requiredParams
  type: any
  val: allParams.filter((p) => p.requirement === "required")
- name: authorUsername
  type: any
  val: 'typeof recipe.author === "string" ? recipe.author : recipe.author?.contact'
functions:
- name: handleCopyCLI
  sig: handleCopyCLI()
  lines:
  - 63
  - 73
  detail:
    calls:
    - navigator.clipboard.writeText
    - setParamValues
    - setShowParamsPrompt
    - toast.success
- name: handleSubmitParams
  sig: handleSubmitParams()
  lines:
  - 75
  - 88
  detail:
    calls:
    - Object.entries
    - |-
      Object.entries(paramValues)
            .filter
    - |-
      Object.entries(paramValues)
            .filter(([, val]) => val !== "")
            .map
    - |-
      Object.entries(paramValues)
            .filter(([, val]) => val !== "")
            .map(([key, val]) => `${key...
    - navigator.clipboard.writeText
    - setShowParamsPrompt
    - toast.success
exports:
- name: handleCopyCLI
  kind: func
- name: handleSubmitParams
  kind: func
- name: colorMap
  kind: data
- name: location
  kind: data
- name: '[recipe, setRecipe]'
  kind: data
- name: '[loading, setLoading]'
  kind: data
- name: '[error, setError]'
  kind: data
- name: '[showParamsPrompt, setShowParamsPrompt]'
  kind: data
- name: '[paramValues, setParamValues]'
  kind: data
- name: allParams
  kind: data
- name: requiredParams
  kind: data
- name: authorUsername
  kind: data
