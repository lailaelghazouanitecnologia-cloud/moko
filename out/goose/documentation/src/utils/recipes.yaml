## Roska File Descriptor — utils/recipes.ts
file: utils/recipes.ts
lines: 93
layer: logic
imports:
- sym: '@site/src/components/recipe-card.Recipe'
  kind: typing
data:
- name: recipeFiles
  type: any
  val: |-
    require.context(
      "@site/src/pages/recipes/data/recipes",
      false,
      /\.ya?ml$/
    )
functions:
- name: getRecipeById
  sig: 'getRecipeById(id: string): Recipe | null'
  lines:
  - 10
  - 13
  detail:
    calls:
    - allRecipes.find
    - loadAllRecipes
- name: searchRecipes
  sig: 'searchRecipes(query: string): Promise<Recipe[]>'
  is_async: true
  lines:
  - 15
  - 26
  detail:
    calls:
    - allRecipes.filter
    - loadAllRecipes
- name: loadAllRecipes
  sig: 'loadAllRecipes(): Recipe[]'
  lines:
  - 28
  - 34
  detail:
    calls:
    - recipeFiles.keys
    - recipeFiles.keys().map
- name: normalizeRecipe
  sig: 'normalizeRecipe(recipe: any): Recipe'
  lines:
  - 36
  - 85
  detail:
    calls:
    - Array.isArray
    - JSON.stringify
    - recipe.extensions.map
    - recipe.title?.toLowerCase
    - recipe.title?.toLowerCase().replace
    - toBase64
- name: toBase64
  sig: 'toBase64(str: string): string'
  lines:
  - 88
  - 93
  detail:
    calls:
    - Buffer.from
    - Buffer.from(str).toString
    - encodeURIComponent
    - unescape
    - window.btoa
exports:
- name: getRecipeById
  kind: func
- name: searchRecipes
  kind: func
- name: loadAllRecipes
  kind: func
- name: normalizeRecipe
  kind: func
- name: toBase64
  kind: func
- name: recipeFiles
  kind: data
