## Roska File Descriptor — utils/install-links.ts
file: utils/install-links.ts
lines: 56
layer: logic
imports:
- sym: ../types/server.MCPServer
  kind: typing
functions:
- name: getGooseInstallLink
  sig: 'getGooseInstallLink(server: MCPServer): string'
  lines:
  - 3
  - 56
  detail:
    calls:
    - |-
      (server.headers || [])
              .filter
    - |-
      (server.headers || [])
              .filter((header) => header.required)
              .map
    - |-
      [
            'cmd=goosed',
            'arg=mcp',
            `arg=${encodeURIComponent(server.id)}`,
            `descript...
    - |-
      [
            // Map the type to the expected format for the deep link
            ...(server.type === "streamab...
    - |-
      [
          `cmd=${encodeURIComponent(baseCmd)}`,
          ...args.map((arg) => `arg=${encodeURIComponent(arg)}...
    - args.map
    - encodeURIComponent
    - parts.slice
    - server.command.split
    - |-
      server.environmentVariables
              .filter
    - |-
      server.environmentVariables
              .filter((env) => env.required)
              .map
    - |-
      server.environmentVariables
            .filter
    - |-
      server.environmentVariables
            .filter((env) => env.required)
            .map
exports:
- name: getGooseInstallLink
  kind: func
