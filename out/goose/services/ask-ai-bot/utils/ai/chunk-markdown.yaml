## Roska File Descriptor — ai/chunk-markdown.ts
file: ai/chunk-markdown.ts
lines: 96
layer: logic
imports:
- sym: marked.marked
data:
- name: MAX_DISCORD_LENGTH
  type: number
  val: '2000'
functions:
- name: chunkMarkdown
  sig: |-
    chunkMarkdown(
      markdown: string,
      maxLength: number = MAX_DISCORD_LENGTH,
    ): string[]
  lines:
  - 14
  - 60
  detail:
    calls:
    - characterSplit
    - chunks.push
    - marked.lexer
    - splits.slice
- name: characterSplit
  sig: 'characterSplit(text: string, maxLength: number): string[]'
  lines:
  - 70
  - 96
  detail:
    calls:
    - chunks.push
    - remaining.lastIndexOf
    - remaining.slice
    - remaining.slice(splitIndex).trimStart
exports:
- name: chunkMarkdown
  kind: func
- name: characterSplit
  kind: func
- name: MAX_DISCORD_LENGTH
  kind: data
