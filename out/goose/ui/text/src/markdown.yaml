## Roska File Descriptor — src/markdown.tsx
file: src/markdown.tsx
lines: 10
layer: logic
imports:
- sym: marked.marked
- sym: marked-terminal.markedTerminal
functions:
- name: renderMarkdown
  sig: 'renderMarkdown(src: string): string'
  lines:
  - 6
  - 10
  detail:
    calls:
    - marked.parse
    - rendered.replace
exports:
- name: renderMarkdown
  kind: func
