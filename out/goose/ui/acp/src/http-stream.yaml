## Roska File Descriptor — src/http-stream.ts
file: src/http-stream.ts
lines: 120
layer: logic
imports:
- sym: '@agentclientprotocol/sdk.AnyMessage'
  kind: typing
- sym: '@agentclientprotocol/sdk.Stream'
  kind: typing
data:
- name: ACP_SESSION_HEADER
  type: string
  val: '"Acp-Session-Id"'
functions:
- name: createHttpStream
  sig: 'createHttpStream(serverUrl: string): Stream'
  lines:
  - 5
  - 120
  detail:
    calls:
    - JSON.stringify
    - consumeSSE
    - controller.enqueue
    - fetch
    - |-
      fetch(`${serverUrl}/acp`, {
                method: "POST",
                headers,
                body: JSON.string...
    - incoming.shift
    - new AbortController
    - new ReadableStream
    - new WritableStream
    - response.headers.get
    - setTimeout
    - sseAbort.abort
    - waitForMessage
exports:
- name: createHttpStream
  kind: func
- name: ACP_SESSION_HEADER
  kind: data
