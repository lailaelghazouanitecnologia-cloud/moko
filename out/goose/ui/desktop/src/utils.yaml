## Roska File Descriptor — src/utils.ts
file: src/utils.ts
lines: 13
layer: logic
imports:
- sym: clsx.clsx
- sym: clsx.type ClassValue
- sym: tailwind-merge.twMerge
functions:
- name: cn
  sig: 'cn(...inputs: ClassValue[])'
  lines:
  - 4
  - 6
- name: snakeToTitleCase
  sig: 'snakeToTitleCase(snake: string): string'
  lines:
  - 8
  - 13
  detail:
    calls:
    - |-
      snake
          .split
    - |-
      snake
          .split('_')
          .map
    - |-
      snake
          .split('_')
          .map((word) => word.charAt(0).toUpperCase() + word.slice(1).toLowerCase())...
exports:
- name: cn
  kind: func
- name: snakeToTitleCase
  kind: func
