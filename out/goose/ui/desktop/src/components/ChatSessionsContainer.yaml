## Roska File Descriptor — components/ChatSessionsContainer.tsx
file: components/ChatSessionsContainer.tsx
lines: 62
layer: ui
imports:
- sym: react-router-dom.useSearchParams
- sym: ./BaseChat.BaseChat
- sym: ../types/chat.ChatType
- sym: ../types/message.UserInput
types:
- name: ChatSessionsContainerProps
  kind: trait
  decorators:
  - interface
  fields:
  - name: setChat
    type: '(chat: ChatType) => void'
  - name: activeSessions
    type: |-
      Array<{
          sessionId: string;
          initialMessage?: UserInput;
        }>
  lines:
  - 6
  - 12
functions:
- name: ChatSessionsContainer
  sig: |-
    ChatSessionsContainer({
      setChat,
      activeSessions,
    }: ChatSessionsContainerProps)
  lines:
  - 19
  - 62
  detail:
    calls:
    - activeSessions.some
    - searchParams.get
    - useSearchParams
    - '{sessionsToRender.map'
exports:
- name: ChatSessionsContainerProps
  kind: type
- name: ChatSessionsContainer
  kind: func
