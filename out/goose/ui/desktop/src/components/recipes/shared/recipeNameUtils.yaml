## Roska File Descriptor — shared/recipeNameUtils.ts
file: shared/recipeNameUtils.ts
lines: 38
layer: logic
imports:
- sym: zod.z
data:
- name: recipeNameSchema
  type: any
  val: z.string().min(3, 'Recipe name must be at least 3 characters')
- name: RECIPE_NAME_PLACEHOLDER
  type: string
  val: '''my-awesome-recipe'''
functions:
- name: transformToRecipeName
  sig: 'transformToRecipeName(input: string): string'
  lines:
  - 15
  - 23
  detail:
    calls:
    - |-
      input
          .toLowerCase
    - |-
      input
          .toLowerCase()
          .replace
    - |-
      input
          .toLowerCase()
          .replace(/[^a-zA-Z0-9\s-]/g, '') // Remove invalid characters
          .repl...
- name: generateRecipeNameFromTitle
  sig: 'generateRecipeNameFromTitle(title: string): string'
  lines:
  - 28
  - 33
  detail:
    calls:
    - title.trim
    - transformToRecipeName
exports:
- name: transformToRecipeName
  kind: func
- name: generateRecipeNameFromTitle
  kind: func
- name: recipeNameSchema
  kind: data
- name: RECIPE_NAME_PLACEHOLDER
  kind: data
