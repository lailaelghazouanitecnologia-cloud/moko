## Roska File Descriptor — extensions/deeplink.ts
file: extensions/deeplink.ts
lines: 198
layer: logic
imports:
- sym: ../../../api.ExtensionConfig
  kind: typing
- sym: ../../../toasts.toastService
- sym: ./utils.DEFAULT_EXTENSION_TIMEOUT
functions:
- name: getStdioConfig
  sig: |-
    getStdioConfig(
      cmd: string,
      parsedUrl: URL,
      name: string,
      description: string,
      timeout: number
    )
  lines:
  - 8
  - 65
  detail:
    calls:
    - Object.fromEntries
    - allowedCommands.includes
    - allowedCommands.join
    - args.includes
    - envList.map
    - parsedUrl.searchParams.getAll
    - toastService.handleError
- name: getStreamableHttpConfig
  sig: |-
    getStreamableHttpConfig(
      remoteUrl: string,
      name: string,
      description: string,
      timeout: number,
      headers?: { [key: string]: string },
      envs?: { [key: string]: string }
    )
  lines:
  - 70
  - 89
  detail: {}
- name: addExtensionFromDeepLink
  sig: |-
    addExtensionFromDeepLink(
      url: string,
      addExtensionFn: (
        name: string,
        extensionConfig: ExtensionConfig,
        enabled: boolean
      ) => Promise<void>,
      setView: (
        view: string,
        options: { showEnvVars: boolean; deepLinkConfig?: ExtensionConfig }
      ) => void
    )
  is_async: true
  lines:
  - 94
  - 198
  detail:
    calls:
    - Object.fromEntries
    - Object.keys
    - addExtensionFn
    - console.log
    - envList.map
    - getStdioConfig
    - getStreamableHttpConfig
    - headerParams.map
    - new URL
    - parseInt
    - parsedUrl.searchParams.get
    - parsedUrl.searchParams.getAll
    - setView
    - toastService.handleError
    - toastService.success
    - value.trim
exports:
- name: getStdioConfig
  kind: func
- name: getStreamableHttpConfig
  kind: func
- name: addExtensionFromDeepLink
  kind: func
