## Roska File Descriptor — forms/DefaultProviderSetupForm.tsx
file: forms/DefaultProviderSetupForm.tsx
lines: 217
layer: logic
imports:
- sym: react.useEffect
- sym: react.useMemo
- sym: react.useState
- sym: react.useCallback
- sym: ../../../../../ui/input.Input
- sym: ../../../../../ConfigContext.useConfig
- sym: ../../../../../../api.ProviderDetails
- sym: ../../../../../../api.ConfigKey
- sym: ../../../../../ui/collapsible.Collapsible
- sym: ../../../../../ui/collapsible.CollapsibleContent
- sym: ../../../../../ui/collapsible.CollapsibleTrigger
data:
- name: parameters
  type: any
  val: |-
    useMemo(
        () => provider.metadata.config_keys || [],
        [provider.metadata.config_keys]
      )
- name: '[isLoading, setIsLoading]'
  type: any
  val: useState(true)
- name: '[optionalExpanded, setOptionalExpanded]'
  type: any
  val: useState(false)
- name: '{ read }'
  type: any
  val: useConfig()
- name: loadConfigValues
  type: any
  val: |-
    useCallback(async () => {
        setIsLoading(true);
        try {
          const values: { [k: string]: ConfigInput } = {};

        ...
- name: belowFoldParameters
  type: any
  val: |-
    parameters.filter(
        (p) => !p.primary && !(p.required && (p.default === undefined || p.default === null))
      )
- name: expandCtaText
  type: string
  val: '`${optionalExpanded ? ''Hide'' : ''Show''} ${belowFoldParameters.length} options `'
types:
- name: ValidationErrors
  kind: typealias
  decorators:
  - type
  lines:
  - 7
  - 7
- name: ConfigValue
  kind: typealias
  decorators:
  - type
  lines:
  - 9
  - 9
- name: ConfigInput
  kind: trait
  decorators:
  - interface
  fields:
  - name: serverValue
    type: ConfigValue
  - name: value
    type: string
  lines:
  - 10
  - 13
- name: DefaultProviderSetupFormProps
  kind: trait
  decorators:
  - interface
  fields:
  - name: configValues
    type: Record<string, ConfigInput>
  - name: setConfigValues
    type: React.Dispatch<React.SetStateAction<Record<string, ConfigInput>>>
  - name: provider
    type: ProviderDetails
  - name: validationErrors
    type: ValidationErrors
  - name: showOptions
    type: boolean
  lines:
  - 15
  - 21
functions:
- name: envToPrettyName
  sig: 'envToPrettyName(envVar: string)'
  lines:
  - 23
  - 37
  detail:
    calls:
    - |-
      envVar
          .toLowerCase
    - |-
      envVar
          .toLowerCase()
          .split
    - |-
      envVar
          .toLowerCase()
          .split('_')
          .map
    - |-
      envVar
          .toLowerCase()
          .split('_')
          .map((word) => word.charAt(0).toUpperCase() + word.sli...
- name: getPlaceholder
  sig: 'getPlaceholder(parameter: ConfigKey): string'
  lines:
  - 84
  - 105
  detail:
    calls:
    - name.includes
    - |-
      parameter.name
            .replace
    - |-
      parameter.name
            .replace(/_/g, ' ')
            .replace
    - |-
      parameter.name
            .replace(/_/g, ' ')
            .replace(/^./, (str) => str.toUpperCase())
            .tri...
    - parameter.name.toLowerCase
- name: getFieldLabel
  sig: 'getFieldLabel(parameter: ConfigKey)'
  lines:
  - 107
  - 124
  detail:
    calls:
    - envToPrettyName
    - name.includes
    - parameter.name.toLowerCase
    - parameter.name.toUpperCase
    - parameter_name.slice
    - parameter_name.startsWith
    - provider.name.toUpperCase
    - provider.name.toUpperCase().replace
- name: getRenderValue
  sig: 'getRenderValue(parameter: ConfigKey): string | undefined'
  lines:
  - 130
  - 137
  detail: {}
exports:
- name: ValidationErrors
  kind: type
- name: ConfigValue
  kind: type
- name: ConfigInput
  kind: type
- name: DefaultProviderSetupFormProps
  kind: type
- name: envToPrettyName
  kind: func
- name: getPlaceholder
  kind: func
- name: getFieldLabel
  kind: func
- name: getRenderValue
  kind: func
- name: parameters
  kind: data
- name: '[isLoading, setIsLoading]'
  kind: data
- name: '[optionalExpanded, setOptionalExpanded]'
  kind: data
- name: '{ read }'
  kind: data
- name: loadConfigValues
  kind: data
- name: belowFoldParameters
  kind: data
- name: expandCtaText
  kind: data
