## Roska File Descriptor — handlers/DefaultSubmitHandler.tsx
file: handlers/DefaultSubmitHandler.tsx
lines: 112
layer: logic
imports:
- sym: ../../../../../../api.getProviderModels
- sym: ../../../../../../api.readConfig
functions:
- name: providerConfigSubmitHandler
  sig: |-
    providerConfigSubmitHandler(
      upsertFn: (key: string, value: unknown, isSecret: boolean) => Promise<void>,
      provider: {
        name: string;
        metadata: {
          config_keys?: Array<{
            name: string;
            required?: boolean;
            default?: unknown;
            secret?: boolean;
          }>;
        };
      },
      configValues: Record<string, string>
    )
  is_async: true
  lines:
  - 7
  - 112
  detail:
    calls:
    - Object.entries
    - Promise.all
    - getProviderModels
    - nonSecretParams.map
    - parameters.every
    - parameters.filter
    - parameters.map
    - promises.push
    - rollbackPromises.push
    - upsertFn
exports:
- name: providerConfigSubmitHandler
  kind: func
