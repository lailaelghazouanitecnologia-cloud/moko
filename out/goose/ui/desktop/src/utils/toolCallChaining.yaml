## Roska File Descriptor — utils/toolCallChaining.ts
file: utils/toolCallChaining.ts
lines: 62
layer: logic
imports:
- sym: ../types/message.getToolRequests
- sym: ../types/message.getTextAndImageContent
- sym: ../types/message.getToolResponses
- sym: ../api.Message
functions:
- name: identifyConsecutiveToolCalls
  sig: 'identifyConsecutiveToolCalls(messages: Message[]): number[][]'
  lines:
  - 4
  - 48
  detail:
    calls:
    - chains.push
    - currentChain.push
    - getTextAndImageContent
    - getToolRequests
    - getToolResponses
    - textContent.trim
- name: shouldHideTimestamp
  sig: 'shouldHideTimestamp(messageIndex: number, chains: number[][]): boolean'
  lines:
  - 50
  - 58
  detail:
    calls:
    - chain.includes
- name: isInChain
  sig: 'isInChain(messageIndex: number, chains: number[][]): boolean'
  lines:
  - 60
  - 62
  detail:
    calls:
    - chains.some
exports:
- name: identifyConsecutiveToolCalls
  kind: func
- name: shouldHideTimestamp
  kind: func
- name: isInChain
  kind: func
