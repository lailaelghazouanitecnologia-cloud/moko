## Roska File Descriptor — utils/date.ts
file: utils/date.ts
lines: 57
layer: logic
functions:
- name: formatToLocalDateTime
  sig: 'formatToLocalDateTime(dateString?: string | null): string'
  lines:
  - 1
  - 16
  detail:
    calls:
    - console.error
    - date.getTime
    - date.toLocaleString
    - isNaN
    - new Date
- name: formatDate
  sig: 'formatDate(dateString?: string | null): string'
  lines:
  - 18
  - 32
  detail:
    calls:
    - console.error
    - date.getTime
    - date.toLocaleDateString
    - isNaN
    - new Date
- name: formatToLocalDateWithTimezone
  sig: 'formatToLocalDateWithTimezone(dateString?: string | null): string'
  lines:
  - 34
  - 57
  detail:
    calls:
    - console.error
    - date.getTime
    - date.toLocaleString
    - isNaN
    - new Date
exports:
- name: formatToLocalDateTime
  kind: func
- name: formatDate
  kind: func
- name: formatToLocalDateWithTimezone
  kind: func
