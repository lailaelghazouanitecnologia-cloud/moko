## Roska File Descriptor — utils/htmlSecurity.ts
file: utils/htmlSecurity.ts
lines: 59
layer: logic
purpose: HTML Security Detection Utilities These functions detect potentially dangerous HTML content in markdown and wrap it safely in code blocks to prevent execution.
functions:
- name: containsHTML
  sig: 'containsHTML(str: string): boolean'
  lines:
  - 13
  - 27
  detail:
    calls:
    - commentRegex.test
    - dangerousHTMLRegex.test
    - str.replace
    - str.replace(/```[\s\S]*?```/g, '').replace
- name: wrapHTMLInCodeBlock
  sig: 'wrapHTMLInCodeBlock(content: string): string'
  lines:
  - 34
  - 59
  detail:
    calls:
    - content.split
    - lines.map
    - processedLines.join
exports:
- name: containsHTML
  kind: func
- name: wrapHTMLInCodeBlock
  kind: func
