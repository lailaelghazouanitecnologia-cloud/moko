## Roska File Descriptor — src/validator.ts
file: src/validator.ts
lines: 184
layer: logic
imports:
- sym: node:fs.existsSync
- sym: node:fs.readFileSync
- sym: node:fs.statSync
- sym: node:child_process.execSync
- sym: node:path.join
- sym: ./types.js.ValidationRule
  kind: typing
types:
- name: ValidationResult
  kind: trait
  decorators:
  - interface
  fields:
  - name: passed
    type: boolean
  - name: message
    type: string
  lines:
  - 6
  - 9
functions:
- name: validateRule
  sig: |-
    validateRule(
      rule: ValidationRule,
      workdir: string
    ): ValidationResult
  lines:
  - 11
  - 174
  detail:
    calls:
    - JSON.stringify
    - Object.entries
    - String
    - String(actual).toLowerCase
    - String(expected).toLowerCase
    - actualStr.includes
    - content.includes
    - content.trim
    - content.trim().split
    - content.trim().split("\n").filter
    - execSync
    - existsSync
    - expected.endsWith
    - expected.slice
    - expected.startsWith
    - join
    - |-
      lines
              .map
    - |-
      lines
              .map((line) => {
                try {
                  return JSON.parse(line);
                } catc...
    - new RegExp
    - pattern.test
    - readFileSync
    - regex.test
    - statSync
- name: validateAll
  sig: |-
    validateAll(
      rules: ValidationRule[],
      workdir: string
    ): Array<{ rule: ValidationRule; result: ValidationResult }>
  lines:
  - 176
  - 184
  detail:
    calls:
    - rules.map
exports:
- name: ValidationResult
  kind: type
- name: validateRule
  kind: func
- name: validateAll
  kind: func
