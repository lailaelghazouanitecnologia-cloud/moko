## Roska File Descriptor — common/CodeAccordion.tsx
file: common/CodeAccordion.tsx
lines: 131
layer: logic
imports:
- sym: react.memo
- sym: react.useMemo
- sym: '@vscode/webview-ui-toolkit/react.VSCodeProgressRing'
- sym: '@roo-code/types.type ToolProgressStatus'
- sym: '@src/utils/getLanguageFromPath.getLanguageFromPath'
- sym: '@src/utils/formatPathTooltip.formatPathTooltip'
- sym: ./ToolUseBlock.ToolUseBlock
- sym: ./ToolUseBlock.ToolUseBlockHeader
- sym: ./CodeBlock.CodeBlock
- sym: ../ui/PathTooltip.PathTooltip
- sym: ./DiffView.DiffView
data:
- name: inferredLanguage
  type: any
  val: 'useMemo(() => language ?? (path ? getLanguageFromPath(path) : "txt"), [path, language])'
- name: source
  type: any
  val: useMemo(() => code.trim(), [code])
- name: hasHeader
  type: any
  val: Boolean(path || isFeedback || header)
- name: derivedStats
  type: any
  val: "useMemo(() => {\n\t\tif (diffStats && (diffStats.added > 0 || diffStats.removed > 0)) return diffStats\n\t\treturn null\n\t}, [d..."
- name: hasValidStats
  type: any
  val: Boolean(derivedStats && (derivedStats.added > 0 || derivedStats.removed > 0))
types:
- name: CodeAccordionProps
  kind: trait
  decorators:
  - interface
  fields:
  - name: path
    type: string
  - name: code
    type: string
  - name: language
    type: string
  - name: progressStatus
    type: ToolProgressStatus
  - name: isLoading
    type: boolean
  - name: isExpanded
    type: boolean
  - name: isFeedback
    type: boolean
  - name: onToggleExpand
    type: () => void
  - name: header
    type: string
  - name: onJumpToFile
    type: () => void
  - name: diffStats
    type: '{ added: number; removed: number }'
  lines:
  - 12
  - 25
exports:
- name: CodeAccordionProps
  kind: type
- name: inferredLanguage
  kind: data
- name: source
  kind: data
- name: hasHeader
  kind: data
- name: derivedStats
  kind: data
- name: hasValidStats
  kind: data
