## Roska File Descriptor — chat/ContextWindowProgress.tsx
file: chat/ContextWindowProgress.tsx
lines: 98
layer: logic
imports:
- sym: react.useMemo
- sym: react-i18next.useTranslation
- sym: '@/utils/format.formatLargeNumber'
- sym: '@/utils/model-utils.calculateTokenDistribution'
- sym: '@/components/ui.StandardTooltip'
data:
- name: '{ t }'
  type: any
  val: useTranslation()
- name: tokenDistribution
  type: any
  val: "useMemo(\n\t\t() => calculateTokenDistribution(contextWindow, contextTokens, maxTokens),\n\t\t[contextWindow, contextTokens, m..."
- name: '{ currentPercent, reservedPercent, availableSize, reservedForOutput, availablePercent }'
  type: any
  val: tokenDistribution
- name: safeContextWindow
  type: any
  val: Math.max(0, contextWindow)
- name: safeContextTokens
  type: any
  val: Math.max(0, contextTokens)
types:
- name: ContextWindowProgressProps
  kind: trait
  decorators:
  - interface
  fields:
  - name: contextWindow
    type: number
  - name: contextTokens
    type: number
  - name: maxTokens
    type: number
  lines:
  - 8
  - 12
exports:
- name: ContextWindowProgressProps
  kind: type
- name: '{ t }'
  kind: data
- name: tokenDistribution
  kind: data
- name: '{ currentPercent, reservedPercent, availableSize, reservedForOutput, availablePercent }'
  kind: data
- name: safeContextWindow
  kind: data
- name: safeContextTokens
  kind: data
