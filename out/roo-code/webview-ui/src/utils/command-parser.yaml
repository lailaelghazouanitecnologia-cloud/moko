## Roska File Descriptor — utils/command-parser.ts
file: utils/command-parser.ts
lines: 68
layer: logic
imports:
- sym: shell-quote.parse
functions:
- name: extractPatternsFromCommand
  sig: 'extractPatternsFromCommand(command: string): string[]'
  lines:
  - 8
  - 42
  detail:
    calls:
    - Array.from
    - Array.from(patterns).sort
    - command.trim
    - command.trim().split
    - command?.trim
    - commandSeparators.has
    - console.warn
    - currentTokens.push
    - extractFromTokens
    - new Set
    - parse
    - patterns.add
- name: extractFromTokens
  sig: 'extractFromTokens(tokens: string[], patterns: Set<string>): void'
  lines:
  - 44
  - 68
  detail:
    calls:
    - /^\d+$/.test
    - Math.min
    - breakingExps.some
    - pattern.trim
    - patterns.add
    - tokens.slice
    - tokens.slice(0, i + 1).join
exports:
- name: extractPatternsFromCommand
  kind: func
- name: extractFromTokens
  kind: func
