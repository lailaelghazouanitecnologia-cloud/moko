## Roska File Descriptor — utils/batchConsecutive.ts
file: utils/batchConsecutive.ts
lines: 38
layer: logic
purpose: Walk an item array and batch runs of consecutive items that match `predicate` into synthetic items produced by `synthesize`. - Runs of length 1 are passed through unchanged. - Runs of length >= 2 are replaced by a single synthetic item. - Non-matching items are preserved in-order.
functions:
- name: batchConsecutive
  sig: 'batchConsecutive(items: T[], predicate: (item: T) => boolean, synthesize: (batch: T[]) => T): T[]'
  lines:
  - 9
  - 38
  detail:
    calls:
    - batch.push
    - predicate
    - result.push
    - synthesize
exports:
- name: batchConsecutive
  kind: func
