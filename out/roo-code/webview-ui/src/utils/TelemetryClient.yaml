## Roska File Descriptor — utils/TelemetryClient.ts
file: utils/TelemetryClient.ts
lines: 48
layer: logic
imports:
- sym: posthog-js.posthog
- sym: '@roo-code/types.TelemetrySetting'
  kind: typing
data:
- name: telemetryClient
  type: any
  val: TelemetryClient.getInstance()
types:
- name: TelemetryClient
  kind: struct
  fields:
  - name: instance
    type: TelemetryClient
  - name: telemetryEnabled
    type: boolean
    default: 'false'
  methods:
  - name: updateTelemetryState
    sig: 'updateTelemetryState(telemetrySetting: TelemetrySetting, apiKey?: string, distinctId?: string)'
    lines:
    - 9
    - 27
    detail:
      calls:
      - posthog.init
      - posthog.reset
  - name: getInstance
    sig: 'getInstance(): TelemetryClient'
    decorators:
    - static
    lines:
    - 29
    - 35
    detail:
      calls:
      - new TelemetryClient
  - name: capture
    sig: 'capture(eventName: string, properties?: Record<string, any>)'
    lines:
    - 37
    - 45
    detail:
      calls:
      - posthog.capture
  lines:
  - 5
  - 46
exports:
- name: TelemetryClient
  kind: type
- name: telemetryClient
  kind: data
