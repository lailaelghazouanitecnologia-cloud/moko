## Roska File Descriptor — utils/path-mentions.ts
file: utils/path-mentions.ts
lines: 80
layer: logic
purpose: Utilities for handling path-related operations in mentions
functions:
- name: escapeSpaces
  sig: 'escapeSpaces(path: string): string'
  lines:
  - 11
  - 13
  detail:
    calls:
    - path.replace
- name: convertToMentionPath
  sig: 'convertToMentionPath(path: string, cwd?: string): string'
  lines:
  - 25
  - 80
  detail:
    calls:
    - console.error
    - cwd.replace
    - decodeURIComponent
    - escapeSpaces
    - lowerPath.startsWith
    - normalizedCwd.endsWith
    - normalizedCwd.slice
    - normalizedCwd.toLowerCase
    - normalizedPath.substring
    - normalizedPath.toLowerCase
    - path.startsWith
    - path.substring
    - pathWithoutProtocol.replace
    - pathWithoutProtocol.startsWith
    - pathWithoutProtocol.substring
    - protocolStripped.indexOf
    - protocolStripped.substring
    - relativePath.startsWith
exports:
- name: escapeSpaces
  kind: func
- name: convertToMentionPath
  kind: func
