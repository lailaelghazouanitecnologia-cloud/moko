## Roska File Descriptor — utils/highlight.ts
file: utils/highlight.ts
lines: 73
layer: logic
imports:
- sym: lru-cache.LRUCache
data:
- name: escapeHtmlCache
  type: any
  val: 'new LRUCache<string, string>({ max: 500 })'
functions:
- name: escapeHtml
  sig: 'escapeHtml(text: string): string'
  lines:
  - 6
  - 25
  detail:
    calls:
    - escapeHtmlCache.get
    - escapeHtmlCache.set
    - "text\n\t\t.replace"
    - "text\n\t\t.replace(/&/g, \"&amp;\")\n\t\t.replace"
    - "text\n\t\t.replace(/&/g, \"&amp;\")\n\t\t.replace(/</g, \"&lt;\")\n\t\t.replace"
    - "text\n\t\t.replace(/&/g, \"&amp;\")\n\t\t.replace(/</g, \"&lt;\")\n\t\t.replace(/>/g, \"&gt;\")\n\t\t.replace"
    - "text\n\t\t.replace(/&/g, \"&amp;\")\n\t\t.replace(/</g, \"&lt;\")\n\t\t.replace(/>/g, \"&gt;\")\n\t\t.replace(/\"/g, \"&..."
- name: highlightFzfMatch
  sig: "highlightFzfMatch(\n\ttext: string,\n\tpositions: number[],\n\thighlightClassName: string = \"history-item-highlight\",\n)"
  lines:
  - 27
  - 73
  detail:
    calls:
    - "parts\n\t\t.map"
    - "parts\n\t\t.map((part) => {\n\t\t\tconst escapedText = escapeHtml(part.text)\n\t\t\treturn part.highlight ? `<s..."
    - parts.push
    - positions.forEach
    - positions.sort
    - text.substring
exports:
- name: escapeHtml
  kind: func
- name: highlightFzfMatch
  kind: func
- name: escapeHtmlCache
  kind: data
