## Roska File Descriptor — utils/markdown.ts
file: utils/markdown.ts
lines: 23
layer: logic
purpose: 'Counts the number of markdown headings in the given text. Matches headings from level 1 to 6 (e.g. #, ##, ###, etc.). Code fences are stripped before matching to avoid false positives.'
functions:
- name: countMarkdownHeadings
  sig: 'countMarkdownHeadings(text: string | undefined): number'
  lines:
  - 6
  - 16
  detail:
    calls:
    - text.replace
    - withoutCodeBlocks.match
- name: hasComplexMarkdown
  sig: 'hasComplexMarkdown(text: string | undefined): boolean'
  lines:
  - 21
  - 23
exports:
- name: countMarkdownHeadings
  kind: func
- name: hasComplexMarkdown
  kind: func
