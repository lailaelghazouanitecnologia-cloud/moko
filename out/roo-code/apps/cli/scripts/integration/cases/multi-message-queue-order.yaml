## Roska File Descriptor — cases/multi-message-queue-order.ts
file: cases/multi-message-queue-order.ts
lines: 184
layer: logic
imports:
- sym: ../lib/stream-harness.runStreamCase
- sym: ../lib/stream-harness.StreamEvent
data:
- name: LONG_PROMPT
  type: string
  val: '''Run exactly this command and do not summarize until it finishes: sleep 6 && echo "done". After it finishes, reply with ...'
- name: MESSAGE_ONE_PROMPT
  type: string
  val: '''For this follow-up, reply with only "ALPHA".'''
- name: MESSAGE_TWO_PROMPT
  type: string
  val: '''For this follow-up, reply with only "BETA".'''
functions:
- name: main
  sig: main()
  is_async: true
  lines:
  - 8
  - 179
  detail:
    calls:
    - Date.now
    - '[firstMessageRequestId, secondMessageRequestId].join'
    - context.sendCommand
    - new Error
    - resultOrder.join
    - resultOrder.push
    - runStreamCase
exports:
- name: main
  kind: func
- name: LONG_PROMPT
  kind: data
- name: MESSAGE_ONE_PROMPT
  kind: data
- name: MESSAGE_TWO_PROMPT
  kind: data
