## Roska File Descriptor — cases/mixed-command-ordering.ts
file: cases/mixed-command-ordering.ts
lines: 148
layer: logic
imports:
- sym: ../lib/stream-harness.runStreamCase
- sym: ../lib/stream-harness.StreamEvent
data:
- name: START_PROMPT
  type: string
  val: '''Run exactly this command and do not summarize until it finishes: sleep 8 && echo "done". After it finishes, reply with ...'
functions:
- name: main
  sig: main()
  is_async: true
  lines:
  - 6
  - 143
  detail:
    calls:
    - (eventOrderByRequestId.get(messageRequestId) ?? []).join
    - (eventOrderByRequestId.get(pingARequestId) ?? []).join
    - (eventOrderByRequestId.get(pingBRequestId) ?? []).join
    - Date.now
    - "[\n\t\t\t\t\"timed out waiting for mixed-command-ordering validation\",\n\t\t\t\t`initSeen=${initSeen}`,\n\t\t\t\t`se..."
    - context.sendCommand
    - eventOrderByRequestId.get
    - messageOrder.join
    - new Error
    - new Map
    - pingAOrder.join
    - pingBOrder.join
    - recordControlEvent
    - runStreamCase
exports:
- name: main
  kind: func
- name: START_PROMPT
  kind: data
