## Roska File Descriptor — lib/formatters.ts
file: lib/formatters.ts
lines: 59
layer: logic
data:
- name: formatter
  type: any
  val: "new Intl.NumberFormat(\"en-US\", {\n\tstyle: \"currency\",\n\tcurrency: \"USD\",\n})"
functions:
- name: formatCurrency
  sig: 'formatCurrency(amount: number)'
  lines:
  - 6
  - 6
- name: formatDuration
  sig: 'formatDuration(durationMs: number)'
  lines:
  - 8
  - 18
  detail:
    calls:
    - Math.floor
    - minutes.toString
    - minutes.toString().padStart
    - remainingSeconds.toString
    - remainingSeconds.toString().padStart
- name: formatTokens
  sig: 'formatTokens(tokens: number)'
  lines:
  - 20
  - 46
  detail:
    calls:
    - (tokens / 1000000000).toFixed
    - Math.round
    - rounded.toFixed
    - tokens.toString
- name: formatToolUsageSuccessRate
  sig: 'formatToolUsageSuccessRate(usage: { attempts: number; failures: number })'
  lines:
  - 48
  - 49
- name: formatDateTime
  sig: 'formatDateTime(date: Date)'
  lines:
  - 51
  - 59
  detail:
    calls:
    - new Intl.DateTimeFormat
    - "new Intl.DateTimeFormat(\"en-US\", {\n\t\tmonth: \"short\",\n\t\tday: \"numeric\",\n\t\thour: \"numeric\",\n\t\tminute: ..."
exports:
- name: formatCurrency
  kind: func
- name: formatDuration
  kind: func
- name: formatTokens
  kind: func
- name: formatToolUsageSuccessRate
  kind: func
- name: formatDateTime
  kind: func
- name: formatter
  kind: data
