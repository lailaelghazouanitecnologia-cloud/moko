## Roska File Descriptor — __tests__/roo-last-model-selection.spec.ts
file: __tests__/roo-last-model-selection.spec.ts
lines: 78
layer: test
imports:
- sym: ../roo-last-model-selection.loadRooLastModelSelection
- sym: ../roo-last-model-selection.ROO_LAST_MODEL_SELECTION_KEY
- sym: ../roo-last-model-selection.saveRooLastModelSelection
types:
- name: LocalStorageMock
  kind: struct
  bases:
  - Storage
  fields:
  - name: store
    type: any
    default: new Map<string, string>()
  methods:
  - name: length
    sig: 'length(): number'
    lines:
    - 10
    - 12
    detail: {}
  - name: clear
    sig: 'clear(): void'
    lines:
    - 14
    - 16
    detail:
      calls:
      - this.store.clear
  - name: getItem
    sig: 'getItem(key: string): string | null'
    lines:
    - 18
    - 20
    detail:
      calls:
      - this.store.get
  - name: key
    sig: 'key(index: number): string | null'
    lines:
    - 22
    - 24
    detail:
      calls:
      - Array.from
      - this.store.keys
  - name: removeItem
    sig: 'removeItem(key: string): void'
    lines:
    - 26
    - 28
    detail:
      calls:
      - this.store.delete
  - name: setItem
    sig: 'setItem(key: string, value: string): void'
    lines:
    - 30
    - 32
    detail:
      calls:
      - this.store.set
  lines:
  - 7
  - 33
exports:
- name: LocalStorageMock
  kind: type
