## Roska File Descriptor — cli/messageLogDeduper.ts
file: cli/messageLogDeduper.ts
lines: 50
layer: logic
types:
- name: MessageLogDeduper
  kind: struct
  ctx: 'Returns true if this message should be logged. Dedupe key: `${action}:${message.ts}`. Dedupe rule: skip if payload is identical to the last logged payload for that key.'
  fields:
  - name: lastLoggedByKey
    type: any
    default: new Map<string, string>()
  methods:
  - name: constructor
    sig: constructor(private readonly maxEntries = 10_000)
    lines:
    - 4
    - 4
    detail: {}
  - name: shouldLog
    sig: 'shouldLog(action: string | undefined, message: unknown): boolean'
    lines:
    - 11
    - 49
    detail:
      calls:
      - JSON.stringify
      - this.lastLoggedByKey.delete
      - this.lastLoggedByKey.get
      - this.lastLoggedByKey.has
      - this.lastLoggedByKey.keys
      - this.lastLoggedByKey.keys().next
      - this.lastLoggedByKey.set
  lines:
  - 1
  - 50
exports:
- name: MessageLogDeduper
  kind: type
