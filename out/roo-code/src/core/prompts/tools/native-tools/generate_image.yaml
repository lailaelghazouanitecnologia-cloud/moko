## Roska File Descriptor — native-tools/generate_image.ts
file: native-tools/generate_image.ts
lines: 51
layer: logic
imports:
- sym: openai.OpenAI
  kind: typing
data:
- name: GENERATE_IMAGE_DESCRIPTION
  type: string
  val: '`Request to generate or edit an image using AI models through OpenRouter API. This tool can create new images from text ...'
- name: PROMPT_PARAMETER_DESCRIPTION
  type: string
  val: '`Text description of the image to generate or the edits to apply`'
- name: PATH_PARAMETER_DESCRIPTION
  type: string
  val: '`Filesystem path (relative to the workspace) where the resulting image should be saved`'
- name: IMAGE_PARAMETER_DESCRIPTION
  type: string
  val: '`Optional path (relative to the workspace) to an existing image to edit; supports PNG, JPG, JPEG, GIF, and WEBP`'
exports:
- name: GENERATE_IMAGE_DESCRIPTION
  kind: data
- name: PROMPT_PARAMETER_DESCRIPTION
  kind: data
- name: PATH_PARAMETER_DESCRIPTION
  kind: data
- name: IMAGE_PARAMETER_DESCRIPTION
  kind: data
