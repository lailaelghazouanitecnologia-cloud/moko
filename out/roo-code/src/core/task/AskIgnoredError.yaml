## Roska File Descriptor — task/AskIgnoredError.ts
file: task/AskIgnoredError.ts
lines: 15
layer: logic
purpose: Error thrown when an ask promise is superseded by a newer one. This is used as an internal control flow signal - not an actual error. It occurs when multiple asks are sent in rapid succession and an older ask is invalidated by a newer one (e.g., during streaming updates).
types:
- name: AskIgnoredError
  kind: struct
  bases:
  - Error
  methods:
  - name: constructor
    sig: 'constructor(reason?: string)'
    lines:
    - 9
    - 14
    detail:
      calls:
      - Object.setPrototypeOf
      - super
  lines:
  - 8
  - 15
exports:
- name: AskIgnoredError
  kind: type
