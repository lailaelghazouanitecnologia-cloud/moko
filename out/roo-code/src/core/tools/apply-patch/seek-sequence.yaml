## Roska File Descriptor — apply-patch/seek-sequence.ts
file: apply-patch/seek-sequence.ts
lines: 153
layer: logic
purpose: Fuzzy sequence matching for the apply_patch tool. Implements multi-pass sequence matching (exact, trim-end, trim, Unicode-normalized) to locate old_lines and change_context within a file.
functions:
- name: normalizeUnicode
  sig: 'normalizeUnicode(s: string): string'
  lines:
  - 12
  - 36
  detail:
    calls:
    - "s\n\t\t.trim"
    - "s\n\t\t.trim()\n\t\t.split"
    - "s\n\t\t.trim()\n\t\t.split(\"\")\n\t\t.map"
    - "s\n\t\t.trim()\n\t\t.split(\"\")\n\t\t.map((c) => {\n\t\t\t// Various dash/hyphen code-points → ASCII '-'\n\t\t\tif (..."
- name: exactMatch
  sig: 'exactMatch(lines: string[], pattern: string[], startIndex: number): boolean'
  lines:
  - 41
  - 48
  detail: {}
- name: trimEndMatch
  sig: 'trimEndMatch(lines: string[], pattern: string[], startIndex: number): boolean'
  lines:
  - 53
  - 60
  detail:
    calls:
    - lines[startIndex + i]?.trimEnd
    - pattern[i]?.trimEnd
- name: trimMatch
  sig: 'trimMatch(lines: string[], pattern: string[], startIndex: number): boolean'
  lines:
  - 65
  - 72
  detail:
    calls:
    - lines[startIndex + i]?.trim
    - pattern[i]?.trim
- name: normalizedMatch
  sig: 'normalizedMatch(lines: string[], pattern: string[], startIndex: number): boolean'
  lines:
  - 77
  - 84
  detail:
    calls:
    - normalizeUnicode
- name: seekSequence
  sig: 'seekSequence(lines: string[], pattern: string[], start: number, eof: boolean): number | null'
  lines:
  - 110
  - 153
  detail:
    calls:
    - exactMatch
    - normalizedMatch
    - trimEndMatch
    - trimMatch
exports:
- name: normalizeUnicode
  kind: func
- name: exactMatch
  kind: func
- name: trimEndMatch
  kind: func
- name: trimMatch
  kind: func
- name: normalizedMatch
  kind: func
- name: seekSequence
  kind: func
