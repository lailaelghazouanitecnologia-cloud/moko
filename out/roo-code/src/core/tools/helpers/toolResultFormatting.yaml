## Roska File Descriptor — helpers/toolResultFormatting.ts
file: helpers/toolResultFormatting.ts
lines: 10
layer: logic
purpose: Formats tool invocation parameters for display.
functions:
- name: formatToolInvocation
  sig: 'formatToolInvocation(toolName: string, params: Record<string, any>): string'
  lines:
  - 4
  - 10
  detail:
    calls:
    - Object.entries
    - "Object.entries(params)\n\t\t.map"
    - "Object.entries(params)\n\t\t.map(([key, value]) => `${key}: ${typeof value === \"string\" ? value : JSON...."
exports:
- name: formatToolInvocation
  kind: func
