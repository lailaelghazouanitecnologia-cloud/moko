## Roska File Descriptor — tools/SearchReplaceTool.ts
file: tools/SearchReplaceTool.ts
lines: 278
layer: logic
imports:
- sym: fs/promises.fs
- sym: path.path
- sym: '@roo-code/types.type ClineSayTool'
- sym: '@roo-code/types.DEFAULT_WRITE_DELAY_MS'
- sym: ../../utils/path.getReadablePath
- sym: ../../utils/pathUtils.isPathOutsideWorkspace
- sym: ../task/Task.Task
- sym: ../prompts/responses.formatResponse
- sym: ../context-tracking/FileContextTrackerTypes.RecordSource
- sym: ../../utils/fs.fileExistsAtPath
- sym: ../../shared/experiments.EXPERIMENT_IDS
- sym: ../../shared/experiments.experiments
- sym: ../diff/stats.sanitizeUnifiedDiff
- sym: ../diff/stats.computeDiffStats
- sym: ../../shared/tools.ToolUse
  kind: typing
- sym: ./BaseTool.BaseTool
- sym: ./BaseTool.ToolCallbacks
data:
- name: searchReplaceTool
  type: any
  val: new SearchReplaceTool()
types:
- name: SearchReplaceParams
  kind: trait
  decorators:
  - interface
  fields:
  - name: file_path
    type: string
  - name: old_string
    type: string
  - name: new_string
    type: string
  lines:
  - 18
  - 22
- name: SearchReplaceTool
  kind: struct
  bases:
  - BaseTool
  fields:
  - name: name
    type: any
    default: '"search_replace" as const'
  methods:
  - name: execute
    sig: 'execute(params: SearchReplaceParams, task: Task, callbacks: ToolCallbacks): Promise<void>'
    is_async: true
    lines:
    - 27
    - 240
    detail:
      calls:
      - JSON.stringify
      - askApproval
      - computeDiffStats
      - experiments.isEnabled
      - fileContent.replace
      - fileContent.split
      - fileExistsAtPath
      - formatResponse.createPrettyPatch
      - formatResponse.rooIgnoreError
      - formatResponse.toolError
      - fs.readFile
      - getReadablePath
      - handleError
      - isPathOutsideWorkspace
      - new_string.replace
      - old_string.replace
      - path.isAbsolute
      - path.relative
      - path.resolve
      - provider?.getState
      - pushToolResult
      - sanitizeUnifiedDiff
      - task.diffViewProvider.open
      - task.diffViewProvider.pushToolWriteResult
      - task.diffViewProvider.reset
      - task.diffViewProvider.revertChanges
      - task.diffViewProvider.saveChanges
      - task.diffViewProvider.saveDirectly
      - task.diffViewProvider.scrollToFirstDiff
      - task.diffViewProvider.update
      - task.fileContextTracker.trackFileContext
      - task.processQueuedMessages
      - task.providerRef.deref
      - task.recordToolError
      - task.recordToolUsage
      - task.rooIgnoreController?.validateAccess
      - task.rooProtectedController?.isWriteProtected
      - task.say
      - task.sayAndCreateMissingParamError
      - this.resetPartialState
  - name: handlePartial
    sig: 'handlePartial(task: Task, block: ToolUse<"search_replace">): Promise<void>'
    is_async: true
    lines:
    - 242
    - 275
    detail:
      calls:
      - JSON.stringify
      - getReadablePath
      - isPathOutsideWorkspace
      - oldString.substring
      - path.isAbsolute
      - path.relative
      - path.resolve
      - task.ask
      - task.ask("tool", JSON.stringify(sharedMessageProps), block.partial).catch
      - this.hasPathStabilized
  lines:
  - 24
  - 276
exports:
- name: SearchReplaceParams
  kind: type
- name: SearchReplaceTool
  kind: type
- name: searchReplaceTool
  kind: data
