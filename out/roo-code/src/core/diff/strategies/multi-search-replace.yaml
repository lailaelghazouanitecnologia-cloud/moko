## Roska File Descriptor — strategies/multi-search-replace.ts
file: strategies/multi-search-replace.ts
lines: 546
layer: logic
imports:
- sym: fastest-levenshtein.distance
- sym: '@roo-code/types.ToolProgressStatus'
- sym: ../../../integrations/misc/extract-text.addLineNumbers
- sym: ../../../integrations/misc/extract-text.everyLineHasLineNumbers
- sym: ../../../integrations/misc/extract-text.stripLineNumbers
- sym: ../../../shared/tools.ToolUse
- sym: ../../../shared/tools.DiffStrategy
- sym: ../../../shared/tools.DiffResult
- sym: ../../../utils/text-normalization.normalizeString
data:
- name: BUFFER_LINES
  type: number
  val: '40'
types:
- name: MultiSearchReplaceDiffStrategy
  kind: struct
  bases:
  - DiffStrategy
  fields:
  - name: fuzzyThreshold
    type: number
  - name: bufferLines
    type: number
  methods:
  - name: getName
    sig: 'getName(): string'
    lines:
    - 79
    - 81
    detail: {}
  - name: constructor
    sig: 'constructor(fuzzyThreshold?: number, bufferLines?: number)'
    lines:
    - 83
    - 89
    detail: {}
  - name: unescapeMarkers
    sig: 'unescapeMarkers(content: string): string'
    vis: private
    lines:
    - 91
    - 99
    detail:
      calls:
      - "content\n\t\t\t.replace"
      - "content\n\t\t\t.replace(/^\\\\<<<<<<</gm, \"<<<<<<<\")\n\t\t\t.replace"
      - "content\n\t\t\t.replace(/^\\\\<<<<<<</gm, \"<<<<<<<\")\n\t\t\t.replace(/^\\\\=======/gm, \"=======\")\n\t\t\t.replace"
      - "content\n\t\t\t.replace(/^\\\\<<<<<<</gm, \"<<<<<<<\")\n\t\t\t.replace(/^\\\\=======/gm, \"=======\")\n\t\t\t.replace(/^..."
  - name: validateMarkerSequencing
    sig: 'validateMarkerSequencing(diffContent: string): { success: boolean; error?: string }'
    vis: private
    lines:
    - 101
    - 243
    detail:
      calls:
      - SEARCH_PATTERN.source.replace
      - SEARCH_PATTERN.test
      - diffContent.split
      - line.trim
      - line.trim().startsWith
      - lines.filter
      - marker.startsWith
      - reportInvalidDiffError
      - reportLineMarkerInReplaceError
      - reportMergeConflictError
  - name: applyDiff
    sig: "applyDiff(\n\t\toriginalContent: string,\n\t\tdiffContent: string,\n\t\t_paramStartLine?: number,\n\t\t_paramEndLine?: number,\n\t): Promise<DiffResult>"
    is_async: true
    lines:
    - 245
    - 521
    detail:
      calls:
      - Math.floor
      - Math.max
      - Math.min
      - addLineNumbers
      - aggressiveSearchContent.split
      - aggressiveSearchLines.join
      - diffContent.matchAll
      - diffResults.push
      - everyLineHasLineNumbers
      - fuzzySearch
      - getSimilarity
      - matchedLines.map
      - "matches\n\t\t\t.map"
      - "matches\n\t\t\t.map((match) => ({\n\t\t\t\tstartLine: Number(match[2] ?? 0),\n\t\t\t\tsearchContent: match[6],\n\t\t\t..."
      - originalContent.includes
      - originalContent.split
      - parseInt
      - replaceContent.split
      - replaceContent.trim
      - replaceLines.map
      - "resultLines\n\t\t\t\t\t\t\t\t\t\t.slice"
      - "resultLines\n\t\t\t\t\t\t\t\t\t\t.slice(\n\t\t\t\t\t\t\t\t\t\t\tMath.max(0, startLine - 1 - this.bufferLines),\n\t\t\t\t\t\t\t\t\t\t\tM..."
      - resultLines.join
      - resultLines.slice
      - resultLines.slice(exactStartIndex, exactEndIndex + 1).join
      - searchContent.split
      - searchContent.split("\n")[0].split
      - searchLines.join
      - searchLines.map
      - stripLineNumbers
      - this.unescapeMarkers
      - this.validateMarkerSequencing
  - name: getProgressStatus
    sig: 'getProgressStatus(toolUse: ToolUse, result?: DiffResult): ToolProgressStatus'
    lines:
    - 523
    - 545
    detail:
      calls:
      - Math.floor
      - diffContent.match
  lines:
  - 75
  - 546
functions:
- name: getSimilarity
  sig: 'getSimilarity(original: string, search: string): number'
  lines:
  - 11
  - 31
  detail:
    calls:
    - Math.max
    - distance
    - normalizeString
- name: fuzzySearch
  sig: 'fuzzySearch(lines: string[], searchChunk: string, startIndex: number, endIndex: number)'
  lines:
  - 37
  - 73
  detail:
    calls:
    - Math.floor
    - getSimilarity
    - lines.slice
    - lines.slice(leftIndex, leftIndex + searchLen).join
    - lines.slice(rightIndex, rightIndex + searchLen).join
    - searchChunk.split
exports:
- name: MultiSearchReplaceDiffStrategy
  kind: type
- name: getSimilarity
  kind: func
- name: fuzzySearch
  kind: func
- name: BUFFER_LINES
  kind: data
