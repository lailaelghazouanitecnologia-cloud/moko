## Roska File Descriptor — shared/get-relative-path.ts
file: shared/get-relative-path.ts
lines: 31
layer: logic
imports:
- sym: path.path
functions:
- name: generateNormalizedAbsolutePath
  sig: 'generateNormalizedAbsolutePath(filePath: string, workspaceRoot: string): string'
  lines:
  - 11
  - 16
  detail:
    calls:
    - path.normalize
    - path.resolve
- name: generateRelativeFilePath
  sig: 'generateRelativeFilePath(normalizedAbsolutePath: string, workspaceRoot: string): string'
  lines:
  - 26
  - 31
  detail:
    calls:
    - path.normalize
    - path.relative
exports:
- name: generateNormalizedAbsolutePath
  kind: func
- name: generateRelativeFilePath
  kind: func
