## Roska File Descriptor — fixtures/sample-zig.ts
file: fixtures/sample-zig.ts
lines: 42
layer: test
data:
- name: sampleZig
  type: string
  val: |-
    `
    const std = @import("std");

    // A basic struct
    pub const Point = struct {
        x: f32,
        y: f32,

        pub fn init(x: ...
exports:
- name: sampleZig
  kind: data
