## Roska File Descriptor — __mocks__/list-files.ts
file: __mocks__/list-files.ts
lines: 63
layer: logic
purpose: 'Mock implementation of list-files module IMPORTANT NOTES: 1. This file must be placed in src/services/glob/__mocks__/ to properly mock the module 2. DO NOT IMPORT any modules from the application code to avoid circular dependencies 3. All dependencies are mocked/stubbed locally for isolation This implementation provides predictable behavior for tests without requiring actual filesystem access or ripgrep binary.'
data:
- name: listFiles
  type: any
  val: "vi.fn((dirPath: string, _recursive: boolean, limit: number) => {\n\t// Early return for limit of 0 - matches the actual im..."
functions:
- name: mockResolve
  sig: 'mockResolve(dirPath: string): string'
  lines:
  - 20
  - 22
  detail:
    calls:
    - dirPath.startsWith
exports:
- name: mockResolve
  kind: func
- name: listFiles
  kind: data
