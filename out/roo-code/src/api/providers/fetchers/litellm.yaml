## Roska File Descriptor — fetchers/litellm.ts
file: fetchers/litellm.ts
lines: 82
layer: logic
imports:
- sym: axios.axios
- sym: '@roo-code/types.ModelRecord'
  kind: typing
- sym: ../constants.DEFAULT_HEADERS
functions:
- name: getLiteLLMModels
  sig: 'getLiteLLMModels(apiKey: string, baseUrl: string): Promise<ModelRecord>'
  is_async: true
  lines:
  - 14
  - 82
  detail:
    calls:
    - Array.isArray
    - Boolean
    - axios.get
    - axios.isAxiosError
    - console.error
    - new Error
    - new URL
    - urlObj.pathname.replace
    - urlObj.pathname.replace(/\/+$/, "").replace
exports:
- name: getLiteLLMModels
  kind: func
