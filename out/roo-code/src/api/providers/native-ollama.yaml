## Roska File Descriptor — providers/native-ollama.ts
file: providers/native-ollama.ts
lines: 378
layer: logic
imports:
- sym: '@anthropic-ai/sdk.Anthropic'
- sym: openai.OpenAI
- sym: ollama.Message
- sym: ollama.Ollama
- sym: ollama.Tool as OllamaTool
- sym: ollama.type Config as OllamaOptions
- sym: '@roo-code/types.ModelInfo'
- sym: '@roo-code/types.openAiModelInfoSaneDefaults'
- sym: '@roo-code/types.DEEP_SEEK_DEFAULT_TEMPERATURE'
- sym: ../transform/stream.ApiStream
- sym: ./base-provider.BaseProvider
- sym: ../../shared/api.ApiHandlerOptions
  kind: typing
- sym: ./fetchers/ollama.getOllamaModels
- sym: ../../utils/tag-matcher.TagMatcher
- sym: ../index.SingleCompletionHandler
  kind: typing
- sym: ../index.ApiHandlerCreateMessageMetadata
  kind: typing
types:
- name: OllamaChatOptions
  kind: trait
  decorators:
  - interface
  fields:
  - name: temperature
    type: number
  - name: num_ctx
    type: number
  lines:
  - 12
  - 15
- name: NativeOllamaHandler
  kind: struct
  ctx: Converts OpenAI-format tools to Ollama's native tool format. This allows NativeOllamaHandler to use the same tool definitions that are passed to OpenAI-compatible providers.
  bases:
  - BaseProvider
  - SingleCompletionHandler
  fields:
  - name: options
    type: ApiHandlerOptions
  - name: client
    type: Ollama | undefined
  - name: models
    type: Record<string, ModelInfo>
    default: '{}'
  methods:
  - name: constructor
    sig: 'constructor(options: ApiHandlerOptions)'
    lines:
    - 153
    - 156
    detail:
      calls:
      - super
  - name: createMessage
    sig: "createMessage(\n\t\tsystemPrompt: string,\n\t\tmessages: Anthropic.Messages.MessageParam[],\n\t\tmetadata?: ApiHandlerCreateMessageMetadata,\n\t): ApiStream"
    is_async: true
    lines:
    - 203
    - 332
    detail:
      calls:
      - JSON.stringify
      - client.chat
      - console.error
      - convertToOllamaMessages
      - matcher.final
      - matcher.update
      - modelId.toLowerCase
      - modelId.toLowerCase().includes
      - new Error
      - new TagMatcher
      - this.convertToolsToOllama
      - this.ensureClient
      - this.fetchModel
      - this.getModel
      - toolCallIds.push
  - name: fetchModel
    sig: fetchModel()
    is_async: true
    lines:
    - 334
    - 337
    detail:
      calls:
      - getOllamaModels
      - this.getModel
  - name: getModel
    sig: 'getModel(): { id: string; info: ModelInfo }'
    lines:
    - 339
    - 345
    detail: {}
  - name: completePrompt
    sig: 'completePrompt(prompt: string): Promise<string>'
    is_async: true
    lines:
    - 347
    - 377
    detail:
      calls:
      - client.chat
      - modelId.toLowerCase
      - modelId.toLowerCase().includes
      - new Error
      - this.ensureClient
      - this.fetchModel
  lines:
  - 148
  - 378
functions:
- name: convertToOllamaMessages
  sig: 'convertToOllamaMessages(anthropicMessages: Anthropic.Messages.MessageParam[]): Message[]'
  lines:
  - 17
  - 146
  detail:
    calls:
    - anthropicMessage.content.reduce
    - "nonToolMessages\n\t\t\t\t\t\t.filter"
    - "nonToolMessages\n\t\t\t\t\t\t.filter((part) => part.type === \"text\")\n\t\t\t\t\t\t.map"
    - "nonToolMessages\n\t\t\t\t\t\t.filter((part) => part.type === \"text\")\n\t\t\t\t\t\t.map((part) => part.text)\n\t\t\t\t\t\t..."
    - "nonToolMessages\n\t\t\t\t\t\t.map"
    - "nonToolMessages\n\t\t\t\t\t\t.map((part) => {\n\t\t\t\t\t\t\tif (part.type === \"image\") {\n\t\t\t\t\t\t\t\treturn \"\" // impo..."
    - nonToolMessages.forEach
    - ollamaMessages.push
    - toolMessages.forEach
    - toolMessages.map
exports:
- name: OllamaChatOptions
  kind: type
- name: NativeOllamaHandler
  kind: type
- name: convertToOllamaMessages
  kind: func
