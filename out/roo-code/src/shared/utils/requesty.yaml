## Roska File Descriptor — utils/requesty.ts
file: utils/requesty.ts
lines: 56
layer: logic
data:
- name: REQUESTY_BASE_URL
  type: string
  val: '"https://router.requesty.ai/v1"'
types:
- name: URLType
  kind: enum
  decorators:
  - type
  variants:
  - name: '"api"'
  lines:
  - 3
  - 3
functions:
- name: replaceCname
  sig: 'replaceCname(baseUrl: string, type: URLType): string'
  lines:
  - 11
  - 32
  detail:
    calls:
    - baseUrl.replace
    - baseUrl.replace("router", type).replace
    - new URL
    - url.hostname.includes
    - url.hostname.replace
    - url.pathname.endsWith
    - url.pathname.slice
    - url.toString
- name: toRequestyServiceUrl
  sig: 'toRequestyServiceUrl(baseUrl?: string | null, service: URLType = "router"): string'
  lines:
  - 40
  - 56
  detail:
    calls:
    - baseUrl.trim
    - console.warn
    - new URL
    - new URL(urlToUse).toString
    - replaceCname
exports:
- name: URLType
  kind: type
- name: replaceCname
  kind: func
- name: toRequestyServiceUrl
  kind: func
- name: REQUESTY_BASE_URL
  kind: data
