## Roska File Descriptor — performance/processCarriageReturns.benchmark.ts
file: performance/processCarriageReturns.benchmark.ts
lines: 441
layer: logic
imports:
- sym: ../../extract-text.processCarriageReturns
- sym: ../../extract-text.applyRunLengthEncoding
- sym: ../../extract-text.truncateOutput
data:
- name: SEED
  type: number
  val: '12345'
- name: seed
  type: any
  val: SEED
functions:
- name: random
  sig: random()
  lines:
  - 19
  - 22
  detail:
    calls:
    - Math.floor
    - Math.sin
- name: generateTestData
  sig: 'generateTestData(size: number, complexity: "simple" | "medium" | "complex" = "medium"): string'
  lines:
  - 25
  - 95
  detail:
    calls:
    - '"#".repeat'
    - '"-".repeat'
    - '"=".repeat'
    - '"\n".repeat'
    - Math.floor
    - Math.max
    - random
- name: generateLongLineTestData
  sig: 'generateLongLineTestData(lineLengthKB: number, updateCount: number): string'
  lines:
  - 98
  - 128
  detail:
    calls:
    - Math.floor
    - String.fromCharCode
    - baseString.substring
- name: generateHighDensityCRData
  sig: 'generateHighDensityCRData(size: number): string'
  lines:
  - 131
  - 154
  detail:
    calls:
    - Math.floor
    - String.fromCharCode
    - random
- name: getIterationCount
  sig: 'getIterationCount(size: number): number'
  lines:
  - 157
  - 162
  detail: {}
- name: calculateStats
  sig: 'calculateStats(durations: number[])'
  lines:
  - 165
  - 181
  detail:
    calls:
    - Math.floor
    - Math.sqrt
    - '[...durations].sort'
    - durations.map
    - durations.map((x) => Math.pow(x - mean, 2)).reduce
    - durations.reduce
- name: runPerformanceTest
  sig: "runPerformanceTest(\n\tname: string,\n\tfn: (input: string, ...args: any[]) => string,\n\tinput: string,\n\titerations: number,\n\targs: any[] = [],\n)"
  lines:
  - 184
  - 257
  detail:
    calls:
    - (100 - (warmupResult.length / input.length) * 100).toFixed
    - (input.length / (1024 * 1024) / (stats.min / 1000)).toFixed
    - (input.length / (1024 * 1024) / (stats.p95 / 1000)).toFixed
    - (totalSizeProcessed / totalBenchmarkTime).toFixed
    - (warmupResult.length / (1024 * 1024)).toFixed
    - Math.floor
    - calculateStats
    - console.log
    - durations.push
    - durations.reduce
    - fn
    - global.gc
    - parseFloat
    - performance.now
    - process.stdout.write
    - stats.max.toFixed
    - stats.mean.toFixed
    - stats.median.toFixed
    - stats.min.toFixed
    - stats.p95.toFixed
    - stats.p99.toFixed
- name: runBaselineTest
  sig: 'runBaselineTest(input: string, iterations: number)'
  lines:
  - 260
  - 287
  detail:
    calls:
    - (input.length / (1024 * 1024)).toFixed
    - Math.max
    - avgMean.toFixed
    - avgThroughput.toFixed
    - console.log
    - maxVariation.toFixed
    - meanTimes.map
    - meanTimes.reduce
    - results.map
    - results.push
    - runPerformanceTest
    - throughputVariation.toFixed
    - throughputs.map
    - throughputs.reduce
- name: runBenchmark
  sig: runBenchmark()
  lines:
  - 290
  - 432
  detail:
    calls:
    - '"-".repeat'
    - '"=".repeat'
    - (testData.length / (1024 * 1024)).toFixed
    - Math.floor
    - Math.max
    - Math.min
    - applyRunLengthEncoding
    - console.log
    - genTime.toFixed
    - generateHighDensityCRData
    - generateLongLineTestData
    - generateTestData
    - getIterationCount
    - performance.now
    - processCarriageReturns
    - runBaselineTest
    - runPerformanceTest
    - testData.match
    - truncateOutput
exports:
- name: random
  kind: func
- name: generateTestData
  kind: func
- name: generateLongLineTestData
  kind: func
- name: generateHighDensityCRData
  kind: func
- name: getIterationCount
  kind: func
- name: calculateStats
  kind: func
- name: runPerformanceTest
  kind: func
- name: runBaselineTest
  kind: func
- name: runBenchmark
  kind: func
- name: SEED
  kind: data
- name: seed
  kind: data
