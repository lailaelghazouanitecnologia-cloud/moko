## Roska File Descriptor — __tests__/indentation-reader.spec.ts
file: __tests__/indentation-reader.spec.ts
lines: 639
layer: test
imports:
- sym: vitest.describe
- sym: vitest.it
- sym: vitest.expect
- sym: ../indentation-reader.parseLines
- sym: ../indentation-reader.formatWithLineNumbers
- sym: ../indentation-reader.readWithIndentation
- sym: ../indentation-reader.readWithSlice
- sym: ../indentation-reader.computeEffectiveIndents
- sym: ../indentation-reader.type LineRecord
- sym: ../indentation-reader.type IndentationReadResult
data:
- name: PYTHON_CODE
  type: string
  val: |-
    `#!/usr/bin/env python3
    """Module docstring."""
    import os
    import sys
    from typing import List

    class Calculator:
        """A...
- name: TYPESCRIPT_CODE
  type: string
  val: |-
    `import { something } from "./module"
    import type { SomeType } from "./types"

    // Constants
    const MAX_VALUE = 100

    inter...
- name: SIMPLE_CODE
  type: string
  val: |-
    `function outer() {
        function inner() {
            console.log("hello")
        }
        inner()
    }
    `
- name: CODE_WITH_BLANKS
  type: string
  val: "`class Example:\n    def method_one(self):\n        x = 1\n        \n        y = 2\n        \n        return x + y\n    \n    de..."
exports:
- name: PYTHON_CODE
  kind: data
- name: TYPESCRIPT_CODE
  kind: data
- name: SIMPLE_CODE
  kind: data
- name: CODE_WITH_BLANKS
  kind: data
