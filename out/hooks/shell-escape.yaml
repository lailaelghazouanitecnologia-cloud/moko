## Roska File Descriptor — hooks/shell-escape.ts
file: hooks/shell-escape.ts
lines: 67
purpose: 'Platform-specific shell escaping utilities for hook script paths. Ensures paths with spaces and special characters work correctly when executed through a shell (shell: true in spawn).'
functions:
- name: escapeWindowsShellPath
  sig: 'escapeWindowsShellPath(path: string): string'
  lines:
  - 19
  - 26
  detail:
    calls:
    - escaped.replace
    - path.replace
- name: escapeUnixShellPath
  sig: 'escapeUnixShellPath(path: string): string'
  lines:
  - 40
  - 45
  detail:
    calls:
    - path.replace
- name: escapeShellPath
  sig: 'escapeShellPath(path: string): string'
  lines:
  - 65
  - 67
exports:
- name: escapeWindowsShellPath
  kind: func
- name: escapeUnixShellPath
  kind: func
- name: escapeShellPath
  kind: func
