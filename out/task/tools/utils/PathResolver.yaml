## Roska File Descriptor — utils/PathResolver.ts
file: utils/PathResolver.ts
lines: 45
imports:
- sym: '@/core/workspace.resolveWorkspacePath'
- sym: ../ToolValidator.ToolValidator
  kind: typing
- sym: ../types/TaskConfig.TaskConfig
  kind: typing
types:
- name: PathResolver
  kind: struct
  methods:
  - name: constructor
    sig: "constructor(\n\t\tprivate config: TaskConfig,\n\t\tprivate validator: ToolValidator,\n\t)"
    lines:
    - 9
    - 12
    detail: {}
  - name: resolve
    sig: 'resolve(filePath: string, caller: string): { absolutePath: string; resolvedPath: string } | undefined'
    lines:
    - 14
    - 23
    detail:
      calls:
      - resolveWorkspacePath
  - name: validate
    sig: 'validate(resolvedPath: string): { ok: boolean; error?: string }'
    lines:
    - 25
    - 27
    detail:
      calls:
      - this.validator.checkClineIgnorePath
  - name: resolveAndValidate
    sig: "resolveAndValidate(\n\t\tfilePath: string,\n\t\tcaller: string,\n\t): Promise<{ absolutePath: string; resolvedPath: string } | undefined>"
    is_async: true
    lines:
    - 29
    - 44
    detail:
      calls:
      - this.resolve
      - this.validate
  lines:
  - 8
  - 45
exports:
- name: PathResolver
  kind: type
