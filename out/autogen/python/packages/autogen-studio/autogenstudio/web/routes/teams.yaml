## Roska File Descriptor — routes/teams.py
file: routes/teams.py
lines: 50
imports:
- sym: fastapi.APIRouter
- sym: fastapi.Depends
- sym: fastapi.HTTPException
data:
- name: router
  type: Any
  val: APIRouter()
functions:
- name: list_teams
  sig: 'list_teams(user_id: str, db=Depends(get_db)) -> Dict'
  is_async: true
  decorators:
  - router.get("/")
  lines:
  - 14
  - 25
  detail:
    calls:
    - Team
    - create_default_gallery
    - db.get
    - db.upsert
    - default_gallery.components.teams[0].model_dump
    - len
- name: get_team
  sig: 'get_team(team_id: int, user_id: str, db=Depends(get_db)) -> Dict'
  is_async: true
  decorators:
  - router.get("/{team_id}")
  lines:
  - 29
  - 34
  detail:
    calls:
    - HTTPException
    - db.get
- name: create_team
  sig: 'create_team(team: Team, db=Depends(get_db)) -> Dict'
  is_async: true
  decorators:
  - router.post("/")
  lines:
  - 38
  - 43
  detail:
    calls:
    - HTTPException
    - db.upsert
- name: delete_team
  sig: 'delete_team(team_id: int, user_id: str, db=Depends(get_db)) -> Dict'
  is_async: true
  decorators:
  - router.delete("/{team_id}")
  lines:
  - 47
  - 50
  detail:
    calls:
    - db.delete
exports:
- name: list_teams
  kind: func
- name: get_team
  kind: func
- name: create_team
  kind: func
- name: delete_team
  kind: func
- name: router
  kind: data
