## Roska File Descriptor — core_streaming_handoffs_fastapi/tools.py
file: core_streaming_handoffs_fastapi/tools.py
lines: 32
imports:
- sym: autogen_core.tools.FunctionTool
data:
- name: execute_order_tool
  type: Any
  val: FunctionTool(execute_order, description="Price should be in USD.")
- name: look_up_item_tool
  type: Any
  val: |-
    FunctionTool(
        look_up_item, description="Use to find item ID.\nSearch query can be a description or keywords."
    )
- name: execute_refund_tool
  type: Any
  val: FunctionTool(execute_refund, description="")
functions:
- name: execute_order
  sig: 'execute_order(product: str, price: int) -> Dict[str, Union[str, int]]'
  lines:
  - 5
  - 10
  detail:
    calls:
    - print
- name: look_up_item
  sig: 'look_up_item(search_query: str) -> Dict[str, str]'
  lines:
  - 14
  - 16
- name: execute_refund
  sig: 'execute_refund(item_id: str, reason: str = "not provided") -> Dict[str, str]'
  lines:
  - 19
  - 25
  detail:
    calls:
    - print
exports:
- name: execute_order
  kind: func
- name: look_up_item
  kind: func
- name: execute_refund
  kind: func
- name: execute_order_tool
  kind: data
- name: look_up_item_tool
  kind: data
- name: execute_refund_tool
  kind: data
