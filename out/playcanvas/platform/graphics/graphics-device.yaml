## PlayCanvas — GraphicsDevice
name: graphics-device.js
path: src/platform/graphics/graphics-device.js
module: platform/graphics
layer: graphics
lines: 850
types:
  - name: GraphicsDevice
    kind: class
    extends: EventHandler
    fields: [canvas, scope, maxTextures, maxTextureSize, maxCubeMapSize, maxVolumeSize, maxAnisotropy, supportsInstancing, supportsUniformBuffers]
    methods: [setViewport, setScissor, clear, draw, setBlendState, setDepthState, setCullMode, setStencilState, copyRenderTarget, pushMarker, popMarker]
functions:
  - name: createGraphicsDevice
    signature: "createGraphicsDevice(canvas: HTMLCanvasElement, options?: object): Promise<GraphicsDevice>"
    description: "Factory — tries WebGPU, falls back to WebGL2, then Null"
imports: [EventHandler, Vec2, Color]
calls: [WebglGraphicsDevice, WebgpuGraphicsDevice, NullGraphicsDevice]
