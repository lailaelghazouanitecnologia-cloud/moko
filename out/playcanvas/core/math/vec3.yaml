## PlayCanvas — Vec3 Math
name: vec3.js
path: src/core/math/vec3.js
module: core/math
layer: core
lines: 450
types:
  - name: Vec3
    kind: class
    fields: [x, y, z]
    methods: [add, sub, mul, mulScalar, div, dot, cross, normalize, length, lengthSq, lerp, distance, min, max, floor, ceil, round, equals, clone, copy, set, toString]
    static_methods: [add, sub, cross, ZERO, ONE, UP, DOWN, LEFT, RIGHT, FORWARD, BACK]
  - name: Vec2
    kind: class
    fields: [x, y]
    methods: [add, sub, mul, dot, normalize, length, lerp, distance, equals]
  - name: Mat4
    kind: class
    fields: ["data: Float32Array(16)"]
    methods: [mul, translate, rotate, scale, invert, transpose, setIdentity, setTRS, getTranslation, getScale, getEulerAngles]
    static_methods: [perspective, ortho, lookAt]
  - name: Quat
    kind: class
    fields: [x, y, z, w]
    methods: [mul, normalize, slerp, setFromEulerAngles, setFromMat4, getEulerAngles, transformVector]
description: "Math library — Vec2/Vec3/Vec4/Mat3/Mat4/Quat. Float32Array storage, column-major matrices."
