## PlayCanvas — ForwardRenderer
name: forward-renderer.js
path: src/scene/renderer/forward-renderer.js
module: scene/renderer
layer: scene
lines: 1200
types:
  - name: ForwardRenderer
    kind: class
    extends: Renderer
    fields: [scene, graphicsDevice, layers, camera, shadowMapCache, worldClustersAllocator]
    methods: [render, cull, renderShadows, sortMeshInstances, setCamera, dispatchGlobalLights, drawInstance]
  - name: Renderer
    kind: class
    fields: [scene, device]
    methods: [render, _cull]
constants:
  - SORTMODE_NONE: 0
  - SORTMODE_MANUAL: 1
  - SORTMODE_MATERIALMESH: 2
  - SORTMODE_BACK2FRONT: 3
  - SORTMODE_FRONT2BACK: 4
imports: [GraphicsDevice, Scene, Layer, Camera, Light, MeshInstance]
description: "Forward rendering pipeline — per-layer sort + frustum cull + shadow passes"
