## PlayCanvas — Entity (ECS core)
name: entity.js
path: src/framework/entity.js
module: framework
layer: framework
lines: 700
types:
  - name: Entity
    kind: class
    extends: GraphNode
    fields: [c, _guid, anim, camera, collision, light, model, render, script, sprite, sound]
    methods: [addComponent, removeComponent, getComponent, destroy, clone, findByGuid, findByName, findByPath]
  - name: Component
    kind: class
    extends: EventHandler
    fields: [system, entity, enabled, data]
    methods: [onEnable, onDisable, onPostStateChange]
  - name: ComponentSystem
    kind: class
    extends: EventHandler
    fields: [id, app, store, schema]
    methods: [addComponent, removeComponent, initializeComponentData, cloneComponent]
imports: [GraphNode, EventHandler]
description: "Entity-Component-System — entities are GraphNodes with component bags, systems manage lifecycle"
