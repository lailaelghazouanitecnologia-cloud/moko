## Roska File Descriptor — language-utils/index.ts
file: language-utils/index.ts
lines: 75
layer: logic
imports:
- sym: path.path
- sym: lodash-es.has
- sym: lodash-es.isNil
- sym: '@/lib/gql/generates/graphql.Language as ProgrammingLanguage'
- sym: ./language-colors.json.languageColors
- sym: ./languages.languages
data:
- name: languageColorMapping
  type: Record<string, string>
  val: |-
    Object.entries(
      languageColors
    ).reduce((acc, cur) => {
      const [lan, color] = cur
      return { ...acc, [lan.toLocaleLow...
functions:
- name: filename2prism
  sig: 'filename2prism(): (filename: string) => Array<string>'
  lines:
  - 18
  - 47
  detail:
    calls:
    - Object.entries
    - extnames[extname].push
    - filenames[filename].push
    - has
    - path.basename
    - path.extname
    - path.extname(filename).substring
    - |-
      result
          .concat
    - |-
      result
          .concat(
            filenames[path.basename(filename)],
            extnames[path.extname(filename)....
- name: getLanguageDisplayName
  sig: |-
    getLanguageDisplayName(
      lan?: string,
      defaultLan?: string
    ): string
  lines:
  - 49
  - 71
  detail:
    calls:
    - Object.keys
    - Object.values
    - |-
      Object.values(ProgrammingLanguage)
          .map
    - |-
      Object.values(ProgrammingLanguage)
          .map(lan => lan.toLocaleLowerCase())
          .indexOf
    - displayName.toLocaleLowerCase
    - lan.toLocaleLowerCase
    - returnDefault
- name: getLanguageColor
  sig: 'getLanguageColor(lan: string): string | undefined'
  lines:
  - 73
  - 75
  detail:
    calls:
    - lan.toLowerCase
exports:
- name: filename2prism
  kind: func
- name: getLanguageDisplayName
  kind: func
- name: getLanguageColor
  kind: func
- name: languageColorMapping
  kind: data
