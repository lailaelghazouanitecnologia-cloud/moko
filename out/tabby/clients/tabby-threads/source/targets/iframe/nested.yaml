## Roska File Descriptor — iframe/nested.ts
file: iframe/nested.ts
lines: 95
layer: logic
imports:
- sym: '@quilted/events.NestedAbortController'
- sym: ../target.createThread
- sym: ../target.type ThreadOptions
- sym: ./shared.CHECK_MESSAGE
- sym: ./shared.RESPONSE_MESSAGE
functions:
- name: createThreadFromInsideIframe
  sig: |-
    createThreadFromInsideIframe({
      targetOrigin = "*",
      ...options
    }: ThreadOptions<Self, Target> & {
      /**
       * The target origin to use when sending `postMessage` events to the parent frame.
       *
       * @see https://developer.mozilla.org/en-US/docs/Web/API/Window/postMessage#targetorigin
       * @default '*'
       */
      targetOrigin?: string;
    } = {})
  lines:
  - 18
  - 95
  detail:
    calls:
    - createThread
    - document.addEventListener
    - new AbortController
    - new Error
    - new NestedAbortController
    - parent.postMessage
    - ready
    - self.addEventListener
exports:
- name: createThreadFromInsideIframe
  kind: func
