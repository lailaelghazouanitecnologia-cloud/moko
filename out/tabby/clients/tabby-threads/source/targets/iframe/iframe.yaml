## Roska File Descriptor — iframe/iframe.ts
file: iframe/iframe.ts
lines: 98
layer: logic
imports:
- sym: '@quilted/events.NestedAbortController'
- sym: ../target.createThread
- sym: ../target.type ThreadTarget
- sym: ../target.type ThreadOptions
- sym: ./shared.CHECK_MESSAGE
- sym: ./shared.RESPONSE_MESSAGE
functions:
- name: createThreadFromIframe
  sig: |-
    createThreadFromIframe(
      iframe: HTMLIFrameElement,
      {
        targetOrigin = "*",
        ...options
      }: ThreadOptions<Self, Target> & {
        /**
         * The target origin to use when sending `postMessage` events to the child frame.
         *
         * @see https://developer.mozilla.org/en-US/docs/Web/API/Window/postMessage#targetorigin
         * @default '*'
         */
        targetOrigin?: string;
      } = {}
    )
  lines:
  - 19
  - 98
  detail:
    calls:
    - connectedPromise.then
    - createThread
    - iframe.contentWindow?.postMessage
    - new Promise
    - self.addEventListener
    - sendMessage
exports:
- name: createThreadFromIframe
  kind: func
