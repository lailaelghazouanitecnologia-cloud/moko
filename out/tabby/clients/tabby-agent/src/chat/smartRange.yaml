## Roska File Descriptor — chat/smartRange.ts
file: chat/smartRange.ts
lines: 45
layer: logic
imports:
- sym: js-levenshtein.levenshtein
- sym: vscode-languageserver-protocol.Position
- sym: vscode-languageserver-protocol.Range
- sym: vscode-languageserver-textdocument.TextDocument
functions:
- name: getSmartApplyRange
  sig: |-
    getSmartApplyRange(
      document: TextDocument,
      snippet: string,
    ): { range: Range; action: "insert" | "replace" } | undefined
  lines:
  - 6
  - 19
  detail:
    calls:
    - document.getText
    - document.getText().trim
    - fuzzyApplyRange
- name: fuzzyApplyRange
  sig: 'fuzzyApplyRange(document: TextDocument, snippet: string): { range: Range; score: number } | null'
  lines:
  - 21
  - 45
  detail:
    calls:
    - document.getText
    - document.getText().split
    - levenshtein
    - lines.slice
    - lines.slice(i, i + snippetLines.length).join
    - snippet.split
exports:
- name: getSmartApplyRange
  kind: func
- name: fuzzyApplyRange
  kind: func
