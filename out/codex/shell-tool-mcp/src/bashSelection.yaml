## Roska File Descriptor — src/bashSelection.ts
file: src/bashSelection.ts
lines: 115
imports:
- sym: node:path.path
- sym: node:os.os
- sym: ./constants.DARWIN_BASH_VARIANTS
- sym: ./constants.LINUX_BASH_VARIANTS
- sym: ./types.BashSelection
- sym: ./types.OsReleaseInfo
functions:
- name: supportedDetail
  sig: 'supportedDetail(variants: ReadonlyArray<{ name: string }>): string'
  lines:
  - 6
  - 8
  detail:
    calls:
    - variants.map
    - variants.map((variant) => variant.name).join
- name: selectLinuxBash
  sig: |-
    selectLinuxBash(
      bashRoot: string,
      info: OsReleaseInfo,
    ): BashSelection
  lines:
  - 10
  - 66
  detail:
    calls:
    - Boolean
    - candidates.filter
    - candidates.push
    - new Error
    - path.join
    - pickVariant
    - supportedDetail
    - variant.ids.includes
    - variant.ids.some
    - variant.versions.some
- name: selectDarwinBash
  sig: |-
    selectDarwinBash(
      bashRoot: string,
      darwinRelease: string,
    ): BashSelection
  lines:
  - 68
  - 95
  detail:
    calls:
    - DARWIN_BASH_VARIANTS.find
    - Number.parseInt
    - darwinRelease.split
    - new Error
    - path.join
    - supportedDetail
- name: resolveBashPath
  sig: |-
    resolveBashPath(
      targetRoot: string,
      platform: NodeJS.Platform,
      darwinRelease = os.release(),
      osInfo: OsReleaseInfo | null = null,
    ): BashSelection
  lines:
  - 97
  - 115
  detail:
    calls:
    - new Error
    - path.join
    - selectDarwinBash
    - selectLinuxBash
exports:
- name: supportedDetail
  kind: func
- name: selectLinuxBash
  kind: func
- name: selectDarwinBash
  kind: func
- name: resolveBashPath
  kind: func
