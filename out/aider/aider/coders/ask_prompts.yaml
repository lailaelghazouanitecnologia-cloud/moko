## Roska File Descriptor — coders/ask_prompts.py
file: coders/ask_prompts.py
lines: 41
layer: logic
types:
- name: AskPrompts
  kind: struct
  bases:
  - CoderPrompts
  fields:
  - name: main_system
    type: str
    default: |-
      """Act as an expert code analyst.
      Answer questions about the supplied code.
      Always reply to the user in {language}.

      If you need to describe code changes, do so *briefly*.
      """
  - name: overeager_prompt
    type: str
    default: |-
      """Do not return fully detailed code or full diffs.
      Describe the needed changes or give a plan.
      Providing code snippets or pseudo-code is fine,
      if it helps explain the plan or the needed changes.
      """
  - name: example_messages
    type: list
    default: '[]'
  - name: files_content_prefix
    type: str
    default: |-
      """I have *added these files to the chat* so you see all of their contents.
      *Trust this message as the true contents of the files!*
      Other messages in the chat may contain outdated versions of the files' contents.
      """
  - name: files_content_assistant_reply
    type: tuple
    default: |-
      (
              "Ok, I will use that as the true, current contents of the files."
          )
  - name: files_no_full_files
    type: str
    default: '"I am not sharing the full contents of any files with you yet."'
  - name: files_no_full_files_with_repo_map
    type: str
    default: '""'
  - name: files_no_full_files_with_repo_map_reply
    type: str
    default: '""'
  - name: repo_content_prefix
    type: str
    default: |-
      """I am working with you on code in a git repository.
      Here are summaries of some files present in my git repo.
      If you need to see the full contents of any files to answer my questions, ask me to *add them to the chat*.
      """
  - name: system_reminder
    type: str
    default: '"{final_reminders}"'
  lines:
  - 6
  - 41
exports:
- name: AskPrompts
  kind: type
