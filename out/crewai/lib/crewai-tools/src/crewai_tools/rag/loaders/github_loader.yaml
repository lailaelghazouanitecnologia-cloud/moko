## Roska File Descriptor — loaders/github_loader.py
file: loaders/github_loader.py
lines: 110
purpose: GitHub repository content loader.
imports:
- sym: github.Github
- sym: github.GithubException
- sym: crewai_tools.rag.base_loader.BaseLoader
- sym: crewai_tools.rag.base_loader.LoaderResult
- sym: crewai_tools.rag.source_content.SourceContent
types:
- name: GithubLoader
  kind: struct
  ctx: Loader for GitHub repository content.
  bases:
  - BaseLoader
  methods:
  - name: load
    sig: 'load(self, source: SourceContent, **kwargs) -> LoaderResult'
    lines:
    - 12
    - 110
    detail:
      calls:
      - '"\n".join'
      - Github
      - LoaderResult
      - ValueError
      - all_content.append
      - g.get_repo
      - isinstance
      - issue.body[:200].replace
      - kwargs.get
      - len
      - list
      - metadata.get
      - pr.body[:200].replace
      - readme.decoded_content.decode
      - repo.get_contents
      - repo.get_issues
      - repo.get_pulls
      - repo.get_readme
      - repo_url.replace
      - repo_url.replace("https://github.com/", "").strip
      - repo_url.replace("https://github.com/", "").strip("/").split
      - repo_url.startswith
      - self.generate_doc_id
  lines:
  - 9
  - 110
exports:
- name: GithubLoader
  kind: type
