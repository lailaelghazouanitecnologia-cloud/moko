## Roska File Descriptor — internal/meta.py
file: internal/meta.py
lines: 81
purpose: Generic metaclass for agent extensions. This metaclass enables extension capabilities for agents by detecting extension fields in class annotations and applying appropriate wrappers.
imports:
- sym: pydantic.model_validator
- sym: pydantic._internal._model_construction.ModelMetaclass
types:
- name: AgentMeta
  kind: struct
  ctx: Generic metaclass for agent extensions. Detects extension fields (like 'a2a') in class annotations and applies the appropriate wrapper logic to enable extension functionality.
  bases:
  - ModelMetaclass
  methods:
  - name: __new__
    sig: |-
      __new__(
              mcs,
              name: str,
              bases: tuple[type, ...],
              namespace: dict[str, Any],
              **kwargs: Any,
          ) -> type
    lines:
    - 21
    - 81
    detail:
      calls:
      - hasattr
      - model_validator
      - model_validator(mode="after")
      - namespace.get
      - super
      - super().__new__
      - warnings.catch_warnings
      - warnings.filterwarnings
  lines:
  - 14
  - 81
exports:
- name: AgentMeta
  kind: type
