## Roska File Descriptor — utilities/config.py
file: utilities/config.py
lines: 38
imports:
- sym: pydantic.BaseModel
functions:
- name: process_config
  sig: |-
    process_config(
        values: dict[str, Any], model_class: type[BaseModel]
    ) -> dict[str, Any]
  lines:
  - 6
  - 38
  detail:
    calls:
    - config.items
    - isinstance
    - values.get
    - values.pop
    - values[key].update
exports:
- name: process_config
  kind: func
