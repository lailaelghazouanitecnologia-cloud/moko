## Roska File Descriptor — browser/broadcast.ts
file: browser/broadcast.ts
lines: 70
layer: logic
purpose: '--------------------------------------------------------------------------------------------- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See License.txt in the project root for license information. --------------------------------------------------------------------------------------------'
imports:
- sym: ./window.js.mainWindow
- sym: ../common/errors.js.getErrorMessage
- sym: ../common/event.js.Emitter
- sym: ../common/lifecycle.js.Disposable
- sym: ../common/lifecycle.js.toDisposable
types:
- name: BroadcastDataChannel
  kind: struct
  ctx: Sends the data to other BroadcastChannel objects set up for this channel. Data can be structured objects, e.g. nested objects and arrays.
  bases:
  - Disposable
  fields:
  - name: broadcastChannel
    type: BroadcastChannel | undefined
  - name: _onDidReceiveData
    type: any
    default: this._register(new Emitter<T>())
  - name: onDidReceiveData
    type: any
    default: this._onDidReceiveData.event
  methods:
  - name: constructor
    sig: 'constructor(private readonly channelName: string)'
    lines:
    - 18
    - 45
    detail:
      calls:
      - console.warn
      - getErrorMessage
      - new BroadcastChannel
      - super
      - this._register
      - this.broadcastChannel.addEventListener
      - this.createBroadcastChannel
      - toDisposable
  - name: postData
    sig: 'postData(data: T): void'
    lines:
    - 61
    - 69
    detail:
      calls:
      - JSON.stringify
      - localStorage.removeItem
      - localStorage.setItem
      - this.broadcastChannel.postMessage
  lines:
  - 11
  - 70
exports:
- name: BroadcastDataChannel
  kind: type
