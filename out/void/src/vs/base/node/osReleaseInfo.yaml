## Roska File Descriptor — node/osReleaseInfo.ts
file: node/osReleaseInfo.ts
lines: 72
layer: logic
purpose: '--------------------------------------------------------------------------------------------- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See License.txt in the project root for license information. --------------------------------------------------------------------------------------------'
imports:
- sym: fs.constants as FSConstants
- sym: fs.promises as FSPromises
- sym: readline.createInterface as readLines
- sym: ../common/platform.js.Platform
types:
- name: ReleaseInfo
  kind: typealias
  decorators:
  - type
  fields:
  - name: id
    type: string
  - name: id_like
    type: string
  - name: version_id
    type: string
  lines:
  - 10
  - 14
functions:
- name: getOSReleaseInfo
  sig: 'getOSReleaseInfo(errorLogger: (error: any) => void): Promise<ReleaseInfo | undefined>'
  is_async: true
  lines:
  - 16
  - 72
  detail:
    calls:
    - FSPromises.open
    - errorLogger
    - handle.createReadStream
    - line.includes
    - line.split
    - line.split('=')[0].toUpperCase
    - line.split('=')[0].toUpperCase().trim
    - line.split('=')[1].replace
    - line.split('=')[1].replace(/"/g, '').toLowerCase
    - line.split('=')[1].replace(/"/g, '').toLowerCase().trim
    - new Set
    - osReleaseKeys.has
    - readLines
exports:
- name: ReleaseInfo
  kind: type
- name: getOSReleaseInfo
  kind: func
