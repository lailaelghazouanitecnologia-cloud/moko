## Roska File Descriptor — common/configRemotes.ts
file: common/configRemotes.ts
lines: 114
layer: logic
purpose: '--------------------------------------------------------------------------------------------- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See License.txt in the project root for license information. --------------------------------------------------------------------------------------------'
imports:
- sym: ../../../base/common/uri.js.URI
data:
- name: SshProtocolMatcher
  type: any
  val: /^([^@:]+@)?([^:]+):/
- name: SshUrlMatcher
  type: any
  val: /^([^@:]+@)?([^:]+):(.+)$/
- name: AuthorityMatcher
  type: any
  val: /^([^@]+@)?([^:]+)(:\d+)?$/
- name: SecondLevelDomainMatcher
  type: any
  val: /([^@:.]+\.[^@:.]+)(:\d+)?$/
- name: RemoteMatcher
  type: any
  val: /^\s*url\s*=\s*(.+\S)\s*$/mg
- name: AnyButDot
  type: any
  val: /[^.]/g
- name: AllowedSecondLevelDomains
  type: array
  val: "[\n\t'github.com',\n\t'bitbucket.org',\n\t'visualstudio.com',\n\t'gitlab.com',\n\t'heroku.com',\n\t'azurewebsites.net',\n\t'ibm.com',\n..."
functions:
- name: stripLowLevelDomains
  sig: 'stripLowLevelDomains(domain: string): string | null'
  lines:
  - 31
  - 34
  detail:
    calls:
    - domain.match
- name: extractDomain
  sig: 'extractDomain(url: string): string | null'
  lines:
  - 36
  - 54
  detail:
    calls:
    - URI.parse
    - stripLowLevelDomains
    - url.indexOf
    - url.match
- name: getDomainsOfRemotes
  sig: 'getDomainsOfRemotes(text: string, allowedDomains: readonly string[]): string[]'
  lines:
  - 56
  - 69
  detail:
    calls:
    - Array.from
    - "Array.from(domains)\n\t\t.map"
    - RemoteMatcher.exec
    - domains.add
    - extractDomain
    - new Set
- name: stripPort
  sig: 'stripPort(authority: string): string | null'
  lines:
  - 71
  - 74
  detail:
    calls:
    - authority.match
- name: normalizeRemote
  sig: 'normalizeRemote(host: string | null, path: string, stripEndingDotGit: boolean): string | null'
  lines:
  - 76
  - 84
  detail:
    calls:
    - path.endsWith
    - path.indexOf
    - path.substr
- name: extractRemote
  sig: 'extractRemote(url: string, stripEndingDotGit: boolean): string | null'
  lines:
  - 86
  - 102
  detail:
    calls:
    - URI.parse
    - normalizeRemote
    - stripPort
    - url.indexOf
    - url.match
- name: getRemotes
  sig: 'getRemotes(text: string, stripEndingDotGit: boolean = false): string[]'
  lines:
  - 104
  - 114
  detail:
    calls:
    - RemoteMatcher.exec
    - extractRemote
    - remotes.push
exports:
- name: stripLowLevelDomains
  kind: func
- name: extractDomain
  kind: func
- name: getDomainsOfRemotes
  kind: func
- name: stripPort
  kind: func
- name: normalizeRemote
  kind: func
- name: extractRemote
  kind: func
- name: getRemotes
  kind: func
- name: SshProtocolMatcher
  kind: data
- name: SshUrlMatcher
  kind: data
- name: AuthorityMatcher
  kind: data
- name: SecondLevelDomainMatcher
  kind: data
- name: RemoteMatcher
  kind: data
- name: AnyButDot
  kind: data
- name: AllowedSecondLevelDomains
  kind: data
