## Roska File Descriptor — common/webviewPortMapping.ts
file: common/webviewPortMapping.ts
lines: 85
layer: ui
purpose: '--------------------------------------------------------------------------------------------- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See License.txt in the project root for license information. --------------------------------------------------------------------------------------------'
imports:
- sym: ../../../base/common/lifecycle.js.IDisposable
- sym: ../../../base/common/network.js.Schemas
- sym: ../../../base/common/uri.js.URI
- sym: ../../remote/common/remoteAgentConnection.js.IAddress
- sym: ../../tunnel/common/tunnel.js.extractLocalHostUriMetaDataForPortMapping
- sym: ../../tunnel/common/tunnel.js.ITunnelService
- sym: ../../tunnel/common/tunnel.js.RemoteTunnel
types:
- name: IWebviewPortMapping
  kind: trait
  decorators:
  - interface
  fields:
  - name: readonly
    type: number
  - name: readonly
    type: number
  lines:
  - 12
  - 15
- name: WebviewPortMappingManager
  kind: struct
  bases:
  - IDisposable
  fields:
  - name: _tunnels
    type: any
    default: new Map<number, RemoteTunnel>()
  methods:
  - name: constructor
    sig: "constructor(\n\t\tprivate readonly _getExtensionLocation: () => URI | undefined,\n\t\tprivate readonly _getMappings: () => readonly IWebviewPortMapping[],\n\t\tprivate readonly tunnelService: ITunnelService\n\t)"
    lines:
    - 24
    - 28
    detail: {}
  - name: getRedirect
    sig: 'getRedirect(resolveAuthority: IAddress | null | undefined, url: string): Promise<string | undefined>'
    is_async: true
    lines:
    - 30
    - 61
    detail:
      calls:
      - URI.parse
      - encodeURI
      - extractLocalHostUriMetaDataForPortMapping
      - this._getExtensionLocation
      - this._getMappings
      - this.getOrCreateTunnel
      - uri.with
      - "uri.with({\n\t\t\t\t\t\t\tauthority: `127.0.0.1:${tunnel.tunnelLocalPort}`,\n\t\t\t\t\t\t}).toString"
      - "uri.with({\n\t\t\t\t\t\tauthority: `${requestLocalHostInfo.address}:${mapping.extensionHostPort}`\n\t\t\t\t\t}).t..."
  - name: dispose
    sig: dispose()
    is_async: true
    lines:
    - 63
    - 68
    detail:
      calls:
      - this._tunnels.clear
      - this._tunnels.values
      - tunnel.dispose
  - name: getOrCreateTunnel
    sig: 'getOrCreateTunnel(remoteAuthority: IAddress, remotePort: number): Promise<RemoteTunnel | undefined>'
    is_async: true
    vis: private
    lines:
    - 70
    - 84
    detail:
      calls:
      - this._tunnels.get
      - this._tunnels.set
      - this.tunnelService.openTunnel
  lines:
  - 20
  - 85
exports:
- name: IWebviewPortMapping
  kind: type
- name: WebviewPortMappingManager
  kind: type
