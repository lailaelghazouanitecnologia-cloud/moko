## Roska File Descriptor — src/serverSetup.ts
file: src/serverSetup.ts
lines: 353
layer: logic
purpose: '--------------------------------------------------------------------------------------------- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See License.txt in the project root for license information. --------------------------------------------------------------------------------------------'
imports:
- sym: crypto.crypto
- sym: ./common/logger.Log
- sym: ./serverConfig.getVSCodeServerConfig
- sym: ./wsl/wslManager.WSLManager
data:
- name: DEFAULT_DOWNLOAD_URL_TEMPLATE
  type: string
  val: '''https://github.com/voideditor/binaries/releases/download/${version}/void-reh-${os}-${arch}-${version}.tar.gz'''
types:
- name: ServerInstallOptions
  kind: trait
  decorators:
  - interface
  fields:
  - name: id
    type: string
  - name: quality
    type: string
  - name: commit
    type: string
  - name: version
    type: string
  - name: release
    type: string
  - name: extensionIds
    type: string[]
  - name: envVariables
    type: string[]
  - name: serverDownloadUrlTemplate
    type: string
  lines:
  - 11
  - 22
- name: ServerInstallResult
  kind: trait
  decorators:
  - interface
  fields:
  - name: exitCode
    type: number
  - name: listeningOn
    type: number
  - name: connectionToken
    type: string
  - name: logFile
    type: string
  - name: osReleaseId
    type: string
  - name: arch
    type: string
  - name: platform
    type: string
  - name: tmpDir
    type: string
  lines:
  - 24
  - 34
- name: ServerInstallError
  kind: struct
  bases:
  - Error
  methods:
  - name: constructor
    sig: 'constructor(message: string)'
    lines:
    - 37
    - 39
    detail:
      calls:
      - super
  lines:
  - 36
  - 40
functions:
- name: installCodeServer
  sig: 'installCodeServer(wslManager: WSLManager, distroName: string, serverDownloadUrlTemplate: string | undefined, extensionIds: string[], envVariables: string[], logger: Log): Promise<ServerInstallResult>'
  is_async: true
  lines:
  - 44
  - 109
  detail:
    calls:
    - Object.entries
    - Object.entries(resultMap).filter
    - Object.fromEntries
    - Promise.race
    - crypto.randomBytes
    - crypto.randomBytes(12).toString
    - generateBashInstallScript
    - getVSCodeServerConfig
    - installServerScript.replace
    - logger.trace
    - new Promise
    - new RegExp
    - new ServerInstallError
    - parseInt
    - parseServerInstallOutput
    - resp.exitPromise.then
    - wslManager.exec
- name: parseServerInstallOutput
  sig: 'parseServerInstallOutput(str: string, scriptId: string): { [k: string]: string } | undefined'
  lines:
  - 111
  - 135
  detail:
    calls:
    - installResult.split
    - line.split
    - str.indexOf
    - str.substring
- name: generateBashInstallScript
  sig: 'generateBashInstallScript({ id, quality, version, commit, release, extensionIds, envVariables, serverApplicationName, serverDataFolderName, serverDownloadUrlTemplate }: ServerInstallOptions)'
  lines:
  - 137
  - 353
  detail:
    calls:
    - crypto.randomUUID
    - envVariables.map
    - envVariables.map(envVar => `echo "${envVar}==$${envVar}=="`).join
    - extensionIds.map
    - extensionIds.map(id => '--install-extension ' + id).join
    - serverDownloadUrlTemplate.replace
exports:
- name: ServerInstallOptions
  kind: type
- name: ServerInstallResult
  kind: type
- name: ServerInstallError
  kind: type
- name: installCodeServer
  kind: func
- name: parseServerInstallOutput
  kind: func
- name: generateBashInstallScript
  kind: func
- name: DEFAULT_DOWNLOAD_URL_TEMPLATE
  kind: data
