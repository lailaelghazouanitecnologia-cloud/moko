## Roska File Descriptor — shell/bash.ts
file: shell/bash.ts
lines: 79
layer: logic
purpose: '--------------------------------------------------------------------------------------------- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See License.txt in the project root for license information. --------------------------------------------------------------------------------------------'
imports:
- sym: vscode.vscode
- sym: ../types.ICompletionResource
  kind: typing
- sym: node:child_process.type ExecOptionsWithStringEncoding
- sym: ./common.execHelper
- sym: ./common.getAliasesHelper
functions:
- name: getBashGlobals
  sig: 'getBashGlobals(options: ExecOptionsWithStringEncoding, existingCommands?: Set<string>): Promise<(string | ICompletionResource)[]>'
  is_async: true
  lines:
  - 11
  - 16
  detail:
    calls:
    - getAliases
    - getBuiltins
- name: getAliases
  sig: 'getAliases(options: ExecOptionsWithStringEncoding): Promise<ICompletionResource[]>'
  is_async: true
  lines:
  - 18
  - 21
- name: getBuiltins
  sig: "getBuiltins(\n\toptions: ExecOptionsWithStringEncoding,\n\tscriptToRun: string,\n\texistingCommands?: Set<string>,\n): Promise<(string | ICompletionResource)[]>"
  is_async: true
  lines:
  - 23
  - 68
  detail:
    calls:
    - (await execHelper(`help ${cmd}`, options))?.trim
    - builtins.find
    - compgenOutput.split
    - compgenOutput.split('\n').filter
    - completions.push
    - console.log
    - execHelper
    - generateDetailAndDocs
    - helpLines.splice
    - helpLines.splice(1).map
    - helpLines.splice(1).map(line => line.trim()).join
    - helpLines?.[0]?.split
    - helpLines?.[0]?.split(' ').slice
    - helpLines?.[0]?.split(' ').slice(1).join
    - helpLines?.[0]?.split(' ').slice(1).join(' ').trim
    - helpOutput?.split
    - new vscode.MarkdownString
- name: generateDetailAndDocs
  sig: 'generateDetailAndDocs(description?: string, args?: string): { detail?: string; documentation?: string; description?: string }'
  lines:
  - 70
  - 79
  detail:
    calls:
    - firstSentence
exports:
- name: getBashGlobals
  kind: func
- name: getAliases
  kind: func
- name: getBuiltins
  kind: func
- name: generateDetailAndDocs
  kind: func
