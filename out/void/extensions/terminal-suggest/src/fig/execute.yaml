## Roska File Descriptor — fig/execute.ts
file: fig/execute.ts
lines: 62
layer: logic
purpose: '--------------------------------------------------------------------------------------------- Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT License. See License.txt in the project root for license information. --------------------------------------------------------------------------------------------'
imports:
- sym: ../helpers/os.osIsWindows
- sym: ../shell/common.spawnHelper2
- sym: ./shared/utils.withTimeout
types:
- name: IFigExecuteExternals
  kind: trait
  decorators:
  - interface
  fields:
  - name: executeCommand
    type: Fig.ExecuteCommandFunction
  - name: executeCommandTimeout
    type: "(\n\t\tinput: Fig.ExecuteCommandInput,\n\t\ttimeout: number,\n\t) => Promise<Fig.ExecuteCommandOutput>"
  lines:
  - 56
  - 62
functions:
- name: cleanOutput
  sig: 'cleanOutput(output: string)'
  lines:
  - 10
  - 15
  detail:
    calls:
    - "output\n\t\t.replace"
    - "output\n\t\t.replace(/\\r\\n/g, '\\n') // Replace carriage returns with just a normal return\n\t\t.replace"
    - "output\n\t\t.replace(/\\r\\n/g, '\\n') // Replace carriage returns with just a normal return\n\t\t.replace(/\\..."
- name: executeCommandTimeout
  sig: "executeCommandTimeout(\n\tinput: Fig.ExecuteCommandInput,\n\ttimeout = osIsWindows() ? 20000 : 5000,\n): Promise<Fig.ExecuteCommandOutput>"
  is_async: true
  lines:
  - 17
  - 50
  detail:
    calls:
    - Math.max
    - '[input.command, ...input.args].join'
    - cleanOutput
    - console.debug
    - console.error
    - console.warn
    - spawnHelper2
    - withTimeout
- name: executeCommand
  sig: 'executeCommand(args): Fig.ExecuteCommandFunction'
  lines:
  - 53
  - 54
exports:
- name: IFigExecuteExternals
  kind: type
- name: cleanOutput
  kind: func
- name: executeCommandTimeout
  kind: func
- name: executeCommand
  kind: func
