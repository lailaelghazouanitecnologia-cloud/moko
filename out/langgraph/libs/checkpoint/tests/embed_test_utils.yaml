## Roska File Descriptor — tests/embed_test_utils.py
file: tests/embed_test_utils.py
lines: 55
purpose: Embedding utilities for testing.
imports:
- sym: random
- sym: langchain_core.embeddings.Embeddings
types:
- name: CharacterEmbeddings
  kind: struct
  ctx: Simple character-frequency based embeddings using random projections. (+1 dunder methods)
  bases:
  - Embeddings
  fields:
  - name: _rng
    type: Any
    default: random.Random(seed)
  - name: dims
    type: Any
    default: dims
  - name: _char_projections
    type: Any
    default: defaultdict[str, list[float]]
  methods:
  - name: __init__
    sig: '__init__(self, dims: int = 50, seed: int = 42)'
    lines:
    - 14
    - 23
    detail:
      calls:
      - defaultdict
      - math.sqrt
      - random.Random
      - range
      - self._rng.gauss
  - name: _embed_one
    sig: '_embed_one(self, text: str) -> list[float]'
    vis: protected
    lines:
    - 25
    - 44
    detail:
      calls:
      - Counter
      - counts.items
      - counts.values
      - enumerate
      - math.sqrt
      - sum
  - name: embed_documents
    sig: 'embed_documents(self, texts: list[str]) -> list[list[float]]'
    lines:
    - 46
    - 48
    detail:
      calls:
      - self._embed_one
  - name: embed_query
    sig: 'embed_query(self, text: str) -> list[float]'
    lines:
    - 50
    - 52
    detail:
      calls:
      - self._embed_one
  lines:
  - 11
  - 55
exports:
- name: CharacterEmbeddings
  kind: type
