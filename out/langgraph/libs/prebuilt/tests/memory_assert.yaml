## Roska File Descriptor — tests/memory_assert.py
file: tests/memory_assert.py
lines: 58
imports:
- sym: tempfile
- sym: langgraph.checkpoint.base.ChannelVersions
- sym: langgraph.checkpoint.base.Checkpoint
- sym: langgraph.checkpoint.base.CheckpointMetadata
- sym: langgraph.checkpoint.base.SerializerProtocol
- sym: langgraph.checkpoint.memory.InMemorySaver
- sym: langgraph.checkpoint.memory.PersistentDict
- sym: langgraph.pregel._checkpoint.copy_checkpoint
types:
- name: MemorySaverAssertImmutable
  kind: struct
  bases:
  - InMemorySaver
  fields:
  - name: storage_for_copies
    type: Any
    default: defaultdict[str, dict[str, dict[str, Checkpoint]]]
  - name: put_sleep
    type: Any
    default: put_sleep
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              *,
              serde: SerializerProtocol | None = None,
              put_sleep: float | None = None,
          ) -> None
    lines:
    - 19
    - 31
    detail:
      calls:
      - defaultdict
      - partial
      - self.stack.callback
      - super
      - super().__init__
      - tempfile.mkstemp
  - name: put
    sig: |-
      put(
              self,
              config: dict,
              checkpoint: Checkpoint,
              metadata: CheckpointMetadata,
              new_versions: ChannelVersions,
          ) -> None
    lines:
    - 33
    - 58
    detail:
      calls:
      - copy_checkpoint
      - self.serde.dumps_typed
      - self.serde.loads_typed
      - super
      - super().get
      - super().put
      - time.sleep
  lines:
  - 16
  - 58
exports:
- name: MemorySaverAssertImmutable
  kind: type
