## Roska File Descriptor — auth/exceptions.py
file: auth/exceptions.py
lines: 59
purpose: Exceptions used in the auth system.
imports:
- sym: http
data:
- name: __all__
  type: list
  val: '["HTTPException"]'
types:
- name: HTTPException
  kind: struct
  ctx: 'HTTP exception that you can raise to return a specific HTTP error response. Since this is defined in the auth module, we default to a 401 status code. Args: status_code: HTTP status code for the error. Defaults to 401 "Unauthorized". detail: Detailed error message. If `None`, uses a default message based on the status code. headers: Additional HTTP headers to include in the error response. Example: Default: ```python raise HTTPException() # HTTPException(status_code=401, detail=''Unauthorized'') ``` Add headers: ```python raise HTTPException(headers={"X-Custom-Header": "Custom Value"}) # HTTPException(status_code=401, detail=''Unauthorized'', headers={"WWW-Authenticate": "Bearer"}) ``` Custom error: ```python raise HTTPException(status_code=404, detail="Not found") ``` (+2 dunder methods)'
  bases:
  - Exception
  fields:
  - name: status_code
    type: Any
    default: status_code
  - name: detail
    type: Any
    default: detail
  - name: headers
    type: Any
    default: headers
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              status_code: int = 401,
              detail: str | None = None,
              headers: Mapping[str, str] | None = None,
          ) -> None
    lines:
    - 39
    - 49
    detail:
      calls:
      - http.HTTPStatus
  lines:
  - 9
  - 56
exports:
- name: HTTPException
  kind: type
