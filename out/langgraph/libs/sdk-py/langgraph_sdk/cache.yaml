## Roska File Descriptor — langgraph_sdk/cache.py
file: langgraph_sdk/cache.py
lines: 140
purpose: Key/value cache for use inside LangGraph deployments. Thin wrapper around ``langgraph_api.cache``. Values must be JSON-serializable (dicts, lists, strings, numbers, booleans, ``None``).
data:
- name: T
  type: Any
  val: TypeVar("T")
- name: CacheStatus
  type: Any
  val: Literal["miss", "fresh", "stale", "expired"]
- name: __all__
  type: list
  val: |-
    [
        "SWRResult",
        "cache_get",
        "cache_set",
        "swr",
    ]
functions:
- name: cache_get
  sig: 'cache_get(key: str) -> Any | None'
  is_async: true
  lines:
  - 56
  - 68
  detail:
    calls:
    - RuntimeError
    - _cache_get
- name: cache_set
  sig: 'cache_set(key: str, value: Any, *, ttl: timedelta | None = None) -> None'
  is_async: true
  lines:
  - 71
  - 87
  detail:
    calls:
    - RuntimeError
    - _cache_set
- name: swr
  sig: |-
    swr(
        key: str,
        loader: Callable[[], Awaitable[T]],
        *,
        fresh_for: timedelta | None = None,
        max_age: timedelta | None = None,
        model: type[T] | None = None,
    ) -> SWRResult[T]
  is_async: true
  lines:
  - 90
  - 140
  detail:
    calls:
    - RuntimeError
    - _api_swr
    - timedelta
exports:
- name: SWRResult
  kind: type
- name: cache_get
  kind: type
- name: cache_set
  kind: type
- name: swr
  kind: type
