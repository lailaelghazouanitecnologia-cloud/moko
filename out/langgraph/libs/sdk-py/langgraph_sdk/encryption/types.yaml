## Roska File Descriptor — encryption/types.py
file: encryption/types.py
lines: 147
purpose: Encryption and decryption types for LangGraph. This module defines the core types used for custom at-rest encryption in LangGraph. It includes context types and typed dictionaries for encryption operations.
data:
- name: Json
  type: Any
  val: dict[str, typing.Any]
- name: BlobEncryptor
  type: Any
  val: Callable[[EncryptionContext, bytes], Awaitable[bytes]]
- name: BlobDecryptor
  type: Any
  val: Callable[[EncryptionContext, bytes], Awaitable[bytes]]
- name: JsonEncryptor
  type: Any
  val: Callable[[EncryptionContext, Json], Awaitable[Json]]
- name: JsonDecryptor
  type: Any
  val: Callable[[EncryptionContext, Json], Awaitable[Json]]
- name: ContextHandler
  type: Any
  val: |-
    Callable[
        ["BaseUser", EncryptionContext], Awaitable[dict[str, typing.Any]]
    ]
types:
- name: EncryptionContext
  kind: struct
  ctx: 'Context passed to encryption/decryption handlers. Contains arbitrary non-secret key-values that will be stored on encrypt. These key-values are intended to be sent to an external service that manages keys and handles the actual encryption and decryption of data. Attributes: model: The model type being encrypted (e.g., "assistant", "thread", "run", "checkpoint") field: The specific field being encrypted (e.g., "metadata", "context", "kwargs", "values") metadata: Additional context metadata that can be used for encryption decisions (+1 dunder methods)'
  fields:
  - name: model
    type: Any
    default: model
  - name: field
    type: Any
    default: field
  - name: metadata
    type: Any
    default: metadata or {}
  methods:
  - name: __init__
    sig: __init__(self, ...5 params)
    lines:
    - 32
    - 40
  lines:
  - 17
  - 43
exports:
- name: EncryptionContext
  kind: type
- name: Json
  kind: data
- name: BlobEncryptor
  kind: data
- name: BlobDecryptor
  kind: data
- name: JsonEncryptor
  kind: data
- name: JsonDecryptor
  kind: data
- name: ContextHandler
  kind: data
