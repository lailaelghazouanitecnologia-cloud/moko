## Roska File Descriptor — llms/HuggingFaceTGI.ts
file: llms/HuggingFaceTGI.ts
lines: 78
layer: logic
imports:
- sym: '@continuedev/fetch.streamSse'
- sym: ../../index.js.CompletionOptions
- sym: ../../index.js.LLMOptions
- sym: ../index.js.BaseLLM
types:
- name: HuggingFaceTGI
  kind: struct
  bases:
  - BaseLLM
  fields:
  - name: MAX_STOP_TOKENS
    type: any
    default: '4'
  - name: providerName
    type: any
    default: '"huggingface-tgi"'
  - name: defaultOptions
    type: Partial<LLMOptions>
    default: |-
      {
          apiBase: "http://localhost:8080/",
        }
  methods:
  - name: constructor
    sig: 'constructor(options: LLMOptions)'
    lines:
    - 12
    - 33
    detail:
      calls:
      - new URL
      - super
      - this.fetch
      - |-
        this.fetch(new URL("info", this.apiBase), {
              method: "GET",
            })
              .then
      - |-
        this.fetch(new URL("info", this.apiBase), {
              method: "GET",
            })
              .then(async (response)...
  - name: _streamComplete
    sig: |-
      _streamComplete(
          prompt: string,
          signal: AbortSignal,
          options: CompletionOptions,
        ): AsyncGenerator<string>
    is_async: true
    vis: protected
    lines:
    - 53
    - 75
    detail:
      calls:
      - JSON.stringify
      - new URL
      - streamSse
      - this._convertArgs
      - this.fetch
  lines:
  - 5
  - 76
exports:
- name: HuggingFaceTGI
  kind: type
