## Roska File Descriptor — llm/toolSupport.ts
file: llm/toolSupport.ts
lines: 470
layer: logic
imports:
- sym: '@continuedev/config-yaml.parseProxyModelName'
- sym: '...ModelDescription'
data:
- name: PROVIDER_TOOL_SUPPORT
  type: 'Record<string, (model: string) => boolean>'
  val: |-
    {
        "continue-proxy": (model) => {
          try {
            const { provider, model: _model } = parseProxyModelName(model);...
functions:
- name: isRecommendedAgentModel
  sig: 'isRecommendedAgentModel(modelName: string): boolean'
  lines:
  - 437
  - 459
  detail:
    calls:
    - combo.every
- name: modelSupportsNativeTools
  sig: 'modelSupportsNativeTools(modelDescription: ModelDescription)'
  lines:
  - 460
  - 470
  detail:
    calls:
    - providerSupport
exports:
- name: isRecommendedAgentModel
  kind: func
- name: modelSupportsNativeTools
  kind: func
- name: PROVIDER_TOOL_SUPPORT
  kind: data
