## Roska File Descriptor — llm/defaultSystemMessages.ts
file: llm/defaultSystemMessages.ts
lines: 91
layer: logic
data:
- name: DEFAULT_SYSTEM_MESSAGES_URL
  type: string
  val: '"https://github.com/continuedev/continue/blob/main/core/llm/defaultSystemMessages.ts"'
- name: CODEBLOCK_FORMATTING_INSTRUCTIONS
  type: string
  val: |-
    `\
      Always include the language and file name in the info string when you write code blocks.
      If you are editing "src/...
- name: EDIT_CODE_INSTRUCTIONS
  type: string
  val: |-
    `\
      When addressing code modification requests, present a concise code snippet that
      emphasizes only the necessary cha...
- name: BRIEF_LAZY_INSTRUCTIONS
  type: string
  val: '`For larger codeblocks (>20 lines), use brief language-appropriate placeholders for unmodified sections, e.g. ''// ... ex...'
- name: DEFAULT_CHAT_SYSTEM_MESSAGE
  type: string
  val: |-
    `\
    <important_rules>
      You are in chat mode.

      If the user asks to make changes to files offer that they can use the Ap...
- name: DEFAULT_AGENT_SYSTEM_MESSAGE
  type: string
  val: |-
    `\
    <important_rules>
      You are in agent mode.

      If you need to use multiple tools, you can call multiple read-only tool...
- name: DEFAULT_PLAN_SYSTEM_MESSAGE
  type: string
  val: |-
    `\
    <important_rules>
      You are in plan mode, in which you help the user understand and construct a plan.
      Only use read...
exports:
- name: DEFAULT_SYSTEM_MESSAGES_URL
  kind: data
- name: CODEBLOCK_FORMATTING_INSTRUCTIONS
  kind: data
- name: EDIT_CODE_INSTRUCTIONS
  kind: data
- name: BRIEF_LAZY_INSTRUCTIONS
  kind: data
- name: DEFAULT_CHAT_SYSTEM_MESSAGE
  kind: data
- name: DEFAULT_AGENT_SYSTEM_MESSAGE
  kind: data
- name: DEFAULT_PLAN_SYSTEM_MESSAGE
  kind: data
