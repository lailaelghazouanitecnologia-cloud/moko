## Roska File Descriptor — implementations/grepSearch.ts
file: implementations/grepSearch.ts
lines: 134
layer: logic
imports:
- sym: ..ToolImpl
- sym: ../...ContextItem
- sym: ../../util/errors.ContinueError
- sym: ../../util/errors.ContinueErrorReason
- sym: ../../util/grepSearch.formatGrepSearchResults
- sym: ../../util/regexValidator.prepareQueryForRipgrep
- sym: ../parseArgs.getStringArg
data:
- name: DEFAULT_GREP_SEARCH_RESULTS_LIMIT
  type: number
  val: '100'
- name: DEFAULT_GREP_SEARCH_CHAR_LIMIT
  type: number
  val: '7500'
functions:
- name: splitGrepResultsByFile
  sig: 'splitGrepResultsByFile(content: string): ContextItem[]'
  lines:
  - 11
  - 40
  detail:
    calls:
    - |-
      content
            .substring
    - |-
      content
            .substring(startIndex, endIndex)
            .replace
    - |-
      content
            .substring(startIndex, endIndex)
            .replace(/^\.\/[^\n]+\n/, "") // remove the lin...
    - content.matchAll
    - contextItems.push
- name: grepSearchImpl
  sig: 'grepSearchImpl(args, extras): ToolImpl'
  is_async: true
  lines:
  - 42
  - 134
  detail:
    calls:
    - String
    - contextItems.push
    - errorMessage.includes
    - extras.ide.getSearchResults
    - formatGrepSearchResults
    - getStringArg
    - new ContinueError
    - prepareQueryForRipgrep
    - splitGrepResultsByFile
    - truncationReasons.join
    - truncationReasons.push
    - warnings.push
exports:
- name: splitGrepResultsByFile
  kind: func
- name: grepSearchImpl
  kind: func
- name: DEFAULT_GREP_SEARCH_RESULTS_LIMIT
  kind: data
- name: DEFAULT_GREP_SEARCH_CHAR_LIMIT
  kind: data
