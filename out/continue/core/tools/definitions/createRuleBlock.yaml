## Roska File Descriptor — definitions/createRuleBlock.ts
file: definitions/createRuleBlock.ts
lines: 88
layer: logic
imports:
- sym: ../...Tool
- sym: ../builtIn.BUILT_IN_GROUP_NAME
- sym: ../builtIn.BuiltInToolNames
data:
- name: NAME_ARG_DESC
  type: string
  val: '"Short, descriptive name summarizing the rule''s purpose (e.g. ''React Standards'', ''Type Hints'')"'
- name: RULE_ARG_DESC
  type: string
  val: '"Clear, imperative instruction for future code generation (e.g. ''Use named exports'', ''Add Python type hints''). Each rule...'
- name: DESC_ARG_DESC
  type: string
  val: '"Description of when this rule should be applied. Required for Agent Requested rules (AI decides when to apply). Optiona...'
- name: GLOB_ARG_DESC
  type: string
  val: '"Optional file patterns to which this rule applies (e.g. [''**/*.{ts,tsx}''] or [''src/**/*.ts'', ''tests/**/*.ts''])"'
- name: REGEX_ARG_DESC
  type: string
  val: '"Optional regex patterns to match against file content. Rule applies only to files whose content matches the pattern (e....'
- name: ALWAYS_APPLY_DESC
  type: string
  val: '"Whether this rule should always be applied. Set to false for Agent Requested and Manual rules. Omit or set to true for ...'
- name: createRuleBlock
  type: Tool
  val: |-
    {
      type: "function",
      displayTitle: "Create Rule Block",
      wouldLikeTo: 'create a rule block for "{{{ name }}}"',
      is...
exports:
- name: NAME_ARG_DESC
  kind: data
- name: RULE_ARG_DESC
  kind: data
- name: DESC_ARG_DESC
  kind: data
- name: GLOB_ARG_DESC
  kind: data
- name: REGEX_ARG_DESC
  kind: data
- name: ALWAYS_APPLY_DESC
  kind: data
- name: createRuleBlock
  kind: data
