## Roska File Descriptor — definitions/runTerminalCommand.ts
file: definitions/runTerminalCommand.ts
lines: 80
layer: logic
imports:
- sym: ../...Tool
- sym: ../builtIn.BUILT_IN_GROUP_NAME
- sym: ../builtIn.BuiltInToolNames
- sym: '@continuedev/terminal-security.evaluateTerminalCommandSecurity'
- sym: '@continuedev/terminal-security.ToolPolicy'
data:
- name: PLATFORM_INFO
  type: string
  val: '`Choose terminal commands and scripts optimized for ${os.platform()} and ${os.arch()} and shell ${getPreferredShell()}.`'
- name: RUN_COMMAND_NOTES
  type: string
  val: |-
    `The shell is not stateful and will not remember any previous commands.\
          When a command is run in the background A...
- name: runTerminalCommandTool
  type: Tool
  val: |-
    {
      type: "function",
      displayTitle: "Run Terminal Command",
      wouldLikeTo: "run the following terminal command:",
      is...
functions:
- name: getPreferredShell
  sig: 'getPreferredShell(): string'
  lines:
  - 13
  - 24
  detail:
    calls:
    - os.platform
exports:
- name: getPreferredShell
  kind: func
- name: PLATFORM_INFO
  kind: data
- name: RUN_COMMAND_NOTES
  kind: data
- name: runTerminalCommandTool
  kind: data
