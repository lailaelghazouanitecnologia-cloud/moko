## Roska File Descriptor — providers/HttpContextProvider.test.ts
file: providers/HttpContextProvider.test.ts
lines: 206
layer: test
purpose: Tests for HttpContextProvider - specifically testing the fix for v1.0.19 where missing or null uri fields in server responses caused TypeErrors. The fix uses conditional access (item.uri &&) to safely handle null/undefined uri fields.
imports:
- sym: ../../index.js.ContextProviderExtras
- sym: ./HttpContextProvider.js.HttpContextProvider
