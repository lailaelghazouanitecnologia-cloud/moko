## Roska File Descriptor — config/migrateSharedConfig.ts
file: config/migrateSharedConfig.ts
lines: 209
layer: logic
purpose: I'm disabling this rule for the entire file under the assumption that this is a one-time migration script. I'm expecting this code to be removed in the future.
imports:
- sym: '...IDE'
- sym: ../util.deduplicateArray
- sym: ../util/GlobalContext.GlobalContext
- sym: ./load.resolveSerializedConfig
- sym: ./sharedConfig.SharedConfigSchema
functions:
- name: migrateJsonSharedConfig
  sig: 'migrateJsonSharedConfig(filepath: string, ide: IDE): void'
  lines:
  - 18
  - 209
  detail:
    calls:
    - Object.keys
    - console.error
    - deduplicateArray
    - globalContext.getSharedConfig
    - new GlobalContext
    - new GlobalContext().updateSharedConfig
    - resolveSerializedConfig
exports:
- name: migrateJsonSharedConfig
  kind: func
