## Roska File Descriptor — templating/utils.ts
file: templating/utils.ts
lines: 63
layer: logic
imports:
- sym: ../...Position
- sym: ../constants.INSTINCT_EDITABLE_REGION_END_TOKEN
- sym: ../constants.INSTINCT_EDITABLE_REGION_START_TOKEN
- sym: ../constants.NEXT_EDIT_EDITABLE_REGION_BOTTOM_MARGIN
- sym: ../constants.NEXT_EDIT_EDITABLE_REGION_TOP_MARGIN
functions:
- name: insertCursorToken
  sig: |-
    insertCursorToken(
      lines: string[],
      cursorPos: Position,
      cursorToken: string,
    )
  lines:
  - 9
  - 28
  detail:
    calls:
    - Math.max
    - Math.min
    - lines[cursorPos.line].slice
- name: insertEditableRegionTokensWithStaticRange
  sig: |-
    insertEditableRegionTokensWithStaticRange(
      lines: string[],
      cursorPos: Position,
      editableRegionStart?: number,
      editableRegionEnd?: number,
    )
  lines:
  - 30
  - 63
  detail:
    calls:
    - Math.max
    - Math.min
    - lines.slice
exports:
- name: insertCursorToken
  kind: func
- name: insertEditableRegionTokensWithStaticRange
  kind: func
