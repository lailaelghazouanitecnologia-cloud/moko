## Roska File Descriptor — util/repoUrl.ts
file: util/repoUrl.ts
lines: 70
layer: logic
purpose: Utility functions for normalizing and handling repository URLs.
functions:
- name: normalizeRepoUrl
  sig: 'normalizeRepoUrl(url: string): string'
  lines:
  - 31
  - 70
  detail:
    calls:
    - /^[a-z]+:\/\//i.test
    - |-
      normalized
            .replace
    - |-
      normalized
            .replace("ssh://git@github.com/", "https://github.com/")
            .replace
    - normalized.endsWith
    - normalized.includes
    - normalized.replace
    - normalized.slice
    - normalized.startsWith
    - normalized.toLowerCase
    - url.trim
exports:
- name: normalizeRepoUrl
  kind: func
