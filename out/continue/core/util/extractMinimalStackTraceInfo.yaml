## Roska File Descriptor — util/extractMinimalStackTraceInfo.ts
file: util/extractMinimalStackTraceInfo.ts
lines: 33
layer: logic
purpose: It's helpful to know what functions errors occur in, but absolute file paths are sensitive information, so we want to remove them.
functions:
- name: extractMinimalStackTraceInfo
  sig: 'extractMinimalStackTraceInfo(stack: unknown): string'
  lines:
  - 7
  - 33
  detail:
    calls:
    - lines.filter
    - |-
      minimalLines
          .map
    - |-
      minimalLines
          .map((line) => line.replace("at ", "").split(" (").slice(0, 1))
          .flatMap
    - |-
      minimalLines
          .map((line) => line.replace("at ", "").split(" (").slice(0, 1))
          .flatMap((parts...
    - |-
      stack
          .trim
    - |-
      stack
          .trim()
          .split
    - |-
      stack
          .trim()
          .split("\n")
          .map
exports:
- name: extractMinimalStackTraceInfo
  kind: func
