## Roska File Descriptor — util/ideUtils.ts
file: util/ideUtils.ts
lines: 91
layer: logic
imports:
- sym: '...IDE'
- sym: ./uri.joinEncodedUriPathSegmentToUri
- sym: ./uri.joinPathsToUri
- sym: ./uri.pathToUriPathSegment
functions:
- name: resolveRelativePathInDir
  sig: |-
    resolveRelativePathInDir(
      path: string,
      ide: IDE,
      dirUriCandidates?: string[],
    ): Promise<string | undefined>
  is_async: true
  lines:
  - 14
  - 28
  detail:
    calls:
    - ide.fileExists
    - ide.getWorkspaceDirs
    - joinPathsToUri
- name: inferResolvedUriFromRelativePath
  sig: |-
    inferResolvedUriFromRelativePath(
      _relativePath: string,
      ide: IDE,
      dirCandidates?: string[],
    ): Promise<string>
  is_async: true
  lines:
  - 36
  - 91
  detail:
    calls:
    - Promise.all
    - _relativePath.trim
    - _relativePath.trim().replaceAll
    - activeFile.path.endsWith
    - dirs.map
    - existenceChecks.filter
    - ide.getCurrentFile
    - ide.getWorkspaceDirs
    - joinEncodedUriPathSegmentToUri
    - joinPathsToUri
    - new Error
    - pathToUriPathSegment
    - pathToUriPathSegment(relativePath).split
    - segments.join
    - segments.slice
    - segments.slice(i).join
    - suffixes.push
    - uris.map
exports:
- name: resolveRelativePathInDir
  kind: func
- name: inferResolvedUriFromRelativePath
  kind: func
