## Roska File Descriptor — util/grepSearch.ts
file: util/grepSearch.ts
lines: 90
layer: logic
purpose: Formats the output of a grep search to reduce unnecessary indentation, lines, etc Assumes a command with these params ripgrep -i --ignore-file .continueignore --ignore-file .gitignore -C 2 --heading -m 100 -e <query> . Also can truncate the output to a specified number of characters
functions:
- name: formatGrepSearchResults
  sig: |-
    formatGrepSearchResults(
      results: string,
      maxChars?: number,
    ): {
      formatted: string;
      numResults: number;
      truncated: boolean;
    }
  lines:
  - 8
  - 90
  detail:
    calls:
    - formatted.substring
    - keepLines.join
    - line.startsWith
    - line.trim
    - processResult
    - resultLines.push
    - results.split
    - results.split("\n").filter
exports:
- name: formatGrepSearchResults
  kind: func
