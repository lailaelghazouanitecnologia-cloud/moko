## Roska File Descriptor — util/ranges.ts
file: util/ranges.ts
lines: 103
layer: logic
imports:
- sym: ../index.js.Position
- sym: ../index.js.Range
functions:
- name: getRangeInString
  sig: 'getRangeInString(content: string, range: Range): string'
  lines:
  - 3
  - 25
  detail:
    calls:
    - '[firstLine, ...middleLines, lastLine].join'
    - content.split
    - lines.slice
    - lines[range.end.line]?.substring
    - lines[range.start.line]?.substring
- name: intersection
  sig: 'intersection(a: Range, b: Range): Range | null'
  lines:
  - 27
  - 58
  detail:
    calls:
    - Math.max
    - Math.min
- name: union
  sig: 'union(a: Range, b: Range): Range'
  lines:
  - 60
  - 83
  detail:
    calls:
    - Math.max
    - Math.min
- name: maxPosition
  sig: 'maxPosition(a: Position, b: Position): Position'
  lines:
  - 85
  - 93
  detail: {}
- name: minPosition
  sig: 'minPosition(a: Position, b: Position): Position'
  lines:
  - 95
  - 103
  detail: {}
exports:
- name: getRangeInString
  kind: func
- name: intersection
  kind: func
- name: union
  kind: func
- name: maxPosition
  kind: func
- name: minPosition
  kind: func
