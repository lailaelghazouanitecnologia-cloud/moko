## Roska File Descriptor — lazy/replace.ts
file: lazy/replace.ts
lines: 106
layer: logic
imports:
- sym: ../...ILLM
- sym: ../../autocomplete/filtering/streamTransforms/lineStream.filterLeadingNewline
- sym: ../../diff/util.streamLines
- sym: ../../util.dedent
data:
- name: BUFFER_LINES_BELOW
  type: number
  val: '3'
- name: MATCH_LINES_ABOVE
  type: number
  val: '1'
- name: REPLACE_HERE
  type: string
  val: '"// REPLACE HERE //"'
functions:
- name: getReplacementByMatching
  sig: |-
    getReplacementByMatching(
      oldCode: string,
      linesBefore: string[],
      linesAfter: string[],
    ): string | undefined
  lines:
  - 9
  - 51
  detail:
    calls:
    - Math.min
    - linesAfter.slice
    - linesAfter.slice(0, linesToMatchBelow).join
    - linesBefore.slice
    - linesBefore.slice(-linesToMatchAbove).join
    - oldCode.split
    - |-
      oldLines
          .slice
    - |-
      oldLines
          .slice(startIndex + linesToMatchAbove, endIndex)
          .join
    - oldLines.findIndex
exports:
- name: getReplacementByMatching
  kind: func
- name: BUFFER_LINES_BELOW
  kind: data
- name: MATCH_LINES_ABOVE
  kind: data
- name: REPLACE_HERE
  kind: data
