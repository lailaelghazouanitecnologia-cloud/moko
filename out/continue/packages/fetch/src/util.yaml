## Roska File Descriptor — src/util.ts
file: src/util.ts
lines: 108
layer: logic
imports:
- sym: '@continuedev/config-types.RequestOptions'
functions:
- name: getProxyFromEnv
  sig: 'getProxyFromEnv(protocol: string): string | undefined'
  lines:
  - 8
  - 19
  detail: {}
- name: getProxy
  sig: |-
    getProxy(
      protocol: string,
      requestOptions?: RequestOptions,
    ): string | undefined
  lines:
  - 22
  - 30
  detail:
    calls:
    - getProxyFromEnv
- name: getEnvNoProxyPatterns
  sig: 'getEnvNoProxyPatterns(): string[]'
  lines:
  - 32
  - 42
  detail:
    calls:
    - |-
      envValue
            .split
    - |-
      envValue
            .split(",")
            .map
    - |-
      envValue
            .split(",")
            .map((item) => item.trim().toLowerCase())
            .filter
- name: getReqOptionsNoProxyPatterns
  sig: |-
    getReqOptionsNoProxyPatterns(
      options: RequestOptions | undefined,
    ): string[]
  lines:
  - 44
  - 51
  detail:
    calls:
    - options?.noProxy?.map
    - options?.noProxy?.map((i) => i.trim().toLowerCase()).filter
- name: patternMatchesHostname
  sig: 'patternMatchesHostname(hostname: string, pattern: string)'
  lines:
  - 53
  - 89
  detail:
    calls:
    - hostname.toLowerCase
    - hostname.toLowerCase().split
    - hostnameWithoutPort.endsWith
    - pattern.toLowerCase
    - pattern.toLowerCase().split
    - patternWithoutPort.slice
    - patternWithoutPort.startsWith
    - patternWithoutPort.substring
- name: shouldBypassProxy
  sig: |-
    shouldBypassProxy(
      hostname: string,
      requestOptions: RequestOptions | undefined,
    ): boolean
  lines:
  - 96
  - 108
  detail:
    calls:
    - getEnvNoProxyPatterns
    - getReqOptionsNoProxyPatterns
    - hostname.toLowerCase
    - ignores.some
exports:
- name: getProxyFromEnv
  kind: func
- name: getProxy
  kind: func
- name: getEnvNoProxyPatterns
  kind: func
- name: getReqOptionsNoProxyPatterns
  kind: func
- name: patternMatchesHostname
  kind: func
- name: shouldBypassProxy
  kind: func
