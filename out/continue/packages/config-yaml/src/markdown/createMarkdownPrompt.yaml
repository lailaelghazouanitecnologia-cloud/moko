## Roska File Descriptor — markdown/createMarkdownPrompt.ts
file: markdown/createMarkdownPrompt.ts
lines: 44
layer: logic
imports:
- sym: yaml.YAML
types:
- name: PromptFrontmatter
  kind: trait
  decorators:
  - interface
  fields:
  - name: description
    type: string
  - name: invokable
    type: boolean
  lines:
  - 3
  - 7
functions:
- name: createMarkdownWithPromptFrontmatter
  sig: |-
    createMarkdownWithPromptFrontmatter(
      frontmatter: PromptFrontmatter,
      prompt: string,
    ): string
  lines:
  - 12
  - 18
  detail:
    calls:
    - YAML.stringify
    - YAML.stringify(frontmatter).trim
- name: createPromptMarkdown
  sig: |-
    createPromptMarkdown(
      name: string,
      promptContent: string,
      options: {
        description?: string;
        invokable?: boolean;
      } = {},
    ): string
  lines:
  - 23
  - 44
  detail:
    calls:
    - createMarkdownWithPromptFrontmatter
    - name.trim
    - options.description.trim
exports:
- name: PromptFrontmatter
  kind: type
- name: createMarkdownWithPromptFrontmatter
  kind: func
- name: createPromptMarkdown
  kind: func
