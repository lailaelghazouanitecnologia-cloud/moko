## Roska File Descriptor — markdown/createMarkdownRule.ts
file: markdown/createMarkdownRule.ts
lines: 67
layer: logic
imports:
- sym: yaml.YAML
- sym: ./markdownToRule.js.RuleFrontmatter
data:
- name: RULE_FILE_EXTENSION
  type: string
  val: '"md"'
functions:
- name: sanitizeRuleName
  sig: 'sanitizeRuleName(name: string): string'
  lines:
  - 9
  - 15
  detail:
    calls:
    - |-
      name
          .toLowerCase
    - |-
      name
          .toLowerCase()
          .replace
    - |-
      name
          .toLowerCase()
          .replace(/[^a-z0-9\s-]/g, "")
          .replace
    - |-
      name
          .toLowerCase()
          .replace(/[^a-z0-9\s-]/g, "")
          .replace(/\s+/g, "-")
          .replace
- name: createMarkdownWithFrontmatter
  sig: |-
    createMarkdownWithFrontmatter(
      frontmatter: RuleFrontmatter,
      markdown: string,
    ): string
  lines:
  - 20
  - 26
  detail:
    calls:
    - YAML.stringify
    - YAML.stringify(frontmatter).trim
- name: createRuleMarkdown
  sig: |-
    createRuleMarkdown(
      name: string,
      ruleContent: string,
      options: {
        description?: string;
        globs?: string | string[];
        regex?: string | string[];
        alwaysApply?: boolean;
        invokable?: boolean;
      } = {},
    ): string
  lines:
  - 31
  - 67
  detail:
    calls:
    - createMarkdownWithFrontmatter
    - options.description.trim
    - options.globs.trim
    - options.regex.trim
exports:
- name: sanitizeRuleName
  kind: func
- name: createMarkdownWithFrontmatter
  kind: func
- name: createRuleMarkdown
  kind: func
- name: RULE_FILE_EXTENSION
  kind: data
