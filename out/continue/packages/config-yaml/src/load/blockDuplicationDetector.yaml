## Roska File Descriptor — load/blockDuplicationDetector.ts
file: load/blockDuplicationDetector.ts
lines: 63
layer: logic
imports:
- sym: ./getBlockType.js.BLOCK_TYPES
- sym: ./getBlockType.js.BlockType
types:
- name: BlockDuplicationDetector
  kind: struct
  ctx: Check if the name is duplicated within the same blockType
  fields:
  - name: records
    type: Map<string, Set<string>>
  methods:
  - name: constructor
    sig: constructor()
    lines:
    - 6
    - 11
    detail:
      calls:
      - new Map
      - new Set
      - this.records.set
  - name: check
    sig: 'check(identifier: string, blockType: BlockType): boolean'
    vis: private
    lines:
    - 29
    - 36
    detail:
      calls:
      - this.records.get
      - this.records.get(blockType)!.add
      - this.records.get(blockType)!.has
  - name: isDuplicated
    sig: 'isDuplicated(block: any, blockType: BlockType): boolean'
    lines:
    - 39
    - 62
    detail:
      calls:
      - this.isCommonBlockDuplicated
      - this.isContextDuplicated
      - this.isRuleDuplicated
  lines:
  - 3
  - 63
exports:
- name: BlockDuplicationDetector
  kind: type
