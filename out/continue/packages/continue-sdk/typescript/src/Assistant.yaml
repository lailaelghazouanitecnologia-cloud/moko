## Roska File Descriptor — src/Assistant.ts
file: src/Assistant.ts
lines: 72
layer: logic
imports:
- sym: '@continuedev/config-yaml.AssistantUnrolled'
types:
- name: AssistantConfig
  kind: typealias
  decorators:
  - type
  lines:
  - 3
  - 3
- name: Assistant
  kind: struct
  ctx: The assistant configuration object
  fields:
  - name: config
    type: AssistantConfig
  methods:
  - name: constructor
    sig: 'constructor(config: any)'
    lines:
    - 19
    - 21
    detail: {}
  - name: getModel
    sig: 'getModel(modelName?: string): string'
    lines:
    - 29
    - 55
    detail:
      calls:
      - new Error
      - this.config.models.find
  - name: systemMessage
    sig: 'systemMessage(): string'
    lines:
    - 62
    - 71
    detail:
      calls:
      - Array.isArray
      - |-
        this.config.rules
              ?.filter
      - |-
        this.config.rules
              ?.filter((rule) => !!rule)
              .map
      - |-
        this.config.rules
              ?.filter((rule) => !!rule)
              .map((rule) => (typeof rule === "string" ? ...
  lines:
  - 8
  - 72
exports:
- name: AssistantConfig
  kind: type
- name: Assistant
  kind: type
