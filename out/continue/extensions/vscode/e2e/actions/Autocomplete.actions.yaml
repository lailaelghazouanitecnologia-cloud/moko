## Roska File Descriptor — actions/Autocomplete.actions.ts
file: actions/Autocomplete.actions.ts
lines: 45
layer: logic
imports:
- sym: chai.expect
- sym: vscode-extension-tester.TextEditor
- sym: vscode-extension-tester.VSBrowser
- sym: vscode-extension-tester.Workbench
- sym: ../constants.DEFAULT_TIMEOUT
- sym: ../selectors/Autocomplete.selectors.AutocompleteSelectors
- sym: ../TestUtils.TestUtils
types:
- name: AutocompleteActions
  kind: struct
  methods:
  - name: testCompletions
    sig: 'testCompletions(editor: TextEditor)'
    is_async: true
    decorators:
    - static
    lines:
    - 9
    - 31
    detail:
      calls:
      - TestUtils.generateTestMessagePair
      - TestUtils.waitForSuccess
      - editor.clearText
      - editor.getDriver
      - editor.typeTextAt
      - expect
      - expect(ghostText0).to.equal
      - expect(ghostText1).to.equal
  - name: forceCompletion
    sig: 'forceCompletion(editor: TextEditor): Promise<string>'
    is_async: true
    decorators:
    - static
    lines:
    - 33
    - 44
    detail:
      calls:
      - TestUtils.waitForSuccess
      - editor.moveCursor
      - editor.setText
      - new Workbench
      - new Workbench().executeCommand
  lines:
  - 8
  - 45
exports:
- name: AutocompleteActions
  kind: type
