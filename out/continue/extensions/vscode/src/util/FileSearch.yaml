## Roska File Descriptor — util/FileSearch.ts
file: util/FileSearch.ts
lines: 49
layer: logic
imports:
- sym: core.IDE
- sym: core/indexing/walkDir.walkDirs
- sym: core/util.deduplicateArray
- sym: core/util.splitCamelCaseAndNonAlphaNumeric
- sym: minisearch.MiniSearch
- sym: vscode.vscode
types:
- name: FileMiniSearchResult
  kind: typealias
  decorators:
  - type
  fields:
  - name: relativePath
    type: string
  - name: id
    type: string
  lines:
  - 8
  - 8
- name: FileSearch
  kind: struct
  fields:
  - name: miniSearch
    type: any
    default: |-
      new MiniSearch<FileMiniSearchResult>({
          fields: ["relativePath", "id"],
          s...
  methods:
  - name: constructor
    sig: 'constructor(private readonly ide: IDE)'
    lines:
    - 14
    - 16
    detail:
      calls:
      - this.initializeFileSearchState
  - name: initializeFileSearchState
    sig: initializeFileSearchState()
    is_async: true
    vis: private
    lines:
    - 34
    - 44
    detail:
      calls:
      - results.flat
      - results.flat().map
      - this.miniSearch.addAll
      - walkDirs
  - name: search
    sig: 'search(query: string): FileMiniSearchResult[]'
    lines:
    - 46
    - 48
    detail:
      calls:
      - this.miniSearch.search
  lines:
  - 13
  - 49
exports:
- name: FileMiniSearchResult
  kind: type
- name: FileSearch
  kind: type
