## Roska File Descriptor — quickEdit/EditDecorationManager.ts
file: quickEdit/EditDecorationManager.ts
lines: 91
layer: logic
imports:
- sym: vscode.vscode
types:
- name: EditDecorationManager
  kind: struct
  ctx: Store unique active ranges
  fields:
  - name: _lastEditor
    type: vscode.TextEditor | undefined
  - name: decorationType
    type: vscode.TextEditorDecorationType
  - name: activeRangesMap
    type: Map<string, vscode.Range>
    default: new Map()
  methods:
  - name: constructor
    sig: 'constructor(context: vscode.ExtensionContext)'
    lines:
    - 7
    - 17
    detail:
      calls:
      - new vscode.ThemeColor
      - vscode.window.createTextEditorDecorationType
  - name: rangesCoincide
    sig: 'rangesCoincide(range1: vscode.Range, range2: vscode.Range): boolean'
    vis: private
    lines:
    - 33
    - 40
    detail:
      calls:
      - range1.end.isEqual
      - range1.intersection
      - range1.start.isEqual
      - range2.end.isEqual
  - name: mergeNewRange
    sig: 'mergeNewRange(newRange: vscode.Range): void'
    vis: private
    lines:
    - 43
    - 59
    detail:
      calls:
      - mergedRange.union
      - rangesToPrune.push
      - this.activeRangesMap.delete
      - this.activeRangesMap.entries
      - this.activeRangesMap.set
      - this.rangeToString
      - this.rangesCoincide
  - name: addDecorations
    sig: 'addDecorations(editor: vscode.TextEditor, ranges: vscode.Range[]): void'
    lines:
    - 62
    - 80
    detail:
      calls:
      - Array.from
      - editor.setDecorations
      - this.activeRangesMap.values
      - this.clear
      - this.mergeNewRange
      - this.updateInEditMode
  - name: clear
    sig: clear()
    lines:
    - 82
    - 89
    detail:
      calls:
      - this._lastEditor.setDecorations
      - this.activeRangesMap.clear
      - this.updateInEditMode
  lines:
  - 2
  - 90
exports:
- name: EditDecorationManager
  kind: type
