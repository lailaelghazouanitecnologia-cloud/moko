## Roska File Descriptor — utils/modelCapability.ts
file: utils/modelCapability.ts
lines: 36
layer: logic
purpose: Determines if a model is recommended for use with `cn`
functions:
- name: isModelCapable
  sig: |-
    isModelCapable(
      provider: string,
      name: string,
      model?: string,
    ): boolean
  lines:
  - 4
  - 36
  detail:
    calls:
    - model.toLowerCase
    - name.toLowerCase
    - patterns.some
exports:
- name: isModelCapable
  kind: func
