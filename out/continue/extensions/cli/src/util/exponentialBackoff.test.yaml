## Roska File Descriptor — util/exponentialBackoff.test.ts
file: util/exponentialBackoff.test.ts
lines: 353
layer: test
imports:
- sym: vitest.vi
- sym: ./exponentialBackoff.js.ExponentialBackoffOptions
functions:
- name: isRetryableError
  sig: 'isRetryableError(error: any): boolean'
  lines:
  - 6
  - 53
  detail:
    calls:
    - error.message?.toLowerCase
    - lower?.includes
- name: calculateDelay
  sig: |-
    calculateDelay(
      attempt: number,
      options: Required<ExponentialBackoffOptions>,
    ): number
  lines:
  - 55
  - 69
  detail:
    calls:
    - Math.floor
    - Math.min
    - Math.pow
    - Math.random
exports:
- name: isRetryableError
  kind: func
- name: calculateDelay
  kind: func
