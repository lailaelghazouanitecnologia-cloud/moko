## Roska File Descriptor — util/promptProcessor.ts
file: util/promptProcessor.ts
lines: 49
layer: logic
imports:
- sym: ../hubLoader.js.processRule
- sym: ./logger.js.logger
functions:
- name: processAndCombinePrompts
  sig: |-
    processAndCombinePrompts(
      additionalPrompts?: string[],
      initialPrompt?: string,
    ): Promise<string | undefined>
  is_async: true
  lines:
  - 11
  - 38
  detail:
    calls:
    - logger.warn
    - processRule
    - processedPrompts.join
    - processedPrompts.push
- name: prependPrompt
  sig: |-
    prependPrompt(
      prepend: string | undefined,
      original: string | undefined,
    )
  lines:
  - 41
  - 49
  detail:
    calls:
    - (original ?? "").trim
    - (prepend ?? "").trim
    - '`${(prepend ?? "").trim()}\n\n${(original ?? "").trim()}`.trim'
exports:
- name: processAndCombinePrompts
  kind: func
- name: prependPrompt
  kind: func
