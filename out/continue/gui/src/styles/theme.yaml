## Roska File Descriptor — styles/theme.ts
file: styles/theme.ts
lines: 286
layer: ui
purpose: All vscode variables https://gist.github.com/estruyf/ba49203e1a7d6868e9320a4ea480c27a
data:
- name: THEME_COLORS
  type: object
  val: |-
    {
      background: {
        vars: [
          "--vscode-sideBar-background",
          "--vscode-editor-background",
          "--vscode-p...
- name: THEME_CSS_VARS
  type: any
  val: |-
    Object.values(THEME_COLORS)
      .map((value) => value.vars)
      .flat()
- name: THEME_CSS_VAR_DEFAULTS
  type: any
  val: |-
    Object.entries(THEME_COLORS).reduce(
      (acc, [_, value]) => {
        value.vars.forEach((varName) => {
          acc[varName] =...
- name: THEME_DEFAULTS
  type: any
  val: |-
    Object.entries(THEME_COLORS).reduce(
      (acc, [key, value]) => {
        acc[key] = value.default;
        return acc;
      },
      {} ...
functions:
- name: getRecursiveVar
  sig: 'getRecursiveVar(vars: string[], defaultColor: string)'
  lines:
  - 212
  - 216
  detail:
    calls:
    - '[...vars].reverse'
    - '[...vars].reverse().reduce'
- name: varWithFallback
  sig: 'varWithFallback(colorName: keyof typeof THEME_COLORS)'
  lines:
  - 218
  - 224
  detail:
    calls:
    - getRecursiveVar
    - new Error
- name: setDocumentStylesFromTheme
  sig: |-
    setDocumentStylesFromTheme(
      theme: Record<string, string | undefined | null>,
    )
  lines:
  - 226
  - 266
  detail:
    calls:
    - Object.entries
    - Object.entries(THEME_COLORS).forEach
    - Object.entries(theme).forEach
- name: setDocumentStylesFromLocalStorage
  sig: 'setDocumentStylesFromLocalStorage(checkCache: boolean)'
  lines:
  - 268
  - 280
  detail:
    calls:
    - Object.entries
    - document.body.style.setProperty
    - localStorage.getItem
- name: clearThemeLocalCache
  sig: clearThemeLocalCache()
  lines:
  - 282
  - 286
  detail:
    calls:
    - Object.keys
    - localStorage.removeItem
exports:
- name: getRecursiveVar
  kind: func
- name: varWithFallback
  kind: func
- name: setDocumentStylesFromTheme
  kind: func
- name: setDocumentStylesFromLocalStorage
  kind: func
- name: clearThemeLocalCache
  kind: func
- name: THEME_COLORS
  kind: data
- name: THEME_CSS_VARS
  kind: data
- name: THEME_CSS_VAR_DEFAULTS
  kind: data
- name: THEME_DEFAULTS
  kind: data
