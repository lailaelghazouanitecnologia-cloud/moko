## Roska File Descriptor — selectors/selectToolCalls.ts
file: selectors/selectToolCalls.ts
lines: 68
layer: logic
imports:
- sym: '@reduxjs/toolkit.createSelector'
- sym: core.ToolStatus
- sym: ../store.RootState
- sym: ../util.findAllCurToolCalls
- sym: ../util.findAllCurToolCallsByStatus
- sym: ../util.findToolCallById
- sym: ../util.hasCurrentToolCalls
data:
- name: selectCurrentToolCalls
  type: any
  val: |-
    createSelector(
      (store: RootState) => store.session.history,
      (history) => findAllCurToolCalls(history),
    )
- name: selectHasCurrentToolCalls
  type: any
  val: |-
    createSelector(
      (store: RootState) => store.session.history,
      (history) => hasCurrentToolCalls(history),
    )
- name: selectToolCallsByStatus
  type: any
  val: |-
    createSelector(
      [
        (store: RootState) => store.session.history,
        (_store: RootState, status: ToolStatus) => stat...
- name: selectFirstPendingToolCall
  type: any
  val: |-
    createSelector(
      (store: RootState) => store.session.history,
      (history) => {
        const pendingToolCalls = findAllCurT...
- name: selectToolCallById
  type: any
  val: |-
    createSelector(
      [
        (store: RootState) => store.session.history,
        (_store: RootState, toolCallId: string) => tool...
- name: selectApplyStateByToolCallId
  type: any
  val: |-
    createSelector(
      [
        (store: RootState) => store.session.codeBlockApplyStates,
        (_store: RootState, toolCallId: st...
- name: selectPendingToolCalls
  type: any
  val: |-
    createSelector(
      (store: RootState) => store.session.history,
      (history) => findAllCurToolCallsByStatus(history, "gene...
- name: selectDoneApplyStates
  type: any
  val: |-
    createSelector(
      (store: RootState) => store.session.codeBlockApplyStates.states,
      (states) => states.filter((applySta...
exports:
- name: selectCurrentToolCalls
  kind: data
- name: selectHasCurrentToolCalls
  kind: data
- name: selectToolCallsByStatus
  kind: data
- name: selectFirstPendingToolCall
  kind: data
- name: selectToolCallById
  kind: data
- name: selectApplyStateByToolCallId
  kind: data
- name: selectPendingToolCalls
  kind: data
- name: selectDoneApplyStates
  kind: data
