## Roska File Descriptor — selectors/index.ts
file: selectors/index.ts
lines: 56
layer: logic
imports:
- sym: '@reduxjs/toolkit.createSelector'
- sym: ../../components/mainInput/types.ComboBoxItem
- sym: ../../components/mainInput/types.ComboBoxItemType
- sym: ../store.RootState
data:
- name: selectSlashCommandComboBoxInputs
  type: any
  val: |-
    createSelector(
      [(state: RootState) => state.config.config.slashCommands],
      (slashCommands) => {
        return (
          s...
- name: selectSlashCommands
  type: any
  val: |-
    createSelector(
      [(state: RootState) => state.config.config.slashCommands],
      (slashCommands) => {
        return slashComm...
- name: selectSubmenuContextProviders
  type: any
  val: |-
    createSelector(
      [(state: RootState) => state.config.config.contextProviders],
      (providers) => {
        return providers?...
- name: selectDefaultContextProviders
  type: any
  val: |-
    createSelector(
      [(state: RootState) => state.config.config.experimental?.defaultContext],
      (defaultProviders) => {
      ...
- name: selectUseActiveFile
  type: any
  val: |-
    createSelector(
      [(state: RootState) => state.config.config.experimental?.defaultContext],
      (defaultContext) => defaul...
exports:
- name: selectSlashCommandComboBoxInputs
  kind: data
- name: selectSlashCommands
  kind: data
- name: selectSubmenuContextProviders
  kind: data
- name: selectDefaultContextProviders
  kind: data
- name: selectUseActiveFile
  kind: data
