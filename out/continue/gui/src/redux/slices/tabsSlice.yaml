## Roska File Descriptor — slices/tabsSlice.ts
file: slices/tabsSlice.ts
lines: 140
layer: logic
imports:
- sym: '@reduxjs/toolkit.PayloadAction'
- sym: '@reduxjs/toolkit.createSlice'
data:
- name: INITIAL_TABS_STATE
  type: TabsState
  val: |-
    {
      tabs: [
        {
          id: Date.now().toString(36) + Math.random().toString(36).substring(2),
          title: "Chat 1",
      ...
- name: tabsSlice
  type: any
  val: |-
    createSlice({
      name: "tabs",
      initialState: INITIAL_TABS_STATE,
      reducers: {
        setTabs: (state, action: PayloadActi...
- name: |-
    {
      setTabs,
      updateTab,
      addTab,
      removeTab,
      setActiveTab,
      handleSessionChange,
    }
  type: any
  val: tabsSlice.actions
types:
- name: Tab
  kind: trait
  decorators:
  - interface
  fields:
  - name: id
    type: string
  - name: isActive
    type: boolean
  - name: sessionId
    type: string
  lines:
  - 3
  - 8
- name: TabsState
  kind: trait
  decorators:
  - interface
  fields:
  - name: tabs
    type: Tab[]
  lines:
  - 10
  - 12
exports:
- name: Tab
  kind: type
- name: TabsState
  kind: type
- name: INITIAL_TABS_STATE
  kind: data
- name: tabsSlice
  kind: data
- name: |-
    {
      setTabs,
      updateTab,
      addTab,
      removeTab,
      setActiveTab,
      handleSessionChange,
    }
  kind: data
