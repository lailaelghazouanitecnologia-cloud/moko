## Roska File Descriptor — slices/indexingSlice.ts
file: slices/indexingSlice.ts
lines: 66
layer: logic
imports:
- sym: '@reduxjs/toolkit.PayloadAction'
- sym: '@reduxjs/toolkit.createSlice'
- sym: core.IndexingStatus
data:
- name: INITIAL_INDEXING_STATE
  type: IndexingState
  val: |-
    {
      indexing: {
        statuses: {},
        hiddenChatPeekTypes: {
          docs: false,
        },
      },
    }
- name: indexingSlice
  type: any
  val: |-
    createSlice({
      name: "indexing",
      initialState: INITIAL_INDEXING_STATE,
      reducers: {
        updateIndexingStatus: (
        ...
- name: '{ updateIndexingStatus, setIndexingChatPeekHidden }'
  type: any
  val: indexingSlice.actions
types:
- name: IndexingState
  kind: typealias
  decorators:
  - type
  fields:
  - name: indexing
    type: |-
      {
          hiddenChatPeekTypes: Record<IndexingStatus["type"], boolean>;
          statuses: Record<string, IndexingStatus>; // status id -> status
        }
  lines:
  - 4
  - 9
exports:
- name: IndexingState
  kind: type
- name: INITIAL_INDEXING_STATE
  kind: data
- name: indexingSlice
  kind: data
- name: '{ updateIndexingStatus, setIndexingChatPeekHidden }'
  kind: data
