## Roska File Descriptor — components/ConfigItemSelect.tsx
file: components/ConfigItemSelect.tsx
lines: 91
layer: ui
purpose: A component that displays config items either as tabs (on large screens) or a popover select (on small screens)
imports:
- sym: '@heroicons/react/24/outline.ChevronDownIcon'
- sym: ../../../components/ui.Popover
- sym: ../../../components/ui.PopoverButton
- sym: ../../../components/ui.PopoverPanel
- sym: ../../../components/ui.Transition
- sym: ../../../util.fontSize
data:
- name: activeItem
  type: any
  val: items.find((item) => item.id === activeId)
types:
- name: ConfigItemSelectProps
  kind: trait
  ctx: A component that displays config items either as tabs (on large screens) or a popover select (on small screens)
  decorators:
  - interface
  fields:
  - name: items
    type: |-
      {
          id: string;
          label: string;
          icon: React.ReactNode;
        }[]
  - name: activeId
    type: string
  - name: onSelect
    type: '(id: string) => void'
  lines:
  - 4
  - 12
exports:
- name: ConfigItemSelectProps
  kind: type
- name: activeItem
  kind: data
