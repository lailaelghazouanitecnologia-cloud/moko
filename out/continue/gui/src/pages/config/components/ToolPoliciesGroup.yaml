## Roska File Descriptor — components/ToolPoliciesGroup.tsx
file: components/ToolPoliciesGroup.tsx
lines: 119
layer: ui
imports:
- sym: '@heroicons/react/24/outline.ChevronDownIcon'
- sym: '@heroicons/react/24/outline.WrenchScrewdriverIcon'
- sym: core.Tool
- sym: react.useMemo
- sym: react.useState
- sym: ../../../components/gui/Switch.ToggleSwitch
- sym: ../../../components/gui/Tooltip.ToolTip
- sym: ../../../components/ui.Card
- sym: ../../../redux/hooks.useAppDispatch
- sym: ../../../redux/hooks.useAppSelector
- sym: ../../../redux/slices/uiSlice.toggleToolGroupSetting
- sym: ./ToolPolicyItem.ToolPolicyItem
data:
- name: dispatch
  type: any
  val: useAppDispatch()
- name: '[isExpanded, setIsExpanded]'
  type: any
  val: useState(false)
- name: availableTools
  type: any
  val: |-
    useAppSelector(
        (state) => state.config.config.tools as Tool[],
      )
- name: tools
  type: any
  val: |-
    useMemo(() => {
        return availableTools.filter((t) => t.group === groupName);
      }, [availableTools, groupName])
- name: toolGroupSettings
  type: any
  val: |-
    useAppSelector(
        (state) => state.ui.toolGroupSettings,
      )
- name: toolSettings
  type: any
  val: useAppSelector((state) => state.ui.toolSettings)
- name: isGroupEnabled
  type: any
  val: |-
    useMemo(() => {
        return toolGroupSettings[groupName] !== "exclude";
      }, [toolGroupSettings, groupName])
- name: '{ enabledCount, totalCount }'
  type: any
  val: |-
    useMemo(() => {
        const total = tools.length;
        const enabled = tools.filter(
          (tool) => toolSettings[tool.func...
- name: badgeText
  type: any
  val: |-
    useMemo(() => {
        if (enabledCount === totalCount) {
          return totalCount.toString();
        }
        return `${enabledCo...
types:
- name: ToolPoliciesGroupProps
  kind: trait
  decorators:
  - interface
  fields:
  - name: showIcon
    type: boolean
  - name: allToolsOff
    type: boolean
  - name: duplicateDetection
    type: Record<string, boolean>
  lines:
  - 14
  - 20
exports:
- name: ToolPoliciesGroupProps
  kind: type
- name: dispatch
  kind: data
- name: '[isExpanded, setIsExpanded]'
  kind: data
- name: availableTools
  kind: data
- name: tools
  kind: data
- name: toolGroupSettings
  kind: data
- name: toolSettings
  kind: data
- name: isGroupEnabled
  kind: data
- name: '{ enabledCount, totalCount }'
  kind: data
- name: badgeText
  kind: data
