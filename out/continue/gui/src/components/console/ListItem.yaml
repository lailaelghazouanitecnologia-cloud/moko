## Roska File Descriptor — console/ListItem.tsx
file: console/ListItem.tsx
lines: 55
layer: logic
imports:
- sym: ../../hooks/useLLMLog.LLMInteraction
- sym: ../../hooks/useLLMSummary.useLLMSummary
- sym: ./StatusIcon.StatusIcon
types:
- name: ListItemProps
  kind: trait
  decorators:
  - interface
  fields:
  - name: interactionId
    type: string
  - name: interaction
    type: LLMInteraction
  - name: selected
    type: boolean
  - name: onClickInteraction
    type: '(interactionId: string) => void'
  lines:
  - 5
  - 10
functions:
- name: formatTimestamp
  sig: 'formatTimestamp(timestamp: number)'
  lines:
  - 12
  - 24
  detail:
    calls:
    - Math.floor
    - date.getHours
    - date.getHours().toString
    - date.getHours().toString().padStart
    - date.getMilliseconds
    - date.getMinutes
    - date.getMinutes().toString
    - date.getMinutes().toString().padStart
    - date.getSeconds
    - new Date
    - secondsFormatted.padStart
- name: ListItem
  sig: |-
    ListItem({
      interactionId,
      interaction,
      onClickInteraction,
      selected,
    }: ListItemProps)
  lines:
  - 26
  - 55
  detail:
    calls:
    - formatTimestamp
    - useLLMSummary
exports:
- name: ListItemProps
  kind: type
- name: formatTimestamp
  kind: func
- name: ListItem
  kind: func
