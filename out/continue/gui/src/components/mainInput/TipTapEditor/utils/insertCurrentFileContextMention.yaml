## Roska File Descriptor — utils/insertCurrentFileContextMention.ts
file: utils/insertCurrentFileContextMention.ts
lines: 58
layer: logic
imports:
- sym: '@tiptap/core.Editor'
- sym: core.ContextProviderDescription
- sym: ../extensions.Mention
functions:
- name: isEditorContentEmpty
  sig: 'isEditorContentEmpty(editor: Editor): boolean'
  lines:
  - 5
  - 21
  detail:
    calls:
    - editor.getJSON
- name: insertCurrentFileContextMention
  sig: |-
    insertCurrentFileContextMention(
      editor: Editor,
      contextProviders: ContextProviderDescription[],
    )
  lines:
  - 30
  - 58
  detail:
    calls:
    - contextProviders.find
    - |-
      editor
            .chain
    - |-
      editor
            .chain()
            .insertContent
    - |-
      editor
            .chain()
            .insertContent([node.toJSON(), { type: "text", text: " " }]) // add a sp...
    - editor.schema.nodes[Mention.name].create
    - isEditorContentEmpty
    - node.toJSON
exports:
- name: isEditorContentEmpty
  kind: func
- name: insertCurrentFileContextMention
  kind: func
