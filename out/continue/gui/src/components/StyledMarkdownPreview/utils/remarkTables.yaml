## Roska File Descriptor — utils/remarkTables.tsx
file: utils/remarkTables.tsx
lines: 201
layer: logic
imports:
- sym: unist-util-visit.visit
functions:
- name: remarkTables
  sig: remarkTables()
  lines:
  - 23
  - 193
  detail: {}
- name: splitRow
  sig: 'splitRow(row: string)'
  lines:
  - 195
  - 201
  detail:
    calls:
    - |-
      row
          .trim
    - |-
      row
          .trim()
          .replace
    - |-
      row
          .trim()
          .replace(/^\||\|$/g, "") // Remove leading and trailing pipes
          .split
    - |-
      row
          .trim()
          .replace(/^\||\|$/g, "") // Remove leading and trailing pipes
          .split("|")
        ...
exports:
- name: remarkTables
  kind: func
- name: splitRow
  kind: func
