## Roska File Descriptor — utils/patchNestedMarkdown.ts
file: utils/patchNestedMarkdown.ts
lines: 64
layer: logic
purpose: This is a patch for outputing markdown code that contains codeblocks It notices markdown blocks (including GitHub-specific variants), keeps track of when that specific block is closed, and uses ~~~ instead of ``` for that block Note, this was benchmarked at sub-millisecond
imports:
- sym: ../../../../../core/utils/markdownUtils.headerIsMarkdown
- sym: ../../../../../core/utils/markdownUtils.MarkdownBlockStateTracker
functions:
- name: patchNestedMarkdown
  sig: 'patchNestedMarkdown(source: string): string'
  lines:
  - 15
  - 64
  detail:
    calls:
    - headerIsMarkdown
    - line.replaceAll
    - line.startsWith
    - lines.join
    - lines[i].replaceAll
    - new MarkdownBlockStateTracker
    - source.match
    - source.split
    - stateTracker.getRemainingBareBackticksAfter
    - stateTracker.getTrimmedLines
    - stateTracker.isBareBacktickLine
exports:
- name: patchNestedMarkdown
  kind: func
