## Roska File Descriptor — subagent/SubagentToolName.ts
file: subagent/SubagentToolName.ts
lines: 45
data:
- name: SUBAGENT_TOOL_NAME_PREFIX
  type: string
  val: '"use_subagent_"'
- name: SUBAGENT_TOOL_NAME_MAX_LENGTH
  type: number
  val: '64'
functions:
- name: sanitizeAgentName
  sig: 'sanitizeAgentName(name: string): string'
  lines:
  - 4
  - 11
  detail:
    calls:
    - "name\n\t\t.trim"
    - "name\n\t\t.trim()\n\t\t.toLowerCase"
    - "name\n\t\t.trim()\n\t\t.toLowerCase()\n\t\t.replace"
    - "name\n\t\t.trim()\n\t\t.toLowerCase()\n\t\t.replace(/[^a-z0-9_]+/g, \"_\")\n\t\t.replace"
    - "name\n\t\t.trim()\n\t\t.toLowerCase()\n\t\t.replace(/[^a-z0-9_]+/g, \"_\")\n\t\t.replace(/_+/g, \"_\")\n\t\t.replace"
- name: hashString
  sig: 'hashString(value: string): string'
  lines:
  - 13
  - 20
  detail:
    calls:
    - (hash >>> 0).toString
    - Math.imul
    - value.charCodeAt
- name: trimToolNameToMax
  sig: 'trimToolNameToMax(value: string): string'
  lines:
  - 22
  - 27
  detail:
    calls:
    - value.slice
- name: buildSubagentToolName
  sig: 'buildSubagentToolName(agentName: string): string'
  lines:
  - 29
  - 41
  detail:
    calls:
    - Math.max
    - hashString
    - hashString(agentName).slice
    - sanitizeAgentName
    - sanitized.slice
    - trimToolNameToMax
- name: isSubagentToolName
  sig: 'isSubagentToolName(toolName: string): boolean'
  lines:
  - 43
  - 45
  detail:
    calls:
    - toolName.startsWith
exports:
- name: sanitizeAgentName
  kind: func
- name: hashString
  kind: func
- name: trimToolNameToMax
  kind: func
- name: buildSubagentToolName
  kind: func
- name: isSubagentToolName
  kind: func
- name: SUBAGENT_TOOL_NAME_PREFIX
  kind: data
- name: SUBAGENT_TOOL_NAME_MAX_LENGTH
  kind: data
