## Roska File Descriptor — task/presentation-types.ts
file: task/presentation-types.ts
lines: 9
purpose: 'Priority level for a presentation flush request. - `"immediate"` — flush synchronously (delay = 0 ms). Used at semantic boundaries: first visible token, tool-call transitions, and finalization. - `"normal"` — flush after the configured cadence delay, coalescing intermediate chunks to reduce message-passing overhead.'
types:
- name: PresentationPriority
  kind: enum
  decorators:
  - type
  variants:
  - name: '"immediate"'
  - name: '"normal"'
  lines:
  - 9
  - 9
exports:
- name: PresentationPriority
  kind: type
