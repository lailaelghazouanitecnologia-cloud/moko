## Roska File Descriptor — xs/overrides.ts
file: xs/overrides.ts
lines: 111
layer: logic
imports:
- sym: ../../types.SystemPromptContext
data:
- name: XS_EDITING_FILES
  type: string
  val: |-
    `FILE EDITING RULES
    - Default: replace_in_file; write_to_file for new files or full rewrites.
    - Match the file’s **fin...
- name: XS_ACT_PLAN_MODE
  type: string
  val: |-
    `MODES (STRICT)
    **PLAN MODE (read-only, collaborative & curious):**
    - Allowed: plan_mode_respond, read_file, list_files,...
- name: XS_CAPABILITIES
  type: string
  val: |-
    `CURIOSITY & FIRST CONTACT
    - Ambiguity or missing requirement/success criterion → use <ask_followup_question> (1–2 f...
- name: XS_RULES
  type: string
  val: |-
    `GLOBAL RULES
    - One tool per message; wait for result. Never assume outcomes.
    - Exact XML tags for tool + params.
    - CWD ...
- name: XS_OBJECTIVES
  type: string
  val: |-
    `EXECUTION FLOW
    - Understand request → PLAN explore (read-only) → propose collaborative plan with options/risks/test...
- name: xsComponentOverrides
  type: object
  val: "{\n\tAGENT_ROLE:\n\t\t\"You are Cline, a senior software engineer + precise task runner. Thinks before acting, uses tools corr..."
functions:
- name: XS_TOOLS_OVERRIDE
  sig: 'XS_TOOLS_OVERRIDE(context: SystemPromptContext)'
  lines:
  - 42
  - 100
  detail: {}
exports:
- name: XS_TOOLS_OVERRIDE
  kind: func
- name: XS_EDITING_FILES
  kind: data
- name: XS_ACT_PLAN_MODE
  kind: data
- name: XS_CAPABILITIES
  kind: data
- name: XS_RULES
  kind: data
- name: XS_OBJECTIVES
  kind: data
- name: xsComponentOverrides
  kind: data
