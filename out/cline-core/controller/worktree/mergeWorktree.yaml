## Roska File Descriptor — worktree/mergeWorktree.ts
file: worktree/mergeWorktree.ts
lines: 215
layer: logic
imports:
- sym: '@shared/proto/cline/worktree.MergeWorktreeRequest'
- sym: '@shared/proto/cline/worktree.MergeWorktreeResult'
- sym: '@utils/git-worktree.listWorktrees'
- sym: '@utils/path.getWorkspacePath'
- sym: simple-git.simpleGit
- sym: '@/services/telemetry.telemetryService'
- sym: '...Controller'
functions:
- name: mergeWorktree
  sig: 'mergeWorktree(_controller: Controller, request: MergeWorktreeRequest): Promise<MergeWorktreeResult>'
  is_async: true
  lines:
  - 14
  - 215
  detail:
    calls:
    - MergeWorktreeResult.create
    - String
    - "diffResult\n\t\t\t\t\t.trim"
    - "diffResult\n\t\t\t\t\t.trim()\n\t\t\t\t\t.split"
    - "diffResult\n\t\t\t\t\t.trim()\n\t\t\t\t\t.split(\"\\n\")\n\t\t\t\t\t.filter"
    - getWorkspacePath
    - git.deleteLocalBranch
    - git.diff
    - git.merge
    - git.raw
    - git.status
    - listWorktrees
    - simpleGit
    - sourceBranch.trim
    - status.isClean
    - targetStatus.isClean
    - telemetryService.captureWorktreeMergeAttempted
    - worktreeGit.revparse
    - worktreeGit.status
    - worktrees.find
exports:
- name: mergeWorktree
  kind: func
