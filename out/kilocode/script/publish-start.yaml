## Roska File Descriptor — script/publish-start.ts
file: script/publish-start.ts
lines: 103
imports:
- sym: bun.$
- sym: '@opencode-ai/script.Script'
- sym: ./changelog.buildNotes
- sym: ./changelog.getLatestRelease
data:
- name: highlightsTemplate
  type: string
  val: |-
    `## Highlights

    <!--
    Add highlights before publishing. Delete this section if no highlights.

    - For multiple highlights,...
- name: notes
  type: string[]
  val: '[]'
- name: skipNotes
  type: any
  val: process.env["KILO_SKIP_NOTES"] === "1"
- name: pkgjsons
  type: any
  val: |-
    await Array.fromAsync(
      new Bun.Glob("**/package.json").scan({
        absolute: true,
      }),
    ).then((arr) => arr.filter((x)...
- name: extensionToml
  type: any
  val: new URL("../packages/extensions/zed/extension.toml", import.meta.url).pathname
- name: toml
  type: any
  val: await Bun.file(extensionToml).text()
- name: dir
  type: any
  val: new URL("..", import.meta.url).pathname
- name: output
  type: string
  val: '`version=${Script.version}\n`'
exports:
- name: highlightsTemplate
  kind: data
- name: notes
  kind: data
- name: skipNotes
  kind: data
- name: pkgjsons
  kind: data
- name: extensionToml
  kind: data
- name: toml
  kind: data
- name: dir
  kind: data
- name: output
  kind: data
