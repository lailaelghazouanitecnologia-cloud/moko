## Roska File Descriptor — transforms/transform-extensions.ts
file: transforms/transform-extensions.ts
lines: 294
layer: logic
imports:
- sym: bun.$
- sym: ../utils/logger.info
- sym: ../utils/logger.success
- sym: ../utils/logger.warn
- sym: ../utils/logger.debug
- sym: ../utils/config.defaultConfig
- sym: ../utils/git.oursHasKilocodeChanges
data:
- name: EXTENSION_REPLACEMENTS
  type: ExtensionReplacement[]
  val: |-
    [
      // TOML files (Zed extension)
      {
        pattern: /name\s*=\s*"opencode"/g,
        replacement: 'name = "kilo"',
        descr...
types:
- name: ExtensionTransformResult
  kind: trait
  decorators:
  - interface
  fields:
  - name: file
    type: string
  - name: action
    type: '"transformed" | "skipped" | "failed" | "flagged"'
  - name: replacements
    type: number
  - name: dryRun
    type: boolean
  lines:
  - 14
  - 19
- name: ExtensionTransformOptions
  kind: trait
  decorators:
  - interface
  fields:
  - name: dryRun
    type: boolean
  - name: verbose
    type: boolean
  lines:
  - 21
  - 24
- name: ExtensionReplacement
  kind: trait
  decorators:
  - interface
  fields:
  - name: pattern
    type: RegExp
  - name: replacement
    type: string
  - name: description
    type: string
  - name: fileTypes
    type: string[]
  lines:
  - 26
  - 31
functions:
- name: isExtensionFile
  sig: 'isExtensionFile(file: string): boolean'
  lines:
  - 104
  - 111
  detail:
    calls:
    - patterns.some
- name: getExtension
  sig: 'getExtension(file: string): string'
  lines:
  - 116
  - 119
  detail:
    calls:
    - file.match
- name: applyExtensionTransforms
  sig: |-
    applyExtensionTransforms(
      content: string,
      file: string,
      verbose = false,
    ): { result: string; replacements: number }
  lines:
  - 124
  - 154
  detail:
    calls:
    - debug
    - fileTypes.includes
    - getExtension
    - pattern.test
    - result.replace
- name: transformExtensionFile
  sig: |-
    transformExtensionFile(
      file: string,
      options: ExtensionTransformOptions = {},
    ): Promise<ExtensionTransformResult>
  is_async: true
  lines:
  - 159
  - 197
  detail:
    calls:
    - $
    - $`git add ${file}`.quiet
    - $`git add ${file}`.quiet().nothrow
    - $`git checkout --theirs ${file}`.quiet
    - $`git checkout --theirs ${file}`.quiet().nothrow
    - Bun.file
    - Bun.file(file).text
    - Bun.write
    - applyExtensionTransforms
    - info
    - oursHasKilocodeChanges
    - success
    - warn
- name: transformConflictedExtensions
  sig: |-
    transformConflictedExtensions(
      files: string[],
      options: ExtensionTransformOptions = {},
    ): Promise<ExtensionTransformResult[]>
  is_async: true
  lines:
  - 202
  - 220
  detail:
    calls:
    - debug
    - isExtensionFile
    - results.push
    - transformExtensionFile
- name: transformAllExtensions
  sig: |-
    transformAllExtensions(
      options: ExtensionTransformOptions = {},
    ): Promise<ExtensionTransformResult[]>
  is_async: true
  lines:
  - 225
  - 264
  detail:
    calls:
    - Bun.file
    - Bun.write
    - applyExtensionTransforms
    - file.exists
    - file.text
    - glob.scan
    - import
    - info
    - new Glob
    - path.endsWith
    - results.push
    - success
    - warn
exports:
- name: ExtensionTransformResult
  kind: type
- name: ExtensionTransformOptions
  kind: type
- name: ExtensionReplacement
  kind: type
- name: isExtensionFile
  kind: func
- name: getExtension
  kind: func
- name: applyExtensionTransforms
  kind: func
- name: transformExtensionFile
  kind: func
- name: transformConflictedExtensions
  kind: func
- name: transformAllExtensions
  kind: func
- name: EXTENSION_REPLACEMENTS
  kind: data
