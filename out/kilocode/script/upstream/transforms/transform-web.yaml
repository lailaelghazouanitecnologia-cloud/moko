## Roska File Descriptor — transforms/transform-web.ts
file: transforms/transform-web.ts
lines: 301
layer: logic
imports:
- sym: bun.$
- sym: ../utils/logger.info
- sym: ../utils/logger.success
- sym: ../utils/logger.warn
- sym: ../utils/logger.debug
- sym: ../utils/config.defaultConfig
- sym: ../utils/git.oursHasKilocodeChanges
data:
- name: WEB_REPLACEMENTS
  type: WebReplacement[]
  val: |-
    [
      // GitHub references
      {
        pattern: /github\.com\/anomalyco\/opencode/g,
        replacement: "github.com/Kilo-Org/kil...
- name: PRESERVE_PATTERNS
  type: array
  val: '[/opencode\.json/g, /\.opencode\//g, /`\.opencode`/g]'
types:
- name: WebTransformResult
  kind: trait
  decorators:
  - interface
  fields:
  - name: file
    type: string
  - name: action
    type: '"transformed" | "skipped" | "failed" | "flagged"'
  - name: replacements
    type: number
  - name: dryRun
    type: boolean
  lines:
  - 14
  - 19
- name: WebTransformOptions
  kind: trait
  decorators:
  - interface
  fields:
  - name: dryRun
    type: boolean
  - name: verbose
    type: boolean
  lines:
  - 21
  - 24
- name: WebReplacement
  kind: trait
  decorators:
  - interface
  fields:
  - name: pattern
    type: RegExp
  - name: replacement
    type: string
  - name: description
    type: string
  lines:
  - 26
  - 30
functions:
- name: isWebFile
  sig: 'isWebFile(file: string): boolean'
  lines:
  - 114
  - 121
  detail:
    calls:
    - patterns.some
- name: applyWebTransforms
  sig: 'applyWebTransforms(content: string, verbose = false): { result: string; replacements: number }'
  lines:
  - 126
  - 171
  detail:
    calls:
    - content.split
    - debug
    - pattern.test
    - result.replace
    - transformed.join
    - transformed.push
- name: transformWebFile
  sig: 'transformWebFile(file: string, options: WebTransformOptions = {}): Promise<WebTransformResult>'
  is_async: true
  lines:
  - 176
  - 211
  detail:
    calls:
    - $
    - $`git add ${file}`.quiet
    - $`git add ${file}`.quiet().nothrow
    - $`git checkout --theirs ${file}`.quiet
    - $`git checkout --theirs ${file}`.quiet().nothrow
    - Bun.file
    - Bun.file(file).text
    - Bun.write
    - applyWebTransforms
    - info
    - oursHasKilocodeChanges
    - success
    - warn
- name: transformConflictedWeb
  sig: |-
    transformConflictedWeb(
      files: string[],
      options: WebTransformOptions = {},
    ): Promise<WebTransformResult[]>
  is_async: true
  lines:
  - 216
  - 234
  detail:
    calls:
    - debug
    - isWebFile
    - results.push
    - transformWebFile
- name: transformAllWeb
  sig: 'transformAllWeb(options: WebTransformOptions = {}): Promise<WebTransformResult[]>'
  is_async: true
  lines:
  - 239
  - 271
  detail:
    calls:
    - Bun.file
    - Bun.write
    - applyWebTransforms
    - file.exists
    - file.text
    - glob.scan
    - import
    - info
    - new Glob
    - results.push
    - success
    - warn
exports:
- name: WebTransformResult
  kind: type
- name: WebTransformOptions
  kind: type
- name: WebReplacement
  kind: type
- name: isWebFile
  kind: func
- name: applyWebTransforms
  kind: func
- name: transformWebFile
  kind: func
- name: transformConflictedWeb
  kind: func
- name: transformAllWeb
  kind: func
- name: WEB_REPLACEMENTS
  kind: data
- name: PRESERVE_PATTERNS
  kind: data
