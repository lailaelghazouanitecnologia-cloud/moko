## Roska File Descriptor — transforms/transform-take-theirs.ts
file: transforms/transform-take-theirs.ts
lines: 347
imports:
- sym: bun.$
- sym: ../utils/logger.info
- sym: ../utils/logger.success
- sym: ../utils/logger.warn
- sym: ../utils/logger.debug
- sym: ../utils/config.defaultConfig
- sym: ../utils/git.oursHasKilocodeChanges
data:
- name: BRANDING_REPLACEMENTS
  type: BrandingReplacement[]
  val: |-
    [
      // GitHub repo references
      {
        pattern: /github\.com\/anomalyco\/opencode/g,
        replacement: "github.com/Kilo-Or...
- name: PRESERVE_PATTERNS
  type: array
  val: |-
    [
      /opencode\.json/g, // Config filename
      /\.opencode\//g, // Directory name
      /\.opencode`/g, // Directory name in te...
types:
- name: TakeTheirsResult
  kind: trait
  decorators:
  - interface
  fields:
  - name: file
    type: string
  - name: action
    type: '"transformed" | "skipped" | "failed" | "flagged"'
  - name: replacements
    type: number
  - name: dryRun
    type: boolean
  lines:
  - 19
  - 24
- name: TakeTheirsOptions
  kind: trait
  decorators:
  - interface
  fields:
  - name: dryRun
    type: boolean
  - name: verbose
    type: boolean
  - name: patterns
    type: string[]
  lines:
  - 26
  - 30
- name: BrandingReplacement
  kind: trait
  decorators:
  - interface
  fields:
  - name: pattern
    type: RegExp
  - name: replacement
    type: string
  - name: description
    type: string
  lines:
  - 32
  - 36
functions:
- name: matchesPattern
  sig: 'matchesPattern(file: string, patterns: string[]): boolean'
  lines:
  - 149
  - 155
  detail:
    calls:
    - patterns.some
- name: applyBrandingTransforms
  sig: 'applyBrandingTransforms(content: string, verbose = false): { result: string; replacements: number }'
  lines:
  - 160
  - 206
  detail:
    calls:
    - before.trim
    - content.split
    - debug
    - line.includes
    - pattern.test
    - result.replace
    - result.trim
    - transformed.join
    - transformed.push
- name: transformTakeTheirs
  sig: 'transformTakeTheirs(file: string, options: TakeTheirsOptions = {}): Promise<TakeTheirsResult>'
  is_async: true
  lines:
  - 211
  - 246
  detail:
    calls:
    - $
    - $`git add ${file}`.quiet
    - $`git add ${file}`.quiet().nothrow
    - $`git checkout --theirs ${file}`.quiet
    - $`git checkout --theirs ${file}`.quiet().nothrow
    - Bun.file
    - Bun.file(file).text
    - Bun.write
    - applyBrandingTransforms
    - info
    - oursHasKilocodeChanges
    - success
    - warn
- name: transformConflictedTakeTheirs
  sig: |-
    transformConflictedTakeTheirs(
      files: string[],
      options: TakeTheirsOptions = {},
    ): Promise<TakeTheirsResult[]>
  is_async: true
  lines:
  - 251
  - 270
  detail:
    calls:
    - debug
    - matchesPattern
    - results.push
    - transformTakeTheirs
- name: shouldTakeTheirs
  sig: 'shouldTakeTheirs(file: string, patterns?: string[]): boolean'
  lines:
  - 275
  - 278
- name: transformAllTakeTheirs
  sig: 'transformAllTakeTheirs(options: TakeTheirsOptions = {}): Promise<TakeTheirsResult[]>'
  is_async: true
  lines:
  - 284
  - 317
  detail:
    calls:
    - Bun.file
    - Bun.write
    - applyBrandingTransforms
    - file.exists
    - file.text
    - glob.scan
    - import
    - info
    - new Glob
    - results.push
    - success
    - warn
exports:
- name: TakeTheirsResult
  kind: type
- name: TakeTheirsOptions
  kind: type
- name: BrandingReplacement
  kind: type
- name: matchesPattern
  kind: func
- name: applyBrandingTransforms
  kind: func
- name: transformTakeTheirs
  kind: func
- name: transformConflictedTakeTheirs
  kind: func
- name: shouldTakeTheirs
  kind: func
- name: transformAllTakeTheirs
  kind: func
- name: BRANDING_REPLACEMENTS
  kind: data
- name: PRESERVE_PATTERNS
  kind: data
