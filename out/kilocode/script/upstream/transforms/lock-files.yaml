## Roska File Descriptor — transforms/lock-files.ts
file: transforms/lock-files.ts
lines: 184
layer: logic
imports:
- sym: bun.$
- sym: ../utils/logger.info
- sym: ../utils/logger.success
- sym: ../utils/logger.warn
- sym: ../utils/logger.debug
- sym: ../utils/config.defaultConfig
- sym: ../utils/git.checkoutOurs
- sym: ../utils/git.stageFiles
- sym: ../utils/git.getConflictedFiles
types:
- name: LockFileResult
  kind: trait
  decorators:
  - interface
  fields:
  - name: file
    type: string
  - name: action
    type: '"resolved" | "skipped" | "regenerated" | "failed"'
  - name: dryRun
    type: boolean
  lines:
  - 15
  - 19
- name: LockFileOptions
  kind: trait
  decorators:
  - interface
  fields:
  - name: dryRun
    type: boolean
  - name: verbose
    type: boolean
  - name: patterns
    type: string[]
  lines:
  - 21
  - 25
functions:
- name: isLockFile
  sig: 'isLockFile(path: string, patterns?: string[]): boolean'
  lines:
  - 30
  - 52
  detail:
    calls:
    - lockPatterns.some
- name: resolveLockFileConflicts
  sig: 'resolveLockFileConflicts(options: LockFileOptions = {}): Promise<LockFileResult[]>'
  is_async: true
  lines:
  - 57
  - 96
  detail:
    calls:
    - checkoutOurs
    - conflicted.filter
    - debug
    - getConflictedFiles
    - info
    - results.push
    - stageFiles
    - success
    - warn
- name: regenerateLockFiles
  sig: 'regenerateLockFiles(options: LockFileOptions = {}): Promise<LockFileResult[]>'
  is_async: true
  lines:
  - 101
  - 156
  detail:
    calls:
    - $
    - $`bun install`.quiet
    - $`bun install`.quiet().nothrow
    - $`cargo generate-lockfile`.cwd
    - $`cargo generate-lockfile`.cwd("packages/desktop/src-tauri").quiet
    - $`cargo generate-lockfile`.cwd("packages/desktop/src-tauri").quiet().nothrow
    - Bun.file
    - Bun.file("bun.lock").exists
    - Bun.file(cargoLockPath).exists
    - Bun.file(nixHashesPath).exists
    - info
    - result.stderr.toString
    - results.push
    - success
    - warn
exports:
- name: LockFileResult
  kind: type
- name: LockFileOptions
  kind: type
- name: isLockFile
  kind: func
- name: resolveLockFileConflicts
  kind: func
- name: regenerateLockFiles
  kind: func
