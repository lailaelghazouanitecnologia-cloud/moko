## Roska File Descriptor — script/publish.ts
file: script/publish.ts
lines: 93
imports:
- sym: '@opencode-ai/script.Script'
- sym: bun.$
- sym: url.fileURLToPath
data:
- name: highlightsTemplate
  type: string
  val: |-
    `
    <!--
    Add highlights before publishing. Delete this section if no highlights.

    - For multiple highlights, use multiple ...
- name: pkgjsons
  type: any
  val: |-
    await Array.fromAsync(
      new Bun.Glob("**/package.json").scan({
        absolute: true,
      }),
    ).then((arr) => arr.filter((x)...
- name: extensionToml
  type: any
  val: fileURLToPath(new URL("../packages/extensions/zed/extension.toml", import.meta.url))
- name: toml
  type: any
  val: await Bun.file(extensionToml).text()
- name: dir
  type: any
  val: fileURLToPath(new URL("..", import.meta.url))
exports:
- name: highlightsTemplate
  kind: data
- name: pkgjsons
  kind: data
- name: extensionToml
  kind: data
- name: toml
  kind: data
- name: dir
  kind: data
