## Roska File Descriptor — context/layout.ts
file: context/layout.ts
lines: 41
imports:
- sym: solid-js.createSignal
data:
- name: '[all, setAll]'
  type: any
  val: createSignal<string[]>([])
- name: '[active, setActive]'
  type: any
  val: createSignal<string | undefined>(undefined)
- name: '[reviewOpen, setReviewOpen]'
  type: any
  val: createSignal(false)
- name: tabs
  type: object
  val: |-
    {
      all,
      active,
      open(tab: string) {
        setAll((current) => (current.includes(tab) ? current : [...current, tab]))
    ...
- name: view
  type: object
  val: |-
    {
      reviewPanel: {
        opened: reviewOpen,
        open() {
          setReviewOpen(true)
        },
      },
    }
functions:
- name: useLayout
  sig: useLayout()
  lines:
  - 30
  - 41
  detail: {}
exports:
- name: useLayout
  kind: func
- name: '[all, setAll]'
  kind: data
- name: '[active, setActive]'
  kind: data
- name: '[reviewOpen, setReviewOpen]'
  kind: data
- name: tabs
  kind: data
- name: view
  kind: data
