## Roska File Descriptor — scripts/utils.ts
file: scripts/utils.ts
lines: 53
imports:
- sym: bun.$
data:
- name: SIDECAR_BINARIES
  type: 'Array<{ rustTarget: string; ocBinary: string; assetExt: string }>'
  val: |-
    [
      {
        rustTarget: "aarch64-apple-darwin",
        ocBinary: "@kilocode/cli-darwin-arm64", // kilocode_change
        assetEx...
- name: RUST_TARGET
  type: any
  val: Bun.env.RUST_TARGET
functions:
- name: getCurrentSidecar
  sig: getCurrentSidecar(target = RUST_TARGET)
  lines:
  - 33
  - 40
  detail:
    calls:
    - SIDECAR_BINARIES.find
    - new Error
- name: copyBinaryToSidecarFolder
  sig: 'copyBinaryToSidecarFolder(source: string, target = RUST_TARGET)'
  is_async: true
  lines:
  - 42
  - 48
  detail:
    calls:
    - $
    - console.log
    - windowsify
- name: windowsify
  sig: 'windowsify(path: string)'
  lines:
  - 50
  - 53
  detail:
    calls:
    - path.endsWith
exports:
- name: getCurrentSidecar
  kind: func
- name: copyBinaryToSidecarFolder
  kind: func
- name: windowsify
  kind: func
- name: SIDECAR_BINARIES
  kind: data
- name: RUST_TARGET
  kind: data
