## Roska File Descriptor — auth/device-auth.ts
file: auth/device-auth.ts
lines: 193
layer: logic
purpose: LEGACY CLI VERSION - NOT CURRENTLY USED This file contains the original CLI-based device authorization flow using @clack/prompts. It has been replaced by device-auth-tui.ts which provides a universal flow compatible with both CLI and TUI contexts. Kept for reference and potential future use.
imports:
- sym: open.open
- sym: '@clack/prompts.spinner'
- sym: ../types.js.DeviceAuthInitiateResponse
  kind: typing
- sym: ../types.js.DeviceAuthPollResponse
  kind: typing
- sym: ./polling.js.poll
- sym: ./polling.js.formatTimeRemaining
- sym: ../api/profile.js.getKiloProfile
- sym: ../api/profile.js.getKiloDefaultModel
- sym: ../api/profile.js.promptOrganizationSelection
- sym: ../api/constants.js.KILO_API_BASE
- sym: ../api/constants.js.POLL_INTERVAL_MS
types:
- name: DeviceAuthResult
  kind: trait
  decorators:
  - interface
  fields:
  - name: token
    type: string
  - name: organizationId
    type: string
  - name: model
    type: string
  lines:
  - 74
  - 78
functions:
- name: initiateDeviceAuth
  sig: 'initiateDeviceAuth(): Promise<DeviceAuthInitiateResponse>'
  is_async: true
  lines:
  - 23
  - 40
  detail:
    calls:
    - fetch
    - new Error
    - response.json
- name: pollDeviceAuth
  sig: 'pollDeviceAuth(code: string): Promise<DeviceAuthPollResponse>'
  is_async: true
  lines:
  - 48
  - 72
  detail:
    calls:
    - fetch
    - new Error
    - response.json
- name: authenticateWithDeviceAuth
  sig: 'authenticateWithDeviceAuth(): Promise<DeviceAuthResult>'
  is_async: true
  lines:
  - 85
  - 193
  detail:
    calls:
    - Date.now
    - Math.ceil
    - Math.floor
    - String
    - String(expiresIn % 60).padStart
    - await poll
    - console.log
    - getKiloDefaultModel
    - getKiloProfile
    - initiateDeviceAuth
    - new Error
    - open
    - open(verificationUrl).catch
    - promptOrganizationSelection
    - s.start
    - s.stop
    - spinner
exports:
- name: DeviceAuthResult
  kind: type
- name: initiateDeviceAuth
  kind: func
- name: pollDeviceAuth
  kind: func
- name: authenticateWithDeviceAuth
  kind: func
