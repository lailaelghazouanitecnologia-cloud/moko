## Roska File Descriptor — tui/helpers.ts
file: tui/helpers.ts
lines: 86
purpose: TUI-specific helper functions for Kilo Gateway integration This module provides utilities that are consumed by the TUI layer to implement organization selection, profile display, and team management.
imports:
- sym: ../types.js.KilocodeProfile
  kind: typing
- sym: ../types.js.KilocodeBalance
  kind: typing
- sym: ../types.js.Organization
  kind: typing
functions:
- name: formatProfileInfo
  sig: |-
    formatProfileInfo(
      profile: KilocodeProfile,
      balance: KilocodeBalance | null,
      currentOrgId?: string,
    ): string
  lines:
  - 14
  - 50
  detail:
    calls:
    - balance.balance.toFixed
    - profile.organizations.find
- name: getOrganizationOptions
  sig: |-
    getOrganizationOptions(
      organizations: Organization[],
      currentOrgId?: string,
    ): Array<{
      title: string
      value: string | null
      description?: string
      category: string
    }>
  lines:
  - 56
  - 79
  detail:
    calls:
    - organizations.map
- name: getDefaultOrganizationSelection
  sig: 'getDefaultOrganizationSelection(organizations: Organization[]): string | null'
  lines:
  - 84
  - 86
exports:
- name: formatProfileInfo
  kind: func
- name: getOrganizationOptions
  kind: func
- name: getDefaultOrganizationSelection
  kind: func
