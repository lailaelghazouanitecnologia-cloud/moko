## Roska File Descriptor — nodes/heading.markdoc.ts
file: nodes/heading.markdoc.ts
lines: 32
imports:
- sym: '@markdoc/markdoc.Tag'
- sym: ../../components.Heading
data:
- name: heading
  type: object
  val: |-
    {
      render: Heading,
      children: ["inline"],
      attributes: {
        id: { type: String },
        level: { type: Number, requir...
functions:
- name: generateID
  sig: generateID(children, attributes)
  lines:
  - 5
  - 15
  detail:
    calls:
    - |-
      children
          .filter
    - |-
      children
          .filter((child) => typeof child === "string")
          .join
    - |-
      children
          .filter((child) => typeof child === "string")
          .join(" ")
          .replace
    - |-
      children
          .filter((child) => typeof child === "string")
          .join(" ")
          .replace(/[?]/g, "")
       ...
exports:
- name: generateID
  kind: func
- name: heading
  kind: data
