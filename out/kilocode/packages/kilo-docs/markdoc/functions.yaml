## Roska File Descriptor — markdoc/functions.ts
file: markdoc/functions.ts
lines: 16
purpose: Use this file to export your Markdoc functions
data:
- name: includes
  type: object
  val: |-
    {
      transform(parameters) {
        const [array, value] = Object.values(parameters)

        return Array.isArray(array) ? arra...
- name: upper
  type: object
  val: |-
    {
      transform(parameters) {
        const string = parameters[0]
        return typeof string === "string" ? string.toUpperCase(...
exports:
- name: includes
  kind: data
- name: upper
  kind: data
