## Roska File Descriptor — tool/webfetch.ts
file: tool/webfetch.ts
lines: 206
imports:
- sym: zod.z
- sym: ./tool.Tool
- sym: turndown.TurndownService
- sym: ./webfetch.txt.DESCRIPTION
- sym: ../util/abort.abortAfterAny
data:
- name: MAX_RESPONSE_SIZE
  type: any
  val: 5 * 1024 * 1024
- name: DEFAULT_TIMEOUT
  type: any
  val: 30 * 1000
- name: MAX_TIMEOUT
  type: any
  val: 120 * 1000
- name: WebFetchTool
  type: any
  val: |-
    Tool.define("webfetch", {
      description: DESCRIPTION,
      parameters: z.object({
        url: z.string().describe("The URL to ...
functions:
- name: extractTextFromHTML
  sig: 'extractTextFromHTML(html: string)'
  is_async: true
  lines:
  - 164
  - 194
  detail:
    calls:
    - '["script", "style", "noscript", "iframe", "object", "embed"].includes'
    - new HTMLRewriter
    - |-
      new HTMLRewriter()
          .on
    - |-
      new HTMLRewriter()
          .on("script, style, noscript, iframe, object, embed", {
            element() {
         ...
    - new Response
    - rewriter.text
    - text.trim
- name: convertHTMLToMarkdown
  sig: 'convertHTMLToMarkdown(html: string): string'
  lines:
  - 196
  - 206
  detail:
    calls:
    - new TurndownService
    - turndownService.remove
    - turndownService.turndown
exports:
- name: extractTextFromHTML
  kind: func
- name: convertHTMLToMarkdown
  kind: func
- name: MAX_RESPONSE_SIZE
  kind: data
- name: DEFAULT_TIMEOUT
  kind: data
- name: MAX_TIMEOUT
  kind: data
- name: WebFetchTool
  kind: data
