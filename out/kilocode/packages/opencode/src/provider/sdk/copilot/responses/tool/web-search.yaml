## Roska File Descriptor — tool/web-search.ts
file: tool/web-search.ts
lines: 103
layer: logic
imports:
- sym: '@ai-sdk/provider-utils.createProviderDefinedToolFactory'
- sym: zod/v4.z
data:
- name: webSearchArgsSchema
  type: any
  val: |-
    z.object({
      filters: z
        .object({
          allowedDomains: z.array(z.string()).optional(),
        })
        .optional(),

      s...
- name: webSearchToolFactory
  type: any
  val: |-
    createProviderDefinedToolFactory<
      {
        // Web search doesn't take input parameters - it's controlled by the prompt
      ...
functions:
- name: webSearch
  sig: |-
    webSearch(
      args: Parameters<typeof webSearchToolFactory>[0] = {}, // default
    )
  lines:
  - 99
  - 103
  detail:
    calls:
    - webSearchToolFactory
exports:
- name: webSearch
  kind: func
- name: webSearchArgsSchema
  kind: data
- name: webSearchToolFactory
  kind: data
