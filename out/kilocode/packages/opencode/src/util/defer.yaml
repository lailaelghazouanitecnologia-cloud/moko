## Roska File Descriptor — util/defer.ts
file: util/defer.ts
lines: 12
functions:
- name: defer
  sig: |-
    defer(
      fn: T,
    ): T extends () => Promise<void> ? { [Symbol.asyncDispose]: () => Promise<void> } : { [Symbol.dispose]: () => void }
  lines:
  - 1
  - 12
  detail:
    calls:
    - Promise.resolve
    - fn
exports:
- name: defer
  kind: func
