## Roska File Descriptor — control-plane/sse.ts
file: control-plane/sse.ts
lines: 66
functions:
- name: parseSSE
  sig: |-
    parseSSE(
      body: ReadableStream<Uint8Array>,
      signal: AbortSignal,
      onEvent: (event: unknown) => void,
    )
  is_async: true
  lines:
  - 1
  - 66
  detail:
    calls:
    - body.getReader
    - buf.replace
    - buf.replace(/\r\n/g, "\n").replace
    - buf.split
    - chunks.forEach
    - chunks.pop
    - decoder.decode
    - new TextDecoder
    - reader.read
    - reader.read().catch
    - reader.releaseLock
    - signal.addEventListener
    - signal.removeEventListener
exports:
- name: parseSSE
  kind: func
