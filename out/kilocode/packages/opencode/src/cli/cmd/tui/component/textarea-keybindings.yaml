## Roska File Descriptor — component/textarea-keybindings.ts
file: component/textarea-keybindings.ts
lines: 73
layer: logic
imports:
- sym: solid-js.createMemo
- sym: '@opentui/core.KeyBinding'
  kind: typing
- sym: ../context/keybind.useKeybind
- sym: '@/util/keybind.Keybind'
data:
- name: TEXTAREA_ACTIONS
  type: array
  val: |-
    [
      "submit",
      "newline",
      "move-left",
      "move-right",
      "move-up",
      "move-down",
      "select-left",
      "select-right",...
functions:
- name: mapTextareaKeybindings
  sig: |-
    mapTextareaKeybindings(
      keybinds: Record<string, Keybind.Info[]>,
      action: (typeof TEXTAREA_ACTIONS)[number],
    ): KeyBinding[]
  lines:
  - 44
  - 59
  detail:
    calls:
    - action.replace
    - bindings.map
- name: useTextareaKeybindings
  sig: useTextareaKeybindings()
  lines:
  - 61
  - 73
  detail:
    calls:
    - createMemo
    - useKeybind
exports:
- name: mapTextareaKeybindings
  kind: func
- name: useTextareaKeybindings
  kind: func
- name: TEXTAREA_ACTIONS
  kind: data
