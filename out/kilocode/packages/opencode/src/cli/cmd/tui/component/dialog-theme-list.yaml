## Roska File Descriptor — component/dialog-theme-list.tsx
file: component/dialog-theme-list.tsx
lines: 50
layer: logic
imports:
- sym: ../ui/dialog-select.DialogSelect
- sym: ../ui/dialog-select.type DialogSelectRef
- sym: ../context/theme.useTheme
- sym: ../ui/dialog.useDialog
- sym: solid-js.onCleanup
- sym: solid-js.onMount
functions:
- name: DialogThemeList
  sig: DialogThemeList()
  lines:
  - 6
  - 50
  detail:
    calls:
    - Object.keys
    - |-
      Object.keys(theme.all())
          .sort
    - |-
      Object.keys(theme.all())
          .sort((a, b) => a.localeCompare(b, undefined, { sensitivity: "base" }))...
    - onCleanup
    - theme.all
    - useDialog
    - useTheme
    - |-
      {
                theme.set
exports:
- name: DialogThemeList
  kind: func
