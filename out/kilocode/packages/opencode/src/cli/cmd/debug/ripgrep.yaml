## Roska File Descriptor — debug/ripgrep.ts
file: debug/ripgrep.ts
lines: 87
imports:
- sym: ../../../file/ripgrep.Ripgrep
- sym: ../../../project/instance.Instance
- sym: ../../bootstrap.bootstrap
- sym: ../cmd.cmd
data:
- name: RipgrepCommand
  type: any
  val: |-
    cmd({
      command: "rg",
      describe: "ripgrep debugging utilities",
      builder: (yargs) => yargs.command(TreeCommand).comma...
- name: TreeCommand
  type: any
  val: |-
    cmd({
      command: "tree",
      describe: "show file tree using ripgrep",
      builder: (yargs) =>
        yargs.option("limit", {
     ...
- name: FilesCommand
  type: any
  val: |-
    cmd({
      command: "files",
      describe: "list files using ripgrep",
      builder: (yargs) =>
        yargs
          .option("query",...
- name: SearchCommand
  type: any
  val: |-
    cmd({
      command: "search <pattern>",
      describe: "search file contents using ripgrep",
      builder: (yargs) =>
        yargs
     ...
exports:
- name: RipgrepCommand
  kind: data
- name: TreeCommand
  kind: data
- name: FilesCommand
  kind: data
- name: SearchCommand
  kind: data
