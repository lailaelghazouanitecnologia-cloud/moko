## Roska File Descriptor — kilocode/editor-context.ts
file: kilocode/editor-context.ts
lines: 57
layer: logic
types:
- name: EditorContext
  kind: trait
  decorators:
  - interface
  fields:
  - name: visibleFiles
    type: string[]
  - name: openTabs
    type: string[]
  - name: activeFile
    type: string
  - name: shell
    type: string
  lines:
  - 1
  - 6
functions:
- name: staticEnvLines
  sig: 'staticEnvLines(ctx?: EditorContext): string[]'
  lines:
  - 13
  - 19
  detail:
    calls:
    - lines.push
- name: timestamp
  sig: 'timestamp(): string'
  lines:
  - 27
  - 37
  detail:
    calls:
    - (Math.abs(offset) % 60).toString
    - (Math.abs(offset) % 60).toString().padStart
    - Math.abs
    - Math.floor
    - |-
      Math.floor(Math.abs(offset) / 60)
          .toString
    - |-
      Math.floor(Math.abs(offset) / 60)
          .toString()
          .padStart
    - new Date
    - now.getDate
    - now.getFullYear
    - now.getHours
    - now.getMinutes
    - now.getMonth
    - now.getSeconds
    - now.getTimezoneOffset
    - pad
- name: environmentDetails
  sig: 'environmentDetails(ctx?: EditorContext): string'
  lines:
  - 39
  - 57
  detail:
    calls:
    - '["<environment_details>", ...lines, "</environment_details>"].join'
    - lines.push
    - timestamp
exports:
- name: EditorContext
  kind: type
- name: staticEnvLines
  kind: func
- name: timestamp
  kind: func
- name: environmentDetails
  kind: func
