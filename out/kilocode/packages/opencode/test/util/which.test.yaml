## Roska File Descriptor — util/which.test.ts
file: util/which.test.ts
lines: 100
imports:
- sym: bun:test.describe
- sym: bun:test.expect
- sym: bun:test.test
- sym: fs/promises.fs
- sym: path.path
- sym: ../../src/util/which.which
- sym: ../fixture/fixture.tmpdir
functions:
- name: cmd
  sig: 'cmd(dir: string, name: string, exec = true)'
  is_async: true
  lines:
  - 7
  - 16
  detail:
    calls:
    - fs.chmod
    - fs.writeFile
    - path.join
- name: env
  sig: 'env(PATH: string): NodeJS.ProcessEnv'
  lines:
  - 18
  - 23
  detail: {}
- name: envPath
  sig: 'envPath(Path: string): NodeJS.ProcessEnv'
  lines:
  - 25
  - 30
  detail: {}
- name: same
  sig: 'same(a: string | null, b: string)'
  lines:
  - 32
  - 39
  detail:
    calls:
    - a?.toLowerCase
    - b.toLowerCase
    - expect
    - expect(a).toBe
    - expect(a?.toLowerCase()).toBe
exports:
- name: cmd
  kind: func
- name: env
  kind: func
- name: envPath
  kind: func
- name: same
  kind: func
