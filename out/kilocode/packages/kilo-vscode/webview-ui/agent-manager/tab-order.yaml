## Roska File Descriptor — agent-manager/tab-order.ts
file: agent-manager/tab-order.ts
lines: 62
layer: logic
purpose: Pure tab-ordering logic for the agent manager.
functions:
- name: reorderTabs
  sig: 'reorderTabs(tabs: readonly string[], from: string, to: string): string[] | undefined'
  lines:
  - 9
  - 18
  detail:
    calls:
    - result.splice
    - tabs.indexOf
- name: applyTabOrder
  sig: 'applyTabOrder(items: T[], order: string[] | undefined): T[]'
  lines:
  - 27
  - 40
  detail:
    calls:
    - items.map
    - lookup.delete
    - lookup.get
    - lookup.values
    - new Map
    - ordered.push
- name: firstOrderedTitle
  sig: |-
    firstOrderedTitle(
      items: { id: string; title?: string }[],
      order: string[] | undefined,
      fallback: string,
    ): string
  lines:
  - 48
  - 62
  detail:
    calls:
    - items.find
    - items.map
    - lookup.get
    - new Map
exports:
- name: reorderTabs
  kind: func
- name: applyTabOrder
  kind: func
- name: firstOrderedTitle
  kind: func
