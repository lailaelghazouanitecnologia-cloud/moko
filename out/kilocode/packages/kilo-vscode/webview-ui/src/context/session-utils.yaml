## Roska File Descriptor — context/session-utils.ts
file: context/session-utils.ts
lines: 64
layer: logic
imports:
- sym: ../types/messages.Part
  kind: typing
functions:
- name: computeStatus
  sig: |-
    computeStatus(
      part: Part | undefined,
      t: (key: string, params?: Record<string, string | number>) => string,
    ): string | undefined
  lines:
  - 7
  - 39
  detail:
    calls:
    - t
- name: calcTotalCost
  sig: 'calcTotalCost(messages: Array<{ role: string; cost?: number }>): number'
  lines:
  - 44
  - 46
  detail:
    calls:
    - messages.reduce
- name: calcContextUsage
  sig: |-
    calcContextUsage(
      tokens: {
        input: number
        output: number
        reasoning?: number
        cache?: { read: number; write: number }
      },
      contextLimit: number | undefined,
    ): { tokens: number; percentage: number | null }
  lines:
  - 51
  - 64
  detail:
    calls:
    - Math.round
exports:
- name: computeStatus
  kind: func
- name: calcTotalCost
  kind: func
- name: calcContextUsage
  kind: func
