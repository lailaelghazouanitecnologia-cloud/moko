## Roska File Descriptor — context/model-selection.ts
file: context/model-selection.ts
lines: 43
imports:
- sym: ../types/messages.ModelSelection
  kind: typing
- sym: ../types/messages.Provider
  kind: typing
- sym: ./provider-utils.isModelValid
functions:
- name: validate
  sig: |-
    validate(
      providers: Record<string, Provider>,
      connected: string[],
      selection: ModelSelection | null | undefined,
    ): ModelSelection | null
  lines:
  - 4
  - 12
  detail:
    calls:
    - Object.keys
    - isModelValid
- name: recent
  sig: |-
    recent(
      providers: Record<string, Provider>,
      connected: string[],
      selections: ModelSelection[] | undefined,
    ): ModelSelection | null
  lines:
  - 14
  - 24
  detail:
    calls:
    - validate
- name: resolveModelSelection
  sig: |-
    resolveModelSelection(input: {
      providers: Record<string, Provider>
      connected: string[]
      override?: ModelSelection | null
      mode?: ModelSelection | null
      global?: ModelSelection | null
      recent?: ModelSelection[]
      fallback?: ModelSelection | null
    }): ModelSelection | null
  lines:
  - 26
  - 43
  detail:
    calls:
    - recent
    - validate
exports:
- name: validate
  kind: func
- name: recent
  kind: func
- name: resolveModelSelection
  kind: func
