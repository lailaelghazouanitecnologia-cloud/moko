## Roska File Descriptor — shared/ModeSwitcher.tsx
file: shared/ModeSwitcher.tsx
lines: 150
purpose: ModeSwitcher component Popover-based selector for choosing an agent/mode in the chat prompt area. Uses kilo-ui Popover component (Phase 4.5 of UI implementation plan). ModeSwitcherBase — reusable core that accepts agents/value/onSelect props. ModeSwitcher     — thin wrapper wired to session context for chat usage.
imports:
- sym: solid-js.Component
- sym: solid-js.createSignal
- sym: solid-js.onCleanup
- sym: solid-js.For
- sym: solid-js.Show
- sym: '@kilocode/kilo-ui/popover.Popover'
- sym: '@kilocode/kilo-ui/button.Button'
- sym: ../../context/session.useSession
- sym: ../../types/messages.AgentInfo
  kind: typing
data:
- name: '[open, setOpen]'
  type: any
  val: createSignal(false)
- name: '[focused, setFocused]'
  type: any
  val: createSignal(-1)
- name: listRef
  type: HTMLDivElement | undefined
- name: session
  type: any
  val: useSession()
types:
- name: ModeSwitcherBaseProps
  kind: trait
  decorators:
  - interface
  fields:
  - name: agents
    type: AgentInfo[]
  - name: value
    type: string
  - name: onSelect
    type: '(name: string) => void'
  lines:
  - 20
  - 27
functions:
- name: onTrigger
  sig: onTrigger()
  lines:
  - 35
  - 35
- name: hasAgents
  sig: hasAgents()
  lines:
  - 39
  - 39
- name: pick
  sig: 'pick(name: string)'
  lines:
  - 41
  - 44
  detail:
    calls:
    - props.onSelect
    - setOpen
- name: focusItem
  sig: 'focusItem(idx: number)'
  lines:
  - 46
  - 52
  detail:
    calls:
    - Math.max
    - Math.min
    - items[clamped]?.focus
    - listRef?.querySelectorAll
    - setFocused
- name: onOpen
  sig: 'onOpen(val: boolean)'
  lines:
  - 54
  - 60
  detail:
    calls:
    - props.agents.findIndex
    - requestAnimationFrame
    - setOpen
- name: onKeyDown
  sig: 'onKeyDown(e: KeyboardEvent)'
  lines:
  - 62
  - 81
  detail:
    calls:
    - e.preventDefault
    - focusItem
    - focused
    - pick
- name: triggerLabel
  sig: triggerLabel()
  lines:
  - 83
  - 89
  detail:
    calls:
    - agent.name.charAt
    - agent.name.charAt(0).toUpperCase
    - agent.name.slice
    - props.agents.find
exports:
- name: ModeSwitcherBaseProps
  kind: type
- name: onTrigger
  kind: func
- name: hasAgents
  kind: func
- name: pick
  kind: func
- name: focusItem
  kind: func
- name: onOpen
  kind: func
- name: onKeyDown
  kind: func
- name: triggerLabel
  kind: func
- name: '[open, setOpen]'
  kind: data
- name: '[focused, setFocused]'
  kind: data
- name: listRef
  kind: data
- name: session
  kind: data
