## Roska File Descriptor — marketplace/utils.ts
file: marketplace/utils.ts
lines: 20
layer: logic
imports:
- sym: ../../types/marketplace.MarketplaceInstalledMetadata
  kind: typing
functions:
- name: isInstalled
  sig: |-
    isInstalled(
      id: string,
      type: string,
      metadata: MarketplaceInstalledMetadata,
    ): "project" | "global" | false
  lines:
  - 3
  - 9
  detail:
    calls:
    - installedScopes
- name: installedScopes
  sig: |-
    installedScopes(
      id: string,
      type: string,
      metadata: MarketplaceInstalledMetadata,
    ): ("project" | "global")[]
  lines:
  - 11
  - 20
  detail:
    calls:
    - scopes.push
exports:
- name: isInstalled
  kind: func
- name: installedScopes
  kind: func
