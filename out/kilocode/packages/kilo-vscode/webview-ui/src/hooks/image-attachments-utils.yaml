## Roska File Descriptor — hooks/image-attachments-utils.ts
file: hooks/image-attachments-utils.ts
lines: 15
data:
- name: ACCEPTED_IMAGE_TYPES
  type: array
  val: '["image/png", "image/jpeg", "image/gif", "image/webp"]'
functions:
- name: isAcceptedImageType
  sig: 'isAcceptedImageType(mimeType: string): boolean'
  lines:
  - 4
  - 6
  detail:
    calls:
    - ACCEPTED_IMAGE_TYPES.includes
- name: isDragLeavingComponent
  sig: 'isDragLeavingComponent(relatedTarget: EventTarget | null, currentTarget: HTMLElement): boolean'
  lines:
  - 12
  - 15
  detail:
    calls:
    - currentTarget.contains
exports:
- name: isAcceptedImageType
  kind: func
- name: isDragLeavingComponent
  kind: func
- name: ACCEPTED_IMAGE_TYPES
  kind: data
