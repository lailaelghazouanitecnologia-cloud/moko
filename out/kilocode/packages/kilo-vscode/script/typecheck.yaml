## Roska File Descriptor — script/typecheck.ts
file: script/typecheck.ts
lines: 38
layer: config
imports:
- sym: bun.$
data:
- name: args
  type: any
  val: process.argv.slice(2)
- name: projectIdx
  type: any
  val: args.indexOf("--project")
- name: project
  type: any
  val: 'projectIdx !== -1 ? args[projectIdx + 1] : undefined'
- name: webview
  type: any
  val: project?.includes("webview-ui")
- name: tscArgs
  type: any
  val: 'project ? ["--noEmit", "--project", project] : ["--noEmit"]'
- name: result
  type: any
  val: await $`tsc ${tscArgs}`.nothrow().quiet()
- name: output
  type: any
  val: result.stdout.toString() + result.stderr.toString()
- name: errors
  type: any
  val: |-
    output
      .split("\n")
      .filter((line) => line.includes("error TS"))
      .filter((line) => !line.startsWith(".."))
      .filt...
exports:
- name: args
  kind: data
- name: projectIdx
  kind: data
- name: project
  kind: data
- name: webview
  kind: data
- name: tscArgs
  kind: data
- name: result
  kind: data
- name: output
  kind: data
- name: errors
  kind: data
