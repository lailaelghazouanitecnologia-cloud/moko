## Roska File Descriptor — unit/tab-order.test.ts
file: unit/tab-order.test.ts
lines: 200
layer: test
imports:
- sym: bun:test.describe
- sym: bun:test.it
- sym: bun:test.expect
- sym: ../../webview-ui/agent-manager/tab-order.reorderTabs
- sym: ../../webview-ui/agent-manager/tab-order.applyTabOrder
- sym: ../../webview-ui/agent-manager/tab-order.firstOrderedTitle
functions:
- name: reconcile
  sig: 'reconcile(current: string[], stored: string[]): string[]'
  lines:
  - 150
  - 155
  detail:
    calls:
    - applyTabOrder
    - |-
      applyTabOrder(
          current.map((id) => ({ id })),
          stored,
        ).map
    - current.map
exports:
- name: reconcile
  kind: func
