## Roska File Descriptor — src/webview-html-utils.ts
file: src/webview-html-utils.ts
lines: 34
purpose: Build the Content-Security-Policy connect-src directive value. If a port is specified, restricts connections to that port. Otherwise, allows any localhost/127.0.0.1 port.
functions:
- name: buildConnectSrc
  sig: 'buildConnectSrc(port?: number): string'
  lines:
  - 6
  - 11
  detail: {}
- name: joinCspDirectives
  sig: 'joinCspDirectives(directives: string[]): string'
  lines:
  - 16
  - 18
  detail:
    calls:
    - directives.join
- name: buildCspString
  sig: 'buildCspString(cspSource: string, nonce: string, port?: number): string'
  lines:
  - 23
  - 34
  detail:
    calls:
    - buildConnectSrc
    - joinCspDirectives
exports:
- name: buildConnectSrc
  kind: func
- name: joinCspDirectives
  kind: func
- name: buildCspString
  kind: func
