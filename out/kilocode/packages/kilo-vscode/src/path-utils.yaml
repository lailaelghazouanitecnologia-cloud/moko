## Roska File Descriptor — src/path-utils.ts
file: src/path-utils.ts
lines: 27
purpose: Check whether a file path is absolute. Handles both Unix (`/foo/bar`) and Windows (`C:\foo`, `D:/bar`) conventions. UNC paths (`\\server\share`) are also treated as absolute. Returns false for relative paths, bare filenames, empty strings, and protocol-prefixed strings like `https://…`.
functions:
- name: isAbsolutePath
  sig: 'isAbsolutePath(filePath: string): boolean'
  lines:
  - 10
  - 27
  detail:
    calls:
    - filePath.charCodeAt
exports:
- name: isAbsolutePath
  kind: func
