## Roska File Descriptor — marketplace/paths.ts
file: marketplace/paths.ts
lines: 25
imports:
- sym: path.path
types:
- name: MarketplacePaths
  kind: struct
  ctx: 'Project-scope config file: <workspace>/.kilo/kilo.json'
  methods:
  - name: configPath
    sig: 'configPath(scope: "project" | "global", workspace?: string): string'
    lines:
    - 15
    - 18
    detail:
      calls:
      - globalConfigDir
      - path.join
  - name: skillsDir
    sig: 'skillsDir(scope: "project" | "global", workspace?: string): string'
    lines:
    - 21
    - 24
    detail:
      calls:
      - os.homedir
      - path.join
  lines:
  - 13
  - 25
functions:
- name: globalConfigDir
  sig: 'globalConfigDir(): string'
  lines:
  - 8
  - 11
  detail:
    calls:
    - os.homedir
    - path.join
exports:
- name: MarketplacePaths
  kind: type
- name: globalConfigDir
  kind: func
