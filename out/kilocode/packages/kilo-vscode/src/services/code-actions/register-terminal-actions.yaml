## Roska File Descriptor — code-actions/register-terminal-actions.ts
file: code-actions/register-terminal-actions.ts
lines: 109
layer: logic
imports:
- sym: vscode.vscode
- sym: ../../KiloProvider.KiloProvider
  kind: typing
- sym: ../../agent-manager/AgentManagerProvider.AgentManagerProvider
  kind: typing
- sym: ./support-prompt.createPrompt
functions:
- name: getTerminalContents
  sig: 'getTerminalContents(commands = -1): Promise<string>'
  is_async: true
  lines:
  - 11
  - 50
  detail:
    calls:
    - (await vscode.env.clipboard.readText()).trim
    - Math.max
    - content.split
    - lines.pop
    - lines.pop()?.trim
    - lines.slice
    - lines.slice(Math.max(i, 0)).join
    - lines[i].trim
    - lines[i].trim().startsWith
    - vscode.commands.executeCommand
    - vscode.env.clipboard.readText
    - vscode.env.clipboard.writeText
- name: registerTerminalActions
  sig: |-
    registerTerminalActions(
      context: vscode.ExtensionContext,
      provider: KiloProvider,
      agentManager?: AgentManagerProvider,
    ): void
  lines:
  - 52
  - 109
  detail:
    calls:
    - context.subscriptions.push
    - vscode.commands.registerCommand
exports:
- name: getTerminalContents
  kind: func
- name: registerTerminalActions
  kind: func
