## Roska File Descriptor — cli-backend/server-utils.ts
file: cli-backend/server-utils.ts
lines: 10
layer: logic
purpose: 'Parse the port number from CLI server startup output. Matches lines like: "kilo server listening on http://127.0.0.1:12345" Returns the port number or null if not found.'
functions:
- name: parseServerPort
  sig: 'parseServerPort(output: string): number | null'
  lines:
  - 6
  - 10
  detail:
    calls:
    - output.match
    - parseInt
exports:
- name: parseServerPort
  kind: func
