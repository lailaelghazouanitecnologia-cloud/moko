## Roska File Descriptor — fetch/stream.ts
file: fetch/stream.ts
lines: 138
layer: logic
purpose: Parses a single line from an SSE (Server-Sent Events) stream.
functions:
- name: parseSseLine
  sig: 'parseSseLine(line: string): { done: boolean; data: unknown }'
  lines:
  - 4
  - 20
  detail:
    calls:
    - JSON.parse
    - line.slice
    - line.slice(5).trim
    - line.startsWith
exports:
- name: parseSseLine
  kind: func
