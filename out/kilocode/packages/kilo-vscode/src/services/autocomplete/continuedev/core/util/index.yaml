## Roska File Descriptor — util/index.ts
file: util/index.ts
lines: 51
layer: logic
functions:
- name: dedent
  sig: 'dedent(strings: TemplateStringsArray, ...values: unknown[])'
  lines:
  - 1
  - 51
  detail:
    calls:
    - String
    - lastLine.match
    - lines.join
    - lines.map
    - lines.pop
    - lines.reduce
    - lines.shift
    - lines[0].trim
    - lines[lines.length - 1].trim
    - raw.replace
    - raw.replace(/^\n/, "").replace
    - result.split
    - strings[i].split
    - value.includes
    - value.split
    - valueLines.join
    - valueLines.map
exports:
- name: dedent
  kind: func
