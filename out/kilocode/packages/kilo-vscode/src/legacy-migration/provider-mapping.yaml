## Roska File Descriptor — legacy-migration/provider-mapping.ts
file: legacy-migration/provider-mapping.ts
lines: 302
layer: logic
purpose: legacy-migration - Maps legacy apiProvider values to new provider IDs and key fields. The legacy extension used flat per-provider key names (e.g. apiKey, openRouterApiKey). The new CLI backend uses a per-provider auth endpoint (PUT /auth/:providerId).
data:
- name: PROVIDER_MAP
  type: Record<string, ProviderMapping>
  val: |-
    {
      anthropic: {
        id: "anthropic",
        key: "apiKey",
        name: "Anthropic",
      },
      openrouter: {
        id: "openrouter...
- name: UNSUPPORTED_PROVIDERS
  type: any
  val: |-
    new Set([
      "fake-ai",
      "human-relay",
      "vscode-lm",
      "claude-code",
      "qwen-code",
      "virtual-quota-fallback",
      "gl...
- name: DEFAULT_MODE_SLUGS
  type: any
  val: new Set(["code", "build", "architect", "ask", "debug", "orchestrator", "review"])
types:
- name: ProviderMapping
  kind: trait
  decorators:
  - interface
  fields:
  - name: id
    type: string
  - name: key
    type: string
  - name: modelField
    type: string
  - name: urlField
    type: string
  - name: organizationIdField
    type: string
  - name: oauthSecretKey
    type: string
  - name: skipAuth
    type: boolean
  - name: configFields
    type: 'Array<{ from: string; option: string }>'
  lines:
  - 8
  - 27
exports:
- name: ProviderMapping
  kind: type
- name: PROVIDER_MAP
  kind: data
- name: UNSUPPORTED_PROVIDERS
  kind: data
- name: DEFAULT_MODE_SLUGS
  kind: data
