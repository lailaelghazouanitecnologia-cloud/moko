## Roska Module Descriptor — kilocode/packages/kilo-vscode/src/legacy-migration
name: legacy-migration
total_lines: 1699
files:
- file: legacy-migration/legacy-types.ts
  lines: 330
  purpose: 'legacy-migration - Types for legacy Kilo Code extension (v5.x) data structures. These types represent the shapes stored in VS Code SecretStorage and on disk by the legacy extension (kilocode.kilo-code v5.x, a Roo Code fork). They are intentionally loose (allowing [key: string]: unknown) to tolerate schema drift between legacy versions.'
  types: 16
  functions: 0
  imports: 0
  exports: 16
- file: legacy-migration/migration-service.ts
  lines: 1067
  purpose: legacy-migration - Core migration service. Reads legacy Kilo Code v5.x data from VS Code SecretStorage and the extension's global storage directory, then writes it to the new CLI backend via the SDK.
  types: 2
  functions: 26
  imports: 25
  exports: 34
- file: legacy-migration/provider-mapping.ts
  lines: 302
  purpose: legacy-migration - Maps legacy apiProvider values to new provider IDs and key fields. The legacy extension used flat per-provider key names (e.g. apiKey, openRouterApiKey). The new CLI backend uses a per-provider auth endpoint (PUT /auth/:providerId).
  types: 1
  functions: 0
  imports: 0
  exports: 4
