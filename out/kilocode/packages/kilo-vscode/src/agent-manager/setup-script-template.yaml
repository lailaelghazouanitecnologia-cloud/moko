## Roska File Descriptor — agent-manager/setup-script-template.ts
file: agent-manager/setup-script-template.ts
lines: 82
purpose: Default POSIX template for worktree setup scripts.
data:
- name: SETUP_SCRIPT_TEMPLATE
  type: string
  val: |-
    `#!/bin/sh
    # Kilo Code Worktree Setup Script
    # This script runs before the agent starts in a worktree (new sessions only...
- name: SETUP_SCRIPT_TEMPLATE_POWERSHELL
  type: string
  val: |-
    `# Kilo Code Worktree Setup Script
    # This script runs before the agent starts in a worktree (new sessions only).
    #
    # Ava...
exports:
- name: SETUP_SCRIPT_TEMPLATE
  kind: data
- name: SETUP_SCRIPT_TEMPLATE_POWERSHELL
  kind: data
