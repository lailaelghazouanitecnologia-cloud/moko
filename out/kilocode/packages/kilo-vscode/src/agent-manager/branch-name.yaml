## Roska File Descriptor — agent-manager/branch-name.ts
file: agent-manager/branch-name.ts
lines: 69
layer: logic
imports:
- sym: friendly-words.friendlyWords
data:
- name: MAX_ATTEMPTS
  type: number
  val: '10'
- name: FALLBACK_MAX_SUFFIX
  type: number
  val: '100'
functions:
- name: sanitizeBranchName
  sig: 'sanitizeBranchName(name: string, maxLength = 50): string'
  lines:
  - 10
  - 17
  detail:
    calls:
    - |-
      name
          .slice
    - |-
      name
          .slice(0, maxLength)
          .toLowerCase
    - |-
      name
          .slice(0, maxLength)
          .toLowerCase()
          .replace
    - |-
      name
          .slice(0, maxLength)
          .toLowerCase()
          .replace(/[^a-z0-9]+/g, "-")
          .replace
    - |-
      name
          .slice(0, maxLength)
          .toLowerCase()
          .replace(/[^a-z0-9]+/g, "-")
          .replace(/^-+|...
- name: generateBranchName
  sig: 'generateBranchName(_prompt: string, existingBranches: string[] = []): string'
  lines:
  - 24
  - 50
  detail:
    calls:
    - Date.now
    - existing.has
    - existingBranches.map
    - new Set
    - random
- name: versionedName
  sig: |-
    versionedName(
      base: string | undefined,
      index: number,
      total: number,
    ): { branch: string | undefined; label: string | undefined }
  lines:
  - 56
  - 69
  detail: {}
exports:
- name: sanitizeBranchName
  kind: func
- name: generateBranchName
  kind: func
- name: versionedName
  kind: func
- name: MAX_ATTEMPTS
  kind: data
- name: FALLBACK_MAX_SUFFIX
  kind: data
