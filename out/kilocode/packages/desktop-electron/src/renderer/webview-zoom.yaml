## Roska File Descriptor — renderer/webview-zoom.ts
file: renderer/webview-zoom.ts
lines: 38
layer: ui
purpose: Copyright 2019-2024 Tauri Programme within The Commons Conservancy
imports:
- sym: solid-js.createSignal
data:
- name: OS_NAME
  type: any
  val: |-
    (() => {
      if (navigator.userAgent.includes("Mac")) return "macos"
      if (navigator.userAgent.includes("Windows")) return...
- name: '[webviewZoom, setWebviewZoom]'
  type: any
  val: createSignal(1)
- name: MAX_ZOOM_LEVEL
  type: number
  val: '10'
- name: MIN_ZOOM_LEVEL
  type: number
  val: '0.2'
functions:
- name: clamp
  sig: 'clamp(value: number)'
  lines:
  - 19
  - 19
  detail:
    calls:
    - Math.max
- name: applyZoom
  sig: 'applyZoom(next: number)'
  lines:
  - 21
  - 24
  detail:
    calls:
    - setWebviewZoom
    - window.api.setZoomFactor
exports:
- name: clamp
  kind: func
- name: applyZoom
  kind: func
- name: OS_NAME
  kind: data
- name: '[webviewZoom, setWebviewZoom]'
  kind: data
- name: MAX_ZOOM_LEVEL
  kind: data
- name: MIN_ZOOM_LEVEL
  kind: data
