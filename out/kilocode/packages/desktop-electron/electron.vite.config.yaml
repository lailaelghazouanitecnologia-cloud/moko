## Roska File Descriptor — desktop-electron/electron.vite.config.ts
file: desktop-electron/electron.vite.config.ts
lines: 41
imports:
- sym: electron-vite.defineConfig
- sym: '@opencode-ai/app/vite.appPlugin'
data:
- name: channel
  type: any
  val: |-
    (() => {
      const raw = process.env.OPENCODE_CHANNEL
      if (raw === "dev" || raw === "beta" || raw === "prod") return raw
    ...
exports:
- name: channel
  kind: data
