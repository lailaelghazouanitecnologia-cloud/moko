## Roska File Descriptor — terminal/terminal-tabs.spec.ts
file: terminal/terminal-tabs.spec.ts
lines: 139
layer: test
imports:
- sym: '@playwright/test.Page'
  kind: typing
- sym: ../fixtures.test
- sym: ../fixtures.expect
- sym: ../selectors.terminalSelector
- sym: ../utils.terminalToggleKey
- sym: ../utils.workspacePersistKey
types:
- name: State
  kind: typealias
  decorators:
  - type
  fields:
  - name: active
    type: string
  - name: all
    type: |-
      Array<{
          id: string
          title: string
          titleNumber: number
          buffer?: string
        }>
  lines:
  - 6
  - 14
functions:
- name: open
  sig: 'open(page: Page)'
  is_async: true
  lines:
  - 16
  - 22
  detail:
    calls:
    - expect
    - expect(terminal).toBeVisible
    - expect(terminal.locator("textarea")).toHaveCount
    - page.keyboard.press
    - page.locator
    - terminal.isVisible
    - terminal.isVisible().catch
    - terminal.locator
- name: run
  sig: 'run(page: Page, cmd: string)'
  is_async: true
  lines:
  - 24
  - 30
  detail:
    calls:
    - expect
    - expect(terminal).toBeVisible
    - page.keyboard.press
    - page.keyboard.type
    - page.locator
    - terminal.click
- name: store
  sig: 'store(page: Page, key: string)'
  is_async: true
  lines:
  - 32
  - 45
  detail:
    calls:
    - page.evaluate
exports:
- name: State
  kind: type
- name: open
  kind: func
- name: run
  kind: func
- name: store
  kind: func
