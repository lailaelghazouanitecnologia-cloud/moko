## Roska File Descriptor — components/dialog-select-mcp.tsx
file: components/dialog-select-mcp.tsx
lines: 103
imports:
- sym: solid-js.Component
- sym: solid-js.createMemo
- sym: solid-js.createSignal
- sym: solid-js.Show
- sym: '@/context/sync.useSync'
- sym: '@/context/sdk.useSDK'
- sym: '@opencode-ai/ui/dialog.Dialog'
- sym: '@opencode-ai/ui/list.List'
- sym: '@opencode-ai/ui/switch.Switch'
- sym: '@/context/language.useLanguage'
data:
- name: statusLabels
  type: object
  val: |-
    {
      connected: "mcp.status.connected",
      failed: "mcp.status.failed",
      needs_auth: "mcp.status.needs_auth",
      disabled:...
- name: sync
  type: any
  val: useSync()
- name: sdk
  type: any
  val: useSDK()
- name: language
  type: any
  val: useLanguage()
- name: '[loading, setLoading]'
  type: any
  val: createSignal<string | null>(null)
- name: items
  type: any
  val: |-
    createMemo(() =>
        Object.entries(sync.data.mcp ?? {})
          .map(([name, status]) => ({ name, status: status.status }...
- name: enabledCount
  type: any
  val: createMemo(() => items().filter((i) => i.status === "connected").length)
- name: totalCount
  type: any
  val: createMemo(() => items().length)
functions:
- name: toggle
  sig: 'toggle(name: string)'
  is_async: true
  lines:
  - 28
  - 44
  detail:
    calls:
    - loading
    - sdk.client.mcp.connect
    - sdk.client.mcp.disconnect
    - sdk.client.mcp.status
    - setLoading
    - sync.set
exports:
- name: toggle
  kind: func
- name: statusLabels
  kind: data
- name: sync
  kind: data
- name: sdk
  kind: data
- name: language
  kind: data
- name: '[loading, setLoading]'
  kind: data
- name: items
  kind: data
- name: enabledCount
  kind: data
- name: totalCount
  kind: data
