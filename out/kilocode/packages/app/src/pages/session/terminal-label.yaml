## Roska File Descriptor — session/terminal-label.ts
file: session/terminal-label.ts
lines: 16
functions:
- name: terminalTabLabel
  sig: |-
    terminalTabLabel(input: {
      title?: string
      titleNumber?: number
      t: (key: string, vars?: Record<string, string | number | boolean>) => string
    })
  lines:
  - 1
  - 16
  detail:
    calls:
    - Number
    - Number.isFinite
    - input.t
    - title.match
exports:
- name: terminalTabLabel
  kind: func
