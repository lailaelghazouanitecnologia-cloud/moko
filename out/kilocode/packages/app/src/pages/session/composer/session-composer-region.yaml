## Roska File Descriptor — composer/session-composer-region.tsx
file: composer/session-composer-region.tsx
lines: 238
layer: logic
imports:
- sym: solid-js.Show
- sym: solid-js.createEffect
- sym: solid-js.createMemo
- sym: solid-js.createSignal
- sym: solid-js.onCleanup
- sym: solid-js/store.createStore
- sym: '@solidjs/router.useParams'
- sym: '@opencode-ai/ui/motion-spring.useSpring'
- sym: '@/components/prompt-input.PromptInput'
- sym: '@/context/language.useLanguage'
- sym: '@/context/prompt.usePrompt'
- sym: '@/pages/session/handoff.getSessionHandoff'
- sym: '@/pages/session/handoff.setSessionHandoff'
- sym: '@/pages/session/composer/session-permission-dock.SessionPermissionDock'
- sym: '@/pages/session/composer/session-question-dock.SessionQuestionDock'
- sym: '@/pages/session/composer/session-composer-state.SessionComposerState'
  kind: typing
- sym: '@/pages/session/composer/session-todo-dock.SessionTodoDock'
data:
- name: params
  type: any
  val: useParams()
- name: prompt
  type: any
  val: usePrompt()
- name: language
  type: any
  val: useLanguage()
- name: sessionKey
  type: any
  val: 'createMemo(() => `${params.dir}${params.id ? "/" + params.id : ""}`)'
- name: handoffPrompt
  type: any
  val: createMemo(() => getSessionHandoff(sessionKey())?.prompt)
- name: '[gate, setGate]'
  type: any
  val: |-
    createStore({
        ready: false,
      })
- name: timer
  type: number | undefined
- name: frame
  type: number | undefined
- name: open
  type: any
  val: createMemo(() => gate.ready && props.state.dock() && !props.state.closing())
- name: config
  type: any
  val: |-
    createMemo(() =>
        open()
          ? {
              visualDuration: props.dockOpenVisualDuration ?? props.visualDuration ?? ...
- name: progress
  type: any
  val: 'useSpring(() => (open() ? 1 : 0), config)'
- name: value
  type: any
  val: createMemo(() => Math.max(0, Math.min(1, progress())))
- name: '[height, setHeight]'
  type: any
  val: createSignal(320)
- name: dock
  type: any
  val: createMemo(() => (gate.ready && props.state.dock()) || value() > 0.001)
- name: full
  type: any
  val: createMemo(() => Math.max(78, height()))
- name: '[contentRef, setContentRef]'
  type: any
  val: createSignal<HTMLDivElement>()
functions:
- name: previewPrompt
  sig: previewPrompt()
  lines:
  - 50
  - 60
  detail:
    calls:
    - |-
      prompt
            .current
    - |-
      prompt
            .current()
            .map
    - |-
      prompt
            .current()
            .map((part) => {
              if (part.type === "file") return `[file:${par...
- name: clear
  sig: clear()
  lines:
  - 73
  - 82
  detail:
    calls:
    - cancelAnimationFrame
    - window.clearTimeout
exports:
- name: previewPrompt
  kind: func
- name: clear
  kind: func
- name: params
  kind: data
- name: prompt
  kind: data
- name: language
  kind: data
- name: sessionKey
  kind: data
- name: handoffPrompt
  kind: data
- name: '[gate, setGate]'
  kind: data
- name: timer
  kind: data
- name: frame
  kind: data
- name: open
  kind: data
- name: config
  kind: data
- name: progress
  kind: data
- name: value
  kind: data
- name: '[height, setHeight]'
  kind: data
- name: dock
  kind: data
- name: full
  kind: data
- name: '[contentRef, setContentRef]'
  kind: data
