## Roska File Descriptor — composer/session-request-tree.ts
file: composer/session-request-tree.ts
lines: 52
layer: logic
imports:
- sym: '@kilocode/sdk/v2/client.PermissionRequest'
  kind: typing
- sym: '@kilocode/sdk/v2/client.QuestionRequest'
  kind: typing
- sym: '@kilocode/sdk/v2/client.Session'
  kind: typing
functions:
- name: sessionTreeRequest
  sig: |-
    sessionTreeRequest(
      session: Session[],
      request: Record<string, T[] | undefined>,
      sessionID?: string,
      include: (item: T) => boolean = () => true,
    )
  lines:
  - 3
  - 34
  detail:
    calls:
    - ids.find
    - ids.push
    - map.get
    - new Map
    - new Set
    - request[id]?.find
    - seen.add
    - seen.has
    - session.reduce
- name: sessionPermissionRequest
  sig: |-
    sessionPermissionRequest(
      session: Session[],
      request: Record<string, PermissionRequest[] | undefined>,
      sessionID?: string,
      include?: (item: PermissionRequest) => boolean,
    )
  lines:
  - 36
  - 43
  detail:
    calls:
    - sessionTreeRequest
- name: sessionQuestionRequest
  sig: |-
    sessionQuestionRequest(
      session: Session[],
      request: Record<string, QuestionRequest[] | undefined>,
      sessionID?: string,
      include?: (item: QuestionRequest) => boolean,
    )
  lines:
  - 45
  - 52
  detail:
    calls:
    - sessionTreeRequest
exports:
- name: sessionTreeRequest
  kind: func
- name: sessionPermissionRequest
  kind: func
- name: sessionQuestionRequest
  kind: func
