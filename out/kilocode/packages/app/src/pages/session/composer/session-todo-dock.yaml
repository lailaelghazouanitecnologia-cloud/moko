## Roska File Descriptor — composer/session-todo-dock.tsx
file: composer/session-todo-dock.tsx
lines: 316
imports:
- sym: '@kilocode/sdk/v2.Todo'
  kind: typing
- sym: '@opencode-ai/ui/animated-number.AnimatedNumber'
- sym: '@opencode-ai/ui/checkbox.Checkbox'
- sym: '@opencode-ai/ui/dock-surface.DockTray'
- sym: '@opencode-ai/ui/icon-button.IconButton'
- sym: '@opencode-ai/ui/motion-spring.useSpring'
- sym: '@opencode-ai/ui/text-reveal.TextReveal'
- sym: '@opencode-ai/ui/text-strikethrough.TextStrikethrough'
- sym: solid-js.Index
- sym: solid-js.createEffect
- sym: solid-js.createMemo
- sym: solid-js.createSignal
- sym: solid-js.on
- sym: solid-js.onCleanup
- sym: solid-js/store.createStore
data:
- name: '[store, setStore]'
  type: any
  val: |-
    createStore({
        collapsed: false,
      })
- name: total
  type: any
  val: createMemo(() => props.todos.length)
- name: done
  type: any
  val: createMemo(() => props.todos.filter((todo) => todo.status === "completed").length)
- name: label
  type: any
  val: createMemo(() => `${done()} of ${total()} ${props.title.toLowerCase()} completed`)
- name: active
  type: any
  val: |-
    createMemo(
        () =>
          props.todos.find((todo) => todo.status === "in_progress") ??
          props.todos.find((todo) =...
- name: preview
  type: any
  val: createMemo(() => active()?.content ?? "")
- name: config
  type: any
  val: |-
    createMemo(() =>
        store.collapsed
          ? {
              visualDuration: props.collapseVisualDuration ?? props.visualDur...
- name: collapse
  type: any
  val: 'useSpring(() => (store.collapsed ? 1 : 0), config)'
- name: dock
  type: any
  val: createMemo(() => Math.max(0, Math.min(1, props.dockProgress ?? 1)))
- name: shut
  type: any
  val: createMemo(() => 1 - dock())
- name: value
  type: any
  val: createMemo(() => Math.max(0, Math.min(1, collapse())))
- name: hide
  type: any
  val: createMemo(() => Math.max(value(), shut()))
- name: off
  type: any
  val: createMemo(() => hide() > 0.98)
- name: turn
  type: any
  val: createMemo(() => Math.max(0, Math.min(1, value())))
- name: '[height, setHeight]'
  type: any
  val: createSignal(320)
- name: full
  type: any
  val: createMemo(() => Math.max(78, height()))
- name: contentRef
  type: HTMLDivElement | undefined
- name: '[stuck, setStuck]'
  type: any
  val: createSignal(false)
- name: '[scrolling, setScrolling]'
  type: any
  val: createSignal(false)
- name: scrollRef
  type: HTMLDivElement
- name: timer
  type: number | undefined
- name: inProgress
  type: any
  val: createMemo(() => props.todos.findIndex((todo) => todo.status === "in_progress"))
functions:
- name: dot
  sig: 'dot(status: Todo["status"])'
  lines:
  - 12
  - 35
  detail: {}
- name: toggle
  sig: toggle()
  lines:
  - 61
  - 61
- name: ensure
  sig: ensure()
  lines:
  - 217
  - 241
  detail:
    calls:
    - Math.max
    - el.getBoundingClientRect
    - scrollRef.getBoundingClientRect
    - scrollRef.querySelector
    - scrolling
    - setStuck
exports:
- name: dot
  kind: func
- name: toggle
  kind: func
- name: ensure
  kind: func
- name: '[store, setStore]'
  kind: data
- name: total
  kind: data
- name: done
  kind: data
- name: label
  kind: data
- name: active
  kind: data
- name: preview
  kind: data
- name: config
  kind: data
- name: collapse
  kind: data
- name: dock
  kind: data
- name: shut
  kind: data
- name: value
  kind: data
- name: hide
  kind: data
- name: off
  kind: data
- name: turn
  kind: data
- name: '[height, setHeight]'
  kind: data
- name: full
  kind: data
- name: contentRef
  kind: data
- name: '[stuck, setStuck]'
  kind: data
- name: '[scrolling, setScrolling]'
  kind: data
- name: scrollRef
  kind: data
- name: timer
  kind: data
- name: inProgress
  kind: data
