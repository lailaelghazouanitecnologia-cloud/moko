## Roska File Descriptor — file/watcher.ts
file: file/watcher.ts
lines: 53
layer: logic
imports:
- sym: '@kilocode/sdk/v2.FileNode'
  kind: typing
types:
- name: WatcherEvent
  kind: typealias
  decorators:
  - type
  fields:
  - name: type
    type: string
  - name: properties
    type: unknown
  lines:
  - 3
  - 6
- name: WatcherOps
  kind: typealias
  decorators:
  - type
  fields:
  - name: normalize
    type: '(input: string) => string'
  - name: hasFile
    type: '(path: string) => boolean'
  - name: isOpen
    type: '(path: string) => boolean'
  - name: loadFile
    type: '(path: string) => void'
  - name: node
    type: '(path: string) => FileNode | undefined'
  - name: isDirLoaded
    type: '(path: string) => boolean'
  - name: refreshDir
    type: '(path: string) => void'
  lines:
  - 8
  - 16
functions:
- name: invalidateFromWatcher
  sig: 'invalidateFromWatcher(event: WatcherEvent, ops: WatcherOps)'
  lines:
  - 18
  - 53
  detail:
    calls:
    - |-
      (() => {
            if (path === "") return ""
            const node = ops.node(path)
            if (node?.type !==...
    - ops.hasFile
    - ops.isDirLoaded
    - ops.isOpen
    - ops.loadFile
    - ops.normalize
    - ops.refreshDir
    - path.split
    - path.split("/").slice
    - path.split("/").slice(0, -1).join
    - path.startsWith
exports:
- name: WatcherEvent
  kind: type
- name: WatcherOps
  kind: type
- name: invalidateFromWatcher
  kind: func
