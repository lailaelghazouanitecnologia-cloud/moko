## Roska File Descriptor — global-sync/event-reducer.ts
file: global-sync/event-reducer.ts
lines: 356
imports:
- sym: '@opencode-ai/util/binary.Binary'
- sym: solid-js/store.produce
- sym: solid-js/store.reconcile
- sym: solid-js/store.type SetStoreFunction
- sym: solid-js/store.type Store
- sym: '@kilocode/sdk/v2/client.FileDiff'
  kind: typing
- sym: '@kilocode/sdk/v2/client.Message'
  kind: typing
- sym: '@kilocode/sdk/v2/client.Part'
  kind: typing
- sym: '@kilocode/sdk/v2/client.PermissionRequest'
  kind: typing
- sym: '@kilocode/sdk/v2/client.Project'
  kind: typing
- sym: '@kilocode/sdk/v2/client.QuestionRequest'
  kind: typing
- sym: '@kilocode/sdk/v2/client.Session'
  kind: typing
- sym: '@kilocode/sdk/v2/client.SessionStatus'
  kind: typing
- sym: '@kilocode/sdk/v2/client.Todo'
  kind: typing
- sym: ./types.State
  kind: typing
- sym: ./types.VcsCache
  kind: typing
- sym: ./session-trim.trimSessions
- sym: ./session-cache.dropSessionCaches
functions:
- name: applyGlobalEvent
  sig: |-
    applyGlobalEvent(input: {
      event: { type: string; properties?: unknown }
      project: Project[]
      setGlobalProject: (next: Project[] | ((draft: Project[]) => void)) => void
      refresh: () => void
    })
  lines:
  - 18
  - 41
  detail:
    calls:
    - Binary.search
    - input.refresh
    - input.setGlobalProject
- name: cleanupSessionCaches
  sig: |-
    cleanupSessionCaches(
      setStore: SetStoreFunction<State>,
      sessionID: string,
      setSessionTodo?: (sessionID: string, todos: Todo[] | undefined) => void,
    )
  lines:
  - 43
  - 55
  detail:
    calls:
    - produce
    - setSessionTodo
    - setStore
- name: cleanupDroppedSessionCaches
  sig: |-
    cleanupDroppedSessionCaches(
      store: Store<State>,
      setStore: SetStoreFunction<State>,
      next: Session[],
      setSessionTodo?: (sessionID: string, todos: Todo[] | undefined) => void,
    )
  lines:
  - 57
  - 84
  detail:
    calls:
    - Object.keys
    - Object.values
    - |-
      Object.values(store.part)
            .map
    - |-
      Object.values(store.part)
            .map((parts) => parts?.find((part) => !!part?.sessionID)?.sessionID)...
    - |-
      [
          ...Object.keys(store.message),
          ...Object.keys(store.session_diff),
          ...Object.keys(stor...
    - new Set
    - next.map
    - produce
    - setSessionTodo
    - setStore
- name: applyDirectoryEvent
  sig: |-
    applyDirectoryEvent(input: {
      event: { type: string; properties?: unknown }
      store: Store<State>
      setStore: SetStoreFunction<State>
      push: (directory: string) => void
      directory: string
      loadLsp: () => void
      vcsCache?: VcsCache
      setSessionTodo?: (sessionID: string, todos: Todo[] | undefined) => void
    })
  lines:
  - 86
  - 356
  detail:
    calls:
    - Binary.search
    - cleanupDroppedSessionCaches
    - cleanupSessionCaches
    - input.loadLsp
    - input.push
    - input.setSessionTodo
    - input.setStore
    - input.store.session.slice
    - input.vcsCache.setStore
    - next.splice
    - produce
    - reconcile
    - trimSessions
exports:
- name: applyGlobalEvent
  kind: func
- name: cleanupSessionCaches
  kind: func
- name: cleanupDroppedSessionCaches
  kind: func
- name: applyDirectoryEvent
  kind: func
