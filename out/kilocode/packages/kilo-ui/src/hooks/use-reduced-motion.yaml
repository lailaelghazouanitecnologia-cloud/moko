## Roska File Descriptor — hooks/use-reduced-motion.ts
file: hooks/use-reduced-motion.ts
lines: 10
imports:
- sym: '@solid-primitives/lifecycle.isHydrated'
- sym: '@solid-primitives/media.createMediaQuery'
- sym: '@solid-primitives/rootless.createHydratableSingletonRoot'
data:
- name: query
  type: string
  val: '"(prefers-reduced-motion: reduce)"'
- name: useReducedMotion
  type: any
  val: |-
    createHydratableSingletonRoot(() => {
      const value = createMediaQuery(query)
      return () => !isHydrated() || value()
    })
exports:
- name: query
  kind: data
- name: useReducedMotion
  kind: data
