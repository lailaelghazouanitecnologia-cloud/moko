## Roska File Descriptor — components/session-review-search.ts
file: components/session-review-search.ts
lines: 59
types:
- name: SessionSearchHit
  kind: typealias
  decorators:
  - type
  fields:
  - name: file
    type: string
  - name: side
    type: '"additions" | "deletions"'
  - name: line
    type: number
  - name: col
    type: number
  - name: len
    type: number
  lines:
  - 1
  - 7
- name: SessionSearchFile
  kind: typealias
  decorators:
  - type
  fields:
  - name: file
    type: string
  - name: before
    type: string
  - name: after
    type: string
  lines:
  - 9
  - 13
functions:
- name: hitsForSide
  sig: 'hitsForSide(args: { file: string; side: SessionSearchHit["side"]; text: string; needle: string })'
  lines:
  - 15
  - 37
  detail:
    calls:
    - args.text.split
    - args.text.split("\n").flatMap
- name: buildSessionSearchHits
  sig: 'buildSessionSearchHits(args: { query: string; files: SessionSearchFile[] })'
  lines:
  - 39
  - 53
  detail:
    calls:
    - args.files.flatMap
    - args.query.trim
    - args.query.trim().toLowerCase
- name: stepSessionSearchIndex
  sig: 'stepSessionSearchIndex(total: number, current: number, dir: 1 | -1)'
  lines:
  - 55
  - 59
  detail: {}
exports:
- name: SessionSearchHit
  kind: type
- name: SessionSearchFile
  kind: type
- name: hitsForSide
  kind: func
- name: buildSessionSearchHits
  kind: func
- name: stepSessionSearchIndex
  kind: func
