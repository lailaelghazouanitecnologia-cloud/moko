## Roska File Descriptor — components/avatar.tsx
file: components/avatar.tsx
lines: 55
layer: ui
imports:
- sym: solid-js.type ComponentProps
- sym: solid-js.splitProps
- sym: solid-js.Show
data:
- name: segmenter
  type: any
  val: |-
    typeof Intl !== "undefined" && "Segmenter" in Intl
        ? new Intl.Segmenter(undefined, { granularity: "grapheme" })
        ...
- name: '[split, rest]'
  type: any
  val: |-
    splitProps(props, [
        "fallback",
        "src",
        "background",
        "foreground",
        "size",
        "class",
        "classLi...
- name: src
  type: any
  val: split.src
types:
- name: AvatarProps
  kind: trait
  bases:
  - ComponentProps<"div">
  decorators:
  - interface
  fields:
  - name: fallback
    type: string
  - name: src
    type: string
  - name: background
    type: string
  - name: foreground
    type: string
  - name: size
    type: '"small" | "normal" | "large"'
  lines:
  - 14
  - 20
functions:
- name: first
  sig: 'first(value: string)'
  lines:
  - 8
  - 12
  detail:
    calls:
    - Array.from
    - segmenter.segment
    - segmenter.segment(value)[Symbol.iterator]
    - segmenter.segment(value)[Symbol.iterator]().next
exports:
- name: AvatarProps
  kind: type
- name: first
  kind: func
- name: segmenter
  kind: data
- name: '[split, rest]'
  kind: data
- name: src
  kind: data
