## Roska File Descriptor — components/tool-count-label.tsx
file: components/tool-count-label.tsx
lines: 58
layer: ui
imports:
- sym: solid-js.createMemo
- sym: ./animated-number.AnimatedNumber
functions:
- name: split
  sig: 'split(text: string)'
  lines:
  - 4
  - 12
  detail:
    calls:
    - /{{\s*count\s*}}/.exec
    - text.slice
- name: common
  sig: 'common(one: string, other: string)'
  lines:
  - 14
  - 24
  detail:
    calls:
    - Array.from
    - a.slice
    - a.slice(0, i).join
    - a.slice(i).join
    - b.slice
    - b.slice(i).join
- name: AnimatedCountLabel
  sig: 'AnimatedCountLabel(props: { count: number; one: string; other: string; class?: string })'
  lines:
  - 26
  - 58
  detail:
    calls:
    - createMemo
exports:
- name: split
  kind: func
- name: common
  kind: func
- name: AnimatedCountLabel
  kind: func
