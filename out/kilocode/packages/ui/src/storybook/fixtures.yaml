## Roska File Descriptor — storybook/fixtures.ts
file: storybook/fixtures.ts
lines: 51
layer: ui
data:
- name: diff
  type: object
  val: |-
    {
      before: {
        name: "src/greet.ts",
        contents: `export function greet(name: string) {
      return \`Hello, \${name}!...
- name: code
  type: object
  val: |-
    {
      name: "src/calc.ts",
      contents: `export function sum(values: number[]) {
      return values.reduce((total, value) => t...
- name: markdown
  type: array
  val: |-
    [
      "# Markdown",
      "",
      "Use **Markdown** for rich text.",
      "",
      "## Highlights",
      "- Headings, lists, and code blo...
- name: changes
  type: object
  val: |-
    {
      additions: 18,
      deletions: 6,
    }
exports:
- name: diff
  kind: data
- name: code
  kind: data
- name: markdown
  kind: data
- name: changes
  kind: data
