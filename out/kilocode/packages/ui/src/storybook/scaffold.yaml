## Roska File Descriptor — storybook/scaffold.tsx
file: storybook/scaffold.tsx
lines: 62
imports:
- sym: solid-js.ErrorBoundary
- sym: solid-js.type ValidComponent
- sym: solid-js/web.Dynamic
data:
- name: component
  type: any
  val: pick(input.mod, input.name) as unknown as ValidComponent
functions:
- name: fn
  sig: 'fn(value: unknown)'
  lines:
  - 4
  - 6
- name: pick
  sig: 'pick(mod: Record<string, unknown>, name?: string)'
  lines:
  - 8
  - 28
  detail:
    calls:
    - Object.keys
    - |-
      Object.keys(mod)
          .filter
    - |-
      Object.keys(mod)
          .filter((k) => k[0] && k[0] === k[0].toUpperCase())
          .find
    - Object.keys(mod).find
    - fn
exports:
- name: fn
  kind: func
- name: pick
  kind: func
- name: component
  kind: data
