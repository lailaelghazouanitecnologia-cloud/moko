## Roska File Descriptor — 10_human_in_the_loop/mixed_external_and_regular_tools.py
file: 10_human_in_the_loop/mixed_external_and_regular_tools.py
lines: 75
layer: logic
purpose: 'Mixed External and Regular Tools ============================= Human-in-the-Loop: Mix external_execution tools with regular tools in the same agent. When an agent has both external_execution tools (paused for human execution) and regular tools (executed automatically), the agent will: 1. Execute regular tools automatically 2. Pause when external_execution tools need to be called 3. Resume after external tool results are provided'
imports:
- sym: agno.agent.Agent
- sym: agno.db.sqlite.SqliteDb
- sym: agno.models.openai.OpenAIResponses
- sym: agno.tools.tool
- sym: agno.utils.pprint
data:
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIResponses(id="gpt-5-mini"),
        tools=[get_user_location, get_current_date],
        markdown=True,
     ...
functions:
- name: get_current_date
  sig: get_current_date() -> str
  lines:
  - 25
  - 31
  detail:
    calls:
    - datetime.now
    - datetime.now().strftime
- name: get_user_location
  sig: get_user_location() -> str
  decorators:
  - tool(external_execution=True)
  lines:
  - 36
  - 42
  detail:
    calls:
    - json.dumps
exports:
- name: get_current_date
  kind: func
- name: get_user_location
  kind: func
- name: agent
  kind: data
