## Roska File Descriptor — 10_human_in_the_loop/confirmation_with_session_state.py
file: 10_human_in_the_loop/confirmation_with_session_state.py
lines: 97
layer: logic
purpose: Confirmation with Session State ============================= HITL confirmation where the tool modifies session_state before pausing. Verifies that state changes survive the pause/continue round-trip.
imports:
- sym: agno.agent.Agent
- sym: agno.db.sqlite.SqliteDb
- sym: agno.models.openai.OpenAIChat
- sym: agno.run.RunContext
- sym: agno.run.RunStatus
- sym: agno.tools.tool
- sym: agno.utils.pprint
- sym: rich.console.Console
- sym: rich.prompt.Prompt
data:
- name: console
  type: Any
  val: Console()
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o-mini"),
        tools=[add_to_watchlist],
        session_state={"watchlist": []},
        in...
functions:
- name: add_to_watchlist
  sig: 'add_to_watchlist(run_context: RunContext, symbol: str) -> str'
  decorators:
  - tool(requires_confirmation=True)
  lines:
  - 22
  - 39
  detail:
    calls:
    - run_context.session_state.get
    - symbol.upper
    - watchlist.append
exports:
- name: add_to_watchlist
  kind: func
- name: console
  kind: data
- name: agent
  kind: data
