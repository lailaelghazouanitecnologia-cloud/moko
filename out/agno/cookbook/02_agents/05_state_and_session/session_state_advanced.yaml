## Roska File Descriptor — 05_state_and_session/session_state_advanced.py
file: 05_state_and_session/session_state_advanced.py
lines: 102
layer: logic
purpose: Session State Advanced ============================= Session State Advanced.
imports:
- sym: agno.agent.Agent
- sym: agno.db.sqlite.SqliteDb
- sym: agno.models.openai.OpenAIResponses
- sym: agno.run.RunContext
data:
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIResponses(id="gpt-5-mini"),
        # Initialize the session state with an empty shopping list (defau...
functions:
- name: add_item
  sig: 'add_item(run_context: RunContext, item: str) -> str'
  lines:
  - 17
  - 29
  detail:
    calls:
    - i.lower
    - item.lower
    - run_context.session_state["shopping_list"].append
- name: remove_item
  sig: 'remove_item(run_context: RunContext, item: str) -> str'
  lines:
  - 32
  - 43
  detail:
    calls:
    - enumerate
    - item.lower
    - list_item.lower
    - run_context.session_state["shopping_list"].pop
- name: list_items
  sig: 'list_items(run_context: RunContext) -> str'
  lines:
  - 46
  - 57
  detail:
    calls:
    - '"\n".join'
exports:
- name: add_item
  kind: func
- name: remove_item
  kind: func
- name: list_items
  kind: func
- name: agent
  kind: data
