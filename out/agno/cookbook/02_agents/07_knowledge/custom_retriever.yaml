## Roska File Descriptor — 07_knowledge/custom_retriever.py
file: 07_knowledge/custom_retriever.py
lines: 70
layer: logic
purpose: Custom Retriever ============================= Use knowledge_retriever to provide a custom retrieval function. Instead of using a Knowledge instance, you can supply your own callable that returns documents. The agent will use it as its search_knowledge_base tool.
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIResponses
data:
- name: DOCUMENTS
  type: list
  val: |-
    [
        {
            "title": "Python Basics",
            "content": "Python is a high-level programming language known for its ...
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIResponses(id="gpt-5.2"),
        # Use a custom retriever instead of a Knowledge instance
        knowled...
functions:
- name: my_retriever
  sig: |-
    my_retriever(
        query: str, num_documents: Optional[int] = None, **kwargs
    ) -> Optional[List[dict]]
  lines:
  - 37
  - 49
  detail:
    calls:
    - doc["content"].lower
    - doc["title"].lower
    - query.lower
exports:
- name: my_retriever
  kind: func
- name: DOCUMENTS
  kind: data
- name: agent
  kind: data
