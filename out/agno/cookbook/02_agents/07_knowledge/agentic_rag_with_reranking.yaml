## Roska File Descriptor — 07_knowledge/agentic_rag_with_reranking.py
file: 07_knowledge/agentic_rag_with_reranking.py
lines: 45
layer: logic
purpose: 'Agentic Rag With Reranking ============================= 1. Run: `uv pip install openai agno cohere lancedb tantivy sqlalchemy` to install the dependencies.'
imports:
- sym: agno.agent.Agent
- sym: agno.knowledge.embedder.openai.OpenAIEmbedder
- sym: agno.knowledge.knowledge.Knowledge
- sym: agno.knowledge.reranker.cohere.CohereReranker
- sym: agno.models.openai.OpenAIResponses
- sym: agno.vectordb.lancedb.LanceDb
- sym: agno.vectordb.lancedb.SearchType
data:
- name: knowledge
  type: Any
  val: |-
    Knowledge(
        # Use LanceDB as the vector database and store embeddings in the `agno_docs` table
        vector_db=LanceDb(...
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIResponses(id="gpt-5.2"),
        # Agentic RAG is enabled by default when `knowledge` is provided to ...
exports:
- name: knowledge
  kind: data
- name: agent
  kind: data
