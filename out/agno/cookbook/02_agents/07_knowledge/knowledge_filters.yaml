## Roska File Descriptor — 07_knowledge/knowledge_filters.py
file: 07_knowledge/knowledge_filters.py
lines: 70
layer: logic
purpose: Knowledge Filters ============================= Filter knowledge base searches using static filters or agentic filters. Static filters are set at agent creation time and apply to every search. Agentic filters let the agent dynamically choose filter values at runtime.
imports:
- sym: agno.agent.Agent
- sym: agno.filters.EQ
- sym: agno.knowledge.embedder.openai.OpenAIEmbedder
- sym: agno.knowledge.knowledge.Knowledge
- sym: agno.models.openai.OpenAIResponses
- sym: agno.vectordb.pgvector.PgVector
- sym: agno.vectordb.pgvector.SearchType
data:
- name: db_url
  type: str
  val: '"postgresql+psycopg://ai:ai@localhost:5532/ai"'
- name: knowledge
  type: Any
  val: |-
    Knowledge(
        vector_db=PgVector(
            table_name="recipes_filters_demo",
            db_url=db_url,
            search_type...
- name: agent_static
  type: Any
  val: |-
    Agent(
        model=OpenAIResponses(id="gpt-5.2"),
        knowledge=knowledge,
        search_knowledge=True,
        # Use FilterExpr...
- name: agent_agentic
  type: Any
  val: |-
    Agent(
        model=OpenAIResponses(id="gpt-5.2"),
        knowledge=knowledge,
        search_knowledge=True,
        # Let the agent ...
exports:
- name: db_url
  kind: data
- name: knowledge
  kind: data
- name: agent_static
  kind: data
- name: agent_agentic
  kind: data
