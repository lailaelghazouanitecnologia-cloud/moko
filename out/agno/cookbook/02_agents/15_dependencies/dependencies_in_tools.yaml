## Roska File Descriptor — 15_dependencies/dependencies_in_tools.py
file: 15_dependencies/dependencies_in_tools.py
lines: 108
layer: logic
purpose: Dependencies In Tools ============================= Example showing how tools can access dependencies passed to the agent.
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIResponses
- sym: agno.run.RunContext
data:
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIResponses(id="gpt-5.2"),
        tools=[analyze_user],
        name="User Analysis Agent",
        descriptio...
functions:
- name: get_current_context
  sig: get_current_context() -> dict
  lines:
  - 15
  - 21
  detail:
    calls:
    - datetime.now
    - datetime.now().strftime
- name: analyze_user
  sig: 'analyze_user(user_id: str, run_context: RunContext) -> str'
  lines:
  - 24
  - 67
  detail:
    calls:
    - '"\n\n".join'
    - ''', ''.join'
    - dependencies.keys
    - list
    - print
    - profile_data.get
    - results.append
    - user_id.upper
exports:
- name: get_current_context
  kind: func
- name: analyze_user
  kind: func
- name: agent
  kind: data
