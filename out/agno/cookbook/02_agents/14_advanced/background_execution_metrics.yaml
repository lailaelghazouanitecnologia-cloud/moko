## Roska File Descriptor — 14_advanced/background_execution_metrics.py
file: 14_advanced/background_execution_metrics.py
lines: 96
layer: logic
purpose: 'Background Execution Metrics ============================= Demonstrates that metrics are fully tracked for background runs. When an agent runs in the background, the run completes asynchronously and is stored in the database. Once complete, the run output includes the same metrics as a synchronous run: token counts, model details, duration, and time-to-first-token.'
imports:
- sym: asyncio
- sym: agno.agent.Agent
- sym: agno.db.postgres.PostgresDb
- sym: agno.models.openai.OpenAIChat
- sym: agno.run.base.RunStatus
- sym: agno.tools.yfinance.YFinanceTools
- sym: rich.pretty.pprint
data:
- name: db
  type: Any
  val: |-
    PostgresDb(
        db_url="postgresql+psycopg://ai:ai@localhost:5532/ai",
        session_table="bg_metrics_sessions",
    )
- name: agent
  type: Any
  val: |-
    Agent(
        name="BackgroundMetricsAgent",
        model=OpenAIChat(id="gpt-4o-mini"),
        tools=[YFinanceTools(enable_stock_...
functions:
- name: main
  sig: main()
  is_async: true
  lines:
  - 44
  - 92
  detail:
    calls:
    - agent.aget_run_output
    - agent.arun
    - agent.get_session_metrics
    - asyncio.sleep
    - pprint
    - print
    - range
    - result.metrics.details.items
exports:
- name: main
  kind: func
- name: db
  kind: data
- name: agent
  kind: data
