## Roska File Descriptor — scripts/check_style.py
file: scripts/check_style.py
lines: 78
layer: config
functions:
- name: check_style
  sig: 'check_style(code: str) -> dict'
  lines:
  - 16
  - 62
  detail:
    calls:
    - any
    - c.isupper
    - code.split
    - enumerate
    - issues.append
    - len
    - line.endswith
    - line.split
    - line.split("=")[0].strip
    - line.strip
    - line.strip().startswith
    - var[0].isupper
exports:
- name: check_style
  kind: func
