## Roska File Descriptor — agents/logical_puzzle.py
file: agents/logical_puzzle.py
lines: 42
layer: logic
purpose: Missionaries And Cannibals Puzzle ================================= Demonstrates built-in and DeepSeek-backed reasoning for logic puzzle solving.
imports:
- sym: agno.agent.Agent
- sym: agno.models.deepseek.DeepSeek
- sym: agno.models.openai.OpenAIChat
data:
- name: task
  type: tuple
  val: |-
    (
        "Three missionaries and three cannibals need to cross a river. "
        "They have a boat that can carry up to two pe...
- name: cot_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        reasoning=True,
        markdown=True,
    )
- name: deepseek_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        reasoning_model=DeepSeek(id="deepseek-reasoner"),
        markdown=True,
    )
exports:
- name: task
  kind: data
- name: cot_agent
  kind: data
- name: deepseek_agent
  kind: data
