## Roska File Descriptor — agents/analyse_treaty_of_versailles.py
file: agents/analyse_treaty_of_versailles.py
lines: 42
layer: logic
purpose: Treaty Of Versailles Analysis ============================ Demonstrates built-in and DeepSeek-backed reasoning for historical analysis.
imports:
- sym: agno.agent.Agent
- sym: agno.models.deepseek.DeepSeek
- sym: agno.models.openai.OpenAIChat
data:
- name: task
  type: tuple
  val: |-
    (
        "Analyze the key factors that led to the signing of the Treaty of Versailles in 1919. "
        "Discuss the political...
- name: cot_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        reasoning=True,
        markdown=True,
    )
- name: deepseek_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        reasoning_model=DeepSeek(id="deepseek-reasoner"),
        markdown=True,
    )
exports:
- name: task
  kind: data
- name: cot_agent
  kind: data
- name: deepseek_agent
  kind: data
