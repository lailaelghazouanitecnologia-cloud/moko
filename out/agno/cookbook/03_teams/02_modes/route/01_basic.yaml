## Roska File Descriptor — route/01_basic.py
file: route/01_basic.py
lines: 76
layer: logic
purpose: Basic Route Mode Example Demonstrates `mode=route` where the team leader routes each request to a single specialist agent and returns their response directly (no synthesis). This is ideal for language routing, domain dispatch, or any scenario where one specialist should handle the entire request.
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIResponses
- sym: agno.team.mode.TeamMode
- sym: agno.team.team.Team
data:
- name: english_agent
  type: Any
  val: |-
    Agent(
        name="English Agent",
        role="Responds only in English",
        model=OpenAIResponses(id="gpt-5.2"),
        instr...
- name: spanish_agent
  type: Any
  val: |-
    Agent(
        name="Spanish Agent",
        role="Responds only in Spanish",
        model=OpenAIResponses(id="gpt-5.2"),
        instr...
- name: french_agent
  type: Any
  val: |-
    Agent(
        name="French Agent",
        role="Responds only in French",
        model=OpenAIResponses(id="gpt-5.2"),
        instruc...
- name: team
  type: Any
  val: |-
    Team(
        name="Language Router",
        mode=TeamMode.route,
        model=OpenAIResponses(id="gpt-5.2"),
        members=[english...
exports:
- name: english_agent
  kind: data
- name: spanish_agent
  kind: data
- name: french_agent
  kind: data
- name: team
  kind: data
