## Roska File Descriptor — tasks/09_custom_tools.py
file: tasks/09_custom_tools.py
lines: 198
layer: logic
purpose: 'Task Mode with Custom Tools Demonstrates task mode where member agents use custom Python function tools. Shows how agents with specialized tools can be orchestrated via tasks. Run: .venvs/demo/bin/python cookbook/03_teams/02_modes/tasks/09_custom_tools.py'
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIResponses
- sym: agno.team.mode.TeamMode
- sym: agno.team.team.Team
- sym: agno.tools.tool
data:
- name: calculator
  type: Any
  val: |-
    Agent(
        name="Financial Calculator",
        role="Performs financial calculations including interest, loans, and project...
- name: risk_assessor
  type: Any
  val: |-
    Agent(
        name="Risk Assessor",
        role="Evaluates financial risk based on client metrics",
        model=OpenAIResponses(...
- name: advisor
  type: Any
  val: |-
    Agent(
        name="Financial Advisor",
        role="Provides financial advice and recommendations",
        model=OpenAIResponses...
- name: finance_team
  type: Any
  val: |-
    Team(
        name="Financial Advisory Team",
        mode=TeamMode.tasks,
        model=OpenAIResponses(id="gpt-5.2"),
        members=...
functions:
- name: calculate_compound_interest
  sig: |-
    calculate_compound_interest(
        principal: float, annual_rate: float, years: int, compounds_per_year: int = 12
    ) -> str
  decorators:
  - tool
  lines:
  - 22
  - 42
  detail: {}
- name: calculate_monthly_payment
  sig: 'calculate_monthly_payment(principal: float, annual_rate: float, years: int) -> str'
  decorators:
  - tool
  lines:
  - 46
  - 71
  detail: {}
- name: assess_risk_score
  sig: |-
    assess_risk_score(
        debt_to_income_ratio: float, credit_score: int, years_employed: int
    ) -> str
  decorators:
  - tool
  lines:
  - 75
  - 122
  detail: {}
exports:
- name: calculate_compound_interest
  kind: func
- name: calculate_monthly_payment
  kind: func
- name: assess_risk_score
  kind: func
- name: calculator
  kind: data
- name: risk_assessor
  kind: data
- name: advisor
  kind: data
- name: finance_team
  kind: data
