## Roska File Descriptor — tasks/11_streaming_events.py
file: tasks/11_streaming_events.py
lines: 150
layer: logic
purpose: 'Task Mode Streaming Events ========================== Demonstrates how to consume streaming events programmatically in `mode=tasks`. This example shows how to: 1. Use `stream=True` with `run()` to get an iterator of events 2. Handle task iteration events (started/completed) 3. Handle task state updates 4. Process content deltas as they arrive'
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIResponses
- sym: agno.run.agent.RunContentEvent
- sym: agno.run.team.RunContentEvent
- sym: agno.run.team.TaskIterationCompletedEvent
- sym: agno.run.team.TaskIterationStartedEvent
- sym: agno.run.team.TaskStateUpdatedEvent
- sym: agno.run.team.TeamRunEvent
- sym: agno.run.team.ToolCallCompletedEvent
- sym: agno.run.team.ToolCallStartedEvent
- sym: agno.team.mode.TeamMode
- sym: agno.team.team.Team
data:
- name: researcher
  type: Any
  val: |-
    Agent(
        name="Researcher",
        role="Researches topics and gathers information",
        model=OpenAIResponses(id="gpt-5....
- name: summarizer
  type: Any
  val: |-
    Agent(
        name="Summarizer",
        role="Summarizes information into concise points",
        model=OpenAIResponses(id="gpt-5...
- name: team
  type: Any
  val: |-
    Team(
        name="Research Team",
        mode=TeamMode.tasks,
        model=OpenAIResponses(id="gpt-5.1"),
        members=[researche...
functions:
- name: streaming_with_events
  sig: streaming_with_events() -> None
  lines:
  - 74
  - 146
  detail:
    calls:
    - hasattr
    - isinstance
    - print
    - team.run
exports:
- name: streaming_with_events
  kind: func
- name: researcher
  kind: data
- name: summarizer
  kind: data
- name: team
  kind: data
