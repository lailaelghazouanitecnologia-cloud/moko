## Roska File Descriptor — tasks/04_basic_task_mode.py
file: tasks/04_basic_task_mode.py
lines: 84
layer: logic
purpose: 'Basic Task Mode Example Demonstrates a team in `mode=tasks` where the team leader autonomously: 1. Decomposes a user goal into discrete tasks 2. Assigns tasks to the most suitable member agent 3. Executes tasks by delegating to members 4. Collects results and provides a final summary Run: .venvs/demo/bin/python cookbook/03_teams/02_modes/tasks/04_basic_task_mode.py'
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIResponses
- sym: agno.team.mode.TeamMode
- sym: agno.team.team.Team
data:
- name: researcher
  type: Any
  val: |-
    Agent(
        name="Researcher",
        role="Research specialist who finds information on topics",
        model=OpenAIResponses(...
- name: writer
  type: Any
  val: |-
    Agent(
        name="Writer",
        role="Content writer who creates well-structured text",
        model=OpenAIResponses(id="gpt-...
- name: critic
  type: Any
  val: |-
    Agent(
        name="Critic",
        role="Quality reviewer who provides constructive feedback",
        model=OpenAIResponses(id="...
- name: content_team
  type: Any
  val: |-
    Team(
        name="Content Creation Team",
        mode=TeamMode.tasks,
        model=OpenAIResponses(id="gpt-5.2"),
        members=[r...
exports:
- name: researcher
  kind: data
- name: writer
  kind: data
- name: critic
  kind: data
- name: content_team
  kind: data
