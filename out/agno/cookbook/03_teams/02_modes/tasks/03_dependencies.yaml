## Roska File Descriptor — tasks/03_dependencies.py
file: tasks/03_dependencies.py
lines: 82
layer: logic
purpose: Tasks with Dependencies Example Demonstrates task mode with dependency chains. The team leader creates tasks where later tasks depend on earlier ones, ensuring correct execution order.
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIResponses
- sym: agno.team.mode.TeamMode
- sym: agno.team.team.Team
data:
- name: data_collector
  type: Any
  val: |-
    Agent(
        name="Data Collector",
        role="Gathers raw data and facts on a topic",
        model=OpenAIResponses(id="gpt-5....
- name: analyst
  type: Any
  val: |-
    Agent(
        name="Analyst",
        role="Analyzes data and extracts insights",
        model=OpenAIResponses(id="gpt-5.2"),
        ...
- name: report_writer
  type: Any
  val: |-
    Agent(
        name="Report Writer",
        role="Writes polished reports from analysis results",
        model=OpenAIResponses(id=...
- name: team
  type: Any
  val: |-
    Team(
        name="Research Pipeline Team",
        mode=TeamMode.tasks,
        model=OpenAIResponses(id="gpt-5.2"),
        members=[...
exports:
- name: data_collector
  kind: data
- name: analyst
  kind: data
- name: report_writer
  kind: data
- name: team
  kind: data
