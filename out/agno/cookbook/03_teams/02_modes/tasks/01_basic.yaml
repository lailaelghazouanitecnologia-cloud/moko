## Roska File Descriptor — tasks/01_basic.py
file: tasks/01_basic.py
lines: 84
layer: logic
purpose: 'Basic Tasks Mode Example Demonstrates `mode=tasks` where the team leader autonomously: 1. Decomposes the user''s goal into discrete tasks 2. Assigns each task to the best member agent 3. Executes tasks sequentially 4. Synthesizes results into a final response'
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIResponses
- sym: agno.team.mode.TeamMode
- sym: agno.team.team.Team
data:
- name: planner
  type: Any
  val: |-
    Agent(
        name="Planner",
        role="Creates outlines, plans, and structures for content",
        model=OpenAIResponses(id=...
- name: writer
  type: Any
  val: |-
    Agent(
        name="Writer",
        role="Writes polished content based on outlines or instructions",
        model=OpenAIResponse...
- name: editor
  type: Any
  val: |-
    Agent(
        name="Editor",
        role="Reviews and improves content for clarity and quality",
        model=OpenAIResponses(id=...
- name: team
  type: Any
  val: |-
    Team(
        name="Content Pipeline Team",
        mode=TeamMode.tasks,
        model=OpenAIResponses(id="gpt-5.2"),
        members=[p...
exports:
- name: planner
  kind: data
- name: writer
  kind: data
- name: editor
  kind: data
- name: team
  kind: data
