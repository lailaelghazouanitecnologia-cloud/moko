## Roska File Descriptor — 20_human_in_the_loop/confirmation_required_stream.py
file: 20_human_in_the_loop/confirmation_required_stream.py
lines: 86
layer: logic
purpose: 'Team HITL Streaming: Member agent tool requiring confirmation. This example demonstrates how a team pauses when a member agent''s tool requires human confirmation in streaming mode. After confirmation the team resumes with continue_run(). Note: When streaming with member agents, use isinstance() with TeamRunPausedEvent to distinguish the team''s pause from member agent pauses.'
imports:
- sym: agno.agent.Agent
- sym: agno.db.sqlite.SqliteDb
- sym: agno.models.openai.OpenAIResponses
- sym: agno.run.team.RunPausedEvent
- sym: agno.team.team.Team
- sym: agno.tools.tool
- sym: agno.utils.pprint
data:
- name: db
  type: Any
  val: SqliteDb(db_file="tmp/team_hitl_stream.db")
- name: deploy_agent
  type: Any
  val: |-
    Agent(
        name="Deploy Agent",
        role="Handles deployments to production",
        model=OpenAIResponses(id="gpt-5-mini")...
- name: team
  type: Any
  val: |-
    Team(
        name="DevOps Team",
        members=[deploy_agent],
        model=OpenAIResponses(id="gpt-5-mini"),
        db=db,
    )
functions:
- name: deploy_to_production
  sig: 'deploy_to_production(app_name: str, version: str) -> str'
  decorators:
  - tool(requires_confirmation=True)
  lines:
  - 31
  - 38
  detail: {}
exports:
- name: deploy_to_production
  kind: func
- name: db
  kind: data
- name: deploy_agent
  kind: data
- name: team
  kind: data
