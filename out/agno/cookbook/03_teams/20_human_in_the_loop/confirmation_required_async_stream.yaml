## Roska File Descriptor — 20_human_in_the_loop/confirmation_required_async_stream.py
file: 20_human_in_the_loop/confirmation_required_async_stream.py
lines: 89
layer: logic
purpose: 'Team HITL Async Streaming: Member agent tool requiring confirmation. Same as confirmation_required_stream.py but uses async run/continue_run. Note: When streaming with member agents, use isinstance() with TeamRunPausedEvent to distinguish the team''s pause from member agent pauses.'
imports:
- sym: asyncio
- sym: agno.agent.Agent
- sym: agno.db.sqlite.SqliteDb
- sym: agno.models.openai.OpenAIResponses
- sym: agno.run.team.RunPausedEvent
- sym: agno.team.team.Team
- sym: agno.tools.tool
- sym: agno.utils.pprint
data:
- name: db
  type: Any
  val: SqliteDb(db_file="tmp/team_hitl_stream.db")
- name: deploy_agent
  type: Any
  val: |-
    Agent(
        name="Deploy Agent",
        role="Handles deployments to production",
        model=OpenAIResponses(id="gpt-5-mini")...
- name: team
  type: Any
  val: |-
    Team(
        name="DevOps Team",
        members=[deploy_agent],
        model=OpenAIResponses(id="gpt-5-mini"),
        db=db,
    )
functions:
- name: deploy_to_production
  sig: 'deploy_to_production(app_name: str, version: str) -> str'
  decorators:
  - tool(requires_confirmation=True)
  lines:
  - 29
  - 36
  detail: {}
- name: main
  sig: main()
  is_async: true
  lines:
  - 62
  - 82
  detail:
    calls:
    - isinstance
    - pprint.apprint_run_response
    - print
    - req.confirm
    - team.acontinue_run
    - team.arun
exports:
- name: deploy_to_production
  kind: func
- name: main
  kind: func
- name: db
  kind: data
- name: deploy_agent
  kind: data
- name: team
  kind: data
