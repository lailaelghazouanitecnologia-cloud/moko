## Roska File Descriptor — 20_human_in_the_loop/confirmation_rejected.py
file: 20_human_in_the_loop/confirmation_rejected.py
lines: 65
layer: logic
purpose: 'Team HITL: Rejecting a member agent tool call. This example demonstrates how the team handles rejection of a tool call. After rejection, the team continues and the model responds acknowledging the rejection.'
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIResponses
- sym: agno.team.team.Team
- sym: agno.tools.tool
data:
- name: admin_agent
  type: Any
  val: |-
    Agent(
        name="Admin Agent",
        role="Handles account administration tasks",
        model=OpenAIResponses(id="gpt-5-mini...
- name: team
  type: Any
  val: |-
    Team(
        name="Admin Team",
        members=[admin_agent],
        model=OpenAIResponses(id="gpt-5-mini"),
    )
functions:
- name: delete_user_account
  sig: 'delete_user_account(username: str) -> str'
  decorators:
  - tool(requires_confirmation=True)
  lines:
  - 18
  - 24
  detail: {}
exports:
- name: delete_user_account
  kind: func
- name: admin_agent
  kind: data
- name: team
  kind: data
