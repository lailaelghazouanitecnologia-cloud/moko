## Roska File Descriptor — 20_human_in_the_loop/external_tool_execution_stream.py
file: 20_human_in_the_loop/external_tool_execution_stream.py
lines: 89
layer: logic
purpose: 'Team HITL Streaming: Member agent tool with external execution. This example demonstrates how a team pauses when a member agent''s tool requires external execution in streaming mode. The tool result is provided by the caller rather than being executed by the agent. Note: When streaming with member agents, use isinstance() with TeamRunPausedEvent to distinguish the team''s pause from member agent pauses.'
imports:
- sym: shlex
- sym: subprocess
- sym: agno.agent.Agent
- sym: agno.db.sqlite.SqliteDb
- sym: agno.models.openai.OpenAIResponses
- sym: agno.run.team.RunPausedEvent
- sym: agno.team.team.Team
- sym: agno.tools.tool
- sym: agno.utils.pprint
data:
- name: db
  type: Any
  val: SqliteDb(db_file="tmp/team_hitl_stream.db")
- name: ops_agent
  type: Any
  val: |-
    Agent(
        name="Ops Agent",
        role="Handles server operations",
        model=OpenAIResponses(id="gpt-5-mini"),
        tools...
- name: team
  type: Any
  val: |-
    Team(
        name="SRE Team",
        members=[ops_agent],
        model=OpenAIResponses(id="gpt-5-mini"),
        db=db,
    )
functions:
- name: run_shell_command
  sig: 'run_shell_command(command: str) -> str'
  decorators:
  - tool(external_execution=True)
  lines:
  - 32
  - 38
  detail:
    calls:
    - shlex.split
    - subprocess.check_output
    - subprocess.check_output(shlex.split(command)).decode
exports:
- name: run_shell_command
  kind: func
- name: db
  kind: data
- name: ops_agent
  kind: data
- name: team
  kind: data
