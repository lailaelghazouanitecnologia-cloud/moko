## Roska File Descriptor — 20_human_in_the_loop/user_input_required_stream.py
file: 20_human_in_the_loop/user_input_required_stream.py
lines: 85
layer: logic
purpose: 'Team HITL Streaming: Member agent tool requiring user input. This example demonstrates how a team pauses when a member agent''s tool needs additional information from the user before it can be executed in streaming mode. Note: When streaming with member agents, use isinstance() with TeamRunPausedEvent to distinguish the team''s pause from member agent pauses.'
imports:
- sym: agno.agent.Agent
- sym: agno.db.sqlite.SqliteDb
- sym: agno.models.openai.OpenAIResponses
- sym: agno.run.team.RunPausedEvent
- sym: agno.team.team.Team
- sym: agno.tools.tool
- sym: agno.utils.pprint
data:
- name: db
  type: Any
  val: SqliteDb(db_file="tmp/team_hitl_stream.db")
- name: booking_agent
  type: Any
  val: |-
    Agent(
        name="Booking Agent",
        role="Books travel arrangements",
        model=OpenAIResponses(id="gpt-5-mini"),
        t...
- name: team
  type: Any
  val: |-
    Team(
        name="Travel Team",
        members=[booking_agent],
        model=OpenAIResponses(id="gpt-5-mini"),
        db=db,
    )
functions:
- name: book_flight
  sig: 'book_flight(destination: str, date: str, passenger_name: str) -> str'
  decorators:
  - tool(requires_user_input=True, user_input_fields=["passenger_name"])
  lines:
  - 29
  - 37
  detail: {}
exports:
- name: book_flight
  kind: func
- name: db
  kind: data
- name: booking_agent
  kind: data
- name: team
  kind: data
