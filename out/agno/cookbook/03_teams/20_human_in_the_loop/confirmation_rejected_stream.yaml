## Roska File Descriptor — 20_human_in_the_loop/confirmation_rejected_stream.py
file: 20_human_in_the_loop/confirmation_rejected_stream.py
lines: 83
layer: logic
purpose: 'Team HITL Streaming: Rejecting a member agent tool call. This example demonstrates how the team handles rejection of a tool call in streaming mode. After rejection, the team continues and the model responds acknowledging the rejection. Note: When streaming with member agents, use isinstance() with TeamRunPausedEvent to distinguish the team''s pause from member agent pauses.'
imports:
- sym: agno.agent.Agent
- sym: agno.db.sqlite.SqliteDb
- sym: agno.models.openai.OpenAIResponses
- sym: agno.run.team.RunPausedEvent
- sym: agno.team.team.Team
- sym: agno.tools.tool
- sym: agno.utils.pprint
data:
- name: db
  type: Any
  val: SqliteDb(db_file="tmp/team_hitl_stream.db")
- name: admin_agent
  type: Any
  val: |-
    Agent(
        name="Admin Agent",
        role="Handles account administration tasks",
        model=OpenAIResponses(id="gpt-5-mini...
- name: team
  type: Any
  val: |-
    Team(
        name="Admin Team",
        members=[admin_agent],
        model=OpenAIResponses(id="gpt-5-mini"),
        db=db,
    )
functions:
- name: delete_user_account
  sig: 'delete_user_account(username: str) -> str'
  decorators:
  - tool(requires_confirmation=True)
  lines:
  - 29
  - 35
  detail: {}
exports:
- name: delete_user_account
  kind: func
- name: db
  kind: data
- name: admin_agent
  kind: data
- name: team
  kind: data
