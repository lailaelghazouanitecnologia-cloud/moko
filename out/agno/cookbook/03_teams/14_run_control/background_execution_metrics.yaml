## Roska File Descriptor — 14_run_control/background_execution_metrics.py
file: 14_run_control/background_execution_metrics.py
lines: 115
layer: logic
purpose: 'Team Background Execution Metrics ================================== Demonstrates that metrics are fully tracked for team background runs. When a team runs in the background, the run completes asynchronously and is stored in the database. Once complete, the run output includes the same metrics as a synchronous run: token counts, model details, duration, and member-level breakdown.'
imports:
- sym: asyncio
- sym: agno.agent.Agent
- sym: agno.db.postgres.PostgresDb
- sym: agno.models.openai.OpenAIChat
- sym: agno.run.base.RunStatus
- sym: agno.team.Team
- sym: agno.tools.yfinance.YFinanceTools
- sym: rich.pretty.pprint
data:
- name: db
  type: Any
  val: |-
    PostgresDb(
        db_url="postgresql+psycopg://ai:ai@localhost:5532/ai",
        session_table="team_bg_metrics_sessions",
    )
- name: stock_searcher
  type: Any
  val: |-
    Agent(
        name="Stock Searcher",
        model=OpenAIChat(id="gpt-4o-mini"),
        role="Searches for stock information.",
      ...
- name: team
  type: Any
  val: |-
    Team(
        name="Stock Research Team",
        model=OpenAIChat(id="gpt-4o-mini"),
        members=[stock_searcher],
        db=db,
     ...
functions:
- name: main
  sig: main()
  is_async: true
  lines:
  - 54
  - 111
  detail:
    calls:
    - asyncio.sleep
    - pprint
    - print
    - range
    - result.metrics.details.items
    - team.aget_run_output
    - team.arun
    - team.get_session_metrics
exports:
- name: main
  kind: func
- name: db
  kind: data
- name: stock_searcher
  kind: data
- name: team
  kind: data
