## Roska File Descriptor — 12_learning/01_team_always_learn.py
file: 12_learning/01_team_always_learn.py
lines: 88
layer: logic
purpose: 'Team Learning: Always Mode ========================== Set learning=True on a Team to enable automatic learning. The team automatically captures: - User profile: name, role, preferences - User memory: observations, context, patterns Extraction runs in parallel after each response. This is the simplest way to add learning to a team.'
imports:
- sym: agno.agent.Agent
- sym: agno.db.postgres.PostgresDb
- sym: agno.models.openai.OpenAIResponses
- sym: agno.team.Team
data:
- name: db
  type: Any
  val: PostgresDb(db_url="postgresql+psycopg://ai:ai@localhost:5532/ai")
- name: researcher
  type: Any
  val: |-
    Agent(
        name="Researcher",
        model=OpenAIResponses(id="gpt-5.2"),
        role="Research topics and provide detailed in...
- name: writer
  type: Any
  val: |-
    Agent(
        name="Writer",
        model=OpenAIResponses(id="gpt-5.2"),
        role="Write clear, concise content based on resea...
- name: team
  type: Any
  val: |-
    Team(
        name="Research Team",
        model=OpenAIResponses(id="gpt-5.2"),
        members=[researcher, writer],
        db=db,
      ...
exports:
- name: db
  kind: data
- name: researcher
  kind: data
- name: writer
  kind: data
- name: team
  kind: data
