## Roska File Descriptor — 00_quickstart/custom_tool_for_self_learning.py
file: 00_quickstart/custom_tool_for_self_learning.py
lines: 214
layer: logic
purpose: 'Custom Tool for Self-Learning - Write Your Own Tools ===================================================== This example shows how to write custom tools for your agent. A tool is just a Python function — the agent calls it when needed. We''ll build a self-learning agent that can save insights to a knowledge base. The key concept: any function can become a tool. Key concepts: - Tools are Python functions with docstrings (the docstring tells the agent what the tool does) - The agent decides when to call your tool based on the conversation - Return a string to communicate results back to the agent Example prompts to try: - "What''s a good P/E ratio for tech stocks? Save that insight." - "Remember that NVDA''s data center revenue is the key growth driver" - "What learnings do we have saved?"'
imports:
- sym: agno.agent.Agent
- sym: agno.db.sqlite.SqliteDb
- sym: agno.knowledge.Knowledge
- sym: agno.knowledge.embedder.google.GeminiEmbedder
- sym: agno.knowledge.reader.text_reader.TextReader
- sym: agno.models.google.Gemini
- sym: agno.tools.yfinance.YFinanceTools
- sym: agno.vectordb.chroma.ChromaDb
- sym: agno.vectordb.search.SearchType
data:
- name: agent_db
  type: Any
  val: SqliteDb(db_file="tmp/agents.db")
- name: learnings_kb
  type: Any
  val: |-
    Knowledge(
        name="Agent Learnings",
        vector_db=ChromaDb(
            name="learnings",
            collection="learnings",...
- name: instructions
  type: str
  val: |-
    """\
    You are a Finance Agent that learns and improves over time.

    You have two special abilities:
    1. Search your knowled...
- name: self_learning_agent
  type: Any
  val: |-
    Agent(
        name="Self-Learning Agent",
        model=Gemini(id="gemini-3-flash-preview"),
        instructions=instructions,
       ...
functions:
- name: save_learning
  sig: 'save_learning(title: str, learning: str) -> str'
  lines:
  - 61
  - 93
  detail:
    calls:
    - TextReader
    - datetime.now
    - datetime.now(timezone.utc).isoformat
    - json.dumps
    - learning.strip
    - learnings_kb.insert
    - title.strip
exports:
- name: save_learning
  kind: func
- name: agent_db
  kind: data
- name: learnings_kb
  kind: data
- name: instructions
  kind: data
- name: self_learning_agent
  kind: data
