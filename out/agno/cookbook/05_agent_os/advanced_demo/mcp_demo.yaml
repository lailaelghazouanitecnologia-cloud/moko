## Roska File Descriptor — advanced_demo/mcp_demo.py
file: advanced_demo/mcp_demo.py
lines: 90
layer: logic
purpose: 'This example shows how to run an Agent using our MCP integration in the Agno OS. For this example to run you need: - Create a GitHub personal access token following these steps: - https://github.com/modelcontextprotocol/servers/tree/main/src/github#setup - Set the GITHUB_TOKEN environment variable: `export GITHUB_TOKEN=<Your GitHub access token>` - Run: `uv pip install agno mcp openai` to install the dependencies'
imports:
- sym: agno.agent.Agent
- sym: agno.db.sqlite.SqliteDb
- sym: agno.models.openai.OpenAIChat
- sym: agno.os.AgentOS
- sym: agno.tools.mcp.MCPTools
- sym: fastapi.FastAPI
- sym: mcp.StdioServerParameters
data:
- name: agent_storage_file
  type: Any
  val: str
- name: github_token
  type: Any
  val: getenv("GITHUB_TOKEN") or getenv("GITHUB_ACCESS_TOKEN")
- name: server_params
  type: Any
  val: |-
    StdioServerParameters(
        command="npx",
        args=["-y", "@modelcontextprotocol/server-github"],
    )
- name: agent
  type: Any
  val: |-
    Agent(
        name="MCP GitHub Agent",
        instructions=dedent("""\
            You are a GitHub assistant. Help users explore ...
- name: agent_os
  type: Any
  val: |-
    AgentOS(
        description="Example OS setup",
        agents=[agent],
    )
- name: app
  type: Any
  val: agent_os.get_app()
functions:
- name: lifespan
  sig: 'lifespan(app: FastAPI)'
  is_async: true
  decorators:
  - asynccontextmanager
  lines:
  - 42
  - 56
  detail:
    calls:
    - MCPTools
    - mcp_tools.close
    - mcp_tools.connect
exports:
- name: lifespan
  kind: func
- name: agent_storage_file
  kind: data
- name: github_token
  kind: data
- name: server_params
  kind: data
- name: agent
  kind: data
- name: agent_os
  kind: data
- name: app
  kind: data
