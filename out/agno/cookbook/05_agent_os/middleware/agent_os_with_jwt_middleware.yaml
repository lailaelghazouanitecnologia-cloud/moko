## Roska File Descriptor — middleware/agent_os_with_jwt_middleware.py
file: middleware/agent_os_with_jwt_middleware.py
lines: 99
layer: logic
purpose: This example demonstrates how to use our JWT middleware with AgentOS. The middleware extracts JWT claims and stores them in request.state for easy access. This example uses the default Authorization header approach. For cookie-based authentication, see agent_os_with_jwt_cookies.py For both header and cookie support, use token_source=TokenSource.BOTH
imports:
- sym: jwt
- sym: agno.agent.Agent
- sym: agno.db.postgres.PostgresDb
- sym: agno.models.openai.OpenAIChat
- sym: agno.os.AgentOS
- sym: agno.os.middleware.JWTMiddleware
data:
- name: JWT_SECRET
  type: str
  val: '"a-string-secret-at-least-256-bits-long"'
- name: db
  type: Any
  val: PostgresDb(db_url="postgresql+psycopg://ai:ai@localhost:5532/ai")
- name: research_agent
  type: Any
  val: |-
    Agent(
        id="user-agent",
        model=OpenAIChat(id="gpt-4o"),
        db=db,
        tools=[get_user_details],
        instructions...
- name: agent_os
  type: Any
  val: |-
    AgentOS(
        description="JWT Protected AgentOS",
        agents=[research_agent],
    )
- name: app
  type: Any
  val: agent_os.get_app()
functions:
- name: get_user_details
  sig: 'get_user_details(dependencies: dict)'
  lines:
  - 32
  - 40
  detail:
    calls:
    - dependencies.get
exports:
- name: get_user_details
  kind: func
- name: JWT_SECRET
  kind: data
- name: db
  kind: data
- name: research_agent
  kind: data
- name: agent_os
  kind: data
- name: app
  kind: data
