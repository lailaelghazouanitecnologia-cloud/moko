## Roska File Descriptor — middleware/custom_fastapi_app_with_jwt_middleware.py
file: middleware/custom_fastapi_app_with_jwt_middleware.py
lines: 97
layer: logic
purpose: 'This example demonstrates how to use our JWT middleware with your custom FastAPI app. # Note: This example won''t work with the AgentOS UI, because of the token validation mechanism in the JWT middleware.'
imports:
- sym: jwt
- sym: agno.agent.Agent
- sym: agno.db.postgres.PostgresDb
- sym: agno.models.openai.OpenAIChat
- sym: agno.os.AgentOS
- sym: agno.os.middleware.JWTMiddleware
- sym: agno.tools.websearch.WebSearchTools
- sym: fastapi.FastAPI
- sym: fastapi.Form
- sym: fastapi.HTTPException
data:
- name: JWT_SECRET
  type: str
  val: '"a-string-secret-at-least-256-bits-long"'
- name: db
  type: Any
  val: PostgresDb(db_url="postgresql+psycopg://ai:ai@localhost:5532/ai")
- name: research_agent
  type: Any
  val: |-
    Agent(
        id="research-agent",
        name="Research Agent",
        model=OpenAIChat(id="gpt-4o"),
        db=db,
        tools=[WebS...
- name: app
  type: Any
  val: |-
    FastAPI(
        title="Example Custom App",
        version="1.0.0",
    )
- name: agent_os
  type: Any
  val: |-
    AgentOS(
        description="JWT Protected AgentOS",
        agents=[research_agent],
        base_app=app,
    )
- name: app
  type: Any
  val: agent_os.get_app()
functions:
- name: login
  sig: 'login(username: str = Form(...), password: str = Form(...))'
  is_async: true
  decorators:
  - app.post("/auth/login")
  lines:
  - 58
  - 70
  detail:
    calls:
    - HTTPException
    - datetime.now
    - jwt.encode
    - timedelta
exports:
- name: login
  kind: func
- name: JWT_SECRET
  kind: data
- name: db
  kind: data
- name: research_agent
  kind: data
- name: app
  kind: data
- name: agent_os
  kind: data
- name: app
  kind: data
