## Roska File Descriptor — whatsapp/interactive_concierge.py
file: whatsapp/interactive_concierge.py
lines: 78
layer: logic
purpose: 'Interactive Concierge ===================== A WhatsApp concierge that uses every interactive UI feature to help users find restaurants, activities, and entertainment. Showcases: reply buttons, list messages, location pins, reactions, mark-as-read, and image sending — all in one conversational flow. Requires: WHATSAPP_ACCESS_TOKEN, WHATSAPP_PHONE_NUMBER_ID ANTHROPIC_API_KEY uvx (for geocode-mcp server)'
imports:
- sym: agno.agent.Agent
- sym: agno.db.sqlite.SqliteDb
- sym: agno.models.anthropic.Claude
- sym: agno.os.app.AgentOS
- sym: agno.os.interfaces.whatsapp.Whatsapp
- sym: agno.tools.mcp.MCPTools
- sym: agno.tools.websearch.WebSearchTools
- sym: agno.tools.whatsapp.WhatsAppTools
data:
- name: agent_db
  type: Any
  val: SqliteDb(db_file="tmp/concierge.db")
- name: concierge_agent
  type: Any
  val: |-
    Agent(
        name="Concierge",
        model=Claude(id="claude-sonnet-4-6"),
        tools=[
            WhatsAppTools(
                en...
- name: agent_os
  type: Any
  val: |-
    AgentOS(
        agents=[concierge_agent],
        interfaces=[Whatsapp(agent=concierge_agent, send_user_number_to_context=True)...
- name: app
  type: Any
  val: agent_os.get_app()
exports:
- name: agent_db
  kind: data
- name: concierge_agent
  kind: data
- name: agent_os
  kind: data
- name: app
  kind: data
