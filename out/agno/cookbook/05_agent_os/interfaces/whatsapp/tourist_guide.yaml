## Roska File Descriptor — whatsapp/tourist_guide.py
file: whatsapp/tourist_guide.py
lines: 74
layer: logic
purpose: 'Tourist Guide ============== A WhatsApp agent that recommends tourist spots using interactive menus. It asks the user questions via reply buttons and list messages, searches the web for top attractions, and sends a location pin for its recommendation. Requires: WHATSAPP_ACCESS_TOKEN, WHATSAPP_PHONE_NUMBER_ID ANTHROPIC_API_KEY'
imports:
- sym: agno.agent.Agent
- sym: agno.db.sqlite.SqliteDb
- sym: agno.models.anthropic.Claude
- sym: agno.os.app.AgentOS
- sym: agno.os.interfaces.whatsapp.Whatsapp
- sym: agno.tools.websearch.WebSearchTools
- sym: agno.tools.whatsapp.WhatsAppTools
data:
- name: agent_db
  type: Any
  val: SqliteDb(db_file="tmp/tourist_guide.db")
- name: tourist_agent
  type: Any
  val: |-
    Agent(
        name="Tourist Guide",
        model=Claude(id="claude-sonnet-4-6"),
        tools=[
            WhatsAppTools(
              ...
- name: agent_os
  type: Any
  val: |-
    AgentOS(
        agents=[tourist_agent],
        interfaces=[Whatsapp(agent=tourist_agent, send_user_number_to_context=True)],
    )
- name: app
  type: Any
  val: agent_os.get_app()
exports:
- name: agent_db
  kind: data
- name: tourist_agent
  kind: data
- name: agent_os
  kind: data
- name: app
  kind: data
