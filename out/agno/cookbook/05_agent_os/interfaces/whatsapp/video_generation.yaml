## Roska File Descriptor — whatsapp/video_generation.py
file: whatsapp/video_generation.py
lines: 59
layer: logic
purpose: 'Video Generation Agent ======================= A WhatsApp agent that generates short videos from text descriptions using Fal AI''s text-to-video models. Demonstrates outbound video support through the WhatsApp Cloud API. Requires: WHATSAPP_ACCESS_TOKEN, WHATSAPP_PHONE_NUMBER_ID FAL_KEY pip install fal-client'
imports:
- sym: agno.agent.Agent
- sym: agno.db.sqlite.SqliteDb
- sym: agno.models.openai.OpenAIChat
- sym: agno.os.app.AgentOS
- sym: agno.os.interfaces.whatsapp.Whatsapp
- sym: agno.tools.fal.FalTools
data:
- name: agent_db
  type: Any
  val: SqliteDb(db_file="tmp/video_agent.db")
- name: video_agent
  type: Any
  val: |-
    Agent(
        name="Video Generator",
        model=OpenAIChat(id="gpt-5-mini"),
        tools=[FalTools(model="fal-ai/hunyuan-vide...
- name: agent_os
  type: Any
  val: |-
    AgentOS(
        agents=[video_agent],
        interfaces=[Whatsapp(agent=video_agent)],
    )
- name: app
  type: Any
  val: agent_os.get_app()
exports:
- name: agent_db
  kind: data
- name: video_agent
  kind: data
- name: agent_os
  kind: data
- name: app
  kind: data
