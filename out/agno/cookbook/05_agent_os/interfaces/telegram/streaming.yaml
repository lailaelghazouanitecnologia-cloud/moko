## Roska File Descriptor — telegram/streaming.py
file: telegram/streaming.py
lines: 69
layer: logic
purpose: 'Streaming Telegram Agent ======================== Telegram bot that streams responses token-by-token, editing the message in real time so the user sees incremental output instead of waiting for the full response. Key concepts: - ``streaming=True`` on the Telegram interface enables chunked message edits. - Uses OpenAI gpt-4o-mini for fast token generation. - SQLite session persistence keeps conversation history across restarts. Setup: Set TELEGRAM_TOKEN env var from @BotFather.'
imports:
- sym: agno.agent.Agent
- sym: agno.db.sqlite.SqliteDb
- sym: agno.models.openai.OpenAIChat
- sym: agno.os.app.AgentOS
- sym: agno.os.interfaces.telegram.Telegram
data:
- name: agent_db
  type: Any
  val: |-
    SqliteDb(
        session_table="telegram_sessions", db_file="tmp/telegram_streaming.db"
    )
- name: telegram_agent
  type: Any
  val: |-
    Agent(
        name="Telegram Streaming Bot",
        model=OpenAIChat(id="gpt-4o-mini"),
        db=agent_db,
        instructions=[
      ...
- name: agent_os
  type: Any
  val: |-
    AgentOS(
        agents=[telegram_agent],
        interfaces=[
            Telegram(
                agent=telegram_agent,
                r...
- name: app
  type: Any
  val: agent_os.get_app()
exports:
- name: agent_db
  kind: data
- name: telegram_agent
  kind: data
- name: agent_os
  kind: data
- name: app
  kind: data
