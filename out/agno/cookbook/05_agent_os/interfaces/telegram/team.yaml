## Roska File Descriptor — telegram/team.py
file: telegram/team.py
lines: 84
layer: logic
purpose: 'Telegram Team Agent =================== Multi-agent team on Telegram with a Researcher and a Writer. The team leader coordinates: the Researcher gathers facts, then the Writer produces a concise summary suitable for Telegram. Key concepts: - ``Team`` with two specialist ``Agent`` members demonstrates delegation. - The team itself is passed to the Telegram interface, not individual agents. - SQLite session persistence keeps conversation history across restarts. Setup: Set TELEGRAM_TOKEN env var from @BotFather.'
imports:
- sym: agno.agent.Agent
- sym: agno.db.sqlite.SqliteDb
- sym: agno.models.openai.OpenAIChat
- sym: agno.os.app.AgentOS
- sym: agno.os.interfaces.telegram.Telegram
- sym: agno.team.Team
data:
- name: agent_db
  type: Any
  val: |-
    SqliteDb(
        session_table="telegram_team_sessions", db_file="tmp/telegram_team.db"
    )
- name: researcher
  type: Any
  val: |-
    Agent(
        name="Researcher",
        model=OpenAIChat(id="gpt-4o-mini"),
        role="Researches topics and provides detailed ...
- name: writer
  type: Any
  val: |-
    Agent(
        name="Writer",
        model=OpenAIChat(id="gpt-4o-mini"),
        role="Takes research and writes clear, engaging su...
- name: telegram_team
  type: Any
  val: |-
    Team(
        name="Telegram Research Team",
        model=OpenAIChat(id="gpt-4o-mini"),
        members=[researcher, writer],
        d...
- name: agent_os
  type: Any
  val: |-
    AgentOS(
        teams=[telegram_team],
        interfaces=[
            Telegram(
                team=telegram_team,
                reply...
- name: app
  type: Any
  val: agent_os.get_app()
exports:
- name: agent_db
  kind: data
- name: researcher
  kind: data
- name: writer
  kind: data
- name: telegram_team
  kind: data
- name: agent_os
  kind: data
- name: app
  kind: data
