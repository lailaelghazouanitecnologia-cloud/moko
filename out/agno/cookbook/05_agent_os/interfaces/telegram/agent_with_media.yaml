## Roska File Descriptor — telegram/agent_with_media.py
file: telegram/agent_with_media.py
lines: 82
layer: logic
purpose: 'Telegram Media Agent ==================== Multimedia Telegram bot that can generate images with DALL-E, produce speech and sound effects with ElevenLabs, and analyze images, audio, and video that users send. Key concepts: - ``DalleTools`` for image generation from text prompts. - ``ElevenLabsTools`` for text-to-speech and sound effect generation. - Telegram interface automatically sends generated media files back to chat. Setup: Set TELEGRAM_TOKEN env var from @BotFather.'
imports:
- sym: agno.agent.Agent
- sym: agno.db.sqlite.SqliteDb
- sym: agno.models.google.Gemini
- sym: agno.os.app.AgentOS
- sym: agno.os.interfaces.telegram.Telegram
- sym: agno.tools.dalle.DalleTools
- sym: agno.tools.eleven_labs.ElevenLabsTools
data:
- name: agent_db
  type: Any
  val: |-
    SqliteDb(
        session_table="telegram_media_sessions", db_file="tmp/telegram_media.db"
    )
- name: media_agent
  type: Any
  val: |-
    Agent(
        name="Media Agent",
        model=Gemini(id="gemini-2.5-pro"),
        db=agent_db,
        tools=[
            DalleTools(mo...
- name: agent_os
  type: Any
  val: |-
    AgentOS(
        agents=[media_agent],
        interfaces=[
            Telegram(
                agent=media_agent,
                reply_t...
- name: app
  type: Any
  val: agent_os.get_app()
exports:
- name: agent_db
  kind: data
- name: media_agent
  kind: data
- name: agent_os
  kind: data
- name: app
  kind: data
