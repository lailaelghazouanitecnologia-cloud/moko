## Roska File Descriptor — run_params/workflow_all_params.py
file: run_params/workflow_all_params.py
lines: 113
layer: logic
purpose: 'Workflow All Run Params ======================= Demonstrates using all workflow run-level parameters together in a realistic content creation pipeline. This example shows: - metadata: Tagging runs with project and environment info - dependencies: Injecting configuration (tone, word count, target audience) - add_dependencies_to_context: Making config visible to agents - add_session_state_to_context: Making session state visible to agents'
imports:
- sym: asyncio
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIChat
- sym: agno.workflow.step.Step
- sym: agno.workflow.workflow.Workflow
data:
- name: researcher
  type: Any
  val: |-
    Agent(
        name="Content Researcher",
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions=[
            "You are a cont...
- name: writer
  type: Any
  val: |-
    Agent(
        name="Content Writer",
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions=[
            "You are a content ...
- name: research_step
  type: Any
  val: |-
    Step(
        name="Research",
        description="Research the topic",
        agent=researcher,
    )
- name: writing_step
  type: Any
  val: |-
    Step(
        name="Write",
        description="Write the article based on research",
        agent=writer,
    )
- name: content_pipeline
  type: Any
  val: |-
    Workflow(
        name="Content Pipeline",
        steps=[research_step, writing_step],
        # Workflow-level metadata (always pr...
exports:
- name: researcher
  kind: data
- name: writer
  kind: data
- name: research_step
  kind: data
- name: writing_step
  kind: data
- name: content_pipeline
  kind: data
