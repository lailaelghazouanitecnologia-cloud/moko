## Roska File Descriptor — background_execution/websocket_server.py
file: background_execution/websocket_server.py
lines: 263
layer: logic
purpose: Background Execution WebSocket Server ===================================== Demonstrates running background workflows and streaming workflow events over WebSocket.
imports:
- sym: uvicorn
- sym: agno.agent.Agent
- sym: agno.db.sqlite.SqliteDb
- sym: agno.models.openai.OpenAIChat
- sym: agno.tools.hackernews.HackerNewsTools
- sym: agno.tools.websearch.WebSearchTools
- sym: agno.workflow.step.Step
- sym: agno.workflow.workflow.Workflow
- sym: fastapi.FastAPI
- sym: fastapi.WebSocket
- sym: fastapi.WebSocketDisconnect
data:
- name: SECURITY_KEY
  type: Any
  val: os.getenv("SECURITY_KEY", "your-secret-key")
- name: hackernews_agent
  type: Any
  val: |-
    Agent(
        name="HackerNews Researcher",
        model=OpenAIChat(id="gpt-4o-mini"),
        tools=[HackerNewsTools()],
        inst...
- name: search_agent
  type: Any
  val: |-
    Agent(
        name="Search Agent",
        model=OpenAIChat(id="gpt-4o-mini"),
        tools=[WebSearchTools()],
        instructions="...
- name: app
  type: Any
  val: FastAPI(title="Background Workflow WebSocket Server")
- name: active_connections
  type: Any
  val: Dict[str, WebSocket]
- name: authenticated_connections
  type: Any
  val: Dict[str, bool]
functions:
- name: validate_token
  sig: 'validate_token(token: str) -> bool'
  lines:
  - 57
  - 62
  detail: {}
- name: get
  sig: get()
  is_async: true
  decorators:
  - app.get("/")
  lines:
  - 66
  - 77
  detail:
    calls:
    - authenticated_connections.values
    - len
- name: websocket_endpoint
  sig: 'websocket_endpoint(websocket: WebSocket)'
  is_async: true
  decorators:
  - app.websocket("/ws")
  lines:
  - 81
  - 182
  detail:
    calls:
    - authenticated_connections.get
    - handle_start_workflow
    - json.dumps
    - json.loads
    - len
    - message_data.get
    - print
    - str
    - validate_token
    - websocket.accept
    - websocket.receive_text
    - websocket.send_text
- name: handle_start_workflow
  sig: 'handle_start_workflow(websocket: WebSocket, message_data: dict)'
  is_async: true
  lines:
  - 185
  - 245
  detail:
    calls:
    - SqliteDb
    - Step
    - Workflow
    - json.dumps
    - len
    - message_data.get
    - str
    - websocket.send_text
    - workflow.arun
exports:
- name: validate_token
  kind: func
- name: get
  kind: func
- name: websocket_endpoint
  kind: func
- name: handle_start_workflow
  kind: func
- name: SECURITY_KEY
  kind: data
- name: hackernews_agent
  kind: data
- name: search_agent
  kind: data
- name: app
  kind: data
- name: active_connections
  kind: data
- name: authenticated_connections
  kind: data
