## Roska File Descriptor — 03_loop_execution/loop_iterative_accumulation.py
file: 03_loop_execution/loop_iterative_accumulation.py
lines: 55
layer: logic
purpose: 'Loop Iterative Accumulation ============================ Demonstrates that Loop iterations carry forward the output from the previous iteration. Each iteration receives the previous iteration''s output via `step_input.get_last_step_content()`, enabling iterative processing patterns like accumulation, refinement, and convergence. This example increments a numeric value by 10 each iteration, stopping when it reaches 50 or more. Starting from 35, the loop should: - Iteration 1: 35 -> 45 - Iteration 2: 45 -> 55 (>= 50, end condition met)'
imports:
- sym: agno.workflow.Loop
- sym: agno.workflow.Step
- sym: agno.workflow.Workflow
- sym: agno.workflow.types.StepInput
- sym: agno.workflow.types.StepOutput
data:
- name: workflow
  type: Any
  val: |-
    Workflow(
        name="Iterative Accumulation Workflow",
        description="Demonstrates loop iterations carrying forward out...
functions:
- name: increment_executor
  sig: 'increment_executor(step_input: StepInput) -> StepOutput'
  lines:
  - 19
  - 25
  detail:
    calls:
    - StepOutput
    - int
    - last_content.isdigit
    - step_input.get_last_step_content
    - str
exports:
- name: increment_executor
  kind: func
- name: workflow
  kind: data
