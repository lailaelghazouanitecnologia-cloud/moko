## Roska File Descriptor — router/cel_ternary.py
file: router/cel_ternary.py
lines: 66
layer: logic
purpose: 'Router with CEL expression: ternary operator on input content. ============================================================== Uses a CEL ternary to pick between two steps based on whether the input mentions "video" or not. Requirements: pip install cel-python'
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIChat
- sym: agno.workflow.CEL_AVAILABLE
- sym: agno.workflow.Step
- sym: agno.workflow.Workflow
- sym: agno.workflow.router.Router
data:
- name: video_agent
  type: Any
  val: |-
    Agent(
        name="Video Specialist",
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions="You specialize in video co...
- name: image_agent
  type: Any
  val: |-
    Agent(
        name="Image Specialist",
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions="You specialize in image de...
- name: workflow
  type: Any
  val: |-
    Workflow(
        name="CEL Ternary Router",
        steps=[
            Router(
                name="Media Router",
                select...
exports:
- name: video_agent
  kind: data
- name: image_agent
  kind: data
- name: workflow
  kind: data
