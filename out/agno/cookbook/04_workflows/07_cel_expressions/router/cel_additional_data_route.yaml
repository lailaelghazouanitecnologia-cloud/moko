## Roska File Descriptor — router/cel_additional_data_route.py
file: router/cel_additional_data_route.py
lines: 80
layer: logic
purpose: 'Router with CEL expression: route from additional_data field. ============================================================= Uses additional_data.route to let the caller specify which step to run, useful when the routing decision is made upstream (e.g. UI). Requirements: pip install cel-python'
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIChat
- sym: agno.workflow.CEL_AVAILABLE
- sym: agno.workflow.Step
- sym: agno.workflow.Workflow
- sym: agno.workflow.router.Router
data:
- name: email_agent
  type: Any
  val: |-
    Agent(
        name="Email Writer",
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions="You write professional emails....
- name: blog_agent
  type: Any
  val: |-
    Agent(
        name="Blog Writer",
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions="You write engaging blog posts w...
- name: tweet_agent
  type: Any
  val: |-
    Agent(
        name="Tweet Writer",
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions="You write punchy tweets. Keep ...
- name: workflow
  type: Any
  val: |-
    Workflow(
        name="CEL Additional Data Router",
        steps=[
            Router(
                name="Content Format Router",
     ...
exports:
- name: email_agent
  kind: data
- name: blog_agent
  kind: data
- name: tweet_agent
  kind: data
- name: workflow
  kind: data
