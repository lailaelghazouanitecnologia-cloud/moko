## Roska File Descriptor — router/cel_using_step_choices.py
file: router/cel_using_step_choices.py
lines: 78
layer: logic
purpose: 'Router with CEL: route using step_choices index. ================================================ Uses step_choices[0], step_choices[1], etc. to reference steps by their position in the choices list, rather than hardcoding step names. This is useful when you want to: - Avoid typos in step names - Make the CEL expression more maintainable - Reference steps dynamically based on index Requirements: pip install cel-python'
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIChat
- sym: agno.workflow.CEL_AVAILABLE
- sym: agno.workflow.Step
- sym: agno.workflow.Workflow
- sym: agno.workflow.router.Router
data:
- name: quick_analyzer
  type: Any
  val: |-
    Agent(
        name="Quick Analyzer",
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions="Provide a brief, concise ana...
- name: detailed_analyzer
  type: Any
  val: |-
    Agent(
        name="Detailed Analyzer",
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions="Provide a comprehensive, ...
- name: workflow
  type: Any
  val: |-
    Workflow(
        name="CEL Step Choices Router",
        steps=[
            Router(
                name="Analysis Router",
              ...
exports:
- name: quick_analyzer
  kind: data
- name: detailed_analyzer
  kind: data
- name: workflow
  kind: data
