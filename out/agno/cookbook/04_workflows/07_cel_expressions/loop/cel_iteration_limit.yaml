## Roska File Descriptor — loop/cel_iteration_limit.py
file: loop/cel_iteration_limit.py
lines: 59
layer: logic
purpose: 'Loop with CEL end condition: stop after N iterations. ===================================================== Uses current_iteration to stop after a specific number of iterations, independent of max_iterations. Requirements: pip install cel-python'
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIChat
- sym: agno.workflow.CEL_AVAILABLE
- sym: agno.workflow.Loop
- sym: agno.workflow.Step
- sym: agno.workflow.Workflow
data:
- name: writer
  type: Any
  val: |-
    Agent(
        name="Writer",
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions="Write a short paragraph expanding on...
- name: workflow
  type: Any
  val: |-
    Workflow(
        name="CEL Iteration Limit Loop",
        steps=[
            Loop(
                name="Writing Loop",
                ma...
exports:
- name: writer
  kind: data
- name: workflow
  kind: data
