## Roska File Descriptor — condition/cel_basic.py
file: condition/cel_basic.py
lines: 69
layer: logic
purpose: 'Condition with CEL expression: route based on input content. ============================================================ Uses input.contains() to check whether the request is urgent, branching to different agents via if/else steps. Requirements: pip install cel-python'
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIChat
- sym: agno.workflow.CEL_AVAILABLE
- sym: agno.workflow.Condition
- sym: agno.workflow.Step
- sym: agno.workflow.Workflow
data:
- name: urgent_handler
  type: Any
  val: |-
    Agent(
        name="Urgent Handler",
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions="You handle urgent requests w...
- name: normal_handler
  type: Any
  val: |-
    Agent(
        name="Normal Handler",
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions="You handle normal requests t...
- name: workflow
  type: Any
  val: |-
    Workflow(
        name="CEL Input Routing",
        steps=[
            Condition(
                name="Urgent Check",
                eval...
exports:
- name: urgent_handler
  kind: data
- name: normal_handler
  kind: data
- name: workflow
  kind: data
