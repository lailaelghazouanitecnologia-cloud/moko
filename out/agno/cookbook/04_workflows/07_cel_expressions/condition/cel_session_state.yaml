## Roska File Descriptor — condition/cel_session_state.py
file: condition/cel_session_state.py
lines: 99
layer: logic
purpose: 'Condition with CEL expression: branching on session_state. ========================================================== Uses session_state.retry_count to implement retry logic. Runs the workflow multiple times to show the counter incrementing and eventually hitting the max retries branch. Requirements: pip install cel-python'
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIChat
- sym: agno.workflow.CEL_AVAILABLE
- sym: agno.workflow.Condition
- sym: agno.workflow.Step
- sym: agno.workflow.StepInput
- sym: agno.workflow.StepOutput
- sym: agno.workflow.Workflow
data:
- name: retry_agent
  type: Any
  val: |-
    Agent(
        name="Retry Handler",
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions="You are handling a retry atte...
- name: max_retries_agent
  type: Any
  val: |-
    Agent(
        name="Max Retries Handler",
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions="Maximum retries reached...
- name: workflow
  type: Any
  val: |-
    Workflow(
        name="CEL Retry Logic",
        steps=[
            Step(name="Increment Retry", executor=increment_retry_count),
    ...
functions:
- name: increment_retry_count
  sig: 'increment_retry_count(step_input: StepInput, session_state: dict) -> StepOutput'
  lines:
  - 34
  - 41
  detail:
    calls:
    - StepOutput
    - session_state.get
- name: reset_retry_count
  sig: 'reset_retry_count(step_input: StepInput, session_state: dict) -> StepOutput'
  lines:
  - 44
  - 47
exports:
- name: increment_retry_count
  kind: func
- name: reset_retry_count
  kind: func
- name: retry_agent
  kind: data
- name: max_retries_agent
  kind: data
- name: workflow
  kind: data
