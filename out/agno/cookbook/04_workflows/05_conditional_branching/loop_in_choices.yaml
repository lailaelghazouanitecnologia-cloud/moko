## Roska File Descriptor — 05_conditional_branching/loop_in_choices.py
file: 05_conditional_branching/loop_in_choices.py
lines: 85
layer: logic
purpose: Loop In Choices =============== Demonstrates using a `Loop` component as one of the router choices.
imports:
- sym: agno.agent.agent.Agent
- sym: agno.models.openai.OpenAIChat
- sym: agno.workflow.loop.Loop
- sym: agno.workflow.router.Router
- sym: agno.workflow.step.Step
- sym: agno.workflow.types.StepInput
- sym: agno.workflow.types.StepOutput
- sym: agno.workflow.workflow.Workflow
data:
- name: draft_writer
  type: Any
  val: |-
    Agent(
        name="draft_writer",
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions="Write a draft on the given top...
- name: refiner
  type: Any
  val: |-
    Agent(
        name="refiner",
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions="Refine and improve the given draft....
- name: quick_response
  type: Any
  val: |-
    Step(
        name="quick_response",
        executor=lambda x: StepOutput(content=f"Quick answer: {x.input}"),
    )
- name: refinement_loop
  type: Any
  val: |-
    Loop(
        name="refinement_loop",
        steps=[Step(name="refine_step", agent=refiner)],
        max_iterations=2,
    )
- name: workflow
  type: Any
  val: |-
    Workflow(
        name="Loop Choice Routing",
        steps=[
            Router(
                name="Content Router",
                sel...
functions:
- name: loop_selector
  sig: |-
    loop_selector(
        step_input: StepInput,
        step_choices: list,
    ) -> Union[str, Step, List[Step]]
  lines:
  - 51
  - 61
  detail:
    calls:
    - step_input.input.lower
exports:
- name: loop_selector
  kind: func
- name: draft_writer
  kind: data
- name: refiner
  kind: data
- name: quick_response
  kind: data
- name: refinement_loop
  kind: data
- name: workflow
  kind: data
