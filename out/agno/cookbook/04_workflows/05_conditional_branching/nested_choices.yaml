## Roska File Descriptor — 05_conditional_branching/nested_choices.py
file: 05_conditional_branching/nested_choices.py
lines: 57
layer: logic
purpose: Nested Choices ============== Demonstrates nested lists in router choices, which are converted into sequential `Steps` containers.
imports:
- sym: agno.agent.agent.Agent
- sym: agno.models.openai.OpenAIChat
- sym: agno.workflow.router.Router
- sym: agno.workflow.step.Step
- sym: agno.workflow.types.StepInput
- sym: agno.workflow.workflow.Workflow
data:
- name: step_a
  type: Any
  val: Agent(name="step_a", model=OpenAIChat(id="gpt-4o-mini"), instructions="Step A")
- name: step_b
  type: Any
  val: Agent(name="step_b", model=OpenAIChat(id="gpt-4o-mini"), instructions="Step B")
- name: step_c
  type: Any
  val: Agent(name="step_c", model=OpenAIChat(id="gpt-4o-mini"), instructions="Step C")
- name: workflow
  type: Any
  val: |-
    Workflow(
        name="Nested Choices Routing",
        steps=[
            Router(
                name="Nested Router",
                s...
functions:
- name: nested_selector
  sig: |-
    nested_selector(
        step_input: StepInput,
        step_choices: list,
    ) -> Union[str, Step, List[Step]]
  lines:
  - 28
  - 36
  detail:
    calls:
    - step_input.input.lower
exports:
- name: nested_selector
  kind: func
- name: step_a
  kind: data
- name: step_b
  kind: data
- name: step_c
  kind: data
- name: workflow
  kind: data
