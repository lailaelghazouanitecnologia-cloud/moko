## Roska File Descriptor — 05_conditional_branching/step_choices_parameter.py
file: 05_conditional_branching/step_choices_parameter.py
lines: 78
layer: logic
purpose: Step Choices Parameter ====================== Demonstrates using `step_choices` in a router selector for dynamic step selection.
imports:
- sym: agno.agent.agent.Agent
- sym: agno.models.openai.OpenAIChat
- sym: agno.workflow.router.Router
- sym: agno.workflow.step.Step
- sym: agno.workflow.types.StepInput
- sym: agno.workflow.workflow.Workflow
data:
- name: researcher
  type: Any
  val: |-
    Agent(
        name="researcher",
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions="You are a researcher.",
    )
- name: writer
  type: Any
  val: |-
    Agent(
        name="writer",
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions="You are a writer.",
    )
- name: reviewer
  type: Any
  val: |-
    Agent(
        name="reviewer",
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions="You are a reviewer.",
    )
- name: workflow
  type: Any
  val: |-
    Workflow(
        name="Dynamic Routing (step_choices)",
        steps=[
            Router(
                name="Dynamic Router",
        ...
functions:
- name: dynamic_selector
  sig: |-
    dynamic_selector(
        step_input: StepInput,
        step_choices: list,
    ) -> Union[str, Step, List[Step]]
  lines:
  - 42
  - 57
  detail:
    calls:
    - hasattr
    - list
    - print
    - step_input.input.lower
    - step_map.get
    - step_map.keys
exports:
- name: dynamic_selector
  kind: func
- name: researcher
  kind: data
- name: writer
  kind: data
- name: reviewer
  kind: data
- name: workflow
  kind: data
