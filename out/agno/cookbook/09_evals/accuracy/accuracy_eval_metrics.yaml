## Roska File Descriptor — accuracy/accuracy_eval_metrics.py
file: accuracy/accuracy_eval_metrics.py
lines: 86
layer: logic
purpose: Accuracy Eval Metrics ===================== Demonstrates that eval model metrics can be accumulated into the original agent's run_output using the run_response parameter on evaluate_answer. The evaluator agent's token usage appears under "eval_model" in run_output.metrics.details alongside the agent's own "model" entries.
imports:
- sym: agno.agent.Agent
- sym: agno.eval.accuracy.AccuracyEval
- sym: agno.models.openai.OpenAIChat
- sym: rich.pretty.pprint
data:
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions="Answer factual questions concisely.",
    )
- name: evaluation
  type: Any
  val: |-
    AccuracyEval(
        name="Capital Cities",
        model=OpenAIChat(id="gpt-4o-mini"),
        agent=agent,
        input="What is the...
exports:
- name: agent
  kind: data
- name: evaluation
  kind: data
