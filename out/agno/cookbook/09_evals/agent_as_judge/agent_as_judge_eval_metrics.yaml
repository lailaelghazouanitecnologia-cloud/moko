## Roska File Descriptor — agent_as_judge/agent_as_judge_eval_metrics.py
file: agent_as_judge/agent_as_judge_eval_metrics.py
lines: 62
layer: logic
purpose: Agent-as-Judge Eval Metrics ============================ Demonstrates that eval model metrics are accumulated back into the original agent's run_output when AgentAsJudgeEval is used as a post_hook. After the agent runs, the evaluator agent makes its own model call. Those eval tokens show up under "eval_model" in run_output.metrics.details.
imports:
- sym: agno.agent.Agent
- sym: agno.eval.agent_as_judge.AgentAsJudgeEval
- sym: agno.models.openai.OpenAIChat
- sym: rich.pretty.pprint
data:
- name: eval_hook
  type: Any
  val: |-
    AgentAsJudgeEval(
        name="Quality Check",
        model=OpenAIChat(id="gpt-4o-mini"),
        criteria="Response should be acc...
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o-mini"),
        instructions="Answer questions concisely.",
        post_hooks=[eval_hook...
exports:
- name: eval_hook
  kind: data
- name: agent
  kind: data
