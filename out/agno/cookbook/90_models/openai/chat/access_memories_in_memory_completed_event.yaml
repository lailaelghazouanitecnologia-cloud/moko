## Roska File Descriptor — chat/access_memories_in_memory_completed_event.py
file: chat/access_memories_in_memory_completed_event.py
lines: 90
layer: logic
purpose: 'Test script to verify memory events are working correctly. Steps: 1. Run: `./cookbook/scripts/run_pgvector.sh` to start a postgres container with pgvector 2. Run: `pip install openai sqlalchemy ''psycopg[binary]'' pgvector` to install the dependencies 3. Run: `python cookbook/11_models/openai/chat/access_memories_in_memory_completed_event.py`'
imports:
- sym: agno.agent.Agent
- sym: agno.db.postgres.PostgresDb
- sym: agno.models.openai.OpenAIChat
- sym: agno.run.agent.RunEvent
data:
- name: db_url
  type: str
  val: '"postgresql+psycopg://ai:ai@localhost:5532/ai"'
- name: db
  type: Any
  val: PostgresDb(db_url=db_url)
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-5-mini"),
        user_id="test_user",
        session_id="test_session",
        db=db,
        ena...
functions:
- name: run_with_events
  sig: 'run_with_events(message: str)'
  lines:
  - 31
  - 72
  detail:
    calls:
    - agent.run
    - hasattr
    - len
    - print
exports:
- name: run_with_events
  kind: func
- name: db_url
  kind: data
- name: db
  kind: data
- name: agent
  kind: data
