## Roska File Descriptor — dashscope/retry.py
file: dashscope/retry.py
lines: 29
layer: logic
purpose: Example demonstrating how to set up retries with DashScope.
imports:
- sym: agno.agent.Agent
- sym: agno.models.dashscope.DashScope
data:
- name: wrong_model_id
  type: str
  val: '"dashscope-wrong-id"'
- name: agent
  type: Any
  val: |-
    Agent(
        model=DashScope(
            id=wrong_model_id,
            retries=3,  # Number of times to retry the request.
         ...
exports:
- name: wrong_model_id
  kind: data
- name: agent
  kind: data
