## Roska File Descriptor — cohere/memory.py
file: cohere/memory.py
lines: 45
layer: logic
purpose: 'This recipe shows how to use personalized memories and summaries in an agent. Steps: 1. Run: `./cookbook/scripts/run_pgvector.sh` to start a postgres container with pgvector 2. Run: `uv pip install cohere sqlalchemy ''psycopg[binary]'' pgvector` to install the dependencies 3. Run: `python cookbook/92_models/cohere/memory.py` to run the agent'
imports:
- sym: agno.agent.agent.Agent
- sym: agno.db.postgres.PostgresDb
- sym: agno.models.cohere.Cohere
data:
- name: db_url
  type: str
  val: '"postgresql+psycopg://ai:ai@localhost:5532/ai"'
- name: agent
  type: Any
  val: |-
    Agent(
        model=Cohere(id="command-a-03-2025"),
        # Store agent sessions in a database
        db=PostgresDb(db_url=db_ur...
exports:
- name: db_url
  kind: data
- name: agent
  kind: data
