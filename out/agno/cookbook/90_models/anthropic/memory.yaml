## Roska File Descriptor — anthropic/memory.py
file: anthropic/memory.py
lines: 49
layer: logic
purpose: 'This recipe shows how to use personalized memories and summaries in an agent. Steps: 1. Run: `./cookbook/scripts/run_pgvector.sh` to start a postgres container with pgvector 2. Run: `uv pip install anthropic sqlalchemy ''psycopg[binary]'' pgvector` to install the dependencies 3. Run: `python cookbook/92_models/anthropic/memory.py` to run the agent'
imports:
- sym: agno.agent.Agent
- sym: agno.db.postgres.PostgresDb
- sym: agno.models.anthropic.Claude
data:
- name: db_url
  type: str
  val: '"postgresql+psycopg://ai:ai@localhost:5532/ai"'
- name: db
  type: Any
  val: PostgresDb(db_url=db_url)
- name: agent
  type: Any
  val: |-
    Agent(
        model=Claude(id="claude-sonnet-4-20250514"),
        # Pass the database to the Agent
        db=db,
        # Store the ...
exports:
- name: db_url
  kind: data
- name: db
  kind: data
- name: agent
  kind: data
