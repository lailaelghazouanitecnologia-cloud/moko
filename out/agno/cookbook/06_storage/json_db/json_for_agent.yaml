## Roska File Descriptor — json_db/json_for_agent.py
file: json_db/json_for_agent.py
lines: 35
layer: logic
purpose: Use JSON files as the database for an Agent. Useful for simple demos where performance is not critical. Run `uv pip install ddgs openai` to install dependencies.
imports:
- sym: agno.agent.Agent
- sym: agno.db.json.JsonDb
- sym: agno.models.openai.OpenAIChat
- sym: agno.tools.websearch.WebSearchTools
data:
- name: db
  type: Any
  val: JsonDb(db_path="tmp/json_db")
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-5.2"),
        db=db,
        session_id="session_storage",
        tools=[WebSearchTools()],
      ...
exports:
- name: db
  kind: data
- name: agent
  kind: data
