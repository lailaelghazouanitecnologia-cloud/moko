## Roska File Descriptor — async_mongo/async_mongodb_for_agent.py
file: async_mongo/async_mongodb_for_agent.py
lines: 46
layer: logic
purpose: 'Use AsyncMongoDb as the database for an agent. Run `uv pip install openai pymongo motor` to install dependencies Run a local MongoDB server using: ```bash docker run -d \ --name local-mongo \ -p 27017:27017 \ -e MONGO_INITDB_ROOT_USERNAME=mongoadmin \ -e MONGO_INITDB_ROOT_PASSWORD=secret \ mongo ``` or use our script: ```bash ./scripts/run_mongodb.sh ```'
imports:
- sym: asyncio
- sym: agno.agent.Agent
- sym: agno.db.mongo.AsyncMongoDb
- sym: agno.tools.websearch.WebSearchTools
data:
- name: db_url
  type: str
  val: '"mongodb://mongoadmin:secret@localhost:27017"'
- name: db
  type: Any
  val: AsyncMongoDb(db_url=db_url)
- name: agent
  type: Any
  val: |-
    Agent(
        db=db,
        tools=[WebSearchTools()],
        add_history_to_context=True,
    )
exports:
- name: db_url
  kind: data
- name: db
  kind: data
- name: agent
  kind: data
