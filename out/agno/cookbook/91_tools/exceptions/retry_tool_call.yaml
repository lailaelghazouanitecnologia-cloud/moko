## Roska File Descriptor — exceptions/retry_tool_call.py
file: exceptions/retry_tool_call.py
lines: 66
layer: logic
purpose: Retry Tool Call ============================= Demonstrates retry tool call.
imports:
- sym: agno.agent.Agent
- sym: agno.db.sqlite.SqliteDb
- sym: agno.exceptions.RetryAgentRun
- sym: agno.models.openai.OpenAIChat
- sym: agno.run.RunContext
- sym: agno.utils.log.logger
data:
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-5.2"),
        session_id="retry_tool_call_session",
        db=SqliteDb(
            session_ta...
functions:
- name: add_item
  sig: 'add_item(run_context: RunContext, item: str) -> str'
  lines:
  - 20
  - 43
  detail:
    calls:
    - RetryAgentRun
    - len
    - logger.info
    - run_context.session_state.get
    - run_context.session_state["shopping_list"].append
exports:
- name: add_item
  kind: func
- name: agent
  kind: data
