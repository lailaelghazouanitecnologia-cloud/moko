## Roska File Descriptor — 91_tools/agentql_tools.py
file: 91_tools/agentql_tools.py
lines: 71
layer: logic
purpose: 'AgentQL Tools for scraping websites. Prerequisites: - Set the environment variable `AGENTQL_API_KEY` with your AgentQL API key. You can obtain the API key from the AgentQL website: https://agentql.com/ - Run `playwright install` to install a browser extension for playwright. AgentQL will open up a browser instance (don''t close it) and do scraping on the site.'
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIChat
- sym: agno.tools.agentql.AgentQLTools
data:
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            AgentQLTools(
                enable_scrape_website=True,
        ...
- name: agent_all
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[AgentQLTools(all=True, agentql_query="your_query_here")],
    )
- name: custom_query
  type: str
  val: |-
    """
    {
        title
        text_content[]
    }
    """
- name: custom_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            AgentQLTools(
                enable_scrape_website=True,
        ...
exports:
- name: agent
  kind: data
- name: agent_all
  kind: data
- name: custom_query
  kind: data
- name: custom_agent
  kind: data
