## Roska File Descriptor — google/gmail_followup_tracker.py
file: google/gmail_followup_tracker.py
lines: 85
layer: logic
purpose: 'Gmail Follow-Up Tracker ======================= Finds sent emails that never received a reply and drafts follow-ups. Key concepts: - Multi-step reasoning: agent must compare sender vs user email per thread - add_datetime_to_context: agent calculates days_waiting from message dates - output_schema: structured report of pending follow-ups Setup: 1. Create OAuth credentials at https://console.cloud.google.com (enable Gmail API) 2. Export GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_PROJECT_ID env vars 3. pip install openai google-api-python-client google-auth-httplib2 google-auth-oauthlib 4. First run opens browser for OAuth consent, saves token.json for reuse'
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIChat
- sym: agno.tools.google.gmail.GmailTools
- sym: pydantic.BaseModel
- sym: pydantic.Field
data:
- name: agent
  type: Any
  val: |-
    Agent(
        name="Follow-Up Tracker",
        model=OpenAIChat(id="gpt-4o"),
        tools=[GmailTools()],
        instructions=[
       ...
types:
- name: PendingFollowUp
  kind: model
  bases:
  - BaseModel
  fields:
  - name: thread_id
    type: Any
    default: str
  - name: subject
    type: Any
    default: str
  - name: recipient
    type: Any
    default: str
  - name: sent_date
    type: Any
    default: str
  - name: days_waiting
    type: Any
    default: int
  - name: draft_created
    type: Any
    default: bool
  lines:
  - 31
  - 39
- name: FollowUpReport
  kind: model
  bases:
  - BaseModel
  fields:
  - name: total_checked
    type: Any
    default: int
  - name: needs_followup
    type: Any
    default: List[PendingFollowUp]
  - name: summary
    type: Any
    default: str
  lines:
  - 42
  - 47
exports:
- name: PendingFollowUp
  kind: type
- name: FollowUpReport
  kind: type
- name: agent
  kind: data
