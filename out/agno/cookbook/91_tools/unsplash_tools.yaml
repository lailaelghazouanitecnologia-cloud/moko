## Roska File Descriptor — 91_tools/unsplash_tools.py
file: 91_tools/unsplash_tools.py
lines: 116
layer: logic
purpose: 'Unsplash Tools Example This example demonstrates how to use the UnsplashTools toolkit with an AI agent to search for and retrieve high-quality, royalty-free images from Unsplash. UnsplashTools provides: - search_photos: Search photos by keyword with filters (orientation, color) - get_photo: Get detailed info about a specific photo - get_random_photo: Get random photo(s) with optional query - download_photo: Track downloads for Unsplash API compliance Setup: 1. Get a free API key from https://unsplash.com/developers 2. Set the environment variable: export UNSPLASH_ACCESS_KEY="your_access_key" 3. Install dependencies: pip install openai agno Usage: python unsplash_tools.py'
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIChat
- sym: agno.tools.unsplash.UnsplashTools
data:
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[UnsplashTools()],
        instructions=[
            "You are a helpful ass...
- name: agent_with_download
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[UnsplashTools(enable_download_photo=True)],
        instructions=[
        ...
- name: agent_search_only
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            UnsplashTools(
                enable_search_photos=True,
        ...
exports:
- name: agent
  kind: data
- name: agent_with_download
  kind: data
- name: agent_search_only
  kind: data
