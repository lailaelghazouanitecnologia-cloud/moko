## Roska File Descriptor — 91_tools/websearch_tools_advanced.py
file: 91_tools/websearch_tools_advanced.py
lines: 327
layer: logic
purpose: 'WebSearch Tools - Advanced Configuration ========================================= Demonstrates advanced WebSearchTools configuration with timelimit, region, and backend parameters for customized search behavior across multiple search engines. Parameters: - timelimit: Filter results by time ("d" = day, "w" = week, "m" = month, "y" = year) - region: Localize results (e.g., "us-en", "uk-en", "de-de", "fr-fr", "ru-ru") - backend: Search backend ("auto", "duckduckgo", "google", "bing", "brave", "yandex", "yahoo")'
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIChat
- sym: agno.tools.websearch.WebSearchTools
data:
- name: daily_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            WebSearchTools(
                timelimit="d",  # Results from...
- name: weekly_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            WebSearchTools(
                timelimit="w",  # Results from...
- name: monthly_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            WebSearchTools(
                timelimit="m",  # Results from...
- name: yearly_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            WebSearchTools(
                timelimit="y",  # Results from...
- name: us_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            WebSearchTools(
                region="us-en",
                ba...
- name: uk_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            WebSearchTools(
                region="uk-en",
                ba...
- name: de_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            WebSearchTools(
                region="de-de",
                ba...
- name: fr_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            WebSearchTools(
                region="fr-fr",
                ba...
- name: ru_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            WebSearchTools(
                region="ru-ru",
                ba...
- name: duckduckgo_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            WebSearchTools(
                backend="duckduckgo",
            ...
- name: google_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            WebSearchTools(
                backend="google",
                ...
- name: bing_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            WebSearchTools(
                backend="bing",
                ti...
- name: brave_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            WebSearchTools(
                backend="brave",
                t...
- name: yandex_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            WebSearchTools(
                backend="yandex",
                ...
- name: yahoo_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            WebSearchTools(
                backend="yahoo",
                t...
- name: research_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            WebSearchTools(
                backend="auto",  # Auto-select...
- name: news_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            WebSearchTools(
                backend="auto",
                ti...
- name: us_regional
  type: Any
  val: create_regional_agent("us-en", "United States")
- name: uk_regional
  type: Any
  val: create_regional_agent("uk-en", "United Kingdom")
- name: de_regional
  type: Any
  val: create_regional_agent("de-de", "Germany")
functions:
- name: create_regional_agent
  sig: 'create_regional_agent(region: str, region_name: str) -> Agent'
  lines:
  - 264
  - 279
  detail:
    calls:
    - Agent
    - OpenAIChat
    - WebSearchTools
exports:
- name: create_regional_agent
  kind: func
- name: daily_agent
  kind: data
- name: weekly_agent
  kind: data
- name: monthly_agent
  kind: data
- name: yearly_agent
  kind: data
- name: us_agent
  kind: data
- name: uk_agent
  kind: data
- name: de_agent
  kind: data
- name: fr_agent
  kind: data
- name: ru_agent
  kind: data
- name: duckduckgo_agent
  kind: data
- name: google_agent
  kind: data
- name: bing_agent
  kind: data
- name: brave_agent
  kind: data
- name: yandex_agent
  kind: data
- name: yahoo_agent
  kind: data
- name: research_agent
  kind: data
- name: news_agent
  kind: data
- name: us_regional
  kind: data
- name: uk_regional
  kind: data
- name: de_regional
  kind: data
