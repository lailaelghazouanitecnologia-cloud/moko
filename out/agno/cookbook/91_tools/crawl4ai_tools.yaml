## Roska File Descriptor — 91_tools/crawl4ai_tools.py
file: 91_tools/crawl4ai_tools.py
lines: 80
layer: logic
purpose: 'Crawl4AI Tools - Web Scraping and Content Extraction This example demonstrates how to use Crawl4aiTools for web crawling and content extraction. Crawl4aiTools has a single enable_ flag: enable_crawl. Run: `uv pip install crawl4ai` to install the dependencies'
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIChat
- sym: agno.tools.crawl4ai.Crawl4aiTools
data:
- name: agent_full
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            Crawl4aiTools(use_pruning=True)
        ],  # All functions en...
- name: agent_basic
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            Crawl4aiTools(
                use_pruning=True,
                e...
- name: agent_raw
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[Crawl4aiTools(use_pruning=False)],
        description="You are a web i...
exports:
- name: agent_full
  kind: data
- name: agent_basic
  kind: data
- name: agent_raw
  kind: data
