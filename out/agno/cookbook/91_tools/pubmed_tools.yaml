## Roska File Descriptor — 91_tools/pubmed_tools.py
file: 91_tools/pubmed_tools.py
lines: 65
layer: logic
purpose: Pubmed Tools ============================= Demonstrates pubmed tools.
imports:
- sym: agno.agent.Agent
- sym: agno.tools.pubmed.PubmedTools
data:
- name: agent_all
  type: Any
  val: |-
    Agent(
        tools=[
            PubmedTools(
                all=True,  # Enable all PubMed search functions
            )
        ],
       ...
- name: agent_specific
  type: Any
  val: |-
    Agent(
        tools=[
            PubmedTools(
                enable_search_pubmed=True,  # Only enable the main search function
     ...
- name: agent
  type: Any
  val: |-
    Agent(
        tools=[
            PubmedTools(
                enable_search_pubmed=True,
            )
        ],
        markdown=True,
    )
exports:
- name: agent_all
  kind: data
- name: agent_specific
  kind: data
- name: agent
  kind: data
