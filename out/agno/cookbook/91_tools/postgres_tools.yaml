## Roska File Descriptor — 91_tools/postgres_tools.py
file: 91_tools/postgres_tools.py
lines: 75
layer: logic
purpose: Postgres Tools ============================= Demonstrates postgres tools.
imports:
- sym: agno.agent.Agent
- sym: agno.tools.postgres.PostgresTools
data:
- name: agent
  type: Any
  val: |-
    Agent(
        tools=[
            PostgresTools(
                host="localhost",
                port=5532,
                db_name="ai",...
- name: agent_readonly
  type: Any
  val: |-
    Agent(
        tools=[
            PostgresTools(
                host="localhost",
                port=5532,
                db_name="ai",...
- name: agent_safe
  type: Any
  val: |-
    Agent(
        tools=[
            PostgresTools(
                host="localhost",
                port=5532,
                db_name="ai",...
exports:
- name: agent
  kind: data
- name: agent_readonly
  kind: data
- name: agent_safe
  kind: data
