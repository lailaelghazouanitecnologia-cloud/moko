## Roska File Descriptor — 91_tools/duckduckgo_tools.py
file: 91_tools/duckduckgo_tools.py
lines: 55
layer: logic
purpose: Duckduckgo Tools ============================= Demonstrates duckduckgo tools.
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIChat
- sym: agno.tools.duckduckgo.DuckDuckGoTools
- sym: agno.tools.websearch.WebSearchTools
data:
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[DuckDuckGoTools(enable_search=True, enable_news=False)],
    )
- name: agent_all
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[DuckDuckGoTools(enable_search=True, enable_news=True)],
    )
- name: news_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[DuckDuckGoTools(enable_search=False, enable_news=True)],
    )
- name: yandex_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[WebSearchTools(enable_search=True, enable_news=False, backend="yand...
exports:
- name: agent
  kind: data
- name: agent_all
  kind: data
- name: news_agent
  kind: data
- name: yandex_agent
  kind: data
