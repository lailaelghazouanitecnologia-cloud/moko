## Roska File Descriptor — 91_tools/slack_tools.py
file: 91_tools/slack_tools.py
lines: 68
layer: logic
purpose: 'Run: pip install openai slack-sdk'
imports:
- sym: agno.agent.Agent
- sym: agno.tools.slack.SlackTools
data:
- name: agent_all
  type: Any
  val: |-
    Agent(
        tools=[
            SlackTools(
                all=True,  # Enable all Slack tools
            )
        ],
        markdown=Tru...
- name: agent_specific
  type: Any
  val: |-
    Agent(
        tools=[
            SlackTools(
                enable_send_message=True,
                enable_list_channels=True,
        ...
- name: agent_readonly
  type: Any
  val: |-
    Agent(
        tools=[
            SlackTools(
                enable_send_message=False,
                enable_list_channels=True,
       ...
exports:
- name: agent_all
  kind: data
- name: agent_specific
  kind: data
- name: agent_readonly
  kind: data
