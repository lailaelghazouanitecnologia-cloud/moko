## Roska File Descriptor — 91_tools/openweather_tools.py
file: 91_tools/openweather_tools.py
lines: 107
layer: logic
purpose: 'OpenWeatherMap API Integration Example This example demonstrates how to use the OpenWeatherTools to get weather data from the OpenWeatherMap API. Prerequisites: 1. Get an API key from https://openweathermap.org/api 2. Set the OPENWEATHER_API_KEY environment variable or pass it directly to the tool Usage: - Get current weather for a location - Get weather forecast for a location - Get air pollution data for a location - Geocode a location name to coordinates'
imports:
- sym: agno.agent.Agent
- sym: agno.tools.openweather.OpenWeatherTools
data:
- name: agent_all
  type: Any
  val: |-
    Agent(
        tools=[
            OpenWeatherTools(
                all=True,  # Enable all OpenWeather functions
                units...
- name: agent_specific
  type: Any
  val: |-
    Agent(
        tools=[
            OpenWeatherTools(
                enable_current_weather=True,
                enable_forecast=True,
    ...
- name: agent
  type: Any
  val: |-
    Agent(
        tools=[
            OpenWeatherTools(
                enable_current_weather=True,
                enable_forecast=True,
    ...
exports:
- name: agent_all
  kind: data
- name: agent_specific
  kind: data
- name: agent
  kind: data
