## Roska File Descriptor — models/gemini_video_generation.py
file: models/gemini_video_generation.py
lines: 45
layer: logic
purpose: 'Example: Using the GeminiTools Toolkit for Video Generation An Agent using the Gemini video generation tool. Video generation only works with Vertex AI. Make sure you have set the GOOGLE_CLOUD_PROJECT and GOOGLE_CLOUD_LOCATION environment variables. Example prompts to try: - "Generate a 5-second video of a kitten playing a piano" - "Create a short looping animation of a neon city skyline at dusk" Run `uv pip install google-genai agno` to install the necessary dependencies.'
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIChat
- sym: agno.tools.models.gemini.GeminiTools
- sym: agno.utils.media.save_base64_data
data:
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[GeminiTools(vertexai=True)],  # Video Generation only works on Vert...
exports:
- name: agent
  kind: data
