## Roska File Descriptor — 91_tools/duckduckgo_tools_advanced.py
file: 91_tools/duckduckgo_tools_advanced.py
lines: 200
layer: logic
purpose: 'DuckDuckGo Tools - Advanced Configuration ========================================== Demonstrates advanced DuckDuckGoTools configuration with timelimit, region, and backend parameters for customized search behavior. Parameters: - timelimit: Filter results by time ("d" = day, "w" = week, "m" = month, "y" = year) - region: Localize results (e.g., "us-en", "uk-en", "de-de", "fr-fr", "ru-ru") - backend: Search backend ("api", "html", "lite")'
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIChat
- sym: agno.tools.duckduckgo.DuckDuckGoTools
data:
- name: weekly_search_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            DuckDuckGoTools(
                timelimit="w",  # Results fro...
- name: us_region_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            DuckDuckGoTools(
                region="us-en",  # US English...
- name: api_backend_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            DuckDuckGoTools(
                backend="api",
                en...
- name: html_backend_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            DuckDuckGoTools(
                backend="html",
                e...
- name: lite_backend_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            DuckDuckGoTools(
                backend="lite",
                e...
- name: fully_configured_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            DuckDuckGoTools(
                timelimit="w",  # Results fro...
- name: eu_monthly_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            DuckDuckGoTools(
                timelimit="m",  # Results fro...
- name: daily_news_agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            DuckDuckGoTools(
                timelimit="d",  # Results fro...
exports:
- name: weekly_search_agent
  kind: data
- name: us_region_agent
  kind: data
- name: api_backend_agent
  kind: data
- name: html_backend_agent
  kind: data
- name: lite_backend_agent
  kind: data
- name: fully_configured_agent
  kind: data
- name: eu_monthly_agent
  kind: data
- name: daily_news_agent
  kind: data
