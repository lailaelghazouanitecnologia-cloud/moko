## Roska File Descriptor — workflows/save_custom_steps.py
file: workflows/save_custom_steps.py
lines: 110
layer: logic
purpose: Save Custom Executor Workflow Steps =================================== Demonstrates creating a workflow with custom executor steps, saving it to the database, and loading it back with a Registry.
imports:
- sym: agno.agent.Agent
- sym: agno.db.postgres.PostgresDb
- sym: agno.registry.Registry
- sym: agno.workflow.step.Step
- sym: agno.workflow.types.StepInput
- sym: agno.workflow.types.StepOutput
- sym: agno.workflow.workflow.Workflow
- sym: agno.workflow.workflow.get_workflow_by_id
data:
- name: db_url
  type: str
  val: '"postgresql+psycopg://ai:ai@localhost:5532/ai"'
- name: db
  type: Any
  val: PostgresDb(db_url=db_url)
- name: content_agent
  type: Any
  val: |-
    Agent(
        name="Content Creator",
        instructions="Create well-structured content from input data",
    )
- name: registry
  type: Any
  val: |-
    Registry(
        name="Custom Steps Registry",
        functions=[transform_content],
    )
- name: content_step
  type: Any
  val: |-
    Step(
        name="CreateContent",
        description="Create initial content using the agent",
        agent=content_agent,
    )
- name: transform_step
  type: Any
  val: |-
    Step(
        name="TransformContent",
        description="Transform the content using custom function",
        executor=transform...
- name: workflow
  type: Any
  val: |-
    Workflow(
        name="Custom Executor Workflow",
        description="Create content with agent, then transform with custom fu...
functions:
- name: transform_content
  sig: 'transform_content(step_input: StepInput) -> StepOutput'
  lines:
  - 37
  - 46
  detail:
    calls:
    - StepOutput
    - print
exports:
- name: transform_content
  kind: func
- name: db_url
  kind: data
- name: db
  kind: data
- name: content_agent
  kind: data
- name: registry
  kind: data
- name: content_step
  kind: data
- name: transform_step
  kind: data
- name: workflow
  kind: data
