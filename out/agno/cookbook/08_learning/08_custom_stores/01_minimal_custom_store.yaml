## Roska File Descriptor — 08_custom_stores/01_minimal_custom_store.py
file: 08_custom_stores/01_minimal_custom_store.py
lines: 233
layer: logic
purpose: 'Custom Store: Minimal Example ============================= Shows how to create a custom learning store by implementing the LearningStore protocol. This minimal example uses in-memory storage and demonstrates: - The LearningStore protocol methods you must implement - How to pass custom context (like project_id) via the store''s constructor - How to plug the custom store into LearningMachine For a database-backed example, see 02_custom_store_with_db.py'
imports:
- sym: agno.agent.Agent
- sym: agno.learn.LearningMachine
- sym: agno.learn.stores.protocol.LearningStore
- sym: agno.models.openai.OpenAIResponses
data:
- name: _project_data
  type: Any
  val: Dict[str, Dict[str, Any]]
- name: project_store
  type: Any
  val: |-
    ProjectContextStore(
        context={
            "project_id": "learning-machine",
            "team": "platform",
        },
    )
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIResponses(id="gpt-5.2"),
        learning=LearningMachine(
            custom_stores={
                "proje...
functions:
- name: ProjectContextStore
  sig: ''
  decorators:
  - dataclass
  lines:
  - 30
  - 171
exports:
- name: ProjectContextStore
  kind: func
- name: project_store
  kind: data
- name: agent
  kind: data
