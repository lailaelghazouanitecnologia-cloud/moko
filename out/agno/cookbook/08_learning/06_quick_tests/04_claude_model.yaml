## Roska File Descriptor — 06_quick_tests/04_claude_model.py
file: 06_quick_tests/04_claude_model.py
lines: 92
layer: logic
purpose: 'Claude Model Test ================= Tests learning with Claude instead of OpenAI. All other cookbooks use OpenAI (gpt-5.2). This test verifies that learning works with Claude models, ensuring the implementation is model-agnostic. Key things to verify: 1. Profile extraction works with Claude 2. Tool calls work correctly (Claude uses different tool format) 3. Background extraction completes successfully'
imports:
- sym: agno.agent.Agent
- sym: agno.db.postgres.PostgresDb
- sym: agno.learn.LearningMachine
- sym: agno.learn.LearningMode
- sym: agno.learn.UserProfileConfig
- sym: agno.models.anthropic.Claude
data:
- name: db
  type: Any
  val: PostgresDb(db_url="postgresql+psycopg://ai:ai@localhost:5532/ai")
- name: agent
  type: Any
  val: |-
    Agent(
        model=Claude(id="claude-sonnet-4-5"),  # Using Claude
        db=db,
        learning=LearningMachine(
            user_p...
exports:
- name: db
  kind: data
- name: agent
  kind: data
