## Roska File Descriptor — 06_quick_tests/03_no_db_graceful.py
file: 06_quick_tests/03_no_db_graceful.py
lines: 99
layer: logic
purpose: 'No-DB Graceful Handling Test ============================ Tests that learning gracefully handles missing database. Users might accidentally enable learning without providing a database. The system should: 1. Not crash 2. Log a warning (ideally) 3. Still respond to the user 4. Just skip the learning/persistence part This is critical for user experience - a missing DB should degrade gracefully, not explode.'
imports:
- sym: agno.agent.Agent
- sym: agno.learn.LearningMachine
- sym: agno.learn.LearningMode
- sym: agno.learn.UserProfileConfig
- sym: agno.models.openai.OpenAIResponses
data:
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIResponses(id="gpt-5.2"),
        learning=LearningMachine(
            user_profile=UserProfileConfig(
      ...
exports:
- name: agent
  kind: data
