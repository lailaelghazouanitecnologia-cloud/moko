## Roska File Descriptor — 06_quick_tests/01_async_user_profile.py
file: 06_quick_tests/01_async_user_profile.py
lines: 79
layer: logic
purpose: 'Async User Profile Test ======================= Tests the async path for user profile learning. All other cookbooks use sync (print_response). This test verifies that the async path (aprint_response) works correctly. This is critical because: - Background learning uses asyncio tasks in async mode - Different code paths for aprocess vs process - Potential race conditions in async context'
imports:
- sym: asyncio
- sym: agno.agent.Agent
- sym: agno.db.postgres.PostgresDb
- sym: agno.learn.LearningMachine
- sym: agno.learn.LearningMode
- sym: agno.learn.UserProfileConfig
- sym: agno.models.openai.OpenAIResponses
data:
- name: db
  type: Any
  val: PostgresDb(db_url="postgresql+psycopg://ai:ai@localhost:5532/ai")
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIResponses(id="gpt-5.2"),
        db=db,
        learning=LearningMachine(
            user_profile=UserProfil...
functions:
- name: main
  sig: main()
  is_async: true
  lines:
  - 44
  - 75
  detail:
    calls:
    - agent.aprint_response
    - agent.learning_machine.user_profile_store.print
    - print
exports:
- name: main
  kind: func
- name: db
  kind: data
- name: agent
  kind: data
