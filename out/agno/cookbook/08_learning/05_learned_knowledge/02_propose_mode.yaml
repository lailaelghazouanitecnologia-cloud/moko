## Roska File Descriptor — 05_learned_knowledge/02_propose_mode.py
file: 05_learned_knowledge/02_propose_mode.py
lines: 111
layer: logic
purpose: 'Learned Knowledge: Propose Mode (Deep Dive) =========================================== Agent proposes learnings, user confirms before saving. PROPOSE mode adds human quality control: 1. Agent identifies valuable insights 2. Agent proposes them to the user 3. User confirms before saving Use when quality matters more than speed. Compare with: 01_agentic_mode.py for automatic saving. See also: 01_basics/4_learned_knowledge.py for the basics.'
imports:
- sym: agno.agent.Agent
- sym: agno.db.postgres.PostgresDb
- sym: agno.knowledge.Knowledge
- sym: agno.knowledge.embedder.openai.OpenAIEmbedder
- sym: agno.learn.LearnedKnowledgeConfig
- sym: agno.learn.LearningMachine
- sym: agno.learn.LearningMode
- sym: agno.models.openai.OpenAIResponses
- sym: agno.vectordb.pgvector.PgVector
- sym: agno.vectordb.pgvector.SearchType
data:
- name: db_url
  type: str
  val: '"postgresql+psycopg://ai:ai@localhost:5532/ai"'
- name: db
  type: Any
  val: PostgresDb(db_url=db_url)
- name: knowledge
  type: Any
  val: |-
    Knowledge(
        vector_db=PgVector(
            db_url=db_url,
            table_name="propose_learnings",
            search_type=Se...
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIResponses(id="gpt-5.2"),
        db=db,
        instructions=(
            "When you discover a valuable insi...
exports:
- name: db_url
  kind: data
- name: db
  kind: data
- name: knowledge
  kind: data
- name: agent
  kind: data
