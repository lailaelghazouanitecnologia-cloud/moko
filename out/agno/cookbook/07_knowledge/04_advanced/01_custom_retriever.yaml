## Roska File Descriptor — 04_advanced/01_custom_retriever.py
file: 04_advanced/01_custom_retriever.py
lines: 92
layer: logic
purpose: 'Custom Retriever: Bypass the Knowledge Class ============================================== Sometimes you need full control over retrieval logic. Instead of using the Knowledge class, you can provide a custom retriever function. The function receives the query and returns a list of dicts. This is useful for: - Non-vector retrieval (SQL queries, API calls, file lookups) - Custom ranking logic - Combining multiple data sources with custom logic See also: ../01_getting_started/02_agentic_rag.py for standard Knowledge-based RAG.'
imports:
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIResponses
data:
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIResponses(id="gpt-5.2"),
        knowledge_retriever=company_retriever,
        markdown=True,
    )
functions:
- name: company_retriever
  sig: |-
    company_retriever(
        agent: Agent, query: str, num_documents: Optional[int] = None, **kwargs
    ) -> Optional[List[Union[Dict, str]]]
  lines:
  - 26
  - 64
  detail:
    calls:
    - any
    - doc["name"].lower
    - doc["name"].lower().split
    - documents.items
    - documents.values
    - list
    - query.lower
    - results.append
exports:
- name: company_retriever
  kind: func
- name: agent
  kind: data
