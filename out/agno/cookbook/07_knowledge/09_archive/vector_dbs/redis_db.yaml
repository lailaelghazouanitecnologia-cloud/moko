## Roska File Descriptor — vector_dbs/redis_db.py
file: vector_dbs/redis_db.py
lines: 77
layer: logic
purpose: 'Redis Vector DB =============== Demonstrates Redis-backed knowledge with sync and async flows. To get started, either set `REDIS_URL`, or start local Redis with: `./cookbook/scripts/run_redis.sh`'
imports:
- sym: asyncio
- sym: agno.agent.Agent
- sym: agno.knowledge.knowledge.Knowledge
- sym: agno.vectordb.redis.RedisVectorDb
- sym: agno.vectordb.search.SearchType
data:
- name: REDIS_URL
  type: Any
  val: os.getenv("REDIS_URL", "redis://localhost:6379/0")
- name: INDEX_NAME
  type: Any
  val: os.getenv("REDIS_INDEX", "agno_cookbook_vectors")
- name: vector_db
  type: Any
  val: |-
    RedisVectorDb(
        index_name=INDEX_NAME,
        redis_url=REDIS_URL,
        search_type=SearchType.vector,
    )
- name: knowledge
  type: Any
  val: |-
    Knowledge(
        name="My Redis Vector Knowledge Base",
        description="This knowledge base uses Redis + RedisVL as the v...
- name: agent
  type: Any
  val: Agent(knowledge=knowledge)
functions:
- name: run_sync
  sig: run_sync() -> None
  lines:
  - 51
  - 60
  detail:
    calls:
    - agent.print_response
    - knowledge.insert
- name: run_async
  sig: run_async() -> None
  is_async: true
  lines:
  - 63
  - 72
  detail:
    calls:
    - agent.aprint_response
    - knowledge.ainsert
exports:
- name: run_sync
  kind: func
- name: run_async
  kind: func
- name: REDIS_URL
  kind: data
- name: INDEX_NAME
  kind: data
- name: vector_db
  kind: data
- name: knowledge
  kind: data
- name: agent
  kind: data
