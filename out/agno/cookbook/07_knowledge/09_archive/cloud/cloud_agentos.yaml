## Roska File Descriptor — cloud/cloud_agentos.py
file: cloud/cloud_agentos.py
lines: 158
layer: logic
purpose: 'Cloud Content Sources with AgentOS ============================================================ Sets up an AgentOS app with Knowledge connected to multiple cloud storage backends (S3, GCS, SharePoint, GitHub, Azure Blob). Once running, the AgentOS API lets you browse sources, upload content from any configured source, and search the knowledge base. Run: python cookbook/07_knowledge/cloud/cloud_agentos.py Key Concepts: - Each source type has its own config: S3Config, GcsConfig, SharePointConfig, GitHubConfig, AzureBlobConfig - Configs are registered on Knowledge via `content_sources` parameter - Configs have factory methods (.file(), .folder()) to create content references - Content references are passed to knowledge.insert()'
imports:
- sym: agno.agent.Agent
- sym: agno.db.postgres.PostgresDb
- sym: agno.knowledge.knowledge.Knowledge
- sym: agno.knowledge.remote_content.AzureBlobConfig
- sym: agno.knowledge.remote_content.GitHubConfig
- sym: agno.knowledge.remote_content.S3Config
- sym: agno.knowledge.remote_content.SharePointConfig
- sym: agno.models.openai.OpenAIChat
- sym: agno.os.AgentOS
- sym: agno.vectordb.pgvector.PgVector
data:
- name: contents_db
  type: Any
  val: |-
    PostgresDb(
        db_url="postgresql+psycopg://ai:ai@localhost:5532/ai",
        knowledge_table="knowledge_contents",
    )
- name: vector_db
  type: Any
  val: |-
    PgVector(
        table_name="knowledge_vectors",
        db_url="postgresql+psycopg://ai:ai@localhost:5532/ai",
    )
- name: content_sources
  type: list
  val: '[]'
- name: knowledge
  type: Any
  val: |-
    Knowledge(
        name="Company Knowledge Base",
        description="Unified knowledge from multiple sources",
        contents_db...
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o-mini"),
        knowledge=knowledge,
        search_knowledge=True,
    )
- name: agent_os
  type: Any
  val: |-
    AgentOS(
        knowledge=[knowledge],
        agents=[agent],
    )
- name: app
  type: Any
  val: agent_os.get_app()
exports:
- name: contents_db
  kind: data
- name: vector_db
  kind: data
- name: content_sources
  kind: data
- name: knowledge
  kind: data
- name: agent
  kind: data
- name: agent_os
  kind: data
- name: app
  kind: data
