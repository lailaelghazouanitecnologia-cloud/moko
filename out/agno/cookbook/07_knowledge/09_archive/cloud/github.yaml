## Roska File Descriptor — cloud/github.py
file: cloud/github.py
lines: 101
layer: logic
purpose: 'GitHub Content Source for Knowledge ==================================== Load files and folders from GitHub repositories into your Knowledge base, then query them with an Agent. Authentication methods: - Personal Access Token (PAT): simple, set ``token`` - GitHub App: enterprise-grade, set ``app_id``, ``installation_id``, ``private_key`` Requirements: - PostgreSQL with pgvector: ``./cookbook/scripts/run_pgvector.sh`` - For private repos with PAT: GitHub fine-grained PAT with "Contents: read" permission - For GitHub App auth: ``pip install PyJWT cryptography`` Run this cookbook: python cookbook/07_knowledge/cloud/github.py'
imports:
- sym: agno.agent.Agent
- sym: agno.knowledge.knowledge.Knowledge
- sym: agno.knowledge.remote_content.GitHubConfig
- sym: agno.models.openai.OpenAIChat
- sym: agno.vectordb.pgvector.PgVector
data:
- name: github_config
  type: Any
  val: |-
    GitHubConfig(
        id="my-repo",
        name="My Repository",
        repo="owner/repo",  # Format: owner/repo
        token=getenv(...
- name: knowledge
  type: Any
  val: |-
    Knowledge(
        name="GitHub Knowledge",
        vector_db=PgVector(
            table_name="github_knowledge",
            db_url="p...
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-5.1"),
        name="GitHub Agent",
        knowledge=knowledge,
        search_knowledge=True,
    ...
exports:
- name: github_config
  kind: data
- name: knowledge
  kind: data
- name: agent
  kind: data
