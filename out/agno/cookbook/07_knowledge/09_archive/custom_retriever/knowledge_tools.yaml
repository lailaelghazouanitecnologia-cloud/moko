## Roska File Descriptor — custom_retriever/knowledge_tools.py
file: custom_retriever/knowledge_tools.py
lines: 48
layer: logic
purpose: 'Here is a tool with reasoning capabilities to allow agents to search and analyze information from a knowledge base. 1. Run: `uv pip install openai agno lancedb tantivy sqlalchemy` to install the dependencies 2. Export your OPENAI_API_KEY 3. Run: `python cookbook/07_knowledge/knowledge_tools.py` to run the agent'
imports:
- sym: agno.agent.Agent
- sym: agno.knowledge.embedder.openai.OpenAIEmbedder
- sym: agno.knowledge.knowledge.Knowledge
- sym: agno.models.openai.OpenAIChat
- sym: agno.tools.knowledge.KnowledgeTools
- sym: agno.vectordb.lancedb.LanceDb
- sym: agno.vectordb.lancedb.SearchType
data:
- name: agno_docs
  type: Any
  val: |-
    Knowledge(
        # Use LanceDB as the vector database and store embeddings in the `agno_docs` table
        vector_db=LanceDb(...
- name: knowledge_tools
  type: Any
  val: |-
    KnowledgeTools(
        knowledge=agno_docs,
        enable_think=True,
        enable_search=True,
        enable_analyze=True,
        add...
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[knowledge_tools],
        markdown=True,
    )
exports:
- name: agno_docs
  kind: data
- name: knowledge_tools
  kind: data
- name: agent
  kind: data
