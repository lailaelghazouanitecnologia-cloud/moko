## Roska File Descriptor — custom_retriever/retriever_with_dependencies.py
file: custom_retriever/retriever_with_dependencies.py
lines: 115
layer: logic
purpose: 'Example demonstrating custom knowledge retriever with runtime dependencies. This cookbook shows how to access dependencies passed at runtime (e.g., via agent.run(dependencies={...})) in a custom knowledge retriever function. Key points: 1. Add ''run_context'' parameter to your retriever function signature 2. Dependencies are automatically passed from run_context when available 3. Use dependencies to customize retrieval behavior based on user context'
imports:
- sym: asyncio
- sym: agno.agent.Agent
- sym: agno.knowledge.embedder.openai.OpenAIEmbedder
- sym: agno.knowledge.knowledge.Knowledge
- sym: agno.models.openai.OpenAIChat
- sym: agno.run.RunContext
- sym: agno.vectordb.pgvector.PgVector
data:
- name: db_url
  type: str
  val: '"postgresql+psycopg://ai:ai@localhost:5532/ai"'
- name: knowledge
  type: Any
  val: |-
    Knowledge(
        vector_db=PgVector(
            table_name="dependencies_knowledge",
            db_url=db_url,
            embedder=...
- name: agent
  type: Any
  val: |-
    Agent(
        name="KnowledgeAgent",
        model=OpenAIChat(id="gpt-4o"),
        knowledge=knowledge,
        knowledge_retriever=kn...
functions:
- name: knowledge_retriever
  sig: |-
    knowledge_retriever(
        query: str,
        agent: Optional[Agent] = None,
        num_documents: int = 5,
        run_context: Optional[RunContext] = None,
        **kwargs,
    ) -> Optional[list[dict]]
  lines:
  - 41
  - 89
  detail:
    calls:
    - dependencies.get
    - dependencies.keys
    - doc.to_dict
    - knowledge.search
    - len
    - list
    - print
exports:
- name: knowledge_retriever
  kind: func
- name: db_url
  kind: data
- name: knowledge
  kind: data
- name: agent
  kind: data
