## Roska File Descriptor — workflows/langfuse_via_openinference_workflows.py
file: workflows/langfuse_via_openinference_workflows.py
lines: 136
layer: logic
purpose: Langfuse Workflows Via OpenInference ==================================== Demonstrates tracing a multi-step Agno workflow in Langfuse.
imports:
- sym: base64
- sym: agno.agent.Agent
- sym: agno.tools.websearch.WebSearchTools
- sym: agno.workflow.condition.Condition
- sym: agno.workflow.step.Step
- sym: agno.workflow.types.StepInput
- sym: agno.workflow.workflow.Workflow
- sym: openinference.instrumentation.agno.AgnoInstrumentor
- sym: opentelemetry.exporter.otlp.proto.http.trace_exporter.OTLPSpanExporter
- sym: opentelemetry.sdk.trace.TracerProvider
- sym: opentelemetry.sdk.trace.export.SimpleSpanProcessor
data:
- name: LANGFUSE_AUTH
  type: Any
  val: |-
    base64.b64encode(
        f"{os.getenv('LANGFUSE_PUBLIC_KEY')}:{os.getenv('LANGFUSE_SECRET_KEY')}".encode()
    ).decode()
- name: tracer_provider
  type: Any
  val: TracerProvider()
- name: researcher
  type: Any
  val: |-
    Agent(
        name="Researcher",
        instructions="Research the given topic and provide detailed findings.",
        tools=[Web...
- name: summarizer
  type: Any
  val: |-
    Agent(
        name="Summarizer",
        instructions="Create a clear summary of the research findings.",
    )
- name: fact_checker
  type: Any
  val: |-
    Agent(
        name="Fact Checker",
        instructions="Verify facts and check for accuracy in the research.",
        tools=[WebS...
- name: writer
  type: Any
  val: |-
    Agent(
        name="Writer",
        instructions="Write a comprehensive article based on all available research and verificati...
- name: research_step
  type: Any
  val: |-
    Step(
        name="research",
        description="Research the topic",
        agent=researcher,
    )
- name: summarize_step
  type: Any
  val: |-
    Step(
        name="summarize",
        description="Summarize research findings",
        agent=summarizer,
    )
- name: fact_check_step
  type: Any
  val: |-
    Step(
        name="fact_check",
        description="Verify facts and claims",
        agent=fact_checker,
    )
- name: write_article
  type: Any
  val: |-
    Step(
        name="write_article",
        description="Write final article",
        agent=writer,
    )
- name: basic_workflow
  type: Any
  val: |-
    Workflow(
        name="Basic Linear Workflow",
        description="Research -> Summarize -> Condition(Fact Check) -> Write Art...
functions:
- name: needs_fact_checking
  sig: 'needs_fact_checking(step_input: StepInput) -> bool'
  lines:
  - 73
  - 75
exports:
- name: needs_fact_checking
  kind: func
- name: LANGFUSE_AUTH
  kind: data
- name: tracer_provider
  kind: data
- name: researcher
  kind: data
- name: summarizer
  kind: data
- name: fact_checker
  kind: data
- name: writer
  kind: data
- name: research_step
  kind: data
- name: summarize_step
  kind: data
- name: fact_check_step
  kind: data
- name: write_article
  kind: data
- name: basic_workflow
  kind: data
