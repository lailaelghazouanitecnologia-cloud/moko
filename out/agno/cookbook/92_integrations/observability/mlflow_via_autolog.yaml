## Roska File Descriptor — observability/mlflow_via_autolog.py
file: observability/mlflow_via_autolog.py
lines: 47
layer: logic
purpose: 'MLflow Via Autolog ================== Demonstrates tracing an Agno agent with MLflow''s built-in autolog integration. Requirements: pip install mlflow agno Start MLflow: mlflow server --host 127.0.0.1 --port 5000 Then open http://127.0.0.1:5000 to view traces. NOTE: You can also configure the tracking URI and experiment via environment variables instead of calling the Python APIs: export MLFLOW_TRACKING_URI="http://127.0.0.1:5000" export MLFLOW_EXPERIMENT_NAME="Agno Agent"'
imports:
- sym: mlflow
- sym: agno.agent.Agent
- sym: agno.models.openai.OpenAIChat
- sym: agno.tools.yfinance.YFinanceTools
data:
- name: agent
  type: Any
  val: |-
    Agent(
        model=OpenAIChat(id="gpt-5-mini"),
        tools=[YFinanceTools()],
        instructions="Use tables to display data....
exports:
- name: agent
  kind: data
