## Roska File Descriptor — utilities/filesystem.py
file: utilities/filesystem.py
lines: 39
layer: logic
functions:
- name: rmdir_recursive
  sig: 'rmdir_recursive(dir_path: Path) -> bool'
  lines:
  - 4
  - 19
  detail:
    calls:
    - dir_path.exists
    - dir_path.is_dir
    - dir_path.is_file
    - dir_path.unlink
    - rmtree
- name: delete_files_in_dir
  sig: 'delete_files_in_dir(dir: Path) -> None'
  lines:
  - 22
  - 29
  detail:
    calls:
    - dir.iterdir
    - item.is_dir
    - item.unlink
    - rmdir_recursive
- name: delete_from_fs
  sig: 'delete_from_fs(path_to_del: Path) -> bool'
  lines:
  - 32
  - 39
  detail:
    calls:
    - path_to_del.exists
    - path_to_del.is_dir
    - path_to_del.unlink
    - rmdir_recursive
exports:
- name: rmdir_recursive
  kind: func
- name: delete_files_in_dir
  kind: func
- name: delete_from_fs
  kind: func
