## Roska File Descriptor — infra/helpers.py
file: infra/helpers.py
lines: 71
layer: logic
imports:
- sym: agno.utilities.logging.log_debug
functions:
- name: get_infra_dir_from_env
  sig: get_infra_dir_from_env() -> Optional[Path]
  lines:
  - 7
  - 16
  detail:
    calls:
    - Path
    - getenv
    - log_debug
- name: is_docker_only_project
  sig: 'is_docker_only_project(infra_root_path: Path) -> bool'
  lines:
  - 19
  - 36
  detail:
    calls:
    - infra_root_path.joinpath
    - infra_root_path.joinpath(indicator).exists
    - log_debug
- name: get_infra_dir_path
  sig: 'get_infra_dir_path(infra_root_path: Path) -> Optional[Path]'
  lines:
  - 39
  - 71
  detail:
    calls:
    - agno_conf.get
    - agno_conf_infra_dir_path.exists
    - agno_conf_infra_dir_path.is_dir
    - infra_dir.exists
    - infra_dir.is_dir
    - infra_root_path.joinpath
    - log_debug
    - pyproject_toml_path.exists
    - pyproject_toml_path.is_file
    - read_pyproject_agno
exports:
- name: get_infra_dir_from_env
  kind: func
- name: is_docker_only_project
  kind: func
- name: get_infra_dir_path
  kind: func
