## Roska File Descriptor — agno/filters.py
file: agno/filters.py
lines: 507
layer: logic
purpose: 'Search filter expressions for filtering knowledge base documents and search results. This module provides a set of filter operators for constructing complex search queries that can be applied to knowledge bases, vector databases, and other searchable content. Filter Types: - Comparison: EQ (equals), NEQ (not equals), GT (greater than), GTE (greater than or equal), LT (less than), LTE (less than or equal) - Inclusion: IN (value in list) - String: CONTAINS (substring match), STARTSWITH (prefix match) - Logical: AND, OR, NOT Example: >>> from agno.filters import EQ, GT, IN, AND, OR, NOT >>> >>> # Simple equality filter >>> filter = EQ("category", "technology") >>> >>> # Complex filter with multiple conditions >>> filter = AND( ...     EQ("status", "published"), ...     GT("views", 1000), ...     IN("category", ["tech", "science"]) ... ) >>> >>> # Using OR logic >>> filter = OR(EQ("priority", "high"), EQ("urgent", True)) >>> >>> # Negating conditions >>> filter = NOT(EQ("status", "archived")) >>> >>> # Complex nested logic >>> filter = OR( ...     AND(EQ("type", "article"), GT("word_count", 500)), ...     AND(EQ("type", "tutorial"), NOT(EQ("difficulty", "beginner"))) ... )'
data:
- name: MAX_FILTER_DEPTH
  type: Any
  val: int
types:
- name: FilterExpr
  kind: struct
  ctx: 'Base class for all filter expressions. Filters can be combined using AND, OR, and NOT classes: - AND: Combine filters where both expressions must be true - OR: Combine filters where either expression can be true - NOT: Negate a filter expression Example: >>> # Create complex filters using AND, OR, NOT >>> filter = OR(AND(EQ("status", "active"), GT("age", 18)), EQ("role", "admin")) >>> # Equivalent to: (status == "active" AND age > 18) OR role == "admin" (+1 dunder methods)'
  methods:
  - name: __or__
    sig: '__or__(self, other: FilterExpr) -> OR'
    lines:
    - 66
    - 68
    detail:
      calls:
      - OR
  - name: __and__
    sig: '__and__(self, other: FilterExpr) -> AND'
    lines:
    - 70
    - 72
    detail:
      calls:
      - AND
  - name: __invert__
    sig: __invert__(self) -> NOT
    lines:
    - 74
    - 76
    detail:
      calls:
      - NOT
  - name: to_dict
    sig: to_dict(self) -> dict
    lines:
    - 78
    - 80
    detail:
      calls:
      - NotImplementedError
  lines:
  - 51
  - 83
- name: EQ
  kind: struct
  ctx: 'Equality filter - matches documents where a field equals a specific value. Args: key: The field name to compare value: The value to match against Example: >>> # Match documents where status is "published" >>> filter = EQ("status", "published") >>> >>> # Match documents where author_id is 123 >>> filter = EQ("author_id", 123)'
  bases:
  - FilterExpr
  fields:
  - name: key
    type: Any
    default: key
  - name: value
    type: Any
    default: value
  methods:
  - name: __init__
    sig: __init__(self, ...2 params)
    lines:
    - 106
    - 108
  - name: to_dict
    sig: to_dict(self) -> dict
    lines:
    - 110
    - 111
    detail: {}
  lines:
  - 91
  - 111
- name: IN
  kind: struct
  ctx: 'Inclusion filter - matches documents where a field''s value is in a list of values. Args: key: The field name to check values: List of acceptable values Example: >>> # Match documents where category is either "tech", "science", or "engineering" >>> filter = IN("category", ["tech", "science", "engineering"]) >>> >>> # Match documents where status is either "draft" or "published" >>> filter = IN("status", ["draft", "published"])'
  bases:
  - FilterExpr
  fields:
  - name: key
    type: Any
    default: key
  - name: values
    type: Any
    default: values
  methods:
  - name: __init__
    sig: __init__(self, ...2 params)
    lines:
    - 129
    - 131
  - name: to_dict
    sig: to_dict(self) -> dict
    lines:
    - 133
    - 134
    detail: {}
  lines:
  - 114
  - 134
- name: GT
  kind: struct
  ctx: 'Greater than filter - matches documents where a field''s value is greater than a threshold. Args: key: The field name to compare value: The threshold value Example: >>> # Match documents where age is greater than 18 >>> filter = GT("age", 18) >>> >>> # Match documents where price is greater than 100.0 >>> filter = GT("price", 100.0) >>> >>> # Match documents created after a certain timestamp >>> filter = GT("created_at", 1234567890)'
  bases:
  - FilterExpr
  fields:
  - name: key
    type: Any
    default: key
  - name: value
    type: Any
    default: value
  methods:
  - name: __init__
    sig: __init__(self, ...2 params)
    lines:
    - 155
    - 157
  - name: to_dict
    sig: to_dict(self) -> dict
    lines:
    - 159
    - 160
    detail: {}
  lines:
  - 137
  - 160
- name: LT
  kind: struct
  ctx: 'Less than filter - matches documents where a field''s value is less than a threshold. Args: key: The field name to compare value: The threshold value Example: >>> # Match documents where age is less than 65 >>> filter = LT("age", 65) >>> >>> # Match documents where price is less than 50.0 >>> filter = LT("price", 50.0) >>> >>> # Match documents created before a certain timestamp >>> filter = LT("created_at", 1234567890)'
  bases:
  - FilterExpr
  fields:
  - name: key
    type: Any
    default: key
  - name: value
    type: Any
    default: value
  methods:
  - name: __init__
    sig: __init__(self, ...2 params)
    lines:
    - 181
    - 183
  - name: to_dict
    sig: to_dict(self) -> dict
    lines:
    - 185
    - 186
    detail: {}
  lines:
  - 163
  - 186
- name: NEQ
  kind: struct
  ctx: 'Not-equal filter - matches documents where a field does not equal a specific value. Args: key: The field name to compare value: The value to compare against Example: >>> # Match documents where status is not "archived" >>> filter = NEQ("status", "archived")'
  bases:
  - FilterExpr
  fields:
  - name: key
    type: Any
    default: key
  - name: value
    type: Any
    default: value
  methods:
  - name: __init__
    sig: __init__(self, ...2 params)
    lines:
    - 201
    - 203
  - name: to_dict
    sig: to_dict(self) -> dict
    lines:
    - 205
    - 206
    detail: {}
  lines:
  - 189
  - 206
- name: GTE
  kind: struct
  ctx: 'Greater than or equal filter - matches documents where a field''s value is >= a threshold. Args: key: The field name to compare value: The threshold value Example: >>> # Match documents where age is 18 or older >>> filter = GTE("age", 18) >>> >>> # Match documents created on or after a certain timestamp >>> filter = GTE("created_at", "2025-01-01T00:00:00Z")'
  bases:
  - FilterExpr
  fields:
  - name: key
    type: Any
    default: key
  - name: value
    type: Any
    default: value
  methods:
  - name: __init__
    sig: __init__(self, ...2 params)
    lines:
    - 224
    - 226
  - name: to_dict
    sig: to_dict(self) -> dict
    lines:
    - 228
    - 229
    detail: {}
  lines:
  - 209
  - 229
- name: LTE
  kind: struct
  ctx: 'Less than or equal filter - matches documents where a field''s value is <= a threshold. Args: key: The field name to compare value: The threshold value Example: >>> # Match documents where age is 65 or younger >>> filter = LTE("age", 65) >>> >>> # Match documents created on or before a certain timestamp >>> filter = LTE("created_at", "2025-12-31T23:59:59Z")'
  bases:
  - FilterExpr
  fields:
  - name: key
    type: Any
    default: key
  - name: value
    type: Any
    default: value
  methods:
  - name: __init__
    sig: __init__(self, ...2 params)
    lines:
    - 247
    - 249
  - name: to_dict
    sig: to_dict(self) -> dict
    lines:
    - 251
    - 252
    detail: {}
  lines:
  - 232
  - 252
- name: CONTAINS
  kind: struct
  ctx: 'Substring match filter - matches documents where a field contains a substring (case-insensitive). Args: key: The field name to search value: The substring to search for Example: >>> # Match documents where user_id contains "admin" >>> filter = CONTAINS("user_id", "admin") >>> >>> # Match documents where name contains "john" >>> filter = CONTAINS("name", "john")'
  bases:
  - FilterExpr
  fields:
  - name: key
    type: Any
    default: key
  - name: value
    type: Any
    default: value
  methods:
  - name: __init__
    sig: __init__(self, ...2 params)
    lines:
    - 275
    - 277
  - name: to_dict
    sig: to_dict(self) -> dict
    lines:
    - 279
    - 280
    detail: {}
  lines:
  - 260
  - 280
- name: STARTSWITH
  kind: struct
  ctx: 'Prefix match filter - matches documents where a field starts with a given string. Args: key: The field name to search value: The prefix to match Example: >>> # Match documents where name starts with "Agent" >>> filter = STARTSWITH("name", "Agent") >>> >>> # Match documents where session_id starts with "sess_" >>> filter = STARTSWITH("session_id", "sess_")'
  bases:
  - FilterExpr
  fields:
  - name: key
    type: Any
    default: key
  - name: value
    type: Any
    default: value
  methods:
  - name: __init__
    sig: __init__(self, ...2 params)
    lines:
    - 298
    - 300
  - name: to_dict
    sig: to_dict(self) -> dict
    lines:
    - 302
    - 303
    detail: {}
  lines:
  - 283
  - 303
- name: AND
  kind: struct
  ctx: 'Logical AND operator - matches documents where ALL expressions are true. Combines multiple filter expressions where every expression must be satisfied for a document to match. Args: *expressions: Variable number of FilterExpr expressions to combine with AND logic Example: >>> # Match documents where status is "published" AND age > 18 >>> filter = AND(EQ("status", "published"), GT("age", 18)) >>> >>> # Multiple expressions >>> filter = AND( ...     EQ("status", "active"), ...     GT("score", 80), ...     IN("category", ["tech", "science"]) ... )'
  bases:
  - FilterExpr
  fields:
  - name: expressions
    type: Any
    default: list(expressions)
  methods:
  - name: __init__
    sig: '__init__(self, *expressions: FilterExpr)'
    lines:
    - 332
    - 333
    detail:
      calls:
      - list
  - name: to_dict
    sig: to_dict(self) -> dict
    lines:
    - 335
    - 336
    detail:
      calls:
      - e.to_dict
  lines:
  - 311
  - 336
- name: OR
  kind: struct
  ctx: 'Logical OR operator - matches documents where ANY expression is true. Combines multiple filter expressions where at least one expression must be satisfied for a document to match. Args: *expressions: Variable number of FilterExpr expressions to combine with OR logic Example: >>> # Match documents where status is "published" OR status is "archived" >>> filter = OR(EQ("status", "published"), EQ("status", "archived")) >>> >>> # Complex: Match VIP users OR users with high score >>> filter = OR( ...     EQ("membership", "VIP"), ...     GT("score", 1000) ... )'
  bases:
  - FilterExpr
  fields:
  - name: expressions
    type: Any
    default: list(expressions)
  methods:
  - name: __init__
    sig: '__init__(self, *expressions: FilterExpr)'
    lines:
    - 359
    - 360
    detail:
      calls:
      - list
  - name: to_dict
    sig: to_dict(self) -> dict
    lines:
    - 362
    - 363
    detail:
      calls:
      - e.to_dict
  lines:
  - 339
  - 363
- name: NOT
  kind: struct
  ctx: 'Logical NOT operator - matches documents where the expression is NOT true. Negates a filter expression, matching documents that don''t satisfy the expression. Args: expression: The FilterExpr expression to negate Example: >>> # Match documents where status is NOT "draft" >>> filter = NOT(EQ("status", "draft")) >>> >>> # Exclude inactive users with low scores >>> filter = NOT(AND(EQ("status", "inactive"), LT("score", 10))) >>> >>> # Match users who are NOT in the blocked list >>> filter = NOT(IN("user_id", [101, 102, 103]))'
  bases:
  - FilterExpr
  fields:
  - name: expression
    type: Any
    default: expression
  methods:
  - name: __init__
    sig: __init__(self, ...1 params)
    lines:
    - 385
    - 386
  - name: to_dict
    sig: to_dict(self) -> dict
    lines:
    - 388
    - 389
    detail:
      calls:
      - self.expression.to_dict
  lines:
  - 366
  - 389
functions:
- name: from_dict
  sig: 'from_dict(filter_dict: dict, _depth: int = 0) -> FilterExpr'
  lines:
  - 397
  - 507
  detail:
    calls:
    - AND
    - CONTAINS
    - EQ
    - GT
    - GTE
    - IN
    - LT
    - LTE
    - NEQ
    - NOT
    - OR
    - STARTSWITH
    - ValueError
    - from_dict
    - isinstance
exports:
- name: FilterExpr
  kind: type
- name: EQ
  kind: type
- name: IN
  kind: type
- name: GT
  kind: type
- name: LT
  kind: type
- name: NEQ
  kind: type
- name: GTE
  kind: type
- name: LTE
  kind: type
- name: CONTAINS
  kind: type
- name: STARTSWITH
  kind: type
- name: AND
  kind: type
- name: OR
  kind: type
- name: NOT
  kind: type
- name: from_dict
  kind: func
- name: MAX_FILTER_DEPTH
  kind: data
