## Roska File Descriptor — tools/opencv.py
file: tools/opencv.py
lines: 321
layer: logic
imports:
- sym: agno.agent.Agent
- sym: agno.media.Image
- sym: agno.media.Video
- sym: agno.tools.Toolkit
- sym: agno.tools.function.ToolResult
- sym: agno.utils.log.log_debug
- sym: agno.utils.log.log_error
- sym: agno.utils.log.log_info
types:
- name: OpenCVTools
  kind: struct
  ctx: Tools for capturing images and videos from the webcam using OpenCV
  bases:
  - Toolkit
  fields:
  - name: show_preview
    type: Any
    default: show_preview
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              show_preview=False,
              enable_capture_image: bool = True,
              enable_capture_video: bool = True,
              all: bool = False,
              **kwargs,
          )
    lines:
    - 21
    - 41
    detail:
      calls:
      - super
      - super().__init__
      - tools.append
  - name: capture_image
    sig: |-
      capture_image(
              self,
              agent: Agent,
              prompt: str = "Webcam capture",
          ) -> ToolResult
    lines:
    - 43
    - 145
    detail:
      calls:
      - Image
      - ToolResult
      - cam.isOpened
      - cam.read
      - cam.release
      - cam.set
      - cv2.VideoCapture
      - cv2.destroyAllWindows
      - cv2.imencode
      - cv2.imshow
      - cv2.waitKey
      - encoded_image.tobytes
      - frame.copy
      - log_debug
      - log_error
      - log_info
      - ord
      - str
      - uuid4
  - name: capture_video
    sig: |-
      capture_video(
              self,
              agent: Agent,
              duration: int = 10,
              prompt: str = "Webcam video capture",
          ) -> ToolResult
    lines:
    - 147
    - 321
    detail:
      calls:
      - Path
      - ToolResult
      - Video
      - cap.get
      - cap.isOpened
      - cap.read
      - cap.release
      - cv2.VideoCapture
      - cv2.VideoWriter
      - cv2.circle
      - cv2.destroyAllWindows
      - cv2.imshow
      - cv2.putText
      - cv2.waitKey
      - frame.copy
      - getattr
      - getattr(cv2, "VideoWriter_fourcc")
      - int
      - locals
      - log_debug
      - log_error
      - log_info
      - max
      - open
      - os.unlink
      - out.isOpened
      - out.release
      - out.write
      - str
      - temp_path.exists
      - temp_path.stat
      - tempfile.NamedTemporaryFile
      - time.time
      - uuid4
      - video_file.read
  lines:
  - 18
  - 321
exports:
- name: OpenCVTools
  kind: type
