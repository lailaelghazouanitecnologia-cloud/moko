## Roska File Descriptor — tools/calculator.py
file: tools/calculator.py
lines: 151
layer: logic
imports:
- sym: agno.tools.Toolkit
- sym: agno.utils.log.log_debug
- sym: agno.utils.log.logger
types:
- name: CalculatorTools
  kind: struct
  bases:
  - Toolkit
  methods:
  - name: __init__
    sig: __init__(self, ...2 params)
    lines:
    - 10
    - 26
  - name: add
    sig: 'add(self, a: float, b: float) -> str'
    lines:
    - 28
    - 40
    detail:
      calls:
      - json.dumps
      - log_debug
  - name: subtract
    sig: 'subtract(self, a: float, b: float) -> str'
    lines:
    - 42
    - 54
    detail:
      calls:
      - json.dumps
      - log_debug
  - name: multiply
    sig: 'multiply(self, a: float, b: float) -> str'
    lines:
    - 56
    - 68
    detail:
      calls:
      - json.dumps
      - log_debug
  - name: divide
    sig: 'divide(self, a: float, b: float) -> str'
    lines:
    - 70
    - 88
    detail:
      calls:
      - json.dumps
      - log_debug
      - logger.error
      - str
  - name: exponentiate
    sig: 'exponentiate(self, a: float, b: float) -> str'
    lines:
    - 90
    - 102
    detail:
      calls:
      - json.dumps
      - log_debug
      - math.pow
  - name: factorial
    sig: 'factorial(self, n: int) -> str'
    lines:
    - 104
    - 118
    detail:
      calls:
      - json.dumps
      - log_debug
      - logger.error
      - math.factorial
  - name: is_prime
    sig: 'is_prime(self, n: int) -> str'
    lines:
    - 120
    - 134
    detail:
      calls:
      - int
      - json.dumps
      - math.sqrt
      - range
  - name: square_root
    sig: 'square_root(self, n: float) -> str'
    lines:
    - 136
    - 151
    detail:
      calls:
      - json.dumps
      - log_debug
      - logger.error
      - math.sqrt
  lines:
  - 9
  - 151
exports:
- name: CalculatorTools
  kind: type
