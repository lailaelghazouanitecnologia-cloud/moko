## Roska File Descriptor — tools/seltz.py
file: tools/seltz.py
lines: 158
layer: logic
imports:
- sym: agno.tools.Toolkit
- sym: agno.utils.log.log_info
- sym: agno.utils.log.logger
types:
- name: SeltzTools
  kind: struct
  ctx: 'Toolkit for interacting with the Seltz AI-powered search API. Args: api_key: Seltz API key. If not provided, uses the `SELTZ_API_KEY` env var. endpoint: Optional Seltz gRPC endpoint. If not provided, uses SDK default. insecure: Use an insecure gRPC channel. Defaults to False. max_documents: Default maximum number of documents to return per search. context: Default context to improve search quality (e.g., "user is looking for Python docs"). profile: Default search profile to use for ranking. show_results: Log search results for debugging. enable_search: Enable search tool functionality. Defaults to True. all: Enable all tools. Overrides individual flags when True. Defaults to False.'
  bases:
  - Toolkit
  fields:
  - name: api_key
    type: Any
    default: api_key or getenv("SELTZ_API_KEY")
  - name: endpoint
    type: Any
    default: endpoint
  - name: insecure
    type: Any
    default: insecure
  - name: max_documents
    type: Any
    default: max_documents
  - name: context
    type: Any
    default: context
  - name: profile
    type: Any
    default: profile
  - name: show_results
    type: Any
    default: show_results
  - name: client
    type: Any
    default: Optional[Seltz]
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              api_key: Optional[str] = None,
              endpoint: Optional[str] = None,
              insecure: bool = False,
              max_documents: int = 10,
              context: Optional[str] = None,
              profile: Optional[str] = None,
              show_results: bool = False,
              enable_search: bool = True,
              all: bool = False,
              **kwargs: Any,
          )
    lines:
    - 38
    - 78
    detail:
      calls:
      - Seltz
      - ValueError
      - getenv
      - logger.error
      - super
      - super().__init__
      - tools.append
  - name: _parse_documents
    sig: '_parse_documents(self, documents: Any) -> str'
    vis: protected
    lines:
    - 80
    - 98
    detail:
      calls:
      - doc.to_dict
      - getattr
      - hasattr
      - json.dumps
      - parsed.append
  - name: search_seltz
    sig: |-
      search_seltz(
              self,
              query: str,
              max_documents: Optional[int] = None,
              context: Optional[str] = None,
          ) -> str
    lines:
    - 100
    - 158
    detail:
      calls:
      - Includes
      - log_info
      - logger.error
      - self._parse_documents
      - self.client.search
  lines:
  - 23
  - 158
exports:
- name: SeltzTools
  kind: type
