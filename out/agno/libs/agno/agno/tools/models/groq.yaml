## Roska File Descriptor — models/groq.py
file: models/groq.py
lines: 158
layer: logic
imports:
- sym: agno.agent.Agent
- sym: agno.media.Audio
- sym: agno.tools.Toolkit
- sym: agno.tools.function.ToolResult
- sym: agno.utils.log.log_debug
- sym: agno.utils.log.log_error
types:
- name: GroqTools
  kind: struct
  ctx: Tools for interacting with Groq API
  bases:
  - Toolkit
  fields:
  - name: api_key
    type: Any
    default: api_key or getenv("GROQ_API_KEY")
  - name: client
    type: Any
    default: GroqClient(api_key=self.api_key)
  - name: transcription_model
    type: Any
    default: transcription_model
  - name: translation_model
    type: Any
    default: translation_model
  - name: tts_model
    type: Any
    default: tts_model
  - name: tts_voice
    type: Any
    default: tts_voice
  - name: tts_format
    type: str
    default: '"wav"'
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              api_key: Optional[str] = None,
              transcription_model: str = "whisper-large-v3",
              translation_model: str = "whisper-large-v3",
              tts_model: str = "playai-tts",
              tts_voice: str = "Chip-PlayAI",
              enable_transcribe_audio: bool = True,
              enable_translate_audio: bool = True,
              enable_generate_speech: bool = True,
              all: bool = False,
              **kwargs,
          )
    lines:
    - 21
    - 53
    detail:
      calls:
      - GroqClient
      - ValueError
      - getenv
      - super
      - super().__init__
      - tools.append
  - name: transcribe_audio
    sig: 'transcribe_audio(self, audio_source: str) -> str'
    lines:
    - 55
    - 85
    detail:
      calls:
      - Path
      - Path(audio_source).exists
      - audio_file.read
      - log_debug
      - log_error
      - open
      - self.client.audio.transcriptions.create
      - str
  - name: translate_audio
    sig: 'translate_audio(self, audio_source: str) -> str'
    lines:
    - 87
    - 116
    detail:
      calls:
      - Path
      - Path(audio_source).exists
      - audio_file.read
      - log_debug
      - log_error
      - open
      - self.client.audio.translations.create
      - str
  - name: generate_speech
    sig: |-
      generate_speech(
              self,
              agent: Agent,
              text_input: str,
          ) -> ToolResult
    lines:
    - 118
    - 158
    detail:
      calls:
      - Audio
      - ToolResult
      - log_debug
      - log_error
      - response.read
      - self.client.audio.speech.create
      - str
      - uuid4
  lines:
  - 18
  - 158
exports:
- name: GroqTools
  kind: type
