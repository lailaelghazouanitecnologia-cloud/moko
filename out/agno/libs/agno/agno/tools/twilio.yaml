## Roska File Descriptor — tools/twilio.py
file: tools/twilio.py
lines: 186
layer: logic
imports:
- sym: agno.tools.Toolkit
- sym: agno.utils.log.log_info
- sym: agno.utils.log.logger
types:
- name: TwilioTools
  kind: struct
  bases:
  - Toolkit
  fields:
  - name: account_sid
    type: Any
    default: account_sid or getenv("TWILIO_ACCOUNT_SID")
  - name: auth_token
    type: Any
    default: auth_token or getenv("TWILIO_AUTH_TOKEN")
  - name: api_key
    type: Any
    default: api_key or getenv("TWILIO_API_KEY")
  - name: api_secret
    type: Any
    default: api_secret or getenv("TWILIO_API_SECRET")
  - name: region
    type: Any
    default: region or getenv("TWILIO_REGION")
  - name: edge
    type: Any
    default: edge or getenv("TWILIO_EDGE")
  - name: client
    type: Any
    default: |-
      Client(
                      self.api_key,
                      self.api_secret,
                      self.account_sid,
                      region=self.region or None,
                      edge=self.edge or None,
                  )
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              account_sid: Optional[str] = None,
              auth_token: Optional[str] = None,
              api_key: Optional[str] = None,
              api_secret: Optional[str] = None,
              region: Optional[str] = None,
              edge: Optional[str] = None,
              debug: bool = False,
              enable_send_sms: bool = True,
              enable_get_call_details: bool = True,
              enable_list_messages: bool = True,
              all: bool = False,
              **kwargs,
          )
    lines:
    - 16
    - 98
    detail:
      calls:
      - Client
      - getenv
      - logger.error
      - logging.basicConfig
      - self.client.http_client.logger.setLevel
      - super
      - super().__init__
      - tools.append
  - name: validate_phone_number
    sig: 'validate_phone_number(phone: str) -> bool'
    decorators:
    - staticmethod
    lines:
    - 101
    - 103
    detail:
      calls:
      - bool
      - re.match
  - name: send_sms
    sig: 'send_sms(self, to: str, from_: str, body: str) -> str'
    lines:
    - 105
    - 130
    detail:
      calls:
      - body.strip
      - len
      - log_info
      - logger.error
      - self.client.messages.create
      - self.validate_phone_number
      - str
  - name: get_call_details
    sig: 'get_call_details(self, call_sid: str) -> Dict[str, Any]'
    lines:
    - 132
    - 157
    detail:
      calls:
      - log_info
      - logger.error
      - self.client.calls
      - self.client.calls(call_sid).fetch
      - str
  - name: list_messages
    sig: 'list_messages(self, limit: int = 20) -> List[Dict[str, Any]]'
    lines:
    - 159
    - 186
    detail:
      calls:
      - len
      - log_info
      - logger.error
      - messages.append
      - self.client.messages.list
      - str
  lines:
  - 15
  - 186
exports:
- name: TwilioTools
  kind: type
