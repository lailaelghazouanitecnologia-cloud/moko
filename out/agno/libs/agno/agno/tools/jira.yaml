## Roska File Descriptor — tools/jira.py
file: tools/jira.py
lines: 170
layer: logic
imports:
- sym: agno.tools.Toolkit
- sym: agno.utils.log.log_debug
- sym: agno.utils.log.logger
types:
- name: JiraTools
  kind: struct
  bases:
  - Toolkit
  fields:
  - name: server_url
    type: Any
    default: server_url or getenv("JIRA_SERVER_URL")
  - name: username
    type: Any
    default: username or getenv("JIRA_USERNAME")
  - name: password
    type: Any
    default: password or getenv("JIRA_PASSWORD")
  - name: token
    type: Any
    default: token or getenv("JIRA_TOKEN")
  - name: jira
    type: Any
    default: JIRA(server=self.server_url, basic_auth=cast(tuple[str, str], auth))
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              server_url: Optional[str] = None,
              username: Optional[str] = None,
              password: Optional[str] = None,
              token: Optional[str] = None,
              enable_get_issue: bool = True,
              enable_create_issue: bool = True,
              enable_search_issues: bool = True,
              enable_add_comment: bool = True,
              enable_add_worklog: bool = True,
              all: bool = False,
              **kwargs,
          )
    lines:
    - 15
    - 62
    detail:
      calls:
      - JIRA
      - ValueError
      - cast
      - getenv
      - super
      - super().__init__
      - tools.append
  - name: get_issue
    sig: 'get_issue(self, issue_key: str) -> str'
    lines:
    - 64
    - 86
    detail:
      calls:
      - cast
      - json.dumps
      - log_debug
      - logger.error
      - self.jira.issue
      - str
  - name: create_issue
    sig: 'create_issue(self, project_key: str, summary: str, description: str, issuetype: str = "Task") -> str'
    lines:
    - 88
    - 111
    detail:
      calls:
      - json.dumps
      - log_debug
      - logger.error
      - self.jira.create_issue
      - str
  - name: search_issues
    sig: 'search_issues(self, jql_str: str, max_results: int = 50) -> str'
    lines:
    - 113
    - 137
    detail:
      calls:
      - cast
      - json.dumps
      - len
      - log_debug
      - logger.error
      - results.append
      - self.jira.search_issues
      - str
  - name: add_comment
    sig: 'add_comment(self, issue_key: str, comment: str) -> str'
    lines:
    - 139
    - 153
    detail:
      calls:
      - json.dumps
      - log_debug
      - logger.error
      - self.jira.add_comment
      - str
  - name: add_worklog
    sig: 'add_worklog(self, issue_key: str, time_spent: str, comment: Optional[str] = None) -> str'
    lines:
    - 155
    - 170
    detail:
      calls:
      - json.dumps
      - log_debug
      - logger.error
      - self.jira.add_worklog
      - str
  lines:
  - 14
  - 170
exports:
- name: JiraTools
  kind: type
