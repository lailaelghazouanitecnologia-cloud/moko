## Roska File Descriptor — tools/webex.py
file: tools/webex.py
lines: 76
layer: logic
imports:
- sym: agno.tools.Toolkit
- sym: agno.utils.log.logger
types:
- name: WebexTools
  kind: struct
  bases:
  - Toolkit
  fields:
  - name: client
    type: Any
    default: WebexAPI(access_token=access_token)
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              enable_send_message: bool = True,
              enable_list_rooms: bool = True,
              all: bool = False,
              access_token: Optional[str] = None,
              **kwargs,
          )
    lines:
    - 16
    - 36
    detail:
      calls:
      - ValueError
      - WebexAPI
      - getenv
      - super
      - super().__init__
      - tools.append
  - name: send_message
    sig: 'send_message(self, room_id: str, text: str) -> str'
    lines:
    - 38
    - 52
    detail:
      calls:
      - json.dumps
      - logger.error
      - self.client.messages.create
      - str
  - name: list_rooms
    sig: list_rooms(self) -> str
    lines:
    - 54
    - 76
    detail:
      calls:
      - json.dumps
      - logger.error
      - self.client.rooms.list
      - str
  lines:
  - 15
  - 76
exports:
- name: WebexTools
  kind: type
