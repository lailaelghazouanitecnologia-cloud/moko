## Roska File Descriptor — tools/cartesia.py
file: tools/cartesia.py
lines: 187
layer: logic
imports:
- sym: agno.agent.Agent
- sym: agno.media.Audio
- sym: agno.team.team.Team
- sym: agno.tools.Toolkit
- sym: agno.tools.function.ToolResult
- sym: agno.utils.log.log_debug
- sym: agno.utils.log.log_error
- sym: agno.utils.log.log_info
types:
- name: CartesiaTools
  kind: struct
  bases:
  - Toolkit
  fields:
  - name: api_key
    type: Any
    default: api_key or getenv("CARTESIA_API_KEY")
  - name: client
    type: Any
    default: cartesia.Cartesia(api_key=self.api_key)
  - name: model_id
    type: Any
    default: model_id
  - name: default_voice_id
    type: Any
    default: default_voice_id
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              api_key: Optional[str] = None,
              model_id: str = "sonic-2",
              default_voice_id: str = "78ab82d5-25be-4f7d-82b3-7ad64e5b85b2",
              enable_text_to_speech: bool = True,
              enable_list_voices: bool = True,
              enable_localize_voice: bool = False,
              all: bool = False,
              **kwargs,
          )
    lines:
    - 20
    - 48
    detail:
      calls:
      - ValueError
      - cartesia.Cartesia
      - getenv
      - super
      - super().__init__
      - tools.append
  - name: list_voices
    sig: list_voices(self) -> str
    lines:
    - 50
    - 87
    detail:
      calls:
      - filtered_result.append
      - hasattr
      - json.dumps
      - log_error
      - log_info
      - self.client.voices.list
      - str
  - name: localize_voice
    sig: |-
      localize_voice(
              self,
              name: str,
              description: str,
              language: str,
              original_speaker_gender: str,
              voice_id: Optional[str] = None,
          ) -> str
    lines:
    - 89
    - 128
    detail:
      calls:
      - isinstance
      - json.dumps
      - log_debug
      - log_error
      - result.model_dump_json
      - self.client.voices.localize
      - str
      - type
  - name: text_to_speech
    sig: |-
      text_to_speech(
              self,
              agent: Union[Agent, Team],
              transcript: str,
              voice_id: Optional[str] = None,
          ) -> ToolResult
    lines:
    - 130
    - 187
    detail:
      calls:
      - Audio
      - ToolResult
      - b"".join
      - log_error
      - log_info
      - self.client.tts.bytes
      - str
      - uuid4
  lines:
  - 19
  - 187
exports:
- name: CartesiaTools
  kind: type
