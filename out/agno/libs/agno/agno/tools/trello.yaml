## Roska File Descriptor — tools/trello.py
file: tools/trello.py
lines: 274
layer: logic
imports:
- sym: agno.tools.Toolkit
- sym: agno.utils.log.log_debug
- sym: agno.utils.log.log_info
- sym: agno.utils.log.logger
types:
- name: TrelloTools
  kind: struct
  bases:
  - Toolkit
  fields:
  - name: api_key
    type: Any
    default: api_key or getenv("TRELLO_API_KEY")
  - name: api_secret
    type: Any
    default: api_secret or getenv("TRELLO_API_SECRET")
  - name: token
    type: Any
    default: token or getenv("TRELLO_TOKEN")
  - name: client
    type: Any
    default: TrelloClient(api_key=self.api_key, api_secret=self.api_secret, token=self.token)
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              api_key: Optional[str] = None,
              api_secret: Optional[str] = None,
              token: Optional[str] = None,
              **kwargs,
          )
    lines:
    - 15
    - 45
    detail:
      calls:
      - TrelloClient
      - all
      - getenv
      - logger.error
      - logger.warning
      - super
      - super().__init__
  - name: create_card
    sig: 'create_card(self, board_id: str, list_name: str, card_title: str, description: str = "") -> str'
    lines:
    - 47
    - 82
    detail:
      calls:
      - board.list_lists
      - json.dumps
      - list_name.lower
      - log_info
      - lst.name.lower
      - self.client.get_board
      - target_list.add_card
  - name: get_board_lists
    sig: 'get_board_lists(self, board_id: str) -> str'
    lines:
    - 84
    - 108
    detail:
      calls:
      - board.list_lists
      - json.dumps
      - len
      - log_debug
      - lst.list_cards
      - self.client.get_board
  - name: move_card
    sig: 'move_card(self, card_id: str, list_id: str) -> str'
    lines:
    - 110
    - 133
    detail:
      calls:
      - card.change_list
      - json.dumps
      - log_debug
      - self.client.get_card
  - name: get_cards
    sig: 'get_cards(self, list_id: str) -> str'
    lines:
    - 135
    - 168
    detail:
      calls:
      - json.dumps
      - log_debug
      - self.client.get_list
      - trello_list.list_cards
  - name: create_board
    sig: 'create_board(self, name: str, default_lists: bool = False) -> str'
    lines:
    - 170
    - 198
    detail:
      calls:
      - json.dumps
      - log_info
      - self.client.add_board
  - name: create_list
    sig: 'create_list(self, board_id: str, list_name: str, pos: str = "bottom") -> str'
    lines:
    - 200
    - 231
    detail:
      calls:
      - board.add_list
      - json.dumps
      - log_info
      - self.client.get_board
  - name: list_boards
    sig: 'list_boards(self, board_filter: str = "all") -> str'
    lines:
    - 233
    - 274
    detail:
      calls:
      - boards_list.append
      - getattr
      - json.dumps
      - len
      - log_debug
      - self.client.list_boards
  lines:
  - 14
  - 274
exports:
- name: TrelloTools
  kind: type
