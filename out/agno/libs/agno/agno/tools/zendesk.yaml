## Roska File Descriptor — tools/zendesk.py
file: tools/zendesk.py
lines: 82
layer: logic
imports:
- sym: agno.tools.Toolkit
- sym: agno.utils.log.log_debug
- sym: agno.utils.log.logger
types:
- name: ZendeskTools
  kind: struct
  ctx: A toolkit class for interacting with the Zendesk API to search articles. It requires authentication details and the company name to configure the API access.
  bases:
  - Toolkit
  fields:
  - name: username
    type: Any
    default: username or getenv("ZENDESK_USERNAME")
  - name: password
    type: Any
    default: password or getenv("ZENDESK_PASSWORD")
  - name: company_name
    type: Any
    default: company_name or getenv("ZENDESK_COMPANY_NAME")
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              username: Optional[str] = None,
              password: Optional[str] = None,
              company_name: Optional[str] = None,
              enable_search_zendesk: bool = True,
              all: bool = False,
              **kwargs,
          )
    lines:
    - 21
    - 52
    detail:
      calls:
      - getenv
      - logger.error
      - super
      - super().__init__
      - tools.append
  - name: search_zendesk
    sig: 'search_zendesk(self, search_string: str) -> str'
    lines:
    - 54
    - 82
    detail:
      calls:
      - ConnectionError
      - json.dumps
      - log_debug
      - re.compile
      - re.sub
      - requests.get
      - response.json
      - response.raise_for_status
  lines:
  - 15
  - 82
exports:
- name: ZendeskTools
  kind: type
