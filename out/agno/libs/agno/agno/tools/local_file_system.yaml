## Roska File Descriptor — tools/local_file_system.py
file: tools/local_file_system.py
lines: 90
layer: logic
imports:
- sym: agno.tools.Toolkit
- sym: agno.utils.log.log_debug
- sym: agno.utils.log.logger
types:
- name: LocalFileSystemTools
  kind: struct
  bases:
  - Toolkit
  fields:
  - name: target_directory
    type: Any
    default: target_directory or str(Path.cwd())
  - name: default_extension
    type: Any
    default: default_extension.lstrip(".")
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              target_directory: Optional[str] = None,
              default_extension: str = "txt",
              enable_write_file: bool = True,
              all: bool = False,
              **kwargs,
          )
    lines:
    - 10
    - 35
    detail:
      calls:
      - Path
      - Path.cwd
      - default_extension.lstrip
      - str
      - super
      - super().__init__
      - target_path.mkdir
      - tools.append
  - name: write_file
    sig: |-
      write_file(
              self,
              content: str,
              filename: Optional[str] = None,
              directory: Optional[str] = None,
              extension: Optional[str] = None,
          ) -> str
    lines:
    - 37
    - 81
    detail:
      calls:
      - (extension or self.default_extension).lstrip
      - Path
      - dir_path.mkdir
      - file_path.write_text
      - log_debug
      - logger.error
      - path_obj.suffix.lstrip
      - str
      - uuid4
  - name: read_file
    sig: 'read_file(self, filename: str, directory: Optional[str] = None) -> str'
    lines:
    - 83
    - 90
    detail:
      calls:
      - Path
      - file_path.exists
      - file_path.read_text
  lines:
  - 9
  - 90
exports:
- name: LocalFileSystemTools
  kind: type
