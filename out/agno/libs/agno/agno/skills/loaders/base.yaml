## Roska File Descriptor — loaders/base.py
file: loaders/base.py
lines: 27
layer: logic
imports:
- sym: agno.skills.skill.Skill
types:
- name: SkillLoader
  kind: trait
  ctx: Abstract base class for skill loaders. Skill loaders are responsible for loading skills from various sources (local filesystem, GitHub, URLs, etc.) and returning them as Skill objects. Subclasses must implement the `load()` method to define how skills are loaded from their specific source.
  bases:
  - ABC
  methods:
  - name: load
    sig: load(self) -> List[Skill]
    decorators:
    - abstractmethod
    lines:
    - 18
    - 27
    detail: {}
  lines:
  - 7
  - 27
exports:
- name: SkillLoader
  kind: type
