## Roska Module Descriptor — mastra/scripts/gh-dane-command
name: gh-dane-command
purpose: GitHub Actions runner script for @dane-ai-mastra PR commands. Reads a command name and PR number from environment variables, loads the corresponding .claude/commands/ template, processes it, and sends it to a headless MastraCode harness for execution.
total_lines: 131
files:
- file: gh-dane-command/index.ts
  lines: 131
  purpose: GitHub Actions runner script for @dane-ai-mastra PR commands. Reads a command name and PR number from environment variables, loads the corresponding .claude/commands/ template, processes it, and sends it to a headless MastraCode harness for execution.
  types: 0
  functions: 1
  imports: 8
  exports: 3
