## Roska File Descriptor — session/cookie.ts
file: session/cookie.ts
lines: 54
purpose: Session cookie utilities. Handles setting, parsing, and clearing of session cookies.
data:
- name: SESSION_COOKIE_NAME
  type: string
  val: '''mastra_cloud_session'''
functions:
- name: setSessionCookie
  sig: 'setSessionCookie(token: string, isProduction: boolean): string'
  lines:
  - 20
  - 30
  detail: {}
- name: parseSessionCookie
  sig: 'parseSessionCookie(cookieHeader: string | null): string | null'
  lines:
  - 38
  - 45
  detail:
    calls:
    - cookieHeader.match
    - new RegExp
- name: clearSessionCookie
  sig: 'clearSessionCookie(): string'
  lines:
  - 52
  - 54
exports:
- name: setSessionCookie
  kind: func
- name: parseSessionCookie
  kind: func
- name: clearSessionCookie
  kind: func
- name: SESSION_COOKIE_NAME
  kind: data
