## Roska File Descriptor — oauth/network.ts
file: oauth/network.ts
lines: 34
purpose: Network utilities for OAuth flow. Provides fetch wrapper with single retry for transient network errors.
imports:
- sym: ../error.AuthError
functions:
- name: fetchWithRetry
  sig: 'fetchWithRetry(url: string, options: RequestInit): Promise<Response>'
  is_async: true
  lines:
  - 22
  - 34
  detail:
    calls:
    - AuthError.networkError
    - fetch
exports:
- name: fetchWithRetry
  kind: func
