## Roska File Descriptor — src/session-storage.ts
file: src/session-storage.ts
lines: 49
purpose: Hono/Web Request session storage adapter for WorkOS AuthKit. Implements the SessionStorage interface for standard Web Request/Response objects used by Hono and other modern frameworks.
imports:
- sym: '@workos/authkit-session.CookieSessionStorage'
- sym: '@workos/authkit-session.AuthKitConfig'
  kind: typing
types:
- name: WebSessionStorage
  kind: struct
  ctx: Extract the encrypted session cookie from a Request.
  bases:
  - CookieSessionStorage
  methods:
  - name: constructor
    sig: 'constructor(config: AuthKitConfig)'
    lines:
    - 18
    - 20
    detail:
      calls:
      - super
  - name: getSession
    sig: 'getSession(request: Request): Promise<string | null>'
    is_async: true
    lines:
    - 28
    - 48
    detail:
      calls:
      - cookieHeader.split
      - cookieHeader.split(';').reduce
      - request.headers.get
  lines:
  - 17
  - 49
exports:
- name: WebSessionStorage
  kind: type
