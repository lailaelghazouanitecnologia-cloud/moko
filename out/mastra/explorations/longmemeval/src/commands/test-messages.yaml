## Roska File Descriptor — commands/test-messages.ts
file: commands/test-messages.ts
lines: 95
purpose: Test each message individually to find exact trigger(s).
imports:
- sym: '@ai-sdk/google.google'
- sym: ai.generateText
- sym: fs.readFileSync
data:
- name: safetySettings
  type: array
  val: |-
    [
      { category: 'HARM_CATEGORY_HATE_SPEECH' as const, threshold: 'OFF' as const },
      { category: 'HARM_CATEGORY_DANGEROU...
functions:
- name: testContent
  sig: 'testContent(content: string): Promise<boolean>'
  is_async: true
  lines:
  - 17
  - 31
  detail:
    calls:
    - String
    - errorStr.includes
    - generateText
    - google
- name: parseMessages
  sig: 'parseMessages(content: string): string[]'
  lines:
  - 33
  - 49
  detail:
    calls:
    - content.split
    - messages.push
    - part.trim
    - trimmed.startsWith
- name: main
  sig: main()
  is_async: true
  lines:
  - 51
  - 93
  detail:
    calls:
    - console.log
    - messages.filter
    - messages.filter((_, idx) => idx !== i).join
    - messages.join
    - messages.slice
    - messages.slice(0, i + 1).join
    - messages[i].slice
    - messages[i].slice(0, 150).replace
    - messages[i].slice(0, 80).replace
    - parseMessages
    - readFileSync
    - testContent
exports:
- name: testContent
  kind: func
- name: parseMessages
  kind: func
- name: main
  kind: func
- name: safetySettings
  kind: data
