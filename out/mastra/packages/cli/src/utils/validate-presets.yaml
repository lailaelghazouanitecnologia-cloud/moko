## Roska File Descriptor — utils/validate-presets.ts
file: utils/validate-presets.ts
lines: 60
imports:
- sym: node:fs.existsSync
- sym: node:fs/promises.readFile
- sym: node:path.resolve
functions:
- name: loadAndValidatePresets
  sig: 'loadAndValidatePresets(presetsPath: string): Promise<string>'
  is_async: true
  lines:
  - 12
  - 40
  detail:
    calls:
    - Array.isArray
    - JSON.parse
    - Object.entries
    - existsSync
    - new Error
    - process.cwd
    - readFile
    - resolve
- name: escapeJsonForHtml
  sig: 'escapeJsonForHtml(json: string): string'
  lines:
  - 50
  - 60
  detail:
    calls:
    - |-
      json
          .replace
    - |-
      json
          .replace(/\\/g, '\\\\')
          .replace
    - |-
      json
          .replace(/\\/g, '\\\\')
          .replace(/'/g, "\\'")
          .replace
    - |-
      json
          .replace(/\\/g, '\\\\')
          .replace(/'/g, "\\'")
          .replace(/\n/g, '\\n')
          .replace
    - |-
      json
          .replace(/\\/g, '\\\\')
          .replace(/'/g, "\\'")
          .replace(/\n/g, '\\n')
          .replace(/...
exports:
- name: loadAndValidatePresets
  kind: func
- name: escapeJsonForHtml
  kind: func
