## Roska File Descriptor — observational-memory/date-utils.ts
file: observational-memory/date-utils.ts
lines: 244
purpose: Date/time utility functions for Observational Memory. Pure functions for formatting relative timestamps and annotating observations.
functions:
- name: formatRelativeTime
  sig: 'formatRelativeTime(date: Date, currentDate: Date): string'
  lines:
  - 9
  - 33
  detail:
    calls:
    - Math.abs
    - Math.floor
    - currentDate.getTime
    - date.getTime
- name: formatGapBetweenDates
  sig: 'formatGapBetweenDates(prevDate: Date, currDate: Date): string | null'
  lines:
  - 39
  - 58
  detail:
    calls:
    - Math.floor
    - currDate.getTime
    - prevDate.getTime
- name: parseDateFromContent
  sig: 'parseDateFromContent(dateContent: string): Date | null'
  lines:
  - 64
  - 119
  detail:
    calls:
    - dateContent.match
    - isNaN
    - new Date
    - parsed.getTime
    - vagueMatch[1]!.toLowerCase
- name: isFutureIntentObservation
  sig: 'isFutureIntentObservation(line: string): boolean'
  lines:
  - 124
  - 137
  detail:
    calls:
    - futureIntentPatterns.some
- name: expandInlineEstimatedDates
  sig: 'expandInlineEstimatedDates(observations: string, currentDate: Date): string'
  lines:
  - 144
  - 178
  detail:
    calls:
    - observations.replace
- name: addRelativeTimeToObservations
  sig: 'addRelativeTimeToObservations(observations: string, currentDate: Date): string'
  lines:
  - 185
  - 244
  detail:
    calls:
    - dateHeaderRegex.exec
    - dates.push
    - expandInlineEstimatedDates
    - formatGapBetweenDates
    - formatRelativeTime
    - isNaN
    - new Date
    - parsed.getTime
    - withInlineDates.slice
exports:
- name: formatRelativeTime
  kind: func
- name: formatGapBetweenDates
  kind: func
- name: parseDateFromContent
  kind: func
- name: isFutureIntentObservation
  kind: func
- name: expandInlineEstimatedDates
  kind: func
- name: addRelativeTimeToObservations
  kind: func
