## Roska File Descriptor — __tests__/abort-signal.test.ts
file: __tests__/abort-signal.test.ts
lines: 205
purpose: 'Abort Signal Tests Tests abort signal propagation at two levels: 1. Baseline: agent.generate() returns empty result on abort (framework behavior) 2. OM-level: withAbortCheck detects the abort and throws AbortError'
imports:
- sym: '@mastra/core/agent.Agent'
- sym: vitest.describe
- sym: vitest.it
- sym: vitest.expect
functions:
- name: createSlowModel
  sig: 'createSlowModel(delayMs: number)'
  lines:
  - 16
  - 93
  detail:
    calls:
    - controller.close
    - controller.enqueue
    - controller.error
    - new DOMException
    - new Date
    - new Promise
    - new ReadableStream
exports:
- name: createSlowModel
  kind: func
