## Roska File Descriptor — __tests__/mock-om-agent.test.ts
file: __tests__/mock-om-agent.test.ts
lines: 563
purpose: 'Mock OM Agent Integration Test Tests that a mock model correctly triggers multi-step execution and OM observation. This validates the mock setup before using it in Playground E2E tests. Flow: 1. Step 0: Mock model returns tool-call → finishReason: ''tool-calls'' 2. Tool executes, results added to messages 3. Step 1: Mock model returns text → finishReason: ''stop'' 4. OM processor sees stepNumber=1, checks threshold, triggers observation'
imports:
- sym: '@internal/ai-sdk-v5/test.MockLanguageModelV2'
- sym: '@internal/ai-sdk-v5/test.convertArrayToReadableStream'
- sym: '@mastra/core/agent.Agent'
- sym: '@mastra/core/storage.InMemoryStore'
- sym: '@mastra/core/tools.createTool'
- sym: vitest.describe
- sym: vitest.it
- sym: vitest.expect
- sym: vitest.beforeEach
- sym: zod.z
- sym: ../../../...Memory
data:
- name: omTriggerTool
  type: any
  val: |-
    createTool({
      id: 'test',
      description: 'Trigger tool for OM testing',
      inputSchema: z.object({
        action: z.string(...
- name: longResponseText
  type: string
  val: '`I understand your request completely. Let me provide you with a comprehensive and detailed response that covers all the...'
functions:
- name: createMockOmModel
  sig: |-
    createMockOmModel(
      responseText: string,
      toolName = 'test',
      toolInput: Record<string, unknown> = { action: 'trigger' },
    )
  lines:
  - 28
  - 124
  detail:
    calls:
    - new MockLanguageModelV2
- name: createMockObserverModel
  sig: createMockObserverModel()
  lines:
  - 130
  - 169
  detail:
    calls:
    - new MockLanguageModelV2
- name: createMockReflectorModel
  sig: createMockReflectorModel()
  lines:
  - 171
  - 205
  detail:
    calls:
    - new MockLanguageModelV2
exports:
- name: createMockOmModel
  kind: func
- name: createMockObserverModel
  kind: func
- name: createMockReflectorModel
  kind: func
- name: omTriggerTool
  kind: data
- name: longResponseText
  kind: data
