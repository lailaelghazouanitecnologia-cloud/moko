## Roska File Descriptor — src/providers.ts
file: src/providers.ts
lines: 67
purpose: Built-in workspace provider descriptors. These are auto-registered by MastraEditor and always available. External providers (S3, GCS, E2B) are supplied via MastraEditorConfig.
imports:
- sym: '@mastra/core/editor.FilesystemProvider'
  kind: typing
- sym: '@mastra/core/editor.SandboxProvider'
  kind: typing
- sym: '@mastra/core/workspace.LocalFilesystem'
- sym: '@mastra/core/workspace.LocalSandbox'
data:
- name: localFilesystemProvider
  type: |-
    FilesystemProvider<{
      basePath: string;
      contained?: boolean;
      readOnly?: boolean;
    }>
  val: |-
    {
      id: 'local',
      name: 'Local Filesystem',
      description: 'A folder on the local disk',
      configSchema: {
        type: 'o...
- name: localSandboxProvider
  type: |-
    SandboxProvider<{
      workingDirectory?: string;
      timeout?: number;
      isolation?: 'none' | 'seatbelt' | 'bwrap';
      env?: Record<string, string>;
    }>
  val: |-
    {
      id: 'local',
      name: 'Local Sandbox',
      description: 'Execute commands on the local machine',
      configSchema: {
        ...
exports:
- name: localFilesystemProvider
  kind: data
- name: localSandboxProvider
  kind: data
