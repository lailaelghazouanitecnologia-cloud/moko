## Roska File Descriptor — server/base.ts
file: server/base.ts
lines: 60
imports:
- sym: ../base.MastraBase
- sym: ../logger/constants.RegisteredLogger
types:
- name: MastraServerBase
  kind: trait
  ctx: Get the app instance. Returns the server app that was passed to the constructor. This allows users to access the underlying server framework's app for direct operations like calling routes via app.fetch() (Hono) or using the app for testing. ```typescript const app = adapter.getApp<Hono>(); const response = await app.fetch(new Request('http://localhost/api/agents')); ```
  bases:
  - MastraBase
  decorators:
  - abstract
  methods:
  - name: constructor
    sig: 'constructor({ app, name }: { app: TApp; name?: string })'
    lines:
    - 27
    - 31
    detail:
      calls:
      - super
      - this.logger.debug
  - name: getApp
    sig: 'getApp(): T'
    lines:
    - 49
    - 51
    detail: {}
  lines:
  - 24
  - 60
exports:
- name: MastraServerBase
  kind: type
