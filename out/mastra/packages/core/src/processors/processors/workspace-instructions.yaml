## Roska File Descriptor — processors/workspace-instructions.ts
file: processors/workspace-instructions.ts
lines: 69
purpose: 'WorkspaceInstructionsProcessor Injects workspace environment instructions (filesystem paths, sandbox info, mount states) into the system message so agents understand which paths are accessible in shell commands vs. file tools. Auto-wired by Agent when a workspace is configured. ```typescript // Auto-created by Agent when workspace exists const agent = new Agent({ workspace: new Workspace({ filesystem: new LocalFilesystem({ basePath: ''./data'' }), sandbox: new LocalSandbox(), }), }); // Or explicit processor control: const agent = new Agent({ workspace, inputProcessors: [new WorkspaceInstructionsProcessor({ workspace })], }); ```'
imports:
- sym: ../../workspace/workspace.AnyWorkspace
  kind: typing
- sym: ../index.ProcessInputStepArgs
  kind: typing
- sym: ../index.Processor
  kind: typing
types:
- name: WorkspaceInstructionsProcessorOptions
  kind: trait
  decorators:
  - interface
  fields:
  - name: workspace
    type: AnyWorkspace
  lines:
  - 38
  - 43
- name: WorkspaceInstructionsProcessor
  kind: struct
  bases:
  - Processor<'workspace-instructions-processor'>
  fields:
  - name: id
    type: any
    default: '''workspace-instructions-processor'' as const'
  - name: name
    type: any
    default: '''Workspace Instructions Processor'''
  - name: _workspace
    type: AnyWorkspace
  methods:
  - name: constructor
    sig: 'constructor(opts: WorkspaceInstructionsProcessorOptions)'
    lines:
    - 58
    - 60
    detail: {}
  - name: processInputStep
    sig: 'processInputStep({ messageList, requestContext }: ProcessInputStepArgs)'
    is_async: true
    lines:
    - 62
    - 68
    detail:
      calls:
      - messageList.addSystem
      - this._workspace.getInstructions
  lines:
  - 52
  - 69
exports:
- name: WorkspaceInstructionsProcessorOptions
  kind: type
- name: WorkspaceInstructionsProcessor
  kind: type
