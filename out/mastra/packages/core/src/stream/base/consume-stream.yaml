## Roska File Descriptor — base/consume-stream.ts
file: base/consume-stream.ts
lines: 19
functions:
- name: consumeStream
  sig: |-
    consumeStream({
      stream,
      onError,
    }: {
      stream: ReadableStream;
      onError?: (error: unknown) => void;
    })
  is_async: true
  lines:
  - 1
  - 19
  detail:
    calls:
    - onError
    - reader.read
    - reader.releaseLock
    - stream.getReader
exports:
- name: consumeStream
  kind: func
