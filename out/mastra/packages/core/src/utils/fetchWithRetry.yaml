## Roska File Descriptor — utils/fetchWithRetry.ts
file: utils/fetchWithRetry.ts
lines: 60
purpose: Fetches a URL with retry logic
functions:
- name: fetchWithRetry
  sig: |-
    fetchWithRetry(
      url: string,
      options: RequestInit = {},
      maxRetries: number = 3,
    ): Promise<Response>
  is_async: true
  lines:
  - 8
  - 60
  detail:
    calls:
    - Math.min
    - Math.pow
    - String
    - fetch
    - lastError.message.includes
    - new Error
    - new Promise
exports:
- name: fetchWithRetry
  kind: func
