## Roska Module Descriptor — mastra/packages/core/src/auth/defaults
name: defaults
purpose: Default implementations for authentication. For EE defaults (roles, RBAC providers), see `@mastra/core/auth/ee`.
total_lines: 714
files:
- file: defaults/index.ts
  lines: 8
  purpose: Default implementations for authentication. For EE defaults (roles, RBAC providers), see `@mastra/core/auth/ee`.
  types: 0
  functions: 0
  imports: 0
  exports: 0
submodules:
- session
