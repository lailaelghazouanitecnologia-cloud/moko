## Roska File Descriptor — defaults/roles.ts
file: defaults/roles.ts
lines: 250
purpose: Default roles and permissions for Mastra Studio.
imports:
- sym: ../interfaces.RoleDefinition
  kind: typing
- sym: ../interfaces.RoleMapping
  kind: typing
data:
- name: DEFAULT_ROLES
  type: RoleDefinition[]
  val: |-
    [
      {
        id: 'owner',
        name: 'Owner',
        description: 'Full access to all features and settings',
        permissions: ...
functions:
- name: getDefaultRole
  sig: 'getDefaultRole(roleId: string): RoleDefinition | undefined'
  lines:
  - 65
  - 67
  detail:
    calls:
    - DEFAULT_ROLES.find
- name: resolvePermissions
  sig: 'resolvePermissions(roleIds: string[], roles: RoleDefinition[] = DEFAULT_ROLES): string[]'
  lines:
  - 78
  - 106
  detail:
    calls:
    - Array.from
    - new Set
    - resolveRole
- name: matchesPermission
  sig: 'matchesPermission(userPermission: string, requiredPermission: string): boolean'
  lines:
  - 125
  - 192
  detail:
    calls:
    - requiredPermission.split
    - userPermission.split
- name: hasPermission
  sig: 'hasPermission(userPermissions: string[], requiredPermission: string): boolean'
  lines:
  - 201
  - 203
  detail:
    calls:
    - userPermissions.some
- name: resolvePermissionsFromMapping
  sig: 'resolvePermissionsFromMapping(roles: string[], mapping: RoleMapping): string[]'
  lines:
  - 231
  - 250
  detail:
    calls:
    - Array.from
    - new Set
    - permissions.add
exports:
- name: getDefaultRole
  kind: func
- name: resolvePermissions
  kind: func
- name: matchesPermission
  kind: func
- name: hasPermission
  kind: func
- name: resolvePermissionsFromMapping
  kind: func
- name: DEFAULT_ROLES
  kind: data
