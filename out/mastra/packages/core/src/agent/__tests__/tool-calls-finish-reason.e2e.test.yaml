## Roska File Descriptor — __tests__/tool-calls-finish-reason.e2e.test.ts
file: __tests__/tool-calls-finish-reason.e2e.test.ts
lines: 158
purpose: 'E2E test: Verify that different LLM providers correctly continue the agent loop when tool calls are present, regardless of the finishReason returned. Some models return finishReason: ''stop'' even when tool calls are present. The agent loop must continue processing tool results in all cases.'
imports:
- sym: '@ai-sdk/anthropic-v5.anthropic'
- sym: '@ai-sdk/google-v5.google'
- sym: '@ai-sdk/openai-v5.openai as openai_v5'
- sym: '@ai-sdk/openai-v6.openai as openai_v6'
- sym: '@internal/test-utils.createGatewayMock'
- sym: dotenv.config
- sym: vitest.afterAll
- sym: vitest.beforeAll
- sym: vitest.describe
- sym: vitest.expect
- sym: vitest.it
- sym: zod.z
- sym: ../../tools.createTool
- sym: ../agent.Agent
data:
- name: mock
  type: any
  val: createGatewayMock()
- name: getWeather
  type: any
  val: |-
    createTool({
      id: 'getWeather',
      description: 'Get the current weather for a city',
      inputSchema: z.object({
        city...
- name: getPopulation
  type: any
  val: |-
    createTool({
      id: 'getPopulation',
      description: 'Get the population of a city',
      inputSchema: z.object({
        city: z...
- name: getTimezone
  type: any
  val: |-
    createTool({
      id: 'getTimezone',
      description: 'Get the timezone of a city',
      inputSchema: z.object({
        city: z.str...
- name: getLanguage
  type: any
  val: |-
    createTool({
      id: 'getLanguage',
      description: 'Get the primary language spoken in a city',
      inputSchema: z.object({
    ...
- name: getCurrency
  type: any
  val: |-
    createTool({
      id: 'getCurrency',
      description: 'Get the currency used in a city',
      inputSchema: z.object({
        city: ...
functions:
- name: createTestAgent
  sig: 'createTestAgent(model: any)'
  lines:
  - 80
  - 89
  detail:
    calls:
    - new Agent
- name: runToolCallTest
  sig: 'runToolCallTest(agent: Agent, modelName: string)'
  is_async: true
  lines:
  - 91
  - 132
  detail:
    calls:
    - JSON.stringify
    - agent.stream
    - console.log
    - expect
    - expect(finishReasons.length).toBeGreaterThanOrEqual
    - expect(text).toBeTruthy
    - expect(text.length).toBeGreaterThan
    - expect(toolCallCount).toBe
    - expect(toolResultCount).toBe
    - finishReasons.push
exports:
- name: createTestAgent
  kind: func
- name: runToolCallTest
  kind: func
- name: mock
  kind: data
- name: getWeather
  kind: data
- name: getPopulation
  kind: data
- name: getTimezone
  kind: data
- name: getLanguage
  kind: data
- name: getCurrency
  kind: data
