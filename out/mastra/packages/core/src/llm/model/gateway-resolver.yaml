## Roska File Descriptor — model/gateway-resolver.ts
file: model/gateway-resolver.ts
lines: 46
types:
- name: ResolvedModelConfig
  kind: typealias
  decorators:
  - type
  fields:
  - name: url
    type: string | false
  - name: headers
    type: Record<string, string>
  - name: resolvedModelId
    type: string
  - name: fullModelId
    type: string
  lines:
  - 1
  - 6
functions:
- name: parseModelRouterId
  sig: 'parseModelRouterId(routerId: string, gatewayPrefix?: string): { providerId: string; modelId: string }'
  lines:
  - 8
  - 46
  detail:
    calls:
    - idParts.at
    - idParts.slice
    - idParts.slice(1).join
    - 'idParts.slice(gatewayPrefix ? 2 : 1).join'
    - new Error
    - routerId.includes
    - routerId.split
    - routerId.startsWith
exports:
- name: ResolvedModelConfig
  kind: type
- name: parseModelRouterId
  kind: func
