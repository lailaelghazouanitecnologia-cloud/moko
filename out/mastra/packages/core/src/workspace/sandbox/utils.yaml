## Roska File Descriptor — sandbox/utils.ts
file: sandbox/utils.ts
lines: 9
purpose: Shell-quote an argument for safe interpolation into a shell command string. Safe characters (alphanumeric, `.`, `_`, `-`, `/`, `=`, `:`, `@`) pass through. Everything else is wrapped in single quotes with embedded quotes escaped.
functions:
- name: shellQuote
  sig: 'shellQuote(arg: string): string'
  lines:
  - 6
  - 9
  detail:
    calls:
    - /^[a-zA-Z0-9._\-\/=:@]+$/.test
    - arg.replace
exports:
- name: shellQuote
  kind: func
