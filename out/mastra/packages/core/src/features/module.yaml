## Roska Module Descriptor — mastra/packages/core/src/features
name: features
purpose: Core feature flags for @mastra/core This set tracks which features are available in the current version of @mastra/core. Dependent packages can check for feature availability to ensure compatibility. ```ts import { coreFeatures } from "@mastra/core/features" if (coreFeatures.has('workspaces-v1')) { // Workspace features available } ```
total_lines: 24
files:
- file: features/index.ts
  lines: 24
  purpose: Core feature flags for @mastra/core This set tracks which features are available in the current version of @mastra/core. Dependent packages can check for feature availability to ensure compatibility. ```ts import { coreFeatures } from "@mastra/core/features" if (coreFeatures.has('workspaces-v1')) { // Workspace features available } ```
  types: 0
  functions: 0
  imports: 0
  exports: 1
