## Roska Module Descriptor — mastra/packages/_llm-recorder/src
name: src
total_lines: 2927
files:
- file: src/llm-recorder.ts
  lines: 1166
  purpose: eslint-disable no-console
  types: 8
  functions: 25
  imports: 14
  exports: 38
- file: src/llm-contract.test.ts
  lines: 285
  purpose: 'LLM Contract Validation Tests These tests validate that LLM API response schemas haven''t changed. Run nightly to detect API drift before it breaks production. Usage: # Nightly contract validation (makes live API calls) CONTRACT_TEST=true pnpm vitest run packages/_llm-recorder/src/llm-contract.test.ts # Unit tests (no API calls, tests the validation logic) pnpm vitest run packages/_llm-recorder/src/llm-contract.test.ts'
  types: 0
  functions: 0
  imports: 10
  exports: 3
- file: src/vite-plugin.test.ts
  lines: 239
  types: 0
  functions: 0
  imports: 5
  exports: 0
- file: src/index.ts
  lines: 3
  types: 0
  functions: 0
  imports: 0
  exports: 0
- file: src/llm-recorder.test.ts
  lines: 471
  purpose: 'LLM Recorder Tests Demonstrates the unified LLM recording/replay API with mode switching. ## Test Modes ```bash # Auto mode (default) - replay if recording exists, record if not pnpm vitest run src/llm-recorder.test.ts # Force re-record all recordings UPDATE_RECORDINGS=true pnpm vitest run src/llm-recorder.test.ts # Skip recording entirely (real API calls) LLM_TEST_MODE=live pnpm vitest run src/llm-recorder.test.ts ```'
  types: 0
  functions: 0
  imports: 16
  exports: 0
- file: src/auto-recording.ts
  lines: 138
  purpose: 'Auto-Recording Module Provides a simple function to enable LLM recording/replay for a test file with automatic name derivation from the test file path. This is a lighter alternative to the Vite plugin — just import and call `enableAutoRecording()` at the top of your test file. ```typescript import { enableAutoRecording } from ''@internal/llm-recorder''; // Auto-derives recording name from file path enableAutoRecording(); describe(''My Tests'', () => { it(''works'', async () => { const result = await agent.generate(''Hello''); expect(result.text).toBeDefined(); }); }); ``` ```typescript // With custom name enableAutoRecording({ nameOverride: ''my-custom-name'' }); ```'
  types: 1
  functions: 2
  imports: 4
  exports: 3
- file: src/vite-plugin.ts
  lines: 238
  purpose: 'Vitest LLM Recorder Plugin A Vite plugin that automatically injects LLM recording/replay setup into test files. This eliminates the need to manually call `useLLMRecording()` in every test file. The plugin transforms test files at build time, injecting the recording setup code before any test definitions. Recording names are auto-derived from file paths. ```typescript // vitest.config.ts import { llmRecorderPlugin } from ''@internal/llm-recorder/vite-plugin''; export default defineConfig({ plugins: [llmRecorderPlugin()], test: { ... } }); ```'
  types: 1
  functions: 3
  imports: 2
  exports: 4
- file: src/llm-contract.ts
  lines: 387
  purpose: LLM Contract Validation Validates LLM API response structures against known schemas. Useful for detecting API drift before it breaks production code. The validator compares response _structure_ (types, field presence) rather than exact values, making it resilient to normal response variation. ```typescript import { validateLLMContract, extractSchema, formatContractResult } from '@internal/llm-recorder'; // Compare actual response against expected structure const result = validateLLMContract(actualResponse, expectedResponse); console.log(formatContractResult(result)); ```
  types: 4
  functions: 8
  imports: 1
  exports: 14
