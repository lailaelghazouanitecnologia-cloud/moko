## Roska File Descriptor — a2a/store.ts
file: a2a/store.ts
lines: 26
imports:
- sym: '@mastra/core/a2a.Task'
  kind: typing
types:
- name: InMemoryTaskStore
  kind: struct
  fields:
  - name: store
    type: Map<string, Task>
    default: new Map()
  - name: activeCancellations
    type: any
    default: new Set<string>()
  methods:
  - name: load
    sig: 'load({ agentId, taskId }: { agentId: string; taskId: string }): Promise<Task | null>'
    is_async: true
    lines:
    - 7
    - 16
    detail:
      calls:
      - this.store.get
  - name: save
    sig: 'save({ agentId, data }: { agentId: string; data: Task }): Promise<void>'
    is_async: true
    lines:
    - 18
    - 25
    detail:
      calls:
      - new Error
      - this.store.set
  lines:
  - 3
  - 26
exports:
- name: InMemoryTaskStore
  kind: type
