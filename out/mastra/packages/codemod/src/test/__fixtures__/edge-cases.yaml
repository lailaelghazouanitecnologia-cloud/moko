## Roska File Descriptor — __fixtures__/edge-cases.ts
file: __fixtures__/edge-cases.ts
lines: 32
purpose: This file contains code snippets that triggered codemod errors and that should not be transformed.
data:
- name: toStringOne
  type: string
  val: |-
    `
    const providers = {
      cursor: {
        title: "Open in Cursor",
        createUrl: (text: string) => {
          const url = new ...
- name: toStringTwo
  type: string
  val: |-
    `
    function safeJsonStringify(obj: unknown): string {
      return JSON.stringify(obj, (_, value) => {
        if (typeof value =...
- name: EDGE_CASES_FIXTURES
  type: '{ name: string; code: string }[]'
  val: |-
    [
      { name: 'to-string-one', code: toStringOne },
      { name: 'to-string-two', code: toStringTwo },
    ]
exports:
- name: toStringOne
  kind: data
- name: toStringTwo
  kind: data
- name: EDGE_CASES_FIXTURES
  kind: data
