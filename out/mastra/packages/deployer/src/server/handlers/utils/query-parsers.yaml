## Roska File Descriptor — utils/query-parsers.ts
file: utils/query-parsers.ts
lines: 50
purpose: Parse a limit query parameter value to a valid number
functions:
- name: parseLimit
  sig: 'parseLimit(rawLimit: string | undefined): number | undefined'
  lines:
  - 6
  - 17
  detail:
    calls:
    - Number
    - Number.isFinite
    - Number.isInteger
- name: parsePage
  sig: 'parsePage(value: string | undefined, defaultValue: number = 0): number'
  lines:
  - 25
  - 28
  detail:
    calls:
    - Math.max
    - String
    - isNaN
    - parseInt
- name: parsePerPage
  sig: |-
    parsePerPage(
      value: string | undefined,
      defaultValue: number = 100,
      max: number = 1000,
    ): number | false
  lines:
  - 37
  - 50
  detail:
    calls:
    - (value || '').trim
    - (value || '').trim().toLowerCase
    - Math.max
    - Math.min
    - String
    - isNaN
    - parseInt
exports:
- name: parseLimit
  kind: func
- name: parsePage
  kind: func
- name: parsePerPage
  kind: func
