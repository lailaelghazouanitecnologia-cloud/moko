## Roska File Descriptor — components/agent-version-panel.tsx
file: components/agent-version-panel.tsx
lines: 95
imports:
- sym: '@/lib/utils.cn'
- sym: '@/ds/components/ScrollArea.ScrollArea'
- sym: '@/ds/components/Badge.Badge'
- sym: '@/ds/components/Txt.Txt'
- sym: ../hooks/use-agent-versions.useAgentVersions
data:
- name: '{ data, isLoading }'
  type: any
  val: |-
    useAgentVersions({
        agentId,
        params: { sortDirection: 'DESC' },
      })
- name: versions
  type: any
  val: data?.versions ?? []
- name: activeVersion
  type: any
  val: 'activeVersionId ? versions.find(v => v.id === activeVersionId) : undefined'
- name: activeVersionNumber
  type: any
  val: activeVersion?.versionNumber
types:
- name: AgentVersionPanelProps
  kind: trait
  decorators:
  - interface
  fields:
  - name: agentId
    type: string
  - name: selectedVersionId
    type: string
  - name: onVersionSelect
    type: '(versionId: string) => void'
  - name: activeVersionId
    type: string
  lines:
  - 18
  - 23
functions:
- name: formatTimestamp
  sig: 'formatTimestamp(isoString: string): string'
  lines:
  - 7
  - 16
  detail:
    calls:
    - date.toLocaleDateString
    - new Date
exports:
- name: AgentVersionPanelProps
  kind: type
- name: formatTimestamp
  kind: func
- name: '{ data, isLoading }'
  kind: data
- name: versions
  kind: data
- name: activeVersion
  kind: data
- name: activeVersionNumber
  kind: data
