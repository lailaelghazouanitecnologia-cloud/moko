## Roska File Descriptor — agent-metadata/utils.ts
file: agent-metadata/utils.ts
lines: 51
purpose: Removes any provider API suffixes like .chat, .responses, .messages, .completion from provider IDs to get the clean provider name. cleanProviderId('cerebras.chat') // returns 'cerebras' cleanProviderId('anthropic.messages') // returns 'anthropic' cleanProviderId('openai.responses') // returns 'openai' cleanProviderId('openai') // returns 'openai'
functions:
- name: cleanProviderId
  sig: 'cleanProviderId(providerId: string): string'
  lines:
  - 11
  - 13
  detail:
    calls:
    - providerId.includes
    - providerId.split
- name: findProviderById
  sig: 'findProviderById(providers: T[], providerId: string): T | undefined'
  lines:
  - 32
  - 51
  detail:
    calls:
    - cleanId.includes
    - cleanProviderId
    - providers.find
exports:
- name: cleanProviderId
  kind: func
- name: findProviderById
  kind: func
