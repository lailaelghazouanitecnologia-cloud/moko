## Roska File Descriptor — agent-information/agent-memory-config.tsx
file: agent-information/agent-memory-config.tsx
lines: 192
imports:
- sym: '@/lib/utils.cn'
- sym: '@/ds/components/Skeleton.Skeleton'
- sym: lucide-react.ChevronRight
- sym: lucide-react.ChevronDown
- sym: react.useState
- sym: react.useMemo
- sym: '@/domains/memory/hooks.useMemoryConfig'
- sym: '@mastra/core/memory.SemanticRecall'
data:
- name: '{ data, isLoading }'
  type: any
  val: useMemoryConfig(agentId)
- name: '[expandedSections, setExpandedSections]'
  type: any
  val: useState<Set<string>>(new Set(['General', 'Semantic Recall']))
- name: config
  type: any
  val: data?.config
- name: configSections
  type: MemoryConfigSection[]
  val: |-
    useMemo(() => {
        if (!config) return [];

        // Memory is enabled if we have a config
        const memoryEnabled = !!co...
types:
- name: MemoryConfigSection
  kind: trait
  decorators:
  - interface
  fields:
  - name: items
    type: |-
      Array<{
          label: string;
          value: string | number | boolean | undefined;
          badge?: 'success' | 'info' | 'warning';
        }>
  lines:
  - 8
  - 15
- name: AgentMemoryConfigProps
  kind: trait
  decorators:
  - interface
  fields:
  - name: agentId
    type: string
  lines:
  - 17
  - 19
functions:
- name: toggleSection
  sig: 'toggleSection(title: string)'
  lines:
  - 103
  - 111
  detail:
    calls:
    - new Set
    - newExpanded.add
    - newExpanded.delete
    - newExpanded.has
    - setExpandedSections
exports:
- name: MemoryConfigSection
  kind: type
- name: AgentMemoryConfigProps
  kind: type
- name: toggleSection
  kind: func
- name: '{ data, isLoading }'
  kind: data
- name: '[expandedSections, setExpandedSections]'
  kind: data
- name: config
  kind: data
- name: configSections
  kind: data
