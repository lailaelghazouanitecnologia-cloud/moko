## Roska File Descriptor — StatusBadge/StatusBadge.tsx
file: StatusBadge/StatusBadge.tsx
lines: 79
imports:
- sym: react.React
- sym: class-variance-authority.cva
- sym: class-variance-authority.VariantProps
  kind: typing
- sym: '@/lib/utils.cn'
data:
- name: statusBadgeVariants
  type: any
  val: |-
    cva(
      // Base styles
      'inline-flex items-center gap-1.5 rounded-full text-ui-xs font-medium transition-colors duration...
- name: dotVariants
  type: any
  val: |-
    cva('rounded-full', {
      variants: {
        variant: {
          success: 'bg-accent1',
          warning: 'bg-accent6',
          error...
types:
- name: StatusBadgeProps
  kind: typealias
  decorators:
  - type
  lines:
  - 67
  - 70
functions:
- name: StatusBadge
  sig: 'StatusBadge({ className, variant, size, withDot, pulse, children, ...props }: StatusBadgeProps)'
  lines:
  - 72
  - 79
  detail:
    calls:
    - statusBadgeVariants
exports:
- name: StatusBadgeProps
  kind: type
- name: StatusBadge
  kind: func
- name: statusBadgeVariants
  kind: data
- name: dotVariants
  kind: data
