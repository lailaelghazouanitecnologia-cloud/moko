## Roska File Descriptor — Table/utils.ts
file: Table/utils.ts
lines: 16
functions:
- name: formatDateCell
  sig: 'formatDateCell(date: Date)'
  lines:
  - 1
  - 16
  detail:
    calls:
    - date.getDate
    - new Intl.DateTimeFormat
    - |-
      new Intl.DateTimeFormat('en-US', {
          hour: '2-digit',
          minute: '2-digit',
          second: '2-digit'...
    - 'new Intl.DateTimeFormat(''en-US'', { month: ''short'' }).format'
    - 'new Intl.DateTimeFormat(''en-US'', { month: ''short'' }).format(date).toUpperCase'
exports:
- name: formatDateCell
  kind: func
