## Roska File Descriptor — lib/string.ts
file: lib/string.ts
lines: 19
functions:
- name: lodashTitleCase
  sig: 'lodashTitleCase(str: string): string'
  lines:
  - 1
  - 12
  detail:
    calls:
    - |-
      camelCased
          .replace
    - |-
      camelCased
          .replace(/([A-Z])/g, ' $1')
          .replace
    - |-
      camelCased
          .replace(/([A-Z])/g, ' $1')
          .replace(/^./, str => str.toUpperCase())
          .trim
    - |-
      str
          .replace
    - |-
      str
          .replace(/[-_\s]+(.)?/g, (_, char) => (char ? char.toUpperCase() : ''))
          .replace
- name: toTitleCase
  sig: 'toTitleCase(str: string, splitChar = '' '')'
  lines:
  - 14
  - 19
  detail:
    calls:
    - |-
      str
          .split
    - |-
      str
          .split(splitChar)
          .map
    - |-
      str
          .split(splitChar)
          .map(word => word.charAt(0).toUpperCase() + word.slice(1).toLowerCase(...
exports:
- name: lodashTitleCase
  kind: func
- name: toTitleCase
  kind: func
