## Roska File Descriptor — processors/apify.ts
file: processors/apify.ts
lines: 32
imports:
- sym: ../types.ServerEntry
  kind: typing
functions:
- name: processApifyServers
  sig: 'processApifyServers(data: any): ServerEntry[]'
  lines:
  - 7
  - 32
  detail:
    calls:
    - Array.isArray
    - |-
      apifyData
          .filter
    - |-
      apifyData
          .filter((item: any) => item && item.name)
          .map
exports:
- name: processApifyServers
  kind: func
