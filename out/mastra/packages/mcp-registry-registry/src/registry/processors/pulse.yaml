## Roska File Descriptor — processors/pulse.ts
file: processors/pulse.ts
lines: 33
imports:
- sym: ../types.ServerEntry
  kind: typing
functions:
- name: processPulseMcpServers
  sig: 'processPulseMcpServers(data: any): ServerEntry[]'
  lines:
  - 7
  - 33
  detail:
    calls:
    - Array.isArray
    - |-
      serversData
          .filter
    - |-
      serversData
          .filter((item: any) => item && item.name)
          .map
    - |-
      serversData
          .filter((item: any) => item && item.name)
          .map((item: any) => {
            const serv...
exports:
- name: processPulseMcpServers
  kind: func
