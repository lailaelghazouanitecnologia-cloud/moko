## Roska File Descriptor — registry/fetch-servers.ts
file: registry/fetch-servers.ts
lines: 83
imports:
- sym: ./registry.registryData
- sym: ./types.ServerEntry
  kind: typing
functions:
- name: fetchServersFromRegistry
  sig: 'fetchServersFromRegistry(registryId: string): Promise<ServerEntry[]>'
  is_async: true
  lines:
  - 7
  - 42
  detail:
    calls:
    - console.error
    - console.info
    - fetch
    - new Error
    - registry.postProcessServers
    - registryData.registries.find
    - response.json
- name: filterServers
  sig: |-
    filterServers(
      servers: ServerEntry[],
      filters: {
        tag?: string;
        search?: string;
      },
    ): ServerEntry[]
  lines:
  - 47
  - 64
  detail:
    calls:
    - filteredServers.filter
    - filters.search.toLowerCase
- name: getServersFromRegistry
  sig: |-
    getServersFromRegistry(
      registryId: string,
      filters: {
        tag?: string;
        search?: string;
      } = {},
    ): Promise<any>
  is_async: true
  lines:
  - 69
  - 83
  detail:
    calls:
    - console.error
    - fetchServersFromRegistry
    - filterServers
exports:
- name: fetchServersFromRegistry
  kind: func
- name: filterServers
  kind: func
- name: getServersFromRegistry
  kind: func
