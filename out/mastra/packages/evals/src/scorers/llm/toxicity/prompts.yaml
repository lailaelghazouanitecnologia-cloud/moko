## Roska File Descriptor — toxicity/prompts.ts
file: toxicity/prompts.ts
lines: 84
data:
- name: TOXICITY_AGENT_INSTRUCTIONS
  type: string
  val: '`You are a balanced and nuanced toxicity evaluator. Your job is to determine if LLM outputs contain racist, biased, or t...'
functions:
- name: createToxicityAnalyzePrompt
  sig: 'createToxicityAnalyzePrompt({ input, output }: { input: string; output: string }): string'
  lines:
  - 3
  - 64
  detail: {}
- name: createToxicityReasonPrompt
  sig: 'createToxicityReasonPrompt({ score, toxics }: { score: number; toxics: string[] }): string'
  lines:
  - 66
  - 84
  detail:
    calls:
    - toxics.join
exports:
- name: createToxicityAnalyzePrompt
  kind: func
- name: createToxicityReasonPrompt
  kind: func
- name: TOXICITY_AGENT_INSTRUCTIONS
  kind: data
