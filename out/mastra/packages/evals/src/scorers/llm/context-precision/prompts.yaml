## Roska File Descriptor — context-precision/prompts.ts
file: context-precision/prompts.ts
lines: 131
data:
- name: CONTEXT_PRECISION_AGENT_INSTRUCTIONS
  type: string
  val: '`You are a precise context precision evaluator. Your job is to determine if context nodes are relevant for generating th...'
functions:
- name: createContextRelevancePrompt
  sig: |-
    createContextRelevancePrompt({
      input,
      output,
      context,
    }: {
      input: string;
      output: string;
      context: string[];
    })
  lines:
  - 12
  - 80
  detail:
    calls:
    - context.map
    - context.map((ctx, index) => `[${index}] ${ctx}`).join
- name: createContextPrecisionReasonPrompt
  sig: |-
    createContextPrecisionReasonPrompt({
      input,
      output,
      context,
      score,
      scale,
      verdicts,
    }: {
      input: string;
      output: string;
      context: string[];
      score: number;
      scale: number;
      verdicts: { context_index: number; verdict: string; reason: string }[];
    })
  lines:
  - 82
  - 131
  detail:
    calls:
    - JSON.stringify
    - context.map
    - context.map((ctx, index) => `[${index}] ${ctx}`).join
exports:
- name: createContextRelevancePrompt
  kind: func
- name: createContextPrecisionReasonPrompt
  kind: func
- name: CONTEXT_PRECISION_AGENT_INSTRUCTIONS
  kind: data
