## Roska File Descriptor — noise-sensitivity/prompts.ts
file: noise-sensitivity/prompts.ts
lines: 207
data:
- name: NOISE_SENSITIVITY_INSTRUCTIONS
  type: string
  val: '`You are an expert noise sensitivity evaluator. Your job is to analyze how much irrelevant, distracting, or misleading i...'
functions:
- name: createAnalyzePrompt
  sig: |-
    createAnalyzePrompt({
      userQuery,
      baselineResponse,
      noisyQuery,
      noisyResponse,
      noiseType,
    }: {
      userQuery: string;
      baselineResponse: string;
      noisyQuery: string;
      noisyResponse: string;
      noiseType?: string;
    })
  lines:
  - 19
  - 143
  detail: {}
- name: createReasonPrompt
  sig: |-
    createReasonPrompt({
      userQuery,
      score,
      dimensions,
      majorIssues,
      overallAssessment,
    }: {
      userQuery: string;
      score: number;
      dimensions: Array<{
        dimension: string;
        impactLevel: string;
        specificChanges: string;
        noiseInfluence: string;
      }>;
      majorIssues: string[];
      overallAssessment: string;
    })
  lines:
  - 145
  - 207
  detail:
    calls:
    - dimensions.map
    - 'dimensions.map(d => `${d.dimension}: ${d.impactLevel} impact`).join'
    - majorIssues.map
    - majorIssues.map(issue => `- ${issue}`).join
exports:
- name: createAnalyzePrompt
  kind: func
- name: createReasonPrompt
  kind: func
- name: NOISE_SENSITIVITY_INSTRUCTIONS
  kind: data
