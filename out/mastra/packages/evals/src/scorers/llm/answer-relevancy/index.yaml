## Roska File Descriptor — answer-relevancy/index.ts
file: answer-relevancy/index.ts
lines: 93
imports:
- sym: '@mastra/core/evals.createScorer'
- sym: '@mastra/core/llm.MastraModelConfig'
  kind: typing
- sym: zod.z
- sym: ../../utils.roundToTwoDecimals
- sym: ../../utils.getAssistantMessageFromRunOutput
- sym: ../../utils.getUserMessageFromRunInput
- sym: ./prompts.createExtractPrompt
- sym: ./prompts.createReasonPrompt
- sym: ./prompts.createScorePrompt
data:
- name: DEFAULT_OPTIONS
  type: Record<'uncertaintyWeight' | 'scale', number>
  val: |-
    {
      uncertaintyWeight: 0.3,
      scale: 1,
    }
- name: ANSWER_RELEVANCY_AGENT_INSTRUCTIONS
  type: string
  val: |-
    `
        You are a balanced and nuanced answer relevancy evaluator. Your job is to determine if LLM outputs are relevant to...
- name: extractOutputSchema
  type: any
  val: |-
    z.object({
      statements: z.array(z.string()),
    })
functions:
- name: createAnswerRelevancyScorer
  sig: |-
    createAnswerRelevancyScorer({
      model,
      options = DEFAULT_OPTIONS,
    }: {
      model: MastraModelConfig;
      options?: Record<'uncertaintyWeight' | 'scale', number>;
    })
  lines:
  - 28
  - 93
  detail:
    calls:
    - createScorer
    - |-
      createScorer({
          id: 'answer-relevancy-scorer',
          name: 'Answer Relevancy Scorer',
          descripti...
    - z.array
    - z.object
    - z.string
exports:
- name: createAnswerRelevancyScorer
  kind: func
- name: DEFAULT_OPTIONS
  kind: data
- name: ANSWER_RELEVANCY_AGENT_INSTRUCTIONS
  kind: data
- name: extractOutputSchema
  kind: data
