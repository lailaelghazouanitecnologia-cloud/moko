## Roska File Descriptor — context-relevance/prompts.ts
file: context-relevance/prompts.ts
lines: 165
data:
- name: CONTEXT_RELEVANCE_INSTRUCTIONS
  type: string
  val: '`You are an expert context relevance evaluator. Your job is to analyze whether the provided context information was appr...'
functions:
- name: createAnalyzePrompt
  sig: |-
    createAnalyzePrompt({
      userQuery,
      agentResponse,
      providedContext,
    }: {
      userQuery: string;
      agentResponse: string;
      providedContext: string[];
    })
  lines:
  - 18
  - 107
  detail:
    calls:
    - providedContext.map
    - providedContext.map((ctx, index) => `[${index}] ${ctx}`).join
- name: createReasonPrompt
  sig: |-
    createReasonPrompt({
      userQuery,
      score,
      evaluations,
      missingContext,
      scale,
    }: {
      userQuery: string;
      score: number;
      evaluations: Array<{
        context_index: number;
        contextPiece: string;
        relevanceLevel: string;
        wasUsed: boolean;
        reasoning: string;
      }>;
      missingContext: string[];
      scale: number;
    })
  lines:
  - 109
  - 165
  detail:
    calls:
    - |-
      evaluations
        .map
    - |-
      evaluations
        .map(
          evaluation =>
            `[${evaluation.context_index}] Relevance: ${evaluation.r...
    - missingContext.map
    - missingContext.map(item => `- ${item}`).join
exports:
- name: createAnalyzePrompt
  kind: func
- name: createReasonPrompt
  kind: func
- name: CONTEXT_RELEVANCE_INSTRUCTIONS
  kind: data
