## Roska File Descriptor — completeness/index.ts
file: completeness/index.ts
lines: 136
imports:
- sym: '@mastra/core/agent.MastraDBMessage'
  kind: typing
- sym: '@mastra/core/evals.createScorer'
- sym: compromise.nlp
- sym: ../../utils.getTextContentFromMastraDBMessage
functions:
- name: normalizeString
  sig: 'normalizeString(str: string): string'
  lines:
  - 6
  - 12
  detail:
    calls:
    - |-
      str
          .normalize
    - |-
      str
          .normalize('NFD')
          .replace
    - |-
      str
          .normalize('NFD')
          .replace(/[\u0300-\u036f]/g, '')
          .toLowerCase
- name: extractElements
  sig: 'extractElements(doc: any): string[]'
  lines:
  - 14
  - 45
  detail:
    calls:
    - doc.nouns
    - doc.nouns().out
    - doc.terms
    - doc.terms().out
    - doc.topics
    - doc.topics().out
    - doc.verbs
    - doc.verbs().toInfinitive
    - doc.verbs().toInfinitive().out
    - new Set
    - nouns.flatMap
    - terms.flatMap
    - topics.flatMap
    - verbs.flatMap
- name: calculateCoverage
  sig: 'calculateCoverage({ original, simplified }: { original: string[]; simplified: string[] }): number'
  lines:
  - 47
  - 75
  detail:
    calls:
    - original.filter
- name: createCompletenessScorer
  sig: createCompletenessScorer()
  lines:
  - 77
  - 136
  detail:
    calls:
    - createScorer
    - |-
      createScorer({
          id: 'completeness-scorer',
          name: 'Completeness Scorer',
          description:
          ...
exports:
- name: normalizeString
  kind: func
- name: extractElements
  kind: func
- name: calculateCoverage
  kind: func
- name: createCompletenessScorer
  kind: func
