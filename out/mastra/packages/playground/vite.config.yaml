## Roska File Descriptor — playground/vite.config.ts
file: playground/vite.config.ts
lines: 106
imports:
- sym: node:module.builtinModules
- sym: node:path.path
- sym: '@vitejs/plugin-react.react'
- sym: vite.Plugin
  kind: typing
- sym: vite.PluginOption
  kind: typing
- sym: vite.UserConfig
  kind: typing
- sym: vite.defineConfig
data:
- name: nodeOnlyPackages
  type: any
  val: new Set(['execa'])
- name: stubNodeBuiltinsPlugin
  type: Plugin
  val: |-
    {
      name: 'stub-node-builtins',
      enforce: 'pre',
      apply: 'build',
      resolveId(source) {
        if (nodeOnlyPackages.has(s...
functions:
- name: studioStandalonePlugin
  sig: 'studioStandalonePlugin(targetPort: string, targetHost: string): PluginOption'
  lines:
  - 7
  - 20
  detail:
    calls:
    - |-
      html
            .replace
    - |-
      html
            .replace(/%%MASTRA_SERVER_HOST%%/g, targetHost)
            .replace
    - |-
      html
            .replace(/%%MASTRA_SERVER_HOST%%/g, targetHost)
            .replace(/%%MASTRA_SERVER_PORT%%/g,...
exports:
- name: studioStandalonePlugin
  kind: func
- name: nodeOnlyPackages
  kind: data
- name: stubNodeBuiltinsPlugin
  kind: data
