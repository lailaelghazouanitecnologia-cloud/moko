## Roska File Descriptor — processors/tool-summary.ts
file: processors/tool-summary.ts
lines: 164
imports:
- sym: '@mastra/core/agent.Agent'
- sym: '@mastra/core/agent.MastraDBMessage'
  kind: typing
- sym: '@mastra/core/agent.MessageList'
  kind: typing
- sym: '@mastra/core/llm.MastraModelConfig'
  kind: typing
- sym: '@mastra/core/processors.Processor'
  kind: typing
types:
- name: ToolSummaryProcessor
  kind: struct
  ctx: Creates a cache key from tool call arguments
  bases:
  - Processor
  fields:
  - name: id
    type: any
    default: '''tool-summary-processor'''
  - name: name
    type: any
    default: '''ToolSummaryProcessor'''
  - name: summaryAgent
    type: Agent
  - name: summaryCache
    type: Map<string, string>
    default: new Map()
  methods:
  - name: constructor
    sig: 'constructor({ summaryModel }: { summaryModel: MastraModelConfig })'
    lines:
    - 16
    - 24
    detail:
      calls:
      - new Agent
  - name: createCacheKey
    sig: 'createCacheKey(toolCall: any): string'
    lines:
    - 29
    - 45
    detail:
      calls:
      - JSON.stringify
      - Object.keys
      - |-
        Object.keys(args)
              .sort
      - |-
        Object.keys(args)
              .sort()
              .reduce
  - name: clearCache
    sig: 'clearCache(): void'
    lines:
    - 50
    - 52
    detail:
      calls:
      - this.summaryCache.clear
  - name: getCacheStats
    sig: 'getCacheStats(): { size: number; keys: string[] }'
    lines:
    - 57
    - 62
    detail:
      calls:
      - Array.from
      - this.summaryCache.keys
  - name: processInput
    sig: |-
      processInput({
          messages,
          messageList: _messageList,
        }: {
          messages: MastraDBMessage[];
          messageList: MessageList;
          abort: (reason?: string) => never;
        }): Promise<MastraDBMessage[]>
    is_async: true
    lines:
    - 64
    - 163
    detail:
      calls:
      - JSON.stringify
      - Promise.allSettled
      - summaryTasks.forEach
      - summaryTasks.map
      - summaryTasks.push
      - this.createCacheKey
      - this.summaryAgent.generate
      - this.summaryCache.get
  lines:
  - 9
  - 164
exports:
- name: ToolSummaryProcessor
  kind: type
