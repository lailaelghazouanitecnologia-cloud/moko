## Roska File Descriptor — shared/schema.ts
file: shared/schema.ts
lines: 33
imports:
- sym: zod.z
data:
- name: TaskSchema
  type: any
  val: |-
    z.array(
      z.object({
        id: z.string().describe('Unique task ID using kebab-case'),
        content: z.string().describe('...
- name: QuestionSchema
  type: any
  val: |-
    z.array(
      z.object({
        id: z.string().describe('Unique question ID'),
        question: z.string().describe('Clear, speci...
- name: PlanningIterationResultSchema
  type: any
  val: |-
    z.object({
      success: z.boolean(),
      tasks: TaskSchema,
      questions: QuestionSchema,
      reasoning: z.string(),
      planComp...
exports:
- name: TaskSchema
  kind: data
- name: QuestionSchema
  kind: data
- name: PlanningIterationResultSchema
  kind: data
