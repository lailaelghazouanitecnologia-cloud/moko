## Roska File Descriptor — task-planning/prompts.ts
file: task-planning/prompts.ts
lines: 111
purpose: Prompts and instructions for task planning workflow
data:
- name: taskPlanningPrompts
  type: TaskPlanningPrompts
  val: |-
    {
      planningAgent: {
        instructions:
          context => `You are a Mastra workflow planning expert. Your task is to crea...
types:
- name: TaskPlanningPrompts
  kind: trait
  decorators:
  - interface
  fields:
  - name: planningAgent
    type: |-
      {
          instructions: (context: { storedQAPairs: any[] }) => string;
          refinementPrompt: (context: {
            action: string;
            workflowName?: string;
            description?: string;
            requirements?: string;
            discoveredWorkflows: any[];
            projectStructure: any;
            research: any;
            storedQAPairs: any[];
            hasTaskFeedback: boolean;
            userAnswers?: any;
          }) => string;
          initialPrompt: (context: {
            action: string;
            workflowName?: string;
            description?: string;
            requirements?: string;
            discoveredWorkflows: any[];
            projectStructure: any;
            research: any;
          }) => string;
        }
  - name: taskApproval
    type: |-
      {
          message: (questionsCount: number) => string;
          approvalMessage: (tasksCount: number) => string;
        }
  lines:
  - 5
  - 34
exports:
- name: TaskPlanningPrompts
  kind: type
- name: taskPlanningPrompts
  kind: data
