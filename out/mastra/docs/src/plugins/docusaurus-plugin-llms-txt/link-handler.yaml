## Roska File Descriptor — docusaurus-plugin-llms-txt/link-handler.ts
file: docusaurus-plugin-llms-txt/link-handler.ts
lines: 104
purpose: Custom handler for links to convert internal links to llms.txt URLs
imports:
- sym: hast.Element
  kind: typing
- sym: mdast.Link
  kind: typing
- sym: mdast.PhrasingContent
  kind: typing
- sym: hast-util-to-mdast.State
  kind: typing
types:
- name: LinkHandlerOptions
  kind: trait
  decorators:
  - interface
  fields:
  - name: siteUrl
    type: string
  - name: excludeRoutes
    type: string[]
  lines:
  - 9
  - 12
functions:
- name: isInternalLink
  sig: 'isInternalLink(href: string | undefined): boolean'
  lines:
  - 17
  - 36
  detail:
    calls:
    - href.includes
    - href.startsWith
- name: isExcludedRoute
  sig: 'isExcludedRoute(href: string, excludePatterns: string[]): boolean'
  lines:
  - 38
  - 45
  detail:
    calls:
    - href.startsWith
- name: normalizeRoute
  sig: 'normalizeRoute(href: string): string'
  lines:
  - 50
  - 68
  detail:
    calls:
    - href.replace
    - href.replace(/^\.\//, '/').replace
    - normalized.endsWith
    - normalized.slice
    - normalized.split
    - normalized.split('#')[0].split
    - normalized.startsWith
- name: createLinkHandler
  sig: 'createLinkHandler(options: LinkHandlerOptions)'
  lines:
  - 73
  - 104
  detail:
    calls:
    - Array.isArray
    - children.push
    - isExcludedRoute
    - isInternalLink
    - normalizeRoute
    - state.one
exports:
- name: LinkHandlerOptions
  kind: type
- name: isInternalLink
  kind: func
- name: isExcludedRoute
  kind: func
- name: normalizeRoute
  kind: func
- name: createLinkHandler
  kind: func
