## Roska File Descriptor — docusaurus-plugin-llms-txt/options.ts
file: docusaurus-plugin-llms-txt/options.ts
lines: 54
purpose: Plugin options and configuration
data:
- name: DEFAULT_OPTIONS
  type: Omit<ResolvedOptions, 'siteUrl'>
  val: |-
    {
      siteTitle: 'Mastra',
      siteDescription: `Mastra is a framework for building AI-powered applications and agents with ...
types:
- name: LlmsTxtPluginOptions
  kind: trait
  decorators:
  - interface
  fields:
  - name: siteUrl
    type: string
  - name: siteDescription
    type: string
  - name: contentSelectors
    type: string[]
  - name: excludeRoutes
    type: string[]
  - name: enableCache
    type: boolean
  lines:
  - 5
  - 18
- name: ResolvedOptions
  kind: trait
  bases:
  - Required<LlmsTxtPluginOptions>
  decorators:
  - interface
  lines:
  - 20
  - 20
functions:
- name: resolveOptions
  sig: 'resolveOptions(options: LlmsTxtPluginOptions): ResolvedOptions'
  lines:
  - 30
  - 39
  detail: {}
- name: validateOptions
  sig: 'validateOptions(options: LlmsTxtPluginOptions): void'
  lines:
  - 41
  - 54
  detail:
    calls:
    - new Error
    - options.siteUrl.endsWith
    - options.siteUrl.slice
    - options.siteUrl.startsWith
exports:
- name: LlmsTxtPluginOptions
  kind: type
- name: ResolvedOptions
  kind: type
- name: resolveOptions
  kind: func
- name: validateOptions
  kind: func
- name: DEFAULT_OPTIONS
  kind: data
