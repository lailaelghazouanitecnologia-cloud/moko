## Roska File Descriptor — __tests__/component-handlers.test.ts
file: __tests__/component-handlers.test.ts
lines: 322
imports:
- sym: vitest.describe
- sym: vitest.it
- sym: vitest.expect
- sym: unified.unified
- sym: rehype-parse.rehypeParse
- sym: remark-stringify.remarkStringify
- sym: hast.Element
  kind: typing
- sym: ../component-handlers.isPropertiesTable
- sym: ../component-handlers.handlePropertiesTable
data:
- name: mockState
  type: object
  val: '{} as any'
functions:
- name: parseHtml
  sig: 'parseHtml(html: string): Element'
  lines:
  - 10
  - 17
  detail:
    calls:
    - new Error
    - unified
    - unified().use
    - 'unified().use(rehypeParse, { fragment: true }).parse'
- name: toMarkdown
  sig: 'toMarkdown(nodes: ReturnType<typeof handlePropertiesTable>): string'
  lines:
  - 20
  - 24
  detail:
    calls:
    - Array.isArray
    - unified
    - unified().use
    - unified().use(remarkStringify).stringify
    - unified().use(remarkStringify).stringify(root).trim
exports:
- name: parseHtml
  kind: func
- name: toMarkdown
  kind: func
- name: mockState
  kind: data
