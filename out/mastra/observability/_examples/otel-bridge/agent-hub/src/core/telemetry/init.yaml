## Roska File Descriptor — telemetry/init.ts
file: telemetry/init.ts
lines: 70
purpose: OpenTelemetry instrumentation setup with Mastra OtelBridge integration This file sets up standard OTEL auto-instrumentation for the Fastify application. The OtelBridge automatically reads from OTEL's ambient context (via AsyncLocalStorage) and exports Mastra spans back to OTEL. It must be imported BEFORE any other application code to properly instrument modules.
imports:
- sym: '@opentelemetry/auto-instrumentations-node.getNodeAutoInstrumentations'
- sym: '@opentelemetry/exporter-trace-otlp-http.OTLPTraceExporter'
- sym: '@opentelemetry/resources.defaultResource'
- sym: '@opentelemetry/resources.resourceFromAttributes'
- sym: '@opentelemetry/sdk-node.NodeSDK'
- sym: '@opentelemetry/semantic-conventions.ATTR_SERVICE_NAME'
- sym: '@opentelemetry/host-metrics.HostMetrics'
- sym: '@fastify/otel.FastifyOtelInstrumentation'
data:
- name: sdk
  type: any
  val: |-
    new NodeSDK({
      resource: defaultResource().merge(
        resourceFromAttributes({
          [ATTR_SERVICE_NAME]: process.env.O...
- name: hostMetrics
  type: any
  val: new HostMetrics()
types:
- name: IgnoreFunction
  kind: typealias
  decorators:
  - type
  lines:
  - 19
  - 19
functions:
- name: isIgnorableRequest
  sig: 'isIgnorableRequest(): IgnoreFunction'
  lines:
  - 25
  - 26
exports:
- name: IgnoreFunction
  kind: type
- name: isIgnorableRequest
  kind: func
- name: sdk
  kind: data
- name: hostMetrics
  kind: data
