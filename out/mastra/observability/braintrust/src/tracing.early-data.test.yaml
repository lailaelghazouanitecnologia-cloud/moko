## Roska File Descriptor — src/tracing.early-data.test.ts
file: src/tracing.early-data.test.ts
lines: 120
purpose: 'Early Data Handling Tests for Braintrust Exporter These tests verify that the Braintrust exporter correctly handles: - Out-of-order span arrival - Root spans arriving after children - Deep hierarchy cascading - Late events during cleanup delay - Orphaned span handling Braintrust uses skipBuildRootTask = false, meaning: - Root spans create a trace wrapper via _buildRoot - Child spans wait for root before processing'
imports:
- sym: '@observability/test-utils.ExporterFactory'
  kind: typing
- sym: '@observability/test-utils.runAllEarlyDataTests'
- sym: '@observability/test-utils.runLateEventTests'
- sym: '@observability/test-utils.runOrphanedSpanTests'
- sym: vitest.describe
- sym: vitest.beforeEach
- sym: vitest.afterEach
- sym: vitest.vi
- sym: vitest.it
- sym: vitest.expect
- sym: ./tracing.BraintrustExporter
data:
- name: '{ mockLoggerStartSpan, mockInitLogger, createMockSpan }'
  type: any
  val: |-
    vi.hoisted(() => {
      const createMockSpan = (): any => {
        const mockSpan: any = {
          id: `mock-span-${Date.now()}-...
exports:
- name: '{ mockLoggerStartSpan, mockInitLogger, createMockSpan }'
  kind: data
