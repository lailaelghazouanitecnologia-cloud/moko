## Roska File Descriptor — utils/shell-quote.ts
file: utils/shell-quote.ts
lines: 12
purpose: Shell-quote a single argument for safe use in a command string. Arguments containing only safe characters are returned as-is. All others are wrapped in single quotes with embedded single quotes escaped.
functions:
- name: shellQuote
  sig: 'shellQuote(arg: string): string'
  lines:
  - 7
  - 12
  detail:
    calls:
    - /^[a-zA-Z0-9._\-/@:=]+$/.test
    - arg.replace
exports:
- name: shellQuote
  kind: func
