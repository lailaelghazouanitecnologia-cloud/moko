## Roska File Descriptor — scenarios/lsp-large-file.ts
file: scenarios/lsp-large-file.ts
lines: 60
purpose: LSP large file diagnostics integration tests. Generates a ~500-line TypeScript file with a single type error near the end and verifies that the LSP server correctly identifies the error despite the file size. Only runs on local providers — remote FS is too slow for large files.
imports:
- sym: node:path.join
- sym: vitest.describe
- sym: vitest.it
- sym: vitest.expect
- sym: ./test-context.TestContext
  kind: typing
functions:
- name: generateLargeFile
  sig: 'generateLargeFile(totalLines: number, errorLine: number): string'
  lines:
  - 15
  - 25
  detail:
    calls:
    - lines.join
    - lines.push
- name: createLspLargeFileTests
  sig: 'createLspLargeFileTests(getContext: () => TestContext): void'
  lines:
  - 27
  - 60
  detail:
    calls:
    - describe
exports:
- name: generateLargeFile
  kind: func
- name: createLspLargeFileTests
  kind: func
