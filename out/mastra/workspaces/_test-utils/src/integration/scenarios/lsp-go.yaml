## Roska File Descriptor — scenarios/lsp-go.ts
file: scenarios/lsp-go.ts
lines: 72
purpose: 'LSP Go (gopls) diagnostics integration tests. Graceful skip pattern: if getDiagnostics returns [] (gopls not installed), the test passes silently. When gopls is available, asserts that a type error is detected for `var x int = "hello"`.'
imports:
- sym: node:path.join
- sym: vitest.describe
- sym: vitest.it
- sym: vitest.expect
- sym: ./test-context.TestContext
  kind: typing
functions:
- name: createLspGoTests
  sig: 'createLspGoTests(getContext: () => TestContext): void'
  lines:
  - 14
  - 72
  detail:
    calls:
    - describe
exports:
- name: createLspGoTests
  kind: func
