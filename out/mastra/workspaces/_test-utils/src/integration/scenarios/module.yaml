## Roska Module Descriptor — mastra/workspaces/_test-utils/src/integration/scenarios
name: scenarios
total_lines: 2228
files:
- file: scenarios/lsp-diagnostics.ts
  lines: 131
  purpose: 'LSP diagnostics integration tests. Verifies that the LSP subsystem produces real diagnostics when a workspace has `lsp: true` and a sandbox with a process manager. Requires: - typescript and typescript-language-server installed (resolved via node_modules) - vscode-jsonrpc available (optional dep of @mastra/core) - A sandbox that can spawn processes (LocalSandbox or compatible) Tests are skipped gracefully when LSP is not available. Uses the workspace filesystem API to write test files so that walkUpAsync can find project markers (tsconfig.json) on any provider (local, S3, GCS). The TS language server receives file content via LSP protocol and doesn''t need files on disk.'
  types: 0
  functions: 1
  imports: 5
  exports: 1
- file: scenarios/lsp-python.ts
  lines: 73
  purpose: 'LSP Python (Pyright) diagnostics integration tests. Graceful skip pattern: if getDiagnostics returns [] (pyright not installed), the test passes silently. When pyright is available, asserts that a type error is detected for `x: int = "hello"`.'
  types: 0
  functions: 1
  imports: 5
  exports: 1
- file: scenarios/cross-mount-copy.ts
  lines: 106
  purpose: Cross-mount copy integration tests. Tests copying files between different mounts via sandbox commands.
  types: 0
  functions: 1
  imports: 5
  exports: 1
- file: scenarios/write-read-consistency.ts
  lines: 158
  purpose: Write-read consistency integration tests. Verifies that reads immediately after writes return the expected data, testing filesystem and FUSE cache behavior.
  types: 0
  functions: 1
  imports: 7
  exports: 1
- file: scenarios/lsp-external-project.ts
  lines: 92
  purpose: 'LSP external project diagnostics integration tests. Verifies that with contained: false, getDiagnostics works for a file in a completely separate project directory outside the workspace basePath. walkUpAsync should resolve the external dir as the project root. Only meaningful when the filesystem can see absolute host paths (sandboxPathsAligned === true).'
  types: 0
  functions: 1
  imports: 9
  exports: 1
- file: scenarios/mount-isolation.ts
  lines: 122
  purpose: Mount isolation integration tests. Verifies that operations on one mount do not affect another mount. These tests operate through the CompositeFilesystem API — no sandbox needed.
  types: 0
  functions: 1
  imports: 6
  exports: 1
- file: scenarios/file-sync.ts
  lines: 123
  purpose: File sync integration tests. Verifies that files written via filesystem API are accessible via sandbox commands and vice versa.
  types: 0
  functions: 1
  imports: 6
  exports: 1
- file: scenarios/lsp-go.ts
  lines: 72
  purpose: 'LSP Go (gopls) diagnostics integration tests. Graceful skip pattern: if getDiagnostics returns [] (gopls not installed), the test passes silently. When gopls is available, asserts that a type error is detected for `var x int = "hello"`.'
  types: 0
  functions: 1
  imports: 5
  exports: 1
- file: scenarios/lsp-per-file-root.ts
  lines: 147
  purpose: 'LSP per-file root resolution integration tests. Verifies that walkUpAsync resolves different LSP project roots for files in separate project directories. Creates two projects with distinct tsconfig.json files and validates that each file gets diagnostics from its own project''s TS server instance. Local vs remote FS behavior: - Local FS: walkUpAsync finds markers → TS server reads tsconfig.json from disk at the resolved root → different tsconfig settings produce different diagnostics - Remote FS (S3/GCS): walkUpAsync finds markers via filesystem API → but the TS server can''t read tsconfig.json from the remote path → uses default compiler settings → we can only verify that both projects resolve and return diagnostics Uses `sandboxPathsAligned` as the proxy for whether the TS server sees files written via the workspace filesystem API.'
  types: 0
  functions: 1
  imports: 5
  exports: 1
- file: scenarios/lsp-eslint.ts
  lines: 120
  purpose: 'LSP ESLint diagnostics + getDiagnosticsMulti integration tests. Graceful skip pattern: if getDiagnostics returns [] (ESLint language server not installed), the test passes silently. When available, asserts that ESLint rules produce diagnostics. Also tests getDiagnosticsMulti — for a .ts file, both the TypeScript and ESLint servers should contribute diagnostics, with deduplication applied.'
  types: 0
  functions: 1
  imports: 6
  exports: 1
- file: scenarios/test-context.ts
  lines: 13
  purpose: Shared test context type for all integration test scenarios.
  types: 1
  functions: 0
  imports: 1
  exports: 1
- file: scenarios/virtual-directory.ts
  lines: 124
  purpose: Virtual directory integration tests. Verifies that the CompositeFilesystem correctly reports virtual entries for mount points when listing the root and querying mount paths.
  types: 0
  functions: 1
  imports: 5
  exports: 1
- file: scenarios/lsp-large-file.ts
  lines: 60
  purpose: LSP large file diagnostics integration tests. Generates a ~500-line TypeScript file with a single type error near the end and verifies that the LSP server correctly identifies the error despite the file size. Only runs on local providers — remote FS is too slow for large files.
  types: 0
  functions: 2
  imports: 5
  exports: 2
- file: scenarios/cross-mount-api.ts
  lines: 154
  purpose: Cross-mount API integration tests. Tests cross-mount copy and move operations through the CompositeFilesystem API. Unlike cross-mount-copy.ts (sandbox-based), these tests work via the filesystem API and don't require a sandbox.
  types: 0
  functions: 1
  imports: 6
  exports: 1
- file: scenarios/read-only-mount.ts
  lines: 90
  purpose: Read-only mount integration tests. Verifies that readOnly is enforced end-to-end.
  types: 0
  functions: 1
  imports: 5
  exports: 1
- file: scenarios/lsp-cross-file.ts
  lines: 89
  purpose: 'LSP cross-file import diagnostics integration tests. Writes a dependency file (math.ts) to disk via the workspace filesystem, then calls getDiagnostics for a file that imports from it. The TS server resolves the import by reading math.ts from disk, enabling cross-file type checking. Graceful skip: on remote FS where source files aren''t visible on disk for the TS server, getDiagnostics may return [] — the test passes silently.'
  types: 0
  functions: 1
  imports: 5
  exports: 1
- file: scenarios/mount-routing.ts
  lines: 160
  purpose: Mount routing integration tests. Verifies that operations are routed to the correct mount based on path. These tests operate through the CompositeFilesystem API — no sandbox needed.
  types: 0
  functions: 1
  imports: 6
  exports: 1
- file: scenarios/large-file-handling.ts
  lines: 109
  purpose: Large file handling integration tests. Verifies that the filesystem and sandbox handle large files (5MB+) correctly without corruption or truncation.
  types: 0
  functions: 1
  imports: 9
  exports: 3
- file: scenarios/multi-mount.ts
  lines: 80
  purpose: Multi-mount integration tests. Tests multiple filesystems mounted at different paths.
  types: 0
  functions: 1
  imports: 5
  exports: 1
- file: scenarios/concurrent-operations.ts
  lines: 123
  purpose: Concurrent operations integration tests. Verifies that parallel file reads and writes do not corrupt data or produce unexpected errors.
  types: 0
  functions: 1
  imports: 6
  exports: 1
- file: scenarios/lsp-rust.ts
  lines: 82
  purpose: 'LSP Rust (rust-analyzer) diagnostics integration tests. Graceful skip pattern: if getDiagnostics returns [] (rust-analyzer not installed), the test passes silently. When rust-analyzer is available, asserts that a type error is detected for `let x: i32 = "hello"`.'
  types: 0
  functions: 1
  imports: 5
  exports: 1
