## Roska File Descriptor — scenarios/lsp-rust.ts
file: scenarios/lsp-rust.ts
lines: 82
purpose: 'LSP Rust (rust-analyzer) diagnostics integration tests. Graceful skip pattern: if getDiagnostics returns [] (rust-analyzer not installed), the test passes silently. When rust-analyzer is available, asserts that a type error is detected for `let x: i32 = "hello"`.'
imports:
- sym: node:path.join
- sym: vitest.describe
- sym: vitest.it
- sym: vitest.expect
- sym: ./test-context.TestContext
  kind: typing
functions:
- name: createLspRustTests
  sig: 'createLspRustTests(getContext: () => TestContext): void'
  lines:
  - 14
  - 82
  detail:
    calls:
    - describe
exports:
- name: createLspRustTests
  kind: func
