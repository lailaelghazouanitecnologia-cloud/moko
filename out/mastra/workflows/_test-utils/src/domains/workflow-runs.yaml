## Roska File Descriptor — domains/workflow-runs.ts
file: domains/workflow-runs.ts
lines: 239
purpose: Workflow Runs tests for workflows Uses MockRegistry pattern to decouple mocks from workflow definitions, enabling proper test isolation via resetMocks().
imports:
- sym: vitest.describe
- sym: vitest.it
- sym: vitest.expect
- sym: vitest.vi
- sym: zod.z
- sym: ../types.WorkflowTestContext
  kind: typing
- sym: ../types.WorkflowRegistry
  kind: typing
- sym: ../types.WorkflowCreatorContext
  kind: typing
- sym: ../mock-registry.MockRegistry
functions:
- name: createWorkflowRunsWorkflows
  sig: 'createWorkflowRunsWorkflows(ctx: WorkflowCreatorContext)'
  lines:
  - 16
  - 179
  detail:
    calls:
    - createStep
    - createWorkflow
    - mockRegistry.get
    - mockRegistry.register
    - new MockRegistry
    - workflow.then
    - workflow.then(step1).commit
    - workflow.then(step1).then
    - workflow.then(step1).then(step2).then
    - workflow.then(step1).then(step2).then(step3).commit
    - z.number
    - z.object
    - z.string
- name: createWorkflowRunsTests
  sig: 'createWorkflowRunsTests(ctx: WorkflowTestContext, registry?: WorkflowRegistry)'
  lines:
  - 181
  - 239
  detail:
    calls:
    - describe
exports:
- name: createWorkflowRunsWorkflows
  kind: func
- name: createWorkflowRunsTests
  kind: func
