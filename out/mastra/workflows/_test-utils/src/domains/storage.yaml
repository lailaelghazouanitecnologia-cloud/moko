## Roska File Descriptor — domains/storage.ts
file: domains/storage.ts
lines: 468
purpose: Storage persistence tests for workflows Tests workflow storage operations like listWorkflowRuns, getWorkflowRunById, deleteWorkflowRunById, and shouldPersistSnapshot options.
imports:
- sym: vitest.describe
- sym: vitest.it
- sym: vitest.expect
- sym: vitest.vi
- sym: zod.z
- sym: ../types.WorkflowTestContext
  kind: typing
- sym: ../types.WorkflowRegistry
  kind: typing
- sym: ../types.WorkflowCreatorContext
  kind: typing
functions:
- name: createStorageWorkflows
  sig: 'createStorageWorkflows(ctx: WorkflowCreatorContext)'
  lines:
  - 15
  - 263
  detail:
    calls:
    - createStep
    - createWorkflow
    - |-
      createWorkflow({
            id: 'storage-nested-inner-workflow',
            inputSchema: z.object({ value: z.n...
    - parentWorkflow.then
    - parentWorkflow.then(nestedWorkflow).then
    - parentWorkflow.then(nestedWorkflow).then(outerStep).commit
    - vi.fn
    - vi.fn().mockImplementation
    - vi.fn().mockResolvedValue
    - workflow.then
    - workflow.then(step1).commit
    - workflow.then(step1).then
    - workflow.then(step1).then(step2).commit
    - workflow.then(step1).then(step2).then
    - workflow.then(step1).then(step2).then(resumeStep).commit
    - workflow.then(suspendingStep).then
    - workflow.then(suspendingStep).then(finalStep).commit
    - z.any
    - z.boolean
    - z.number
    - z.object
    - z.string
- name: createStorageTests
  sig: 'createStorageTests(ctx: WorkflowTestContext, registry?: WorkflowRegistry)'
  lines:
  - 265
  - 468
  detail:
    calls:
    - describe
exports:
- name: createStorageWorkflows
  kind: func
- name: createStorageTests
  kind: func
