## Roska File Descriptor — domains/callbacks.ts
file: domains/callbacks.ts
lines: 1269
purpose: onFinish and onError callbacks tests for workflows Uses MockRegistry pattern to decouple mocks from workflow definitions, enabling proper test isolation via resetMocks().
imports:
- sym: vitest.describe
- sym: vitest.it
- sym: vitest.expect
- sym: vitest.vi
- sym: zod.z
- sym: ../types.WorkflowTestContext
  kind: typing
- sym: ../types.WorkflowRegistry
  kind: typing
- sym: ../types.WorkflowCreatorContext
  kind: typing
- sym: ../mock-registry.MockRegistry
functions:
- name: createCallbacksWorkflows
  sig: 'createCallbacksWorkflows(ctx: WorkflowCreatorContext)'
  lines:
  - 16
  - 998
  detail:
    calls:
    - createStep
    - createWorkflow
    - mockRegistry.get
    - mockRegistry.register
    - new MockRegistry
    - workflow.then
    - workflow.then(failingStep).commit
    - workflow.then(step1).commit
    - workflow.then(step1).then
    - workflow.then(step1).then(failingStep).commit
    - z.number
    - z.number().optional
    - z.object
    - z.string
- name: createCallbacksTests
  sig: 'createCallbacksTests(ctx: WorkflowTestContext, registry?: WorkflowRegistry)'
  lines:
  - 1000
  - 1269
  detail:
    calls:
    - describe
exports:
- name: createCallbacksWorkflows
  kind: func
- name: createCallbacksTests
  kind: func
