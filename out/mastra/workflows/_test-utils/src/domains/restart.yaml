## Roska File Descriptor — domains/restart.ts
file: domains/restart.ts
lines: 925
purpose: 'Restart domain tests for workflows Tests the ability to restart workflow executions that have completed or failed. NOTE: restart() is only supported on the Default engine. Inngest and Evented engines throw "restart() is not supported on {engine} workflows" Uses MockRegistry pattern for test isolation.'
imports:
- sym: vitest.describe
- sym: vitest.it
- sym: vitest.expect
- sym: vitest.vi
- sym: zod.z
- sym: ../types.WorkflowTestContext
  kind: typing
- sym: ../types.WorkflowRegistry
  kind: typing
- sym: ../types.WorkflowCreatorContext
  kind: typing
- sym: ../mock-registry.MockRegistry
functions:
- name: createRestartWorkflows
  sig: 'createRestartWorkflows(ctx: WorkflowCreatorContext)'
  lines:
  - 19
  - 434
  detail:
    calls:
    - createStep
    - createWorkflow
    - |-
      createWorkflow({
            id: 'restart-nested',
            inputSchema: z.object({ value: z.number() }),
         ...
    - |-
      createWorkflow({
            id: 'restart-nestedWorkflow',
            inputSchema: z.object({ step1Result: z.nu...
    - mockRegistry.get
    - mockRegistry.register
    - new MockRegistry
    - |-
      workflow
            .then
    - |-
      workflow
            .then(getUserInput)
            .then
    - |-
      workflow
            .then(getUserInput)
            .then(promptAgent)
            .then
    - |-
      workflow
            .then(getUserInput)
            .then(promptAgent)
            .then(evaluateTone)
            .then
    - |-
      workflow
            .then(getUserInput)
            .then(promptAgent)
            .then(evaluateTone)
            .then(im...
    - workflow.parallel
    - workflow.parallel([step1, step2]).commit
    - workflow.then
    - workflow.then(counterStep).commit
    - workflow.then(failingStep).commit
    - workflow.then(step1).commit
    - workflow.then(step1).then
    - workflow.then(step1).then(step2).commit
    - z.any
    - z.number
    - z.object
    - z.string
- name: createRestartTests
  sig: 'createRestartTests(ctx: WorkflowTestContext, registry?: WorkflowRegistry)'
  lines:
  - 442
  - 925
  detail:
    calls:
    - describe
exports:
- name: createRestartWorkflows
  kind: func
- name: createRestartTests
  kind: func
