## Roska File Descriptor — domains/abort.ts
file: domains/abort.ts
lines: 445
purpose: Abort tests for workflows
imports:
- sym: vitest.describe
- sym: vitest.it
- sym: vitest.expect
- sym: vitest.vi
- sym: zod.z
- sym: ../types.WorkflowTestContext
  kind: typing
- sym: ../types.WorkflowRegistry
  kind: typing
- sym: ../types.WorkflowCreatorContext
  kind: typing
functions:
- name: createAbortWorkflows
  sig: 'createAbortWorkflows(ctx: WorkflowCreatorContext)'
  lines:
  - 12
  - 330
  detail:
    calls:
    - createStep
    - createWorkflow
    - |-
      createWorkflow({
            id: 'abort-nested-propagation-workflow',
            inputSchema: z.object({ value:...
    - |-
      createWorkflow({
            id: 'abort-nested-workflow',
            inputSchema: z.object({ doubled: z.number(...
    - vi.fn
    - vi.fn().mockImplementation
    - vi.fn().mockResolvedValue
    - workflow.then
    - workflow.then(step1).commit
    - workflow.then(step1).then
    - workflow.then(step1).then(step2).then
    - workflow.then(step1).then(step2).then(step3).commit
    - z.number
    - z.object
    - z.string
- name: createAbortTests
  sig: 'createAbortTests(ctx: WorkflowTestContext, registry?: WorkflowRegistry)'
  lines:
  - 332
  - 445
  detail:
    calls:
    - describe
exports:
- name: createAbortWorkflows
  kind: func
- name: createAbortTests
  kind: func
