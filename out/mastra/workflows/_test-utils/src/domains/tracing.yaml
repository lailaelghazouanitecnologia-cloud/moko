## Roska File Descriptor — domains/tracing.ts
file: domains/tracing.ts
lines: 180
purpose: Tracing/observability tests for workflows Tests that tracingContext is provided to steps and spans are created correctly.
imports:
- sym: vitest.describe
- sym: vitest.it
- sym: vitest.expect
- sym: vitest.vi
- sym: zod.z
- sym: ../types.WorkflowTestContext
  kind: typing
- sym: ../types.WorkflowRegistry
  kind: typing
- sym: ../types.WorkflowCreatorContext
  kind: typing
- sym: ../mock-registry.MockRegistry
functions:
- name: createTracingWorkflows
  sig: 'createTracingWorkflows(ctx: WorkflowCreatorContext)'
  lines:
  - 15
  - 123
  detail:
    calls:
    - createStep
    - createWorkflow
    - mockRegistry.get
    - mockRegistry.register
    - new MockRegistry
    - workflow.then
    - workflow.then(step1).commit
    - workflow.then(step1).then
    - workflow.then(step1).then(step2).commit
    - z.object
    - z.string
- name: createTracingTests
  sig: 'createTracingTests(ctx: WorkflowTestContext, registry?: WorkflowRegistry)'
  lines:
  - 128
  - 180
  detail:
    calls:
    - describe
exports:
- name: createTracingWorkflows
  kind: func
- name: createTracingTests
  kind: func
