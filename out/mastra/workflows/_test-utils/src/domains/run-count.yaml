## Roska File Descriptor — domains/run-count.ts
file: domains/run-count.ts
lines: 124
purpose: Run count tests for workflows Tests that runCount and retryCount are properly provided to step execute functions.
imports:
- sym: vitest.describe
- sym: vitest.it
- sym: vitest.expect
- sym: vitest.vi
- sym: zod.z
- sym: ../types.WorkflowTestContext
  kind: typing
- sym: ../types.WorkflowRegistry
  kind: typing
- sym: ../types.WorkflowCreatorContext
  kind: typing
functions:
- name: createRunCountWorkflows
  sig: 'createRunCountWorkflows(ctx: WorkflowCreatorContext)'
  lines:
  - 14
  - 93
  detail:
    calls:
    - createStep
    - createWorkflow
    - vi.fn
    - vi.fn().mockImplementation
    - |-
      workflow
            .dowhile
    - |-
      workflow
            .dowhile(step1, async ({ inputData }) => inputData.count < 3)
            .dountil
    - |-
      workflow
            .dowhile(step1, async ({ inputData }) => inputData.count < 3)
            .dountil(step2, a...
    - workflow.then
    - workflow.then(step1).then
    - workflow.then(step1).then(step2).commit
    - z.number
    - z.object
- name: createRunCountTests
  sig: 'createRunCountTests(ctx: WorkflowTestContext, registry?: WorkflowRegistry)'
  lines:
  - 95
  - 124
  detail:
    calls:
    - describe
exports:
- name: createRunCountWorkflows
  kind: func
- name: createRunCountTests
  kind: func
