## Roska File Descriptor — domains/index.ts
file: domains/index.ts
lines: 31
purpose: Domain test creators for workflows Each domain exports a function that creates tests for a specific area of workflow functionality. Tests are designed to work across all workflow engines (default, evented, inngest).
