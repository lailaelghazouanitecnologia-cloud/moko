## Roska File Descriptor — domains/branching.ts
file: domains/branching.ts
lines: 735
purpose: if-else branching tests for workflows Uses MockRegistry pattern to decouple mocks from workflow definitions, enabling proper test isolation via resetMocks().
imports:
- sym: vitest.describe
- sym: vitest.it
- sym: vitest.expect
- sym: vitest.vi
- sym: zod.z
- sym: ../types.WorkflowTestContext
  kind: typing
- sym: ../types.WorkflowRegistry
  kind: typing
- sym: ../types.WorkflowCreatorContext
  kind: typing
- sym: ../mock-registry.MockRegistry
functions:
- name: createBranchingWorkflows
  sig: 'createBranchingWorkflows(ctx: WorkflowCreatorContext)'
  lines:
  - 16
  - 606
  detail:
    calls:
    - |-
      counterWorkflow
            .then
    - |-
      counterWorkflow
            .then(firstStep)
            .branch
    - |-
      counterWorkflow
            .then(firstStep)
            .branch([
              [async () => false, wfA as any],
          ...
    - |-
      counterWorkflow
            .then(startStep)
            .branch
    - |-
      counterWorkflow
            .then(startStep)
            .branch([
              [
                async ({ inputData }) => ...
    - createStep
    - createWorkflow
    - |-
      createWorkflow({
                  id: 'nested-workflow-c',
                  inputSchema: startOutputSchema,
        ...
    - |-
      createWorkflow({
                  id: 'nested-workflow-d',
                  inputSchema: startOutputSchema,
        ...
    - |-
      createWorkflow({
            id: 'branch-else-nested',
            inputSchema: z.object({ newValue: z.number() ...
    - |-
      createWorkflow({
            id: 'branch-if-then-else-nested',
            inputSchema: z.object({ newValue: z.n...
    - |-
      createWorkflow({
            id: 'branch-nested-wf-a',
            inputSchema: startInputSchema,
            outputSc...
    - |-
      createWorkflow({
            id: 'branch-nested-wf-b',
            inputSchema: startInputSchema,
            outputSc...
    - finalOutputSchema.optional
    - mockRegistry.get
    - mockRegistry.register
    - new MockRegistry
    - otherOutputSchema.optional
    - |-
      workflow
            .then
    - |-
      workflow
            .then(checkStep)
            .branch
    - |-
      workflow
            .then(checkStep)
            .branch([
              [async ({ inputData }) => inputData.category...
    - |-
      workflow
            .then(initialStep)
            .branch
    - |-
      workflow
            .then(initialStep)
            .branch([
              [async ({ inputData }) => inputData.flag =...
    - z.boolean
    - z.number
    - z.number().optional
    - z.object
    - z.string
- name: createBranchingTests
  sig: 'createBranchingTests(ctx: WorkflowTestContext, registry?: WorkflowRegistry)'
  lines:
  - 611
  - 735
  detail:
    calls:
    - describe
exports:
- name: createBranchingWorkflows
  kind: func
- name: createBranchingTests
  kind: func
