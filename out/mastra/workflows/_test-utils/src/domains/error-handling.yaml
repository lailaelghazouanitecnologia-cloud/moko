## Roska File Descriptor — domains/error-handling.ts
file: domains/error-handling.ts
lines: 852
purpose: Error Handling tests for workflows Uses MockRegistry pattern to decouple mocks from workflow definitions, enabling proper test isolation via resetMocks().
imports:
- sym: vitest.describe
- sym: vitest.it
- sym: vitest.expect
- sym: vitest.vi
- sym: zod.z
- sym: '@mastra/core/error.MastraError'
- sym: '@mastra/core/mastra.Mastra'
- sym: '@mastra/core/storage.MockStore'
- sym: ../types.WorkflowTestContext
  kind: typing
- sym: ../types.WorkflowRegistry
  kind: typing
- sym: ../types.WorkflowCreatorContext
  kind: typing
- sym: ../mock-registry.MockRegistry
functions:
- name: createErrorHandlingWorkflows
  sig: 'createErrorHandlingWorkflows(ctx: WorkflowCreatorContext)'
  lines:
  - 19
  - 473
  detail:
    calls:
    - createStep
    - createWorkflow
    - |-
      createWorkflow({
            id: 'error-nested-main-workflow',
            inputSchema: z.object({}),
            outp...
    - innerWorkflow.parallel
    - innerWorkflow.parallel([step1, step2]).then
    - innerWorkflow.parallel([step1, step2]).then(step3).commit
    - mockRegistry.get
    - mockRegistry.register
    - new MockRegistry
    - |-
      workflow
            .then
    - |-
      workflow
            .then(step1)
            .map
    - |-
      workflow
            .then(step1)
            .map({
              data: { step: step1, path: 'data' },
            })
           ...
    - workflow.parallel
    - workflow.parallel([step1, step2]).then
    - workflow.parallel([step1, step2]).then(step3).commit
    - workflow.then
    - workflow.then(failingStep).commit
    - workflow.then(step1).commit
    - z.object
    - z.string
- name: createErrorHandlingTests
  sig: 'createErrorHandlingTests(ctx: WorkflowTestContext, registry?: WorkflowRegistry)'
  lines:
  - 478
  - 852
  detail:
    calls:
    - describe
exports:
- name: createErrorHandlingWorkflows
  kind: func
- name: createErrorHandlingTests
  kind: func
