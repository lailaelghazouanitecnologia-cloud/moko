## Roska File Descriptor — domains/loops.ts
file: domains/loops.ts
lines: 336
purpose: Loops tests for workflows (dountil and dowhile) Uses MockRegistry pattern to decouple mocks from workflow definitions, enabling proper test isolation via resetMocks().
imports:
- sym: vitest.describe
- sym: vitest.it
- sym: vitest.expect
- sym: vitest.vi
- sym: zod.z
- sym: ../types.WorkflowTestContext
  kind: typing
- sym: ../types.WorkflowRegistry
  kind: typing
- sym: ../types.WorkflowCreatorContext
  kind: typing
- sym: ../mock-registry.MockRegistry
functions:
- name: createLoopsWorkflows
  sig: 'createLoopsWorkflows(ctx: WorkflowCreatorContext)'
  lines:
  - 16
  - 267
  detail:
    calls:
    - |-
      counterWorkflow
            .dountil
    - |-
      counterWorkflow
            .dountil(incrementStep, async ({ inputData }) => {
              return (inputData?....
    - |-
      counterWorkflow
            .dowhile
    - |-
      counterWorkflow
            .dowhile(incrementStep, async ({ inputData }) => {
              return (inputData?....
    - createStep
    - createWorkflow
    - mockRegistry.get
    - mockRegistry.register
    - new MockRegistry
    - |-
      workflow
            .dountil
    - |-
      workflow
            .dountil(step, async ({ inputData }) => {
              return (inputData.count ?? 0) >= 3;...
    - |-
      workflow
            .dowhile
    - |-
      workflow
            .dowhile(step1, async ({ inputData }) => {
              // Condition is false immediately ...
    - z.array
    - z.array(z.string()).optional
    - z.number
    - z.number().optional
    - z.object
    - z.string
- name: createLoopsTests
  sig: 'createLoopsTests(ctx: WorkflowTestContext, registry?: WorkflowRegistry)'
  lines:
  - 272
  - 336
  detail:
    calls:
    - describe
exports:
- name: createLoopsWorkflows
  kind: func
- name: createLoopsTests
  kind: func
