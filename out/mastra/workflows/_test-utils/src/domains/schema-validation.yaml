## Roska File Descriptor — domains/schema-validation.ts
file: domains/schema-validation.ts
lines: 541
purpose: Schema Validation tests for workflows
imports:
- sym: vitest.describe
- sym: vitest.it
- sym: vitest.expect
- sym: vitest.vi
- sym: zod.z
- sym: ../types.WorkflowTestContext
  kind: typing
- sym: ../types.WorkflowRegistry
  kind: typing
- sym: ../types.WorkflowCreatorContext
  kind: typing
functions:
- name: createSchemaValidationWorkflows
  sig: 'createSchemaValidationWorkflows(ctx: WorkflowCreatorContext)'
  lines:
  - 12
  - 366
  detail:
    calls:
    - createStep
    - createWorkflow
    - |-
      createWorkflow({
            id: 'schema-nested-inner',
            inputSchema: z.object({ value: z.string() })...
    - vi.fn
    - vi.fn().mockImplementation
    - vi.fn().mockResolvedValue
    - |-
      workflow
            .foreach
    - |-
      workflow
            .foreach(mapStep)
            .map
    - |-
      workflow
            .foreach(mapStep)
            .map(
              async ({ inputData }) => {
                return {
       ...
    - |-
      workflow
            .then
    - |-
      workflow
            .then(outerStep)
            .then
    - |-
      workflow
            .then(outerStep)
            .then(nestedWorkflow as any)
            .commit
    - |-
      workflow
            .then(step1)
            .map
    - |-
      workflow
            .then(step1)
            .map(async ({ inputData }) => ({ start: inputData.start }))
            ...
    - |-
      workflow
            .then(step1)
            .then
    - |-
      workflow
            .then(step1)
            .then(step2 as any)
            .commit
    - workflow.then
    - workflow.then(step1 as any).commit
    - workflow.then(step1).commit
    - workflow.then(step1).then
    - workflow.then(step1).then(step2).commit
    - |-
      z
              .object
    - |-
      z
              .object({
                value: z.number(),
              })
              .optional
    - |-
      z
              .object({
                value: z.number(),
              })
              .optional()
              .default
    - z.array
    - z.number
    - z.object
    - z.string
    - z.string().default
- name: createSchemaValidationTests
  sig: 'createSchemaValidationTests(ctx: WorkflowTestContext, registry?: WorkflowRegistry)'
  lines:
  - 371
  - 541
  detail:
    calls:
    - describe
exports:
- name: createSchemaValidationWorkflows
  kind: func
- name: createSchemaValidationTests
  kind: func
