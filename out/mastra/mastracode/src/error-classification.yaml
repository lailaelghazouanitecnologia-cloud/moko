## Roska File Descriptor — src/error-classification.ts
file: src/error-classification.ts
lines: 18
purpose: Checks whether an error is an ERR_STREAM_DESTROYED error, which is a non-fatal condition that occurs when code writes to a stream after it has been closed (e.g., client disconnect, cancelled LLM stream, LSP shutdown, killed subprocess). Walks the `.cause` chain up to a depth limit.
functions:
- name: isStreamDestroyedError
  sig: 'isStreamDestroyedError(err: unknown, depth = 0): boolean'
  lines:
  - 9
  - 18
  detail:
    calls:
    - Array.isArray
    - e.errors.some
    - e.message.includes
    - isStreamDestroyedError
exports:
- name: isStreamDestroyedError
  kind: func
