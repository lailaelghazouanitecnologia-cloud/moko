## Roska File Descriptor — utils/plans.ts
file: utils/plans.ts
lines: 33
imports:
- sym: node:fs/promises.fs
- sym: node:path.path
- sym: ./project.js.getAppDataDir
functions:
- name: slugify
  sig: 'slugify(str: string): string'
  lines:
  - 5
  - 11
  detail:
    calls:
    - |-
      str
          .toLowerCase
    - |-
      str
          .toLowerCase()
          .replace
    - |-
      str
          .toLowerCase()
          .replace(/[^a-z0-9]+/g, '-')
          .replace
- name: savePlanToDisk
  sig: |-
    savePlanToDisk(opts: {
      title: string;
      plan: string;
      resourceId: string;
      plansDir?: string;
    }): Promise<void>
  is_async: true
  lines:
  - 13
  - 33
  detail:
    calls:
    - fs.mkdir
    - fs.writeFile
    - getAppDataDir
    - new Date
    - now.toISOString
    - now.toISOString().replace
    - path.join
    - slugify
exports:
- name: slugify
  kind: func
- name: savePlanToDisk
  kind: func
