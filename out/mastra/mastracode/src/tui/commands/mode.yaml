## Roska File Descriptor — commands/mode.ts
file: commands/mode.ts
lines: 22
imports:
- sym: ./types.js.SlashCommandContext
  kind: typing
functions:
- name: handleModeCommand
  sig: 'handleModeCommand(ctx: SlashCommandContext, args: string[]): Promise<void>'
  is_async: true
  lines:
  - 3
  - 22
  detail:
    calls:
    - String
    - ctx.harness.getCurrentMode
    - ctx.harness.listModes
    - ctx.harness.switchMode
    - ctx.showError
    - ctx.showInfo
    - |-
      modes
            .map
    - |-
      modes
            .map(m => `  ${m.id === currentMode?.id ? '* ' : '  '}${m.id}${m.name ? ` - ${m.name}` :...
exports:
- name: handleModeCommand
  kind: func
