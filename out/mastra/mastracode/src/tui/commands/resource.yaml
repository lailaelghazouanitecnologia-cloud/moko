## Roska File Descriptor — commands/resource.ts
file: commands/resource.ts
lines: 66
imports:
- sym: ./types.js.SlashCommandContext
  kind: typing
functions:
- name: handleResourceCommand
  sig: 'handleResourceCommand(ctx: SlashCommandContext, args: string[]): Promise<void>'
  is_async: true
  lines:
  - 3
  - 66
  detail:
    calls:
    - '[...threads].sort'
    - args.join
    - args.join(' ').trim
    - args[0]?.trim
    - ctx.renderExistingMessages
    - ctx.showInfo
    - ctx.updateStatusLine
    - harness.getDefaultResourceId
    - harness.getKnownResourceIds
    - harness.getResourceId
    - harness.listThreads
    - harness.setResourceId
    - harness.switchThread
    - knownIds.map
    - lines.join
    - state.chatContainer.clear
    - state.pendingTools.clear
    - state.ui.requestRender
exports:
- name: handleResourceCommand
  kind: func
