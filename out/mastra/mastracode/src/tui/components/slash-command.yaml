## Roska File Descriptor — components/slash-command.ts
file: components/slash-command.ts
lines: 88
purpose: SlashCommandComponent - renders a bordered box for slash command messages showing the command name as a heading and truncated content that can be expanded with ctrl+e. The full content is still sent to the assistant.
imports:
- sym: '@mariozechner/pi-tui.Container'
- sym: '@mariozechner/pi-tui.Spacer'
- sym: '@mariozechner/pi-tui.Text'
- sym: chalk.chalk
- sym: ../theme.js.BOX_INDENT
- sym: ../theme.js.getTermWidth
- sym: ../theme.js.mastra
data:
- name: MAX_COLLAPSED_LINES
  type: number
  val: '3'
types:
- name: SlashCommandComponent
  kind: struct
  bases:
  - Container
  fields:
  - name: contentLines
    type: string[]
  - name: expanded
    type: any
    default: 'false'
  methods:
  - name: constructor
    sig: 'constructor(commandName: string, content?: string)'
    lines:
    - 19
    - 24
    detail:
      calls:
      - content.split
      - content.split('\n').filter
      - super
      - this.rebuild
  - name: setExpanded
    sig: 'setExpanded(expanded: boolean): void'
    lines:
    - 26
    - 30
    detail:
      calls:
      - this.rebuild
  - name: rebuild
    sig: 'rebuild(): void'
    vis: private
    lines:
    - 32
    - 87
    detail:
      calls:
      - border
      - chalk.hex
      - chalk.hex(mastra.darkGray)
      - chalk.hex(mastra.specialGray)
      - |-
        displayLines
              .map
      - |-
        displayLines
              .map(
                line =>
                  `${border('│')} ${chalk.hex(mastra.mainGray)(li...
      - getTermWidth
      - new Spacer
      - new Text
      - remaining.lastIndexOf
      - remaining.slice
      - remaining.slice(splitAt).trimStart
      - this.addChild
      - this.clear
      - wrappedLines.push
      - wrappedLines.slice
  lines:
  - 14
  - 88
functions:
- name: getBorderColor
  sig: getBorderColor()
  lines:
  - 12
  - 12
exports:
- name: SlashCommandComponent
  kind: type
- name: getBorderColor
  kind: func
- name: MAX_COLLAPSED_LINES
  kind: data
