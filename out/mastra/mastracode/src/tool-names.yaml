## Roska File Descriptor — src/tool-names.ts
file: src/tool-names.ts
lines: 54
purpose: Mastracode tool name constants. These are the names exposed to the LLM via workspace tool name remapping. Used throughout mastracode for permissions, TUI rendering, tool guidance, subagent allowedTools, etc. The workspace tools get remapped from their core names (e.g. `mastra_workspace_read_file`) to these names (e.g. `view`) via the `name` property in workspace tool config.
imports:
- sym: '@mastra/core/workspace.WORKSPACE_TOOLS'
data:
- name: MC_TOOLS
  type: object
  val: |-
    {
      // Filesystem
      VIEW: 'view',
      WRITE_FILE: 'write_file',
      STRING_REPLACE_LSP: 'string_replace_lsp',
      FIND_FILES: ...
- name: TOOL_NAME_OVERRIDES
  type: object
  val: |-
    {
      [WORKSPACE_TOOLS.FILESYSTEM.READ_FILE]: { name: MC_TOOLS.VIEW },
      [WORKSPACE_TOOLS.FILESYSTEM.WRITE_FILE]: { name: ...
exports:
- name: MC_TOOLS
  kind: data
- name: TOOL_NAME_OVERRIDES
  kind: data
