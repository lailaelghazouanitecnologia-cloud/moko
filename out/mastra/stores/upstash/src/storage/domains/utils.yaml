## Roska File Descriptor — domains/utils.ts
file: domains/utils.ts
lines: 38
imports:
- sym: '@mastra/core/storage.serializeDate'
- sym: '@mastra/core/storage.TABLE_MESSAGES'
- sym: '@mastra/core/storage.TABLE_WORKFLOW_SNAPSHOT'
- sym: '@mastra/core/storage.TABLE_SCORERS'
- sym: '@mastra/core/storage.TABLE_NAMES'
  kind: typing
functions:
- name: getKey
  sig: 'getKey(tableName: TABLE_NAMES, keys: Record<string, any>): string'
  lines:
  - 4
  - 9
  detail:
    calls:
    - Object.entries
    - |-
      Object.entries(keys)
          .filter
    - |-
      Object.entries(keys)
          .filter(([_, value]) => value !== undefined)
          .map
    - keyParts.join
- name: processRecord
  sig: 'processRecord(tableName: TABLE_NAMES, record: Record<string, any>)'
  lines:
  - 11
  - 38
  detail:
    calls:
    - getKey
    - serializeDate
exports:
- name: getKey
  kind: func
- name: processRecord
  kind: func
