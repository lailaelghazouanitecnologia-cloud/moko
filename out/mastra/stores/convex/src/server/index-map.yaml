## Roska File Descriptor — server/index-map.ts
file: server/index-map.ts
lines: 94
purpose: Index definitions for automatic query optimization. Maps each typed Convex table to its available indexes and their field lists. Indexes with more fields are listed first so the best (most specific) match is preferred during selection. These must stay in sync with the index definitions in schema.ts.
imports:
- sym: ../storage/types.EqualityFilter
  kind: typing
data:
- name: TABLE_INDEX_MAP
  type: 'Record<string, Array<{ name: string; fields: string[] }>>'
  val: |-
    {
      mastra_messages: [
        { name: 'by_thread_created', fields: ['thread_id', 'createdAt'] },
        { name: 'by_thread', f...
functions:
- name: findBestIndex
  sig: |-
    findBestIndex(
      convexTable: string,
      filters: EqualityFilter[],
    ): { indexName: string; indexedFilters: EqualityFilter[] } | null
  lines:
  - 60
  - 94
  detail:
    calls:
    - filtersByField.get
    - filtersByField.set
    - indexedFilters.push
    - new Map
exports:
- name: findBestIndex
  kind: func
- name: TABLE_INDEX_MAP
  kind: data
