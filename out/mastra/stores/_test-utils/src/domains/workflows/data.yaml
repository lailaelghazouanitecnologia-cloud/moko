## Roska File Descriptor — workflows/data.ts
file: workflows/data.ts
lines: 40
imports:
- sym: '@mastra/core/workflows.WorkflowRunState'
  kind: typing
- sym: node:crypto.randomUUID
- sym: vitest.expect
functions:
- name: checkWorkflowSnapshot
  sig: 'checkWorkflowSnapshot(snapshot: WorkflowRunState | string, stepId: string, status: string)'
  lines:
  - 5
  - 10
  detail:
    calls:
    - expect
    - expect(snapshot.context?.[stepId]?.status).toBe
    - new Error
- name: createSampleWorkflowSnapshot
  sig: 'createSampleWorkflowSnapshot(status: string, createdAt?: Date)'
  lines:
  - 12
  - 40
  detail:
    calls:
    - new Date
    - new Date(timestamp.getTime() + 15000).getTime
    - randomUUID
    - timestamp.getTime
exports:
- name: checkWorkflowSnapshot
  kind: func
- name: createSampleWorkflowSnapshot
  kind: func
