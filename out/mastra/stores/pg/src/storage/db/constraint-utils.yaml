## Roska File Descriptor — db/constraint-utils.ts
file: db/constraint-utils.ts
lines: 37
data:
- name: POSTGRES_IDENTIFIER_MAX_LENGTH
  type: number
  val: '63'
functions:
- name: truncateIdentifier
  sig: 'truncateIdentifier(value: string, maxLength = POSTGRES_IDENTIFIER_MAX_LENGTH): string'
  lines:
  - 3
  - 16
  detail:
    calls:
    - Buffer.byteLength
    - value.slice
- name: buildConstraintName
  sig: |-
    buildConstraintName({
      baseName,
      schemaName,
      maxLength = POSTGRES_IDENTIFIER_MAX_LENGTH,
    }: {
      baseName: string;
      schemaName?: string;
      maxLength?: number;
    }): string
  lines:
  - 26
  - 37
  detail:
    calls:
    - '`${prefix}${baseName}`.toLowerCase'
    - truncateIdentifier
exports:
- name: truncateIdentifier
  kind: func
- name: buildConstraintName
  kind: func
- name: POSTGRES_IDENTIFIER_MAX_LENGTH
  kind: data
