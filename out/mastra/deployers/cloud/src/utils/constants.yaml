## Roska File Descriptor — utils/constants.ts
file: utils/constants.ts
lines: 23
imports:
- sym: node:os.os
data:
- name: LOCAL
  type: any
  val: process.env.LOCAL === 'true'
- name: TEAM_ID
  type: string
  val: process.env.TEAM_ID ?? ''
- name: PROJECT_ID
  type: string
  val: process.env.PROJECT_ID ?? ''
- name: BUILD_ID
  type: string
  val: process.env.BUILD_ID ?? ''
- name: BUILD_URL
  type: string
  val: process.env.BUILD_URL ?? ''
- name: LOG_REDIS_URL
  type: string
  val: process.env.LOG_REDIS_URL ?? 'redis://localhost:6379'
- name: BUSINESS_JWT_TOKEN
  type: string
  val: process.env.BUSINESS_JWT_TOKEN ?? ''
- name: PLAYGROUND_JWT_TOKEN
  type: string
  val: process.env.PLAYGROUND_JWT_TOKEN ?? ''
- name: USER_IP_ADDRESS
  type: string
  val: process.env.USER_IP_ADDRESS ?? ''
- name: MASTRA_DIRECTORY
  type: string
  val: process.env.MASTRA_DIRECTORY ?? 'src/mastra'
- name: PROJECT_ENV_VARS
  type: Record<string, string>
  val: safelyParseJson(process.env.PROJECT_ENV_VARS ?? '{}')
- name: PROJECT_ROOT
  type: any
  val: 'LOCAL ? os.tmpdir() + ''/project'' : (process.env.PROJECT_ROOT ?? ''/project'')'
functions:
- name: safelyParseJson
  sig: 'safelyParseJson(json: string)'
  lines:
  - 17
  - 23
  detail:
    calls:
    - JSON.parse
exports:
- name: safelyParseJson
  kind: func
- name: LOCAL
  kind: data
- name: TEAM_ID
  kind: data
- name: PROJECT_ID
  kind: data
- name: BUILD_ID
  kind: data
- name: BUILD_URL
  kind: data
- name: LOG_REDIS_URL
  kind: data
- name: BUSINESS_JWT_TOKEN
  kind: data
- name: PLAYGROUND_JWT_TOKEN
  kind: data
- name: USER_IP_ADDRESS
  kind: data
- name: MASTRA_DIRECTORY
  kind: data
- name: PROJECT_ENV_VARS
  kind: data
- name: PROJECT_ROOT
  kind: data
