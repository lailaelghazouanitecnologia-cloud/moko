## Roska File Descriptor — tools/generate-questions-from-text-tool.ts
file: tools/generate-questions-from-text-tool.ts
lines: 120
imports:
- sym: '@mastra/core/tools.createTool'
- sym: zod.z
data:
- name: MAX_TEXT_LENGTH
  type: number
  val: '4000'
- name: generateQuestionsFromTextTool
  type: any
  val: |-
    createTool({
      id: 'generate-questions-from-text-tool',
      description: 'Generates comprehensive questions from text cont...
functions:
- name: parseQuestionsFromText
  sig: 'parseQuestionsFromText(text: string, maxQuestions: number): string[]'
  lines:
  - 99
  - 120
  detail:
    calls:
    - |-
      lines
          .map
    - |-
      lines
          .map(line => {
            // Remove numbering patterns like "1.", "1)", etc.
            let cleaned ...
    - |-
      text
          .split
    - |-
      text
          .split('\n')
          .map
    - |-
      text
          .split('\n')
          .map(line => line.trim())
          .filter
    - |-
      text
          .split('\n')
          .map(line => line.trim())
          .filter(line => line.length > 0)
          .filter
exports:
- name: parseQuestionsFromText
  kind: func
- name: MAX_TEXT_LENGTH
  kind: data
- name: generateQuestionsFromTextTool
  kind: data
