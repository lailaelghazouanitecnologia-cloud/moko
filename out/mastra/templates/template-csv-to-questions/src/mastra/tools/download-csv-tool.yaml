## Roska File Descriptor — tools/download-csv-tool.ts
file: tools/download-csv-tool.ts
lines: 183
imports:
- sym: '@mastra/core/tools.createTool'
- sym: zod.z
data:
- name: csvFetcherTool
  type: any
  val: |-
    createTool({
      id: 'download-csv-tool',
      description: 'Downloads a CSV from a URL, parses it, and returns a comprehensi...
functions:
- name: parseCSVRow
  sig: 'parseCSVRow(row: string): string[]'
  lines:
  - 5
  - 27
  detail:
    calls:
    - current.trim
    - result.push
- name: inferDataType
  sig: 'inferDataType(value: string): string'
  lines:
  - 30
  - 53
  detail:
    calls:
    - Number
    - Number.isInteger
    - datePattern.test
    - isNaN
    - parseFloat
    - value.toLowerCase
    - value.trim
exports:
- name: parseCSVRow
  kind: func
- name: inferDataType
  kind: func
- name: csvFetcherTool
  kind: data
