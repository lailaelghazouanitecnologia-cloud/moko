## Roska File Descriptor — workflows/index-pdf.ts
file: workflows/index-pdf.ts
lines: 267
imports:
- sym: '@mastra/core/workflows.createStep'
- sym: '@mastra/core/workflows.createWorkflow'
- sym: '@mastra/core/llm.ModelRouterEmbeddingModel'
- sym: '@mastra/rag.MDocument'
- sym: zod.z
- sym: pdf-parse.PDFParse
- sym: ../lib/vector-store.vectorStore
- sym: ../lib/vector-store.PDF_INDEX_NAME
data:
- name: EMBEDDING_DIMENSION
  type: number
  val: '1536'
- name: downloadAndExtractText
  type: any
  val: |-
    createStep({
      id: 'download-and-extract-text',
      description: 'Download PDF from URL and extract text page by page',
      ...
- name: splitIntoChunks
  type: any
  val: |-
    createStep({
      id: 'split-into-chunks',
      description: 'Split page text into smaller overlapping chunks',
      inputSchema:...
- name: generateAndStoreEmbeddings
  type: any
  val: |-
    createStep({
      id: 'generate-and-store-embeddings',
      description: 'Generate embeddings for each chunk and store in vect...
- name: indexPdfWorkflow
  type: any
  val: |-
    createWorkflow({
      id: 'index-pdf',
      inputSchema: z.object({
        url: z.url().describe('URL of the PDF to ingest'),
      }...
functions:
- name: initializeVectorIndex
  sig: 'initializeVectorIndex(): Promise<void>'
  is_async: true
  lines:
  - 10
  - 19
  detail:
    calls:
    - indexes.includes
    - vectorStore.createIndex
    - vectorStore.listIndexes
- name: extractTitleFromUrl
  sig: 'extractTitleFromUrl(url: string): string'
  lines:
  - 255
  - 263
  detail:
    calls:
    - filename.replace
    - filename.replace(/\.pdf$/i, '').replace
    - new URL
    - pathname.split
    - pathname.split('/').pop
- name: generateDocumentId
  sig: 'generateDocumentId(url: string): string'
  lines:
  - 265
  - 267
  detail:
    calls:
    - Buffer.from
    - Buffer.from(url).toString
    - Buffer.from(url).toString('base64url').slice
exports:
- name: initializeVectorIndex
  kind: func
- name: extractTitleFromUrl
  kind: func
- name: generateDocumentId
  kind: func
- name: EMBEDDING_DIMENSION
  kind: data
- name: downloadAndExtractText
  kind: data
- name: splitIntoChunks
  kind: data
- name: generateAndStoreEmbeddings
  kind: data
- name: indexPdfWorkflow
  kind: data
