## Roska File Descriptor — workflows/deep-search-workflow.ts
file: workflows/deep-search-workflow.ts
lines: 291
imports:
- sym: '@mastra/core/workflows.createStep'
- sym: '@mastra/core/workflows.createWorkflow'
- sym: zod.z
- sym: exa-js.Exa
data:
- name: stateSchema
  type: any
  val: |-
    z.object({
      initialQuery: z.string().optional(),
      clarifiedIntent: z.string().optional(),
      expandedQueries: z.array(z...
- name: clarifyIntent
  type: any
  val: |-
    createStep({
      id: 'clarify-intent',
      inputSchema: z.object({ initialQuery: z.string() }),
      outputSchema: z.object({})...
- name: generateQueries
  type: any
  val: |-
    createStep({
      id: 'generate-queries',
      inputSchema: z.object({}),
      outputSchema: z.object({}),
      stateSchema,
      execu...
- name: search
  type: any
  val: |-
    createStep({
      id: 'search',
      inputSchema: z.object({}),
      outputSchema: z.object({}),
      stateSchema,
      execute: async ...
- name: evaluateResults
  type: any
  val: |-
    createStep({
      id: 'evaluate-results',
      inputSchema: z.object({}),
      outputSchema: z.object({}),
      stateSchema,
      execu...
- name: finalizeAnswer
  type: any
  val: |-
    createStep({
      id: 'finalize-answer',
      inputSchema: z.object({}),
      outputSchema: z.object({ answer: z.string() }),
      s...
- name: searchPass
  type: any
  val: |-
    createWorkflow({
      id: 'search-pass',
      inputSchema: z.object({}),
      outputSchema: z.object({
        answerIsSatisfactory: ...
- name: deepSearch
  type: any
  val: |-
    createWorkflow({
      id: 'deep-search',
      inputSchema: z.object({ initialQuery: z.string() }),
      outputSchema: z.object({ ...
exports:
- name: stateSchema
  kind: data
- name: clarifyIntent
  kind: data
- name: generateQueries
  kind: data
- name: search
  kind: data
- name: evaluateResults
  kind: data
- name: finalizeAnswer
  kind: data
- name: searchPass
  kind: data
- name: deepSearch
  kind: data
