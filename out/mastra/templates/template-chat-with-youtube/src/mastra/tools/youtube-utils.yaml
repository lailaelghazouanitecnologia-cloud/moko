## Roska File Descriptor — tools/youtube-utils.ts
file: tools/youtube-utils.ts
lines: 40
purpose: Extracts a YouTube video ID from various URL formats. Supports standard watch URLs, short URLs, embed URLs, and raw video IDs.
functions:
- name: extractVideoId
  sig: 'extractVideoId(url: string): string | null'
  lines:
  - 5
  - 26
  detail:
    calls:
    - /^[a-zA-Z0-9_-]{11}$/.test
    - url.match
- name: formatTimestamp
  sig: 'formatTimestamp(seconds: number): string'
  lines:
  - 31
  - 40
  detail:
    calls:
    - Math.floor
    - minutes.toString
    - minutes.toString().padStart
    - secs.toString
    - secs.toString().padStart
exports:
- name: extractVideoId
  kind: func
- name: formatTimestamp
  kind: func
