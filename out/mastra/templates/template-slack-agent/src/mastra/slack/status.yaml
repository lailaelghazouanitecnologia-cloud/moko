## Roska File Descriptor — slack/status.ts
file: slack/status.ts
lines: 33
imports:
- sym: ./constants.SPINNER
- sym: ./constants.TOOL_ICONS
- sym: ./constants.WORKFLOW_ICONS
- sym: ./types.StreamState
  kind: typing
functions:
- name: formatChunkType
  sig: 'formatChunkType(type: string): string'
  lines:
  - 5
  - 10
  detail:
    calls:
    - |-
      type
          .split
    - |-
      type
          .split('-')
          .map
    - |-
      type
          .split('-')
          .map(word => word.charAt(0).toUpperCase() + word.slice(1))
          .join
- name: getStatusText
  sig: 'getStatusText(state: StreamState, frame: number): string'
  lines:
  - 13
  - 33
  detail:
    calls:
    - formatChunkType
    - type.includes
    - type.startsWith
exports:
- name: formatChunkType
  kind: func
- name: getStatusText
  kind: func
