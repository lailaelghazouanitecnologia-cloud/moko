## Roska File Descriptor — slack/verify.ts
file: slack/verify.ts
lines: 32
imports:
- sym: crypto.crypto
functions:
- name: verifySlackRequest
  sig: |-
    verifySlackRequest(
      signingSecret: string,
      requestSignature: string,
      timestamp: string,
      body: string,
    ): boolean
  lines:
  - 6
  - 32
  detail:
    calls:
    - Buffer.byteLength
    - Buffer.from
    - Date.now
    - Math.floor
    - crypto.createHmac
    - crypto.createHmac('sha256', signingSecret).update
    - crypto.createHmac('sha256', signingSecret).update(sigBasestring, 'utf8').digest
    - crypto.timingSafeEqual
    - parseInt
exports:
- name: verifySlackRequest
  kind: func
