## Roska File Descriptor — resources/observability.legacy.test.ts
file: resources/observability.legacy.test.ts
lines: 326
purpose: 'Legacy API Tests for Observability Client ========================================== These tests document the OLD API contract from the main branch. They ensure backward compatibility for existing client implementations. Key differences from new API: - `getTraces()` uses `TracesPaginatedArg` type with different structure - `name` filter instead of `entityName` - `dateRange` in pagination instead of `startedAt`/`endedAt` in filters - Query params built manually instead of using `toQueryParams` utility These tests verify the expected URL query string format for the old API.'
imports:
- sym: '@mastra/core/observability.SpanType'
  kind: typing
- sym: vitest.describe
- sym: vitest.expect
- sym: vitest.beforeEach
- sym: vitest.it
- sym: vitest.vi
- sym: ../client.MastraClient
