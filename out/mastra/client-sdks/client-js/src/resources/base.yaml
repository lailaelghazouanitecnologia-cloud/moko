## Roska File Descriptor — resources/base.ts
file: resources/base.ts
lines: 102
imports:
- sym: ../types.RequestOptions
  kind: typing
- sym: ../types.ClientOptions
  kind: typing
- sym: ../types.MastraClientError
- sym: ../utils.normalizeRoutePath
types:
- name: BaseResource
  kind: struct
  ctx: Makes an HTTP request to the API with retries and exponential backoff
  fields:
  - name: options
    type: ClientOptions
  - name: apiPrefix
    type: string
  methods:
  - name: constructor
    sig: 'constructor(options: ClientOptions)'
    lines:
    - 9
    - 12
    detail:
      calls:
      - normalizeRoutePath
  - name: request
    sig: 'request(path: string, options: RequestOptions = {}): Promise<T>'
    is_async: true
    lines:
    - 20
    - 101
    detail:
      calls:
      - JSON.parse
      - JSON.stringify
      - Math.min
      - baseUrl.replace
      - fetchFn
      - new Error
      - new MastraClientError
      - new Promise
      - response.json
      - response.text
  lines:
  - 5
  - 102
exports:
- name: BaseResource
  kind: type
