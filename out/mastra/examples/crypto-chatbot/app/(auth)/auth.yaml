## Roska File Descriptor — (auth)/auth.ts
file: (auth)/auth.ts
lines: 53
imports:
- sym: bcrypt-ts.compare
- sym: next-auth.User
- sym: next-auth.Session
- sym: next-auth/providers/credentials.Credentials
- sym: '@/db/queries.getUser'
- sym: ./auth.config.authConfig
data:
- name: |-
    {
      handlers: { GET, POST },
      auth,
      signIn,
      signOut,
    }
  type: any
  val: |-
    NextAuth({
      ...authConfig,
      providers: [
        Credentials({
          credentials: {},
          async authorize({ email, passw...
types:
- name: ExtendedSession
  kind: trait
  bases:
  - Session
  decorators:
  - interface
  fields:
  - name: user
    type: User
  lines:
  - 9
  - 11
exports:
- name: ExtendedSession
  kind: type
- name: |-
    {
      handlers: { GET, POST },
      auth,
      signIn,
      signOut,
    }
  kind: data
