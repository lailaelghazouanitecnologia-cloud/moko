## Roska File Descriptor — custom/weather.tsx
file: custom/weather.tsx
lines: 311
imports:
- sym: classnames.cx
- sym: date-fns.format
- sym: date-fns.isWithinInterval
- sym: react.useEffect
- sym: react.useState
data:
- name: SAMPLE
  type: object
  val: |-
    {
      latitude: 37.763283,
      longitude: -122.41286,
      generationtime_ms: 0.027894973754882812,
      utc_offset_seconds: 0,
      ...
- name: currentHigh
  type: any
  val: |-
    Math.max(
        ...weatherAtLocation.hourly.temperature_2m.slice(0, 24)
      )
- name: currentLow
  type: any
  val: |-
    Math.min(
        ...weatherAtLocation.hourly.temperature_2m.slice(0, 24)
      )
- name: isDay
  type: any
  val: |-
    isWithinInterval(new Date(weatherAtLocation.current.time), {
        start: new Date(weatherAtLocation.daily.sunrise[0]),
      ...
- name: '[isMobile, setIsMobile]'
  type: any
  val: useState(false)
- name: hoursToShow
  type: any
  val: 'isMobile ? 5 : 6'
- name: currentTimeIndex
  type: any
  val: |-
    weatherAtLocation.hourly.time.findIndex(
        (time) => new Date(time) >= new Date(weatherAtLocation.current.time)
      )
- name: displayTimes
  type: any
  val: |-
    weatherAtLocation.hourly.time.slice(
        currentTimeIndex,
        currentTimeIndex + hoursToShow
      )
- name: displayTemperatures
  type: any
  val: |-
    weatherAtLocation.hourly.temperature_2m.slice(
        currentTimeIndex,
        currentTimeIndex + hoursToShow
      )
types:
- name: WeatherAtLocation
  kind: trait
  decorators:
  - interface
  fields:
  - name: latitude
    type: number
  - name: longitude
    type: number
  - name: generationtime_ms
    type: number
  - name: utc_offset_seconds
    type: number
  - name: timezone
    type: string
  - name: timezone_abbreviation
    type: string
  - name: elevation
    type: number
  - name: current_units
    type: |-
      {
          time: string;
          interval: string;
          temperature_2m: string;
        }
  - name: current
    type: |-
      {
          time: string;
          interval: number;
          temperature_2m: number;
        }
  - name: hourly_units
    type: |-
      {
          time: string;
          temperature_2m: string;
        }
  - name: hourly
    type: |-
      {
          time: string[];
          temperature_2m: number[];
        }
  - name: daily_units
    type: |-
      {
          time: string;
          sunrise: string;
          sunset: string;
        }
  - name: daily
    type: |-
      {
          time: string[];
          sunrise: string[];
          sunset: string[];
        }
  lines:
  - 7
  - 43
functions:
- name: n
  sig: 'n(num: number): number'
  lines:
  - 200
  - 202
  detail:
    calls:
    - Math.ceil
exports:
- name: WeatherAtLocation
  kind: type
- name: n
  kind: func
- name: SAMPLE
  kind: data
- name: currentHigh
  kind: data
- name: currentLow
  kind: data
- name: isDay
  kind: data
- name: '[isMobile, setIsMobile]'
  kind: data
- name: hoursToShow
  kind: data
- name: currentTimeIndex
  kind: data
- name: displayTimes
  kind: data
- name: displayTemperatures
  kind: data
