## Roska File Descriptor — src/index.ts
file: src/index.ts
lines: 348
imports:
- sym: zod.z
- sym: ./mastra/index.mastra
functions:
- name: logTestResult
  sig: 'logTestResult(testName: string, success: boolean, details?: any)'
  lines:
  - 10
  - 13
  detail:
    calls:
    - console.log
- name: testMethod
  sig: 'testMethod(methodName: string, fn: () => any, shouldSucceed = true)'
  is_async: true
  lines:
  - 16
  - 31
  detail:
    calls:
    - JSON.stringify
    - JSON.stringify(result.id || result.name || 'object', null, 2).substring
    - fn
    - logTestResult
- name: validateAllPrimitives
  sig: validateAllPrimitives()
  is_async: true
  lines:
  - 33
  - 342
  detail:
    calls:
    - '''-''.repeat'
    - '''=''.repeat'
    - Object.keys
    - Object.keys(agents).join
    - Object.keys(mcpServers).join
    - Object.keys(processors).join
    - Object.keys(scorers!).join
    - Object.keys(tools).join
    - Object.keys(vectors).join
    - Object.keys(workflows).join
    - console.log
    - mastra.listAgents
    - mastra.listMCPServers
    - mastra.listProcessors
    - mastra.listScorers
    - mastra.listTools
    - mastra.listVectors
    - mastra.listWorkflows
    - testMethod
exports:
- name: logTestResult
  kind: func
- name: testMethod
  kind: func
- name: validateAllPrimitives
  kind: func
