## Roska File Descriptor — auth/index.ts
file: auth/index.ts
lines: 52
purpose: 'Auth configuration for the example agent. Supports multiple authentication providers: - simple: Token-based authentication for development/testing - better-auth: Credentials-based authentication with SQLite - workos: Enterprise SSO (SAML, OIDC) - cloud: Mastra Cloud OAuth with PKCE - composite: Combines SimpleAuth + MastraCloudAuth via CompositeAuth Set AUTH_PROVIDER environment variable to switch between providers.'
imports:
- sym: ./types.AuthResult
  kind: typing
- sym: ./types.AuthProviderType
  kind: typing
data:
- name: AUTH_PROVIDER
  type: AuthProviderType
  val: (process.env.AUTH_PROVIDER as AuthProviderType) || 'simple'
- name: '{ mastraAuth, rbacProvider, auth }'
  type: any
  val: await initAuth()
functions:
- name: initAuth
  sig: 'initAuth(): Promise<AuthResult>'
  is_async: true
  lines:
  - 18
  - 47
  detail:
    calls:
    - import
    - initBetterAuth
    - initCloud
    - initComposite
    - initSimpleAuth
    - initWorkOS
exports:
- name: initAuth
  kind: func
- name: AUTH_PROVIDER
  kind: data
- name: '{ mastraAuth, rbacProvider, auth }'
  kind: data
