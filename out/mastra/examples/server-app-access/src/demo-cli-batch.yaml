## Roska File Descriptor — src/demo-cli-batch.ts
file: src/demo-cli-batch.ts
lines: 56
purpose: 'Demo: Direct Server App Access This demonstrates how to use mastra.getServerApp() to call routes directly without running an HTTP server. Use case: CLI scripts, batch processing, testing, background jobs NOTE: The base URL (http://internal) is just a placeholder - Hono''s app.fetch() processes requests in-memory and only uses the path. The hostname is ignored.'
imports:
- sym: '@mastra/hono.HonoBindings'
- sym: '@mastra/hono.HonoVariables'
- sym: '@mastra/hono.MastraServer'
- sym: hono.Hono
- sym: hono.Hono as HonoType
  kind: typing
- sym: ./mastra.mastra
data:
- name: BASE_URL
  type: string
  val: '''http://internal'''
functions:
- name: main
  sig: main()
  is_async: true
  lines:
  - 22
  - 54
  detail:
    calls:
    - JSON.stringify
    - Object.keys
    - Object.keys(tools).join
    - Object.keys(workflows).join
    - adapter.init
    - calcResponse.json
    - console.log
    - mastra.getServerApp
    - new Error
    - new Hono
    - new MastraServer
    - new Request
    - serverApp.fetch
    - toolsResponse.json
    - workflowsResponse.json
exports:
- name: main
  kind: func
- name: BASE_URL
  kind: data
