## Roska File Descriptor — insert-embedding-in-libsql/index.ts
file: insert-embedding-in-libsql/index.ts
lines: 29
imports:
- sym: '@ai-sdk/openai.openai'
- sym: '@mastra/core/vector/libsql.LibSQLVector'
- sym: '@mastra/rag.MDocument'
- sym: ai.embedMany
data:
- name: doc
  type: any
  val: MDocument.fromText('Your text content...')
- name: chunks
  type: any
  val: await doc.chunk()
- name: '{ embeddings }'
  type: any
  val: |-
    await embedMany({
      model: openai.embedding('text-embedding-3-small'),
      values: chunks.map(chunk => chunk.text),
    })
- name: libsql
  type: any
  val: |-
    new LibSQLVector({
      url: process.env.DATABASE_URL!,
      authToken: process.env.DATABASE_AUTH_TOKEN, // Optional: for Turs...
exports:
- name: doc
  kind: data
- name: chunks
  kind: data
- name: '{ embeddings }'
  kind: data
- name: libsql
  kind: data
