## Roska File Descriptor — insert-embedding-in-chroma/index.ts
file: insert-embedding-in-chroma/index.ts
lines: 32
imports:
- sym: '@ai-sdk/openai.openai'
- sym: '@mastra/chroma.ChromaVector'
- sym: '@mastra/rag.MDocument'
- sym: ai.embedMany
data:
- name: doc
  type: any
  val: MDocument.fromText('Your text content...')
- name: chunks
  type: '{ text: string }[]'
  val: await doc.chunk()
- name: '{ embeddings }'
  type: any
  val: |-
    await embedMany({
      model: openai.textEmbeddingModel('text-embedding-3-small'),
      values: chunks.map(chunk => chunk.text...
- name: chroma
  type: any
  val: |-
    new ChromaVector({
      apiKey: process.env.CHROMA_API_KEY,
      tenant: process.env.CHROMA_TENANT,
      database: process.env.CH...
exports:
- name: doc
  kind: data
- name: chunks
  kind: data
- name: '{ embeddings }'
  kind: data
- name: chroma
  kind: data
