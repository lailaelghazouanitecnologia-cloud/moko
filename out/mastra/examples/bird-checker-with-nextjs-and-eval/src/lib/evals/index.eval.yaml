## Roska File Descriptor — evals/index.eval.ts
file: evals/index.eval.ts
lines: 46
imports:
- sym: braintrust.Eval
- sym: ./data.IMAGES
- sym: ../mastra/actions.BirdResponse
- sym: ../mastra/actions.promptClaude
functions:
- name: containsScorer
  sig: |-
    containsScorer({
      output,
      expected,
    }: {
      output: BirdResponse;
      expected: Omit<BirdResponse, "location">;
    })
  lines:
  - 5
  - 22
  detail:
    calls:
    - expected?.species?.toLocaleLowerCase
    - |-
      output?.species
          ?.toLocaleLowerCase
    - |-
      output?.species
          ?.toLocaleLowerCase()
          ?.includes
exports:
- name: containsScorer
  kind: func
