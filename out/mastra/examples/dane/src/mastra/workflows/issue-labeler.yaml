## Roska File Descriptor — workflows/issue-labeler.ts
file: workflows/issue-labeler.ts
lines: 114
imports:
- sym: '@mastra/core/workflows.Step'
- sym: '@mastra/core/workflows.Workflow'
- sym: zod.z
- sym: ../integrations/index.js.github
data:
- name: githubIssueLabeler
  type: any
  val: |-
    new Workflow({
      name: 'github-issue-labeler',
      triggerSchema: z.object({
        repo: z.string(),
        owner: z.string(),
    ...
- name: getIssue
  type: any
  val: |-
    new Step({
      id: 'getIssue',
      outputSchema: z.object({
        title: z.string(),
        body: z.string(),
        labelNames: z.a...
- name: labelIssue
  type: any
  val: |-
    new Step({
      id: 'labelIssue',
      outputSchema: z.object({
        labels: z.array(z.string()),
      }),
      execute: async (input...
- name: applyLabels
  type: any
  val: |-
    new Step({
      id: 'applyLabels',
      execute: async (inputData, context) => {
        const parentStep = context?.workflow?.sta...
exports:
- name: githubIssueLabeler
  kind: data
- name: getIssue
  kind: data
- name: labelIssue
  kind: data
- name: applyLabels
  kind: data
