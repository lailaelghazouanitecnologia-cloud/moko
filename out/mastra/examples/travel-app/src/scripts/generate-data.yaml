## Roska File Descriptor — scripts/generate-data.ts
file: scripts/generate-data.ts
lines: 58
purpose: eslint-disable @typescript-eslint/no-explicit-any
imports:
- sym: csv-parser.csv
- sym: fs.fs
- sym: csv-stringify/sync.stringify
- sym: ../mastra/tools/attraction-tools.getHotels
- sym: ../mastra/tools/attraction-tools.getAttractionList
functions:
- name: updateCityData
  sig: updateCityData()
  is_async: true
  lines:
  - 9
  - 56
  detail:
    calls:
    - csv
    - fs.createReadStream
    - |-
      fs.createReadStream(filePath)
          .pipe
    - |-
      fs.createReadStream(filePath)
          .pipe(csv())
          .on
    - |-
      fs.createReadStream(filePath)
          .pipe(csv())
          .on("data", (row: Record<string, unknown>) => {
       ...
exports:
- name: updateCityData
  kind: func
