## ThiefEngine — RenderEngine (WebGL)
name: RenderEngine.js
path: src/graphics/RenderEngine.js
module: graphics
layer: graphics
lines: 280
types:
  - name: RenderEngine
    kind: class
    fields: [color, textureBatches, noTextureBatch, renderContext, binded, gl]
    methods: [addRenderers, update, bind, render, setClearColor]
  - name: SpriteBatch
    kind: class
    fields: [material, renderers, renderContext, vboColor, vboPosition, vboTexture, vboNormal, vao]
    methods: [add, bind, update, render]
  - name: Shader
    kind: class
    fields: [programID, vertShaderID, fragShaderID, compiled]
    methods: [compile, enable, disable]
    static: [defaultShader, debugShader]
  - name: Material
    kind: class
    fields: [texture, shader, color]
    methods: [bind, enable, disable, setTexture, setShader, setColor]
description: "WebGL renderer — groups by texture into SpriteBatch, uses VAO + instanced rendering, renders per-layer z-order"
pattern: "Texture batching + instanced draw calls + frustum culling"
