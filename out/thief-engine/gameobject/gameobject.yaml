## ThiefEngine — GameObject + Scene + Transform
name: GameObject.js
path: src/gameobject/GameObject.js
module: gameobject
layer: gameobject
lines: 350
types:
  - name: GameObject
    kind: class
    extends: BaseObject
    fields: [parent, children, components, transform, scene, static, name]
    methods: [addComponent, getComponent, getComponentsInChildren, addChild, getChildren, setParent, enable, disable, destroy]
  - name: Scene
    kind: class
    fields: [name, root, newsRoot, camera, renderContext, loaded, trash]
    methods: [addObject, flush, reset, hasNewObjects, deleteObject, cleanTrash]
  - name: Transform
    kind: class
    extends: Component
    fields: [position, rotation, scale, right, up, forward, matrix, translationMatrix, rotationMatrix, scaleMatrix, dirty]
    methods: [generateLocalSpaceMatrix, generateMatrix, initMatrix, setPosition, setRotation, setScale, translate, rotate, lookAt]
  - name: Component
    kind: class
    extends: BaseObject
    fields: [gameObject, enabled, destroyed]
    methods: [getGameObject, setGameObject, enable, disable, destroy, isDestroyed]
description: "Entity hierarchy — GameObjects hold Components, Scenes have root+newsRoot (two-root staging). Transform uses dirty flag for lazy matrix recomputation."
patterns:
  - "Two-root system: newsRoot stages new objects, flush() merges to root"
  - "Dirty flag on Transform for lazy matrix update"
  - "Hierarchical matrix: parent.matrix * child.localMatrix"
