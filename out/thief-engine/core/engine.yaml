## ThiefEngine — Engine (main loop)
name: Engine.js
path: src/core/Engine.js
module: core
layer: core
lines: 180
types:
  - name: Engine
    kind: class
    fields: [fps, renderEngine, physicsEngine, scriptEngine, currentScene, scenes, input, loaded, physicsEnabled]
    methods: [init, run, updateScene, loadScene, getCurrentScene, setCurrentScene, addScene, setClearColor, enablePhysics]
description: "Main loop orchestrator — fixed timestep physics, variable render. requestAnimationFrame loop."
update_cycle:
  - "Time.tick() — calculate deltaTime"
  - "Check scene loading"
  - "If new objects: gather components"
  - "scriptEngine.update() — run all scripts"
  - "physicsEngine.update(step) — fixed timestep loop"
  - "renderEngine.update() + render()"
  - "scene.cleanTrash() — destroy queued objects"
