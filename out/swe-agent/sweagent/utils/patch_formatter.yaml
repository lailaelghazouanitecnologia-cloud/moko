## Roska File Descriptor — utils/patch_formatter.py
file: utils/patch_formatter.py
lines: 152
layer: logic
imports:
- sym: unidiff.PatchSet
types:
- name: PatchFormatter
  kind: struct
  fields:
  - name: _patch
    type: Any
    default: PatchSet(patch)
  - name: _patched_files
    type: Any
    default: dict[str, str]
  - name: _original_files
    type: Any
    default: dict[str, str]
  - name: _patch_applied
    type: bool
    default: 'True'
  - name: _read_file
    type: Any
    default: read_method
  methods:
  - name: __init__
    sig: |-
      __init__(
              self,
              patch: str,
              read_method: Callable[[str], str],
          )
    lines:
    - 7
    - 25
    detail:
      calls:
      - PatchSet
      - self._read_files
  - name: _merge_intervals
    sig: '_merge_intervals(starts: list[int], stops: list[int]) -> tuple[list[int], list[int]]'
    vis: protected
    decorators:
    - staticmethod
    lines:
    - 28
    - 49
    detail:
      calls:
      - list
      - max
      - merged.append
      - sorted
      - zip
  - name: format_file
    sig: 'format_file(self, text: str, starts: list[int], stops: list[int], *, linenos: bool = True) -> str'
    lines:
    - 51
    - 96
    detail:
      calls:
      - '"\n".join'
      - all
      - enumerate
      - len
      - out.append
      - self._merge_intervals
      - text.splitlines
      - zip
  - name: _get_hunk_lines
    sig: '_get_hunk_lines(self, original: bool, *, context_length: int) -> dict[str, tuple[list[int], list[int]]]'
    vis: protected
    lines:
    - 98
    - 125
    detail:
      calls:
      - max
      - starts.append
      - stops.append
  - name: concat_files_strings
    sig: 'concat_files_strings(files: dict[str, str]) -> str'
    decorators:
    - staticmethod
    lines:
    - 140
    - 145
    detail:
      calls:
      - '"\n\n".join'
      - files.items
      - out.append
  - name: get_files_str
    sig: 'get_files_str(self, *, original: bool, context_length: int | None = 50, linenos: bool = True) -> str'
    lines:
    - 147
    - 152
    detail:
      calls:
      - self._get_hunk_lines
      - self.concat_files_strings
      - self.format_file
      - sources.items
  lines:
  - 6
  - 152
exports:
- name: PatchFormatter
  kind: type
