## Roska File Descriptor — utils/config.py
file: utils/config.py
lines: 80
layer: logic
imports:
- sym: dotenv.load_dotenv
- sym: sweagent.REPO_ROOT
- sym: sweagent.utils.log.get_logger
data:
- name: logger
  type: Any
  val: get_logger("swea-config", emoji="🔧")
functions:
- name: _convert_path_relative_to_repo_root
  sig: '_convert_path_relative_to_repo_root(path: Path | str, root: Path | None = None) -> Path | str'
  vis: protected
  lines:
  - 15
  - 20
  detail:
    calls:
    - Path
    - Path(path).resolve
    - os.getenv
    - path.relative_to
    - str
    - type
- name: _convert_path_to_abspath
  sig: '_convert_path_to_abspath(path: Path | str) -> Path'
  vis: protected
  lines:
  - 42
  - 53
  detail:
    calls:
    - Path
    - os.getenv
    - path.is_absolute
    - path.resolve
    - root.is_dir
- name: load_environment_variables
  sig: 'load_environment_variables(path: Path | None = None)'
  lines:
  - 60
  - 80
  detail:
    calls:
    - FileNotFoundError
    - Path.cwd
    - cwd_path.exists
    - load_dotenv
    - logger.debug
    - logger.info
    - path.is_file
    - repo_path.exists
exports:
- name: load_environment_variables
  kind: func
- name: logger
  kind: data
