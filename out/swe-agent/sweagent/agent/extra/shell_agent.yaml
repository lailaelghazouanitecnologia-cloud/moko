## Roska File Descriptor — extra/shell_agent.py
file: extra/shell_agent.py
lines: 106
layer: logic
imports:
- sym: sweagent.agent.agents.DefaultAgent
- sym: sweagent.agent.agents.ShellAgentConfig
- sym: sweagent.agent.models.HumanModel
- sym: sweagent.agent.models.HumanModelConfig
- sym: sweagent.agent.models.get_model
- sym: sweagent.agent.problem_statement.ProblemStatement
- sym: sweagent.agent.problem_statement.ProblemStatementConfig
- sym: sweagent.environment.swe_env.SWEEnv
- sym: sweagent.tools.parsing.ActionOnlyParser
- sym: sweagent.tools.tools.ToolHandler
- sym: sweagent.types.AgentRunResult
- sym: sweagent.types.StepOutput
types:
- name: ShellAgent
  kind: struct
  bases:
  - DefaultAgent
  methods:
  - name: __init__
    sig: __init__(self, ...2 params)
    lines:
    - 14
    - 15
  - name: from_config
    sig: 'from_config(cls, config: ShellAgentConfig) -> Self'
    decorators:
    - classmethod
    lines:
    - 18
    - 29
    detail:
      calls:
      - ToolHandler
      - cls
      - config.model_copy
      - get_model
  - name: human_step_in
    sig: human_step_in(self) -> None
    lines:
    - 31
    - 42
    detail:
      calls:
      - ActionOnlyParser
      - HumanModelConfig
      - get_model
      - self.logger.info
  - name: human_step_out
    sig: human_step_out(self) -> None
    lines:
    - 44
    - 57
    detail:
      calls:
      - hasattr
      - self.logger.info
  - name: run
    sig: |-
      run(
              self,
              env: SWEEnv,
              problem_statement: ProblemStatement | ProblemStatementConfig,
              *,
              output_dir: Path = Path("."),
          ) -> AgentRunResult
    lines:
    - 59
    - 106
    detail:
      calls:
      - AgentRunResult
      - StepOutput
      - isinstance
      - self._chook.on_run_done
      - self._chook.on_run_start
      - self.get_trajectory_data
      - self.human_step_in
      - self.human_step_out
      - self.logger.info
      - self.save_trajectory
      - self.setup
      - self.step
  lines:
  - 13
  - 106
exports:
- name: ShellAgent
  kind: type
