## Roska File Descriptor — tools/commands.py
file: tools/commands.py
lines: 220
layer: logic
purpose: 'Core module for defining and parsing commands in the SWE Agent system. This module provides the foundational classes and utilities for defining commands that can be executed by the agent. It is used extensively by: - tools.py: For command installation, execution and environment management - parsing.py: For parsing model outputs into executable commands - utils.py: For handling multi-line commands and argument quoting Key Classes: - Command: Represents an executable command with arguments and documentation - Argument: Defines an argument that can be passed to a command The module supports both simple bash commands and complex multi-line commands with typed arguments. Commands can be defined either in bash scripts with YAML docstrings or as bash functions.'
imports:
- sym: pydantic.BaseModel
- sym: pydantic.field_validator
- sym: pydantic.model_validator
- sym: sweagent.utils.jinja_warnings._warn_probably_wrong_jinja_syntax
data:
- name: ARGUMENT_NAME_PATTERN
  type: Any
  val: r"[a-zA-Z_][a-zA-Z0-9_-]*"
- name: BASH_COMMAND
  type: Any
  val: |-
    Command(
        name="bash",
        # name="execute_bash",
        signature="<command>",
        # signature="echo '<command>'\n<comm...
types:
- name: Argument
  kind: model
  ctx: 'f"""Defines an argument that can be passed to a command. Attributes: name: The argument name, must match {ARGUMENT_NAME_PATTERN!r} type: The argument type (e.g. "string", "integer") description: Human readable description of the argument required: Whether this argument must be provided enum: Optional list of allowed values argument_format: Format string for how to render the argument value in the command'
  bases:
  - BaseModel
  fields:
  - name: name
    type: Any
    default: str
  - name: type
    type: Any
    default: str
  - name: items
    type: Any
    default: dict[str, str] | None
  - name: description
    type: Any
    default: str
  - name: required
    type: Any
    default: bool
  - name: enum
    type: Any
    default: list[str] | None
  - name: argument_format
    type: Any
    default: str
  methods:
  - name: validate_argument_format
    sig: 'validate_argument_format(cls, value: str) -> str'
    decorators:
    - field_validator("argument_format")
    lines:
    - 74
    - 76
    detail:
      calls:
      - _warn_probably_wrong_jinja_syntax
  lines:
  - 52
  - 76
- name: Command
  kind: model
  ctx: 'Represents an executable command with arguments and documentation. A command can be either a simple bash command or a multi-line command terminated by an end marker. Attributes: name: The command name docstring: Human readable description of what the command does signature: Optional custom signature override end_name: For multi-line commands, the terminating marker arguments: List of arguments accepted by the command Properties: invoke_format: Format string for constructing the full command invocation'
  bases:
  - BaseModel
  fields:
  - name: name
    type: Any
    default: str
  - name: docstring
    type: Any
    default: str | None
  - name: signature
    type: Any
    default: str | None
  - name: end_name
    type: Any
    default: str | None
  - name: arguments
    type: Any
    default: list[Argument]
  methods:
  - name: invoke_format
    sig: invoke_format(self) -> str
    decorators:
    - cached_property
    lines:
    - 103
    - 131
    detail:
      calls:
      - ValueError
      - re.sub
  - name: get_function_calling_tool
    sig: get_function_calling_tool(self) -> dict
    lines:
    - 133
    - 162
    detail:
      calls:
      - required.append
  - name: validate_arguments
    sig: validate_arguments(self) -> Command
    decorators:
    - model_validator(mode="after")
    lines:
    - 165
    - 202
    detail:
      calls:
      - Counter
      - ValueError
      - _extract_keys
      - name_counts.items
      - re.match
  lines:
  - 79
  - 202
functions:
- name: _extract_keys
  sig: '_extract_keys(format_string: str) -> set[str]'
  vis: protected
  lines:
  - 33
  - 49
  detail:
    calls:
    - formatter.parse
    - keys.add
    - set
    - string.Formatter
exports:
- name: Argument
  kind: type
- name: Command
  kind: type
- name: ARGUMENT_NAME_PATTERN
  kind: data
- name: BASH_COMMAND
  kind: data
