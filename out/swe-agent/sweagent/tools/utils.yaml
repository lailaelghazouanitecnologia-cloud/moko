## Roska File Descriptor — tools/utils.py
file: tools/utils.py
lines: 108
layer: logic
imports:
- sym: sweagent.tools.commands.Command
functions:
- name: _guard_multiline_input
  sig: '_guard_multiline_input(action: str, match_fct: Callable[[str], re.Match | None]) -> str'
  vis: protected
  lines:
  - 8
  - 36
  detail:
    calls:
    - '"\n".join'
    - first_match.end
    - first_match.group
    - first_match.group(3).strip
    - first_match.start
    - guarded_command.replace
    - guarded_command.split
    - match_action.split
    - match_action.split("\n")[0].strip
    - match_action.split("\n")[0].strip().endswith
    - match_action.strip
    - match_fct
    - parsed_action.append
    - pre_action.strip
    - rem_action.strip
- name: get_signature
  sig: get_signature(cmd)
  lines:
  - 46
  - 72
  detail:
    calls:
    - cmd.arguments[-1].keys
    - list
- name: generate_command_docs
  sig: |-
    generate_command_docs(
        commands: list[Command],
        subroutine_types,
        **kwargs,
    ) -> str
  lines:
  - 75
  - 108
  detail:
    calls:
    - cmd.docstring.format
    - get_signature
exports:
- name: get_signature
  kind: func
- name: generate_command_docs
  kind: func
