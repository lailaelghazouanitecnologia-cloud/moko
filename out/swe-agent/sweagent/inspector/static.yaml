## Roska File Descriptor — inspector/static.py
file: inspector/static.py
lines: 169
layer: logic
imports:
- sym: traceback
- sym: argparse.ArgumentParser
- sym: yaml
- sym: tqdm.auto.tqdm
data:
- name: logger
  type: Any
  val: logging.getLogger(__name__)
- name: TEMPLATE
  type: str
  val: |-
    """
    <html>
    <head>
        <title>Trajectory Viewer</title>
        <style>
        {style_sheet}
        </style>
    </head>
    <body>
        <di...
functions:
- name: _load_file
  sig: _load_file(file_name, gold_patches, test_patches)
  vis: protected
  lines:
  - 49
  - 83
  detail:
    calls:
    - enumerate
    - isinstance
    - item.get
    - item.get("content", "").replace
    - item.get("content", "").replace("<", "&lt;").replace
    - item.get("role", "default").lower
    - item.get("role", "default").lower().replace
    - load_content
    - role_map.get
    - traceback.format_exc
- name: save_static_viewer
  sig: save_static_viewer(file_path)
  lines:
  - 96
  - 123
  detail:
    calls:
    - Path
    - Path(file_path.parent / "args.yaml").read_text
    - STYLE_SHEET.replace
    - STYLE_SHEET.replace("url('icons/", f"url('{relative_icons_path.as_posix()}/").replace
    - TEMPLATE.format
    - _load_file
    - _make_file_path_tree
    - data_path.exists
    - file_path.absolute
    - file_path.absolute().as_posix
    - file_path.parent.iterdir
    - file_path.with_suffix
    - find_relative_path
    - isinstance
    - json.load
    - list
    - logger.info
    - map
    - open
    - print
    - relative_icons_path.as_posix
    - yaml.safe_load
- name: find_relative_path
  sig: find_relative_path(from_path, to_path)
  lines:
  - 126
  - 152
  detail:
    calls:
    - Path
    - ValueError
    - from_path.is_dir
    - from_path.is_file
    - from_path.resolve
    - len
    - to_path.is_dir
    - to_path.is_file
    - to_path.resolve
    - zip
- name: save_all_trajectories
  sig: save_all_trajectories(directory)
  lines:
  - 155
  - 162
  detail:
    calls:
    - Path
    - directory.glob
    - isinstance
    - len
    - list
    - logger.info
    - save_static_viewer
    - tqdm
exports:
- name: save_static_viewer
  kind: func
- name: find_relative_path
  kind: func
- name: save_all_trajectories
  kind: func
- name: logger
  kind: data
- name: TEMPLATE
  kind: data
