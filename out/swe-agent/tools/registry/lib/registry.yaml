## Roska File Descriptor — lib/registry.py
file: lib/registry.py
lines: 56
layer: logic
data:
- name: registry
  type: Any
  val: EnvRegistry()
types:
- name: EnvRegistry
  kind: struct
  ctx: Read and write variables into a file. This is used to persist state between tool calls without using environment variables (which are problematic because you cannot set them in a subprocess). The default file location is `/root/.swe-agent-env`, though this can be overridden by the `env_file` argument or the `SWE_AGENT_ENV_FILE` environment variable.
  fields:
  - name: _env_file
    type: Any
    default: env_file
  methods:
  - name: __init__
    sig: __init__(self, ...1 params)
    lines:
    - 16
    - 17
  - name: env_file
    sig: env_file(self) -> Path
    decorators:
    - property
    lines:
    - 20
    - 27
    detail:
      calls:
      - Path
      - env_file.exists
      - env_file.write_text
      - os.environ.get
  - name: __getitem__
    sig: '__getitem__(self, key: str) -> str'
    lines:
    - 29
    - 30
    detail:
      calls:
      - json.loads
      - self.env_file.read_text
  - name: get
    sig: 'get(self, key: str, default_value: Any = None, fallback_to_env: bool = True) -> Any'
    lines:
    - 32
    - 43
    detail:
      calls:
      - json.loads
      - json.loads(self.env_file.read_text()).get
      - self.env_file.read_text
  - name: get_if_none
    sig: 'get_if_none(self, value: Any, key: str, default_value: Any = None) -> Any'
    lines:
    - 45
    - 48
    detail:
      calls:
      - self.get
  - name: __setitem__
    sig: '__setitem__(self, key: str, value: Any)'
    lines:
    - 50
    - 53
    detail:
      calls:
      - json.dumps
      - json.loads
      - self.env_file.read_text
      - self.env_file.write_text
  lines:
  - 7
  - 53
exports:
- name: EnvRegistry
  kind: type
- name: registry
  kind: data
